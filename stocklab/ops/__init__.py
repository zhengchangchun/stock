"""运维子包（P38）：把「定时任务」里属于项目的那部分搬回项目。

2026-09-22 之前，「每 30 分钟巡检」的**判定标准**写在 nanobot cron 的任务正文里
（7 项体检、收盘链顺序、什么算异常、汇报格式）。代价实测暴露过两次：

1. **两个状态口径对不上**：nanobot 的任务状态看「agent 这轮跑完没」→ `ok`；
   项目命令的退出码是「数据链对不对」→ `1`。2026-09-22 10:02 就是
   「任务 `ok` 而巡检在报异常」；
2. **知识不在版本库里**：任务正文丢了（09-18 网关重置丢过一次）只能从
   `docs/ops/*.md` 手抄回来，且没有任何测试钉住它。

本子包只放「调度层」的判定与编排：`patrol` = 体检 + 缺哪步补哪步 + 退出码。
业务口径一律不在本子包实现（ADR-019 / ADR-001 D-05：主干固定不可修改）。
"""
