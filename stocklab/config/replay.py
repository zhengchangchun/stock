"""回放引擎配置常量（中性位置，避免 candidate/ 与 plugin/ 互相 import）。

`REBALANCE_DAYS` 从这里取，不要从 `stocklab.plugin.sandbox` 取。
"""

#: 各候选池调仓周期（单位：交易日）。
REBALANCE_DAYS: dict[str, int] = {"short": 5, "mid": 20, "long": 60}

#: 回放时**每只标的的名义仓位**（元）。用于把「固定 5 元最低佣金」换算成费率。
#:
#: 为什么需要它：用 1 手（100 股，约 1000–5000 元）当名义市值，会把固定成本除成
#: 大费率（10 元股放大 **19 倍**）。而 1 手是 A 股的最小**可交易单位**，不是仓位规模。
#:
#: 为什么是 10 万：佣金 `max(名义额 × 0.025%, 5 元)` 里的最低佣金**只在名义额
#: < 2 万元时生效**（20000 × 0.025% = 5 元）→ **≥ 2 万是一段常数区间**，取任意值
#: 结果相同，不存在「可调的 N」（见设计文档 §3、ADR-017 D-13）。
#: 10 万只是取在离门槛有明显安全垫的位置（离 2 万有 8 万裕量，整股截断压不下去）。
POSITION_NOTIONAL: float = 100_000.0
