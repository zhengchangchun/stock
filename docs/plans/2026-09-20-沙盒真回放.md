# 沙盒真回放 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `_replay_daily_excess` 从 fail-closed 空实现做成真回放，让沙盒能产出 `WIN` / `LOSE` 而不只是恒定的 `INCONCLUSIVE`。

**Architecture:** 把主流程的打分内核从 `run_candidate` 抽出成**只读、不写库、可传插件版本覆盖**的 `score_pipeline`；回放实现放在 `stocklab/candidate/replay.py`（那里可以合法地 import 打分内核），`plugin/sandbox.py` 通过**注入 callable** 调用它——保持 `plugin/` 不 import `candidate/` 的分层，并沿用项目既有的「动作经参数注入」模式。判定按**调仓周期**为观测单元，训练/验证两段切分，verdict 只看验证段。

**Tech Stack:** Python 3.12.9（`.venv/bin/python`）、SQLite（append-only + 触发器）、pytest（全离线）。

**设计文档:** `docs/superpowers/specs/2026-09-20-沙盒真回放-design.md`（提交 `fe13f86`）

## Global Constraints

以下约束适用于**每一个** task：

- **解释器固定** `.venv/bin/python`。跑测试用 `.venv/bin/python -m pytest`，**不要加 `-q`**。
- **测试全离线**（`tests/conftest.py` 的 `no_network` 是 autouse）。真实联网只允许出现在显式标注的采集/录制脚本里。
- **回放必须无副作用**：跑前后 `candidate_snapshots` / `candidate_members` / `candidate_rejects` 行数**完全不变**。
- **回放与生产走同一段打分代码**：同一 `asof` 下 `score_pipeline` 与 `run_candidate` 的池成员必须完全相同。
- **单变量**：只换被比较的那个插件；其余一律用当前 active 版本。未被覆盖的插件没有 active → `NoActivePlugin`，**不兜底**。
- **观测单元 = 调仓周期**（非重叠）；`MIN_VALID_PERIODS = 120`。**有效周期数 < 120 → `INCONCLUSIVE`，不降门槛。**
- **verdict 只看验证段**（后 30% 周期）；过拟合标记用两段差距。
- **依赖方向**：`stocklab/plugin/` **不得** import `stocklab/candidate/`。
- **模块2 冻结**：`predict/`、`verify/`、`chain/`、`paper/`、`portfolio/` 一行不动。
- **PIT**：K 线 `date <= asof`，财报 `notice_date <= asof`。
- **提交信息**用仓库既有约定（`feat:` / `fix:` / `test:` / `docs:`）。**不改 git 作者身份，不用外部 git 包装器。**
- **Staging 纪律**：`stock/` 是未跟踪的人工文件；`.superpowers/` 与 `data/` 已 gitignore。**绝不**用 `git add -A` / `git add .` / `git commit -a`，只用显式路径。

---

## 与设计文档的一处偏离（**刻意，不是遗漏**）

设计文档 §5.1 写「`sandbox._replay_daily_excess` 的真实实现」。但 `sandbox.py` 在 `stocklab/plugin/` 下，而回放必须调用 `candidate/` 的打分内核 —— 直接实现会让 `plugin/` import `candidate/`，**违反本计划 Global Constraints 里的依赖方向**。

**改为**：回放实现落在 `stocklab/candidate/replay.py`；`sandbox.run_sandbox` 增加一个**注入的 callable 参数**（默认 `None` → 保持现在的 fail-closed 空实现）。这与项目既有模式一致（`ingest_daily_bars` 的 `fetch` 也是注入的，理由相同：让编排层不依赖具体实现）。CLI 负责把两者接起来。

---

## File Structure

**新建：**

| 文件 | 职责 |
|---|---|
| `stocklab/candidate/replay.py` | 调仓日生成、周期收益（等权/空池/成本/涨跌停）、candidate vs baseline 的 Δ 序列、训练/验证切分 |
| `tests/test_candidate_replay.py` | 回放引擎（含无副作用、单变量、下游规则） |
| `tests/test_sandbox_replay_wiring.py` | 注入接线 + verdict 只用验证段 + `train_gap` |
| `scripts/extend_history.py` | 把 21 只历史补到上市首日（**唯一允许联网的入口**） |
| `docs/decisions/2026-09-20-ADR-017-沙盒回放口径.md` | ADR |

**修改：**

| 文件 | 改动 |
|---|---|
| `stocklab/plugin/lifecycle.py` | `call_active` 支持指定版本（覆盖优先） |
| `stocklab/candidate/run.py` | 抽出 `score_pipeline`；`run_candidate` 复用它 |
| `stocklab/plugin/sandbox.py` | `MIN_VALID_DAYS` → `MIN_VALID_PERIODS`；`run_sandbox` 接注入的 replay；verdict 用验证段；`overfit_flag` 改 `train_gap` 驱动 |
| `stocklab/cli/plugin.py` | `plugin sandbox` 接上真实回放 |
| `tests/test_candidate_run.py` | 同源测试（`score_pipeline` vs `run_candidate`） |
| `tests/test_plugin_sandbox.py` | 跟随改名与签名变化 |

---

## 依赖顺序

```
Task 1 (call_active 覆盖)
   └─> Task 2 (score_pipeline 抽出 + 同源测试)
          └─> Task 3 (回放：调仓日 + 周期收益)
                 └─> Task 4 (Δ 序列 + 训练/验证切分)
                        └─> Task 5 (sandbox 接线 + MIN_VALID_PERIODS)
                               └─> Task 6 (train_gap 驱动 overfit_flag)
                                      └─> Task 7 (报告：双超额 + script_id)
                                             └─> Task 8 (plugin sandbox CLI)
                                                    └─> Task 9 (历史补到上市首日)
                                                           └─> Task 10 (真实回放验收 + ADR)
```

---

### Task 1: `call_active` 支持指定版本

**Files:**
- Modify: `stocklab/plugin/lifecycle.py`
- Test: `tests/test_plugin_lifecycle.py`

**Interfaces:**
- Consumes: `store.get_script`、`runtime.load_script`
- Produces: `call_active(conn, plugin_id, ctx, *, script_id: int | None = None, timeout_s=...) -> dict`
  —— `script_id` 为 `None` 时解析 active（现有行为）；指定时**用指定版本**，且**不校验它是否为 active**（沙盒要比的就是待审/归档版本）

- [ ] **Step 1: 写失败的测试**

在 `tests/test_plugin_lifecycle.py` 末尾追加：

```python
# ---------- Task 1：call_active 支持指定版本 ----------

def test_call_active_with_explicit_script_id_uses_that_version(conn):
    """指定版本时用它，即使它不是 active。"""
    a = _new(conn, version="1.0.0")
    lifecycle.record_submit(conn, a, actor="t", now=NOW)
    lifecycle.record_sandbox(conn, a, passed=True, reason="ok", now=NOW)
    lifecycle.approve(conn, a, actor="t", reason="ok", now=NOW)

    b = _new(conn, version="2.0.0",
             text=SCORE_SCRIPT.replace("score': 50.0", "score': 90.0"))
    lifecycle.record_submit(conn, b, actor="t", now=NOW)
    lifecycle.record_sandbox(conn, b, passed=True, reason="ok", now=NOW)
    # b 处于 pending_review，不是 active

    assert lifecycle.call_active(conn, "3", {})["score"] == 50.0          # active
    assert lifecycle.call_active(conn, "3", {}, script_id=b)["score"] == 90.0


def test_call_active_explicit_script_id_of_wrong_plugin_is_rejected(conn):
    """指定版本必须属于该 plugin_id —— 否则就是拿 A 插件冒名 B 插件。"""
    sid = store.insert_script(conn, plugin_id="1", version="1.0.0",
                              source_text=SCORE_SCRIPT, note=None, now=NOW)
    with pytest.raises(ValueError) as e:
        lifecycle.call_active(conn, "3", {}, script_id=sid)
    assert "plugin_id" in str(e.value)


def test_call_active_explicit_missing_script_is_lookup_error(conn):
    with pytest.raises(LookupError):
        lifecycle.call_active(conn, "3", {}, script_id=99999)


def test_call_active_without_override_still_requires_active(conn):
    _new(conn)                       # 只有 draft
    with pytest.raises(lifecycle.NoActivePlugin):
        lifecycle.call_active(conn, "3", {})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_plugin_lifecycle.py -k explicit`
Expected: FAIL —— `TypeError: call_active() got an unexpected keyword argument 'script_id'`

- [ ] **Step 3: 实现**

把 `stocklab/plugin/lifecycle.py` 的 `call_active` 替换为：

```python
def call_active(conn: sqlite3.Connection, plugin_id: str, ctx: dict,
                *, script_id: int | None = None,
                timeout_s: float = runtime.DEFAULT_TIMEOUT_S) -> dict:
    """执行插桩。

    `script_id is None`（默认）→ 解析该 plugin_id 的 active 版本，没有则
    `NoActivePlugin`（**不兜底**）。

    `script_id` 指定时 → **用该版本，且不要求它是 active** —— 沙盒要比的
    正是待审/归档版本。但该版本**必须属于同一个 plugin_id**：拿插桩1 的
    版本去跑插桩3，会让「比的是哪个插件」这件事失去意义，直接拒绝。
    """
    if script_id is None:
        script_id = active_script_id(conn, plugin_id, required=True)

    row = store.get_script(conn, script_id)
    if row is None:
        raise LookupError(f"脚本 {script_id} 不存在")
    if row["plugin_id"] != plugin_id:
        raise ValueError(
            f"脚本 {script_id} 的 plugin_id 是 {row['plugin_id']!r}，"
            f"与请求的 {plugin_id!r} 不符 —— 拒绝跨插件执行")
    fn = runtime.load_script(row["source_text"], plugin_id=plugin_id,
                             timeout_s=timeout_s)
    return fn(ctx)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_plugin_lifecycle.py`
Expected: PASS

- [ ] **Step 5: 全量测试**

Run: `.venv/bin/python -m pytest`
Expected: 全绿，总数只增不减

- [ ] **Step 6: 提交**

```bash
git add stocklab/plugin/lifecycle.py tests/test_plugin_lifecycle.py
git commit -m "feat(plugin): call_active 支持指定版本（沙盒比对待审/归档版本用）"
```

---

### Task 2: 抽出 `score_pipeline`

**Files:**
- Modify: `stocklab/candidate/run.py`
- Modify: `stocklab/candidate/score.py`、`stocklab/candidate/risk_adjust.py`（加 `plugin_overrides` 透传）
- Test: `tests/test_candidate_run.py`

**Interfaces:**
- Consumes: `screen` / `pools` / `score` / `risk_adjust` / `snapshot` / `seeds`
- Produces:
  - `PipelineResult`（frozen dataclass：`members: list[MemberRow]`、`rejects: list[RejectRow]`、`params: dict`）
  - `score_pipeline(conn, *, asof: str, plugin_overrides: dict[str, int] | None = None) -> PipelineResult` —— **只读，不写任何表**
  - `score.build_ctx(..., plugin_overrides=None)`、`score.industry_screen(conn, inst, ctx, *, plugin_overrides=None)`、`score.score_pool(conn, inst, pool, ctx, *, plugin_overrides=None)`、`risk_adjust.adjust(conn, outcome, ctx, *, plugin_overrides=None)`

- [ ] **Step 1: 写失败的测试**

在 `tests/test_candidate_run.py` 末尾追加：

```python
# ---------- Task 2：score_pipeline 只读内核 ----------

from stocklab.candidate.run import PipelineResult, score_pipeline


def test_score_pipeline_writes_nothing(_seed_db):
    """内核不许写任何 candidate_* 表。"""
    conn = _seed_db
    before = {
        t: conn.execute(f"SELECT COUNT(*) FROM {t}").fetchone()[0]
        for t in ("candidate_snapshots", "candidate_members",
                  "candidate_rejects")
    }
    result = score_pipeline(conn, asof=ASOF)
    after = {
        t: conn.execute(f"SELECT COUNT(*) FROM {t}").fetchone()[0]
        for t in ("candidate_snapshots", "candidate_members",
                  "candidate_rejects")
    }
    assert before == after, "回放内核不得产生副作用"
    assert isinstance(result, PipelineResult)


def test_score_pipeline_matches_run_candidate_members(_seed_db):
    """回放内核与生产主流程在同一 asof 上必须选出完全相同的池成员。

    这是「回测跑的就是生产逻辑」的机械保证 —— 两者若分叉，所有回放结论
    都无意义。
    """
    conn = _seed_db
    fresh = conn.execute("SELECT COUNT(*) FROM candidate_snapshots").fetchone()[0]
    assert fresh == 0

    prod = run_candidate(conn, asof=ASOF, run_kind="weekly", now=NOW)
    pipe = score_pipeline(conn, asof=ASOF)

    key = lambda ms: sorted((m.code, m.pool, round(m.adj_score, 9)) for m in ms)
    assert key(pipe.members) == key(prod.members)
    assert sorted((r.code, r.stage, r.reason) for r in pipe.rejects) == \
           sorted((r.code, r.stage, r.reason) for r in prod.rejects)


def test_score_pipeline_overrides_plugin_version(_seed_db):
    """plugin_overrides 生效：换一版打分插桩，分数随之改变。"""
    conn = _seed_db
    base = score_pipeline(conn, asof=ASOF)
    sid = store.insert_script(
        conn, plugin_id="1", version="9.9.9",
        source_text="def run(ctx):\n"
                    "    return {'score': 100.0, 'pass_flag': True,"
                    " 'reason': 'v2', 'risk_list': []}\n",
        note=None, now=NOW)
    over = score_pipeline(conn, asof=ASOF, plugin_overrides={"1": sid})
    short_base = {m.code: m.raw_score for m in base.members if m.pool == "short"}
    short_over = {m.code: m.raw_score for m in over.members if m.pool == "short"}
    assert short_over and all(v == 100.0 for v in short_over.values())
    assert short_over != short_base


def test_score_pipeline_unoverridden_plugin_uses_active(_seed_db):
    """没被覆盖的插件仍用 active —— 否则就不是单变量了。"""
    conn = _seed_db
    sid = store.insert_script(
        conn, plugin_id="1", version="9.9.9",
        source_text="def run(ctx):\n"
                    "    return {'score': 100.0, 'pass_flag': True,"
                    " 'reason': 'v2', 'risk_list': []}\n",
        note=None, now=NOW)
    # 只覆盖插桩4，插桩1 应仍走 active（80.0）
    r4 = store.list_scripts(conn, plugin_id="4")[0]["script_id"]
    over = score_pipeline(conn, asof=ASOF, plugin_overrides={"4": r4})
    short = [m for m in over.members if m.pool == "short"]
    assert short and all(m.raw_score == 80.0 for m in short)
```

> `_seed_db` 是 `tests/test_candidate_run.py` 里已有的夹具（返回一个连接），本计划沿用它；若签名不同，按该文件的实际形状调整并在报告里说明。

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_candidate_run.py -k score_pipeline`
Expected: FAIL —— `ImportError: cannot import name 'PipelineResult'`

- [ ] **Step 3: 实现**

**3a.** `stocklab/candidate/score.py`：给三个函数加 `plugin_overrides` 透传。

```python
def industry_screen(conn, inst, ctx, *, plugin_overrides=None) -> dict:
    return lifecycle.call_active(
        conn, INDUSTRY_SCREEN_PLUGIN, ctx,
        script_id=(plugin_overrides or {}).get(INDUSTRY_SCREEN_PLUGIN))


def score_pool(conn, inst, pool, ctx, *, plugin_overrides=None) -> ScoreOutcome:
    if pool not in POOL_PLUGIN:
        raise ValueError(f"未知池 {pool!r}；已知：{sorted(POOL_PLUGIN)}")
    plugin_id = POOL_PLUGIN[pool]
    result = lifecycle.call_active(
        conn, plugin_id, ctx,
        script_id=(plugin_overrides or {}).get(plugin_id))
    return ScoreOutcome(...)          # 其余不变
```

`stocklab/candidate/risk_adjust.py`：

```python
def adjust(conn, outcome, ctx, *, plugin_overrides=None) -> tuple[float, list[str]]:
    payload = dict(ctx)
    payload["raw_score"] = outcome.raw_score
    payload["risk_list"] = list(outcome.risk_list)
    payload["pool"] = outcome.pool
    payload["code"] = outcome.code
    result = lifecycle.call_active(
        conn, RISK_PLUGIN, payload,
        script_id=(plugin_overrides or {}).get(RISK_PLUGIN))
    return float(result["final_score"]), list(result["risk_out"])
```

**3b.** `stocklab/candidate/run.py`：抽出内核。把现在 `run_candidate` 里从「`members: list[...] = []`」到 `select_top` 循环结束的整段搬进新函数：

```python
@dataclass(frozen=True)
class PipelineResult:
    """打分内核的产物。**只读** —— 不含任何落库副作用。"""

    members: list
    rejects: list
    params: dict


def score_pipeline(conn: sqlite3.Connection, *, asof: str,
                   plugin_overrides: dict[str, int] | None = None
                   ) -> PipelineResult:
    """跑一遍候选池打分（步骤 3–6），**只返回、不写库**。

    回放要跑几百个历史调仓日，绝不能往 `candidate_snapshots` 灌历史回放行。
    生产主流程 `run_candidate` 也走这里 —— 两者必须产出相同成员，否则
    「回测跑的就是生产逻辑」不成立。

    `plugin_overrides`：`{plugin_id: script_id}`，用于沙盒的单变量对比
    （只换被比较的那个插件，其余仍解析 active）。
    """
    members: list[snapshot.MemberRow] = []
    rejects: list[snapshot.RejectRow] = []
    scored: dict[str, list[dict]] = {p: [] for p in pools.ALL_POOLS}

    xsec = _cross_section_map(conn, asof=asof)

    for inst in SEED_UNIVERSE:
        bars = _load_bars(conn, inst.code, asof=asof)

        verdict = screen.screen(inst, bars, asof=asof)
        if not verdict.passed:
            rejects.append(snapshot.RejectRow(
                code=inst.code, stage="pre_screen", reason=verdict.reason,
                plugin_id=None))
            continue

        ctx = score.build_ctx(conn, inst, pools.POOL_SHORT, bars, asof=asof,
                              cross_section=xsec)
        industry = score.industry_screen(conn, inst, ctx,
                                         plugin_overrides=plugin_overrides)
        if not industry["pass_flag"]:
            rejects.append(snapshot.RejectRow(
                code=inst.code, stage="industry_screen",
                reason="; ".join(industry["risk_note"]) or "行业排雷未通过",
                plugin_id=score.INDUSTRY_SCREEN_PLUGIN))
            continue

        for pool in pools.eligible_pools(inst):
            pool_ctx = score.build_ctx(conn, inst, pool, bars, asof=asof,
                                       cross_section=xsec)
            outcome = score.score_pool(conn, inst, pool, pool_ctx,
                                       plugin_overrides=plugin_overrides)
            if not outcome.pass_flag:
                rejects.append(snapshot.RejectRow(
                    code=inst.code, stage="score",
                    reason=f"{pool}池打分未通过：{outcome.reason}",
                    plugin_id=score.POOL_PLUGIN[pool]))
                continue
            final, risks = risk_adjust.adjust(conn, outcome, pool_ctx,
                                              plugin_overrides=plugin_overrides)
            scored[pool].append({
                "code": inst.code, "pool": pool,
                "raw_score": outcome.raw_score, "adj_score": final,
                "reason": outcome.reason,
                "risk_json": json.dumps(risks, ensure_ascii=False)})

    for pool in pools.ALL_POOLS:
        for row in pools.select_top(scored[pool], pool):
            members.append(snapshot.MemberRow(**row))

    return PipelineResult(members=members, rejects=rejects,
                          params={"seed_count": len(SEED_UNIVERSE),
                                  "topn": dict(pools.POOL_TOPN)})
```

`run_candidate` 的新鲜路径改为：

```python
    pipe = score_pipeline(conn, asof=asof)
    snapshot_id = snapshot.write_snapshot(
        conn, asof=asof, run_kind=run_kind, params=pipe.params,
        members=pipe.members, rejects=pipe.rejects, now=now)
    loaded = snapshot.load_snapshot(conn, snapshot_id)
    md = report.render_report(asof=asof, run_kind=run_kind, loaded=loaded,
                              generated_at=now)
    members_obj, rejects_obj = _hydrate(loaded)
    return RunResult(snapshot_id=snapshot_id, asof=asof, run_kind=run_kind,
                     members=members_obj, rejects=rejects_obj,
                     report_md=md, skipped=False, params=pipe.params)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_candidate_run.py`
Expected: PASS（含既有的幂等与可复现测试）

- [ ] **Step 5: 全量测试**

Run: `.venv/bin/python -m pytest`
Expected: 全绿

- [ ] **Step 6: 提交**

```bash
git add stocklab/candidate/run.py stocklab/candidate/score.py stocklab/candidate/risk_adjust.py tests/test_candidate_run.py
git commit -m "refactor(candidate): 抽出只读打分内核 score_pipeline，run_candidate 复用它"
```

---

### Task 3: 回放引擎——调仓日与周期收益

**Files:**
- Create: `stocklab/candidate/replay.py`
- Test: `tests/test_candidate_replay.py`

**Interfaces:**
- Consumes: `score_pipeline`、`data.models.Bar`、`config.costs.CostModel`、`backtest.portfolio.LIMIT_BY_BOARD` / `BoardUnknown`
- Produces:
  - `rebalance_dates(trading_days: list[str], *, period: int, start: str, end: str) -> list[str]`
  - `period_returns(conn, *, asof_dates: list[str], pool: str, plugin_overrides=None, costs=None) -> list[float]`
  - `EMPTY_POOL_IS_CASH: bool = True`

- [ ] **Step 1: 写失败的测试**

新建 `tests/test_candidate_replay.py`：

```python
"""Task 3：回放引擎 —— 调仓日、周期收益（等权/空池/成本/涨跌停）。"""

import pytest

from stocklab.candidate import replay
from stocklab.config.costs import CostModel
from stocklab.data.models import Bar
from stocklab.store.db import connect
from stocklab.store.migrate import init_db

NOW = "2026-09-20T16:00:00+08:00"


def _dates(n, start="2026-01-01"):
    from datetime import date, timedelta
    d0 = date.fromisoformat(start)
    return [(d0 + timedelta(days=i)).isoformat() for i in range(n)]


# ---------- 调仓日 ----------

def test_rebalance_dates_take_every_kth_day():
    days = _dates(20)
    got = replay.rebalance_dates(days, period=5, start=days[0], end=days[-1])
    assert got == [days[0], days[5], days[10], days[15]]


def test_rebalance_dates_drop_partial_tail():
    """末尾不足一个周期的部分**丢弃**，不截断成短周期。"""
    days = _dates(13)
    got = replay.rebalance_dates(days, period=5, start=days[0], end=days[-1])
    assert got == [days[0], days[5], days[10]]
    assert days[12] not in got


def test_rebalance_dates_respect_window_bounds():
    days = _dates(30)
    got = replay.rebalance_dates(days, period=10, start=days[5], end=days[25])
    assert got == [days[5], days[15], days[25]]


def test_rebalance_dates_empty_when_window_shorter_than_period():
    days = _dates(3)
    assert replay.rebalance_dates(days, period=5, start=days[0],
                                  end=days[-1]) == [days[0]]


# ---------- 周期收益 ----------

def _db_with_bars(tmp_db, prices: dict[str, list[float]], dates: list[str]):
    """建库：标的 + 日历 + 逐日收盘价（价格序列与 dates 等长）。"""
    init_db(tmp_db)
    c = connect(tmp_db)
    c.executemany(
        "INSERT INTO instruments (code, name, market, board, type, added_at)"
        " VALUES (?,?,'sz','main','stock',?)",
        [(code, code, NOW) for code in prices])
    c.executemany("INSERT INTO trading_calendar (date, is_open, source,"
                  " created_at) VALUES (?,1,'t',?)", [(d, NOW) for d in dates])
    c.executemany(
        "INSERT INTO bars_daily (code, date, open, high, low, close, volume,"
        " adj_mode, source, fetched_at) VALUES (?,?,?,?,?,?,1000,'none','x',?)",
        [(code, d, p, p, p, p, NOW)
         for code, series in prices.items()
         for d, p in zip(dates, series)])
    c.commit()
    return c


def test_empty_pool_holds_cash_but_pays_liquidation_cost(tmp_db):
    """空池 → 不产生持仓收益，但仍要卖掉上一期持仓、付清仓成本。

    收益**不是 0** —— 是清仓成本的负值。这里手工算：第 0 期持有 100 股
    @10.00，第 1 期池空 → 必须卖出，费用 = `CostModel.fees('sell', 10.00, 100)`，
    以「占初始市值 1000 元」的比例计，即 `-fees/1000`。
    """
    from stocklab.config.costs import CostModel as CM

    dates = _dates(6)
    c = _db_with_bars(tmp_db, {"000333": [10.0] * 6}, dates)
    costs = CM()
    expected_fee = costs.fees("sell", costs.fill_price("sell", 10.0), 100)
    expected = -expected_fee / 1000.0

    r = replay.period_returns(
        c, asof_dates=[], pool="short", costs=costs,
        _pools_for_test={dates[0]: ["000333"], dates[5]: []})
    assert len(r) == 1
    assert r[0] == pytest.approx(expected)
    assert r[0] < 0, "空池不是零收益 —— 清仓要付钱"


def test_flat_prices_give_zero_return_before_costs(tmp_db):
    dates = _dates(6)
    c = _db_with_bars(tmp_db, {"000333": [10.0] * 6}, dates)
    r = replay.period_returns(
        c, asof_dates=[], pool="short",
        costs=CostModel(commission_rate=0.0, min_commission=0.0,
                        transfer_fee_rate=0.0, stamp_tax_rate=0.0,
                        slippage_bps=0.0),
        _pools_for_test={dates[0]: [], dates[5]: []})
    assert r == [] or all(x == 0.0 for x in r)
```

> **注**：`period_returns` 的注入口 `_pools_for_test` 是本计划刻意加的**测试接缝** —— 否则要构造「某个 asof 下池子恰好为空」得先造出整套插桩与行情。实现时用**关键字参数 `_pools_for_test`**（下划线前缀表私有），生产路径不传它。若你找到更干净的注入方式（例如注入一个「取池成员」的 callable），按你的方式做并**在报告里说明**。

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_candidate_replay.py`
Expected: FAIL —— `ModuleNotFoundError: No module named 'stocklab.candidate.replay'`

- [ ] **Step 3: 实现**

新建 `stocklab/candidate/replay.py`：

```python
"""沙盒回放引擎（设计文档 `2026-09-20-沙盒真回放-design.md`）。

## 观测单元是**调仓周期**，不是交易日

一个 20 日调仓周期里，20 个日度收益是**同一个持仓**产生的、高度自相关。
把它们当独立样本做 bootstrap 会算出**过窄的置信区间 → 假的 WIN**。所以
每个周期只产生**一个**观测。

代价（设计文档 §2 D1）：门槛从「120 交易日」变成「120 调仓周期」，
严格得多 —— 长期池（60 日）在可预见的历史长度内**永远出不了结论**。
这是算术，不降门槛迁就。

## 下游规则写死

等权、持到下一个调仓日、空池持现金（但计清仓成本）、扣成本、涨跌停
不可成交。**规则不随插桩版本变化** —— 否则「改下游」会成为另一条提分
路径，归因失效。

## 本模块在 candidate/ 下，不在 plugin/ 下

回放要调打分内核；`plugin/` 不得 import `candidate/`。所以回放住在这里，
由 `plugin/sandbox.py` 通过**注入**调用（见本模块 `replay_period_deltas`）。
"""

from __future__ import annotations

import datetime as _dt
import sqlite3
from typing import Callable

from stocklab.backtest.portfolio import BoardUnknown, LIMIT_BY_BOARD
from stocklab.config.costs import CostModel

#: 空池时的处置：持现金。**不是**「跳过该周期」—— 卖出上一期持仓是要付
#: 成本的，跳过会把那笔成本抹掉。
EMPTY_POOL_IS_CASH: bool = True

#: 每期等权买入的目标手数（整手）。
LOT = 100


def rebalance_dates(trading_days: list[str], *, period: int, start: str,
                    end: str) -> list[str]:
    """窗口内每 `period` 个交易日一个调仓日。

    **末尾不足一个周期直接丢弃** —— 不截断成短周期，否则周期长度不一致、
    收益不可比。
    """
    days = [d for d in trading_days if start <= d <= end]
    if not days:
        return []
    out = [days[i] for i in range(0, len(days), period)]
    return [d for d in out if d <= end]


def _board_of(conn: sqlite3.Connection, code: str) -> str:
    row = conn.execute("SELECT board FROM instruments WHERE code = ?",
                       (code,)).fetchone()
    if row is None:
        raise BoardUnknown(f"{code} 不在 instruments 表里，无法判定板别")
    return str(row["board"])


def _limit_hit(prev_close: float, close: float, board: str) -> str | None:
    """返回 `'up'` / `'down'` / `None`。涨停买不进、跌停卖不出。"""
    if board not in LIMIT_BY_BOARD or prev_close <= 0:
        raise BoardUnknown(f"板别 {board!r} 不在 {sorted(LIMIT_BY_BOARD)} 中")
    limit = LIMIT_BY_BOARD[board]
    chg = (close - prev_close) / prev_close
    if chg >= limit - 0.002:
        return "up"
    if chg <= -(limit - 0.002):
        return "down"
    return None


def _close_on(conn, code: str, date: str) -> float | None:
    row = conn.execute(
        "SELECT close FROM bars_daily WHERE code = ? AND date = ?",
        (code, date)).fetchone()
    return None if row is None else float(row["close"])


def _prev_close(conn, code: str, date: str) -> float | None:
    row = conn.execute(
        "SELECT close FROM bars_daily WHERE code = ? AND date < ?"
        " ORDER BY date DESC LIMIT 1", (code, date)).fetchone()
    return None if row is None else float(row["close"])


def period_returns(conn: sqlite3.Connection, *, asof_dates: list[str],
                   pool: str, plugin_overrides: dict[str, int] | None = None,
                   costs: CostModel | None = None,
                   _pools_for_test: dict[str, list[str]] | None = None
                   ) -> list[float]:
    """逐周期收益序列（每个调仓周期一个观测）。

    `asof_dates`：调仓日序列，两两之间构成一个周期（长度 = len-1）。
    返回长度 = `len(asof_dates) - 1`。

    `_pools_for_test`：**测试接缝**，`{调仓日: [code, ...]}`。生产路径不传，
    此时池成员由 `score_pipeline` 现算。
    """
    if len(asof_dates) < 2:
        return []
    costs = costs or CostModel()

    from stocklab.candidate.run import score_pipeline      # 延迟 import，避免环

    def _members_on(day: str) -> list[str]:
        if _pools_for_test is not None:
            return list(_pools_for_test.get(day, []))
        res = score_pipeline(conn, asof=day, plugin_overrides=plugin_overrides)
        return sorted(m.code for m in res.members if m.pool == pool)

    out: list[float] = []
    for i in range(len(asof_dates) - 1):
        d0, d1 = asof_dates[i], asof_dates[i + 1]
        hold = _members_on(d0)
        nxt = _members_on(d1)

        # 期初建仓 / 上一期调出
        entering = [c for c in hold if c not in nxt]     # 下一期不再持有
        staying = [c for c in nxt if c in hold]          # 两期都有
        new = [c for c in nxt if c not in hold]          # 新买入

        p0 = {c: _close_on(conn, c, d0) for c in set(hold) | set(nxt)}
        p1 = {c: _close_on(conn, c, d1) for c in set(hold) | set(nxt)}

        # 周期收益：下一期持仓在 [d0, d1] 的价格变动（等权）
        gains: list[float] = []
        for c in nxt:
            if p0.get(c) and p1.get(c):
                gains.append(p1[c] / p0[c] - 1.0)
        gross = (sum(gains) / len(gains)) if gains else 0.0

        # 成本：卖出 entering，买入 new。整手 + 涨停买不进 / 跌停卖不出
        fee_ratio = 0.0
        for c in entering:
            px = p0.get(c)
            if px is None:
                continue
            board = _board_of(conn, c)
            if _limit_hit(_prev_close(conn, c, d0) or px, px, board) == "down":
                continue                                  # 跌停卖不出
            _, fee = costs.total("sell", px, LOT)
            fee_ratio += fee / (px * LOT) / max(len(nxt) or 1, 1)
        for c in new:
            px = p0.get(c)
            if px is None:
                continue
            board = _board_of(conn, c)
            if _limit_hit(_prev_close(conn, c, d0) or px, px, board) == "up":
                continue                                  # 涨停买不进
            _, fee = costs.total("buy", px, LOT)
            fee_ratio += fee / (px * LOT) / max(len(nxt) or 1, 1)

        out.append(gross - fee_ratio)
    return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_candidate_replay.py`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add stocklab/candidate/replay.py tests/test_candidate_replay.py
git commit -m "feat(candidate): 回放引擎 —— 调仓日与周期收益（等权/空池/成本/涨跌停）"
```

---

### Task 4: Δ 序列与训练/验证切分

**Files:**
- Modify: `stocklab/candidate/replay.py`
- Test: `tests/test_candidate_replay.py`

**Interfaces:**
- Consumes: Task 3 的 `rebalance_dates` / `period_returns`
- Produces:
  - `SPLIT_TRAIN_RATIO: float = 0.7`
  - `split_train_validate(deltas: list[float]) -> tuple[list[float], list[float]]`
  - `replay_period_deltas(conn, *, candidate_script_id, baseline_script_id, pool, window_start, window_end, costs=None) -> tuple[list[float], list[float]]`
    —— 返回 `(训练段 Δ, 验证段 Δ)`

- [ ] **Step 1: 写失败的测试**

在 `tests/test_candidate_replay.py` 末尾追加：

```python
# ---------- Task 4：Δ 与切分 ----------

def test_split_is_by_period_index_not_calendar():
    """按周期**序号**切 70/30。"""
    deltas = list(range(10))
    tr, va = replay.split_train_validate(deltas)
    assert tr == [0, 1, 2, 3, 4, 5, 6]
    assert va == [7, 8, 9]


def test_split_handles_short_series():
    tr, va = replay.split_train_validate([1.0, 2.0])
    assert tr == [1.0]
    assert va == [2.0]


def test_split_empty():
    assert replay.split_train_validate([]) == ([], [])


def test_split_keeps_order():
    tr, va = replay.split_train_validate([1, 2, 3, 4, 5])
    assert tr == [1, 2, 3]
    assert va == [4, 5]


def test_split_ratio_is_constant():
    assert replay.SPLIT_TRAIN_RATIO == 0.7


def test_deltas_are_candidate_minus_baseline(tmp_db):
    """Δ = 候选版本周期收益 − 基线版本周期收益。"""
    dates = _dates(6)
    c = _db_with_bars(tmp_db, {"000333": [10.0, 11.0, 12.0, 13.0, 14.0, 15.0]},
                      dates)
    flat = CostModel(commission_rate=0.0, min_commission=0.0,
                     transfer_fee_rate=0.0, stamp_tax_rate=0.0,
                     slippage_bps=0.0)
    # 候选：第 0 期持有 000333；基线：两期都空仓
    tr, va = replay.replay_period_deltas(
        c, candidate_script_id=1, baseline_script_id=1, pool="short",
        window_start=dates[0], window_end=dates[-1], costs=flat,
        _pools_for={"cand": {dates[0]: ["000333"], dates[5]: []},
                    "base": {dates[0]: [], dates[5]: []}})
    allv = tr + va
    assert allv and all(x > 0 for x in allv)      # 涨了且基线空仓 → Δ > 0


def test_deltas_zero_when_versions_identical(tmp_db):
    dates = _dates(6)
    c = _db_with_bars(tmp_db, {"000333": [10.0] * 6}, dates)
    flat = CostModel(commission_rate=0.0, min_commission=0.0,
                     transfer_fee_rate=0.0, stamp_tax_rate=0.0,
                     slippage_bps=0.0)
    same = {dates[0]: ["000333"], dates[5]: ["000333"]}
    tr, va = replay.replay_period_deltas(
        c, candidate_script_id=1, baseline_script_id=1, pool="short",
        window_start=dates[0], window_end=dates[-1], costs=flat,
        _pools_for={"cand": same, "base": same})
    assert all(x == 0.0 for x in tr + va)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_candidate_replay.py -k "split or delta"`
Expected: FAIL —— `AttributeError: module ... has no attribute 'split_train_validate'`

- [ ] **Step 3: 实现**

在 `stocklab/candidate/replay.py` 追加：

```python
#: 训练段占比。按**周期序号**切，不按日历 —— 保证两段周期长度相同。
SPLIT_TRAIN_RATIO: float = 0.7


def split_train_validate(deltas: list[float]) -> tuple[list[float], list[float]]:
    """按周期序号切训练段（前 70%）与验证段（后 30%）。

    两段**周期长度相同**（都来自同一套 `REBALANCE_DAYS`），所以 Δ 可比。
    """
    if not deltas:
        return [], []
    n_train = int(len(deltas) * SPLIT_TRAIN_RATIO)
    n_train = max(1, min(n_train, len(deltas) - 1)) if len(deltas) > 1 else 1
    return deltas[:n_train], deltas[n_train:]


def replay_period_deltas(conn: sqlite3.Connection, *, candidate_script_id: int,
                         baseline_script_id: int, pool: str,
                         window_start: str, window_end: str,
                         costs: CostModel | None = None,
                         trading_days: list[str] | None = None,
                         _pools_for: dict | None = None
                         ) -> tuple[list[float], list[float]]:
    """回放两个版本，返回 `(训练段 Δ, 验证段 Δ)`。

    Δ = 候选版本周期收益 − 基线版本周期收益。

    **单变量**：只有该 `plugin_id` 的两个版本不同；其余插件由
    `score_pipeline` 解析 active 版本（见 `period_returns`）。
    """
    from stocklab.candidate.run import SEED_UNIVERSE        # noqa: F401 触发注册

    period = REBALANCE_DAYS[pool]
    days = trading_days or _trading_days(conn, window_start, window_end)
    marks = rebalance_dates(days, period=period, start=window_start,
                            end=window_end)

    pid = _plugin_id_of(conn, candidate_script_id)
    base_pid = _plugin_id_of(conn, baseline_script_id)
    if pid != base_pid:
        raise ValueError(
            f"两个版本属于不同插件（{pid!r} vs {base_pid!r}）—— 单变量原则"
            "要求只换同一个 plugin_id 的版本")

    cand = period_returns(
        conn, asof_dates=marks, pool=pool,
        plugin_overrides={pid: candidate_script_id}, costs=costs,
        _pools_for_test=(_pools_for or {}).get("cand"))
    base = period_returns(
        conn, asof_dates=marks, pool=pool,
        plugin_overrides={pid: baseline_script_id}, costs=costs,
        _pools_for_test=(_pools_for or {}).get("base"))

    deltas = [c - b for c, b in zip(cand, base)]
    return split_train_validate(deltas)


def _plugin_id_of(conn: sqlite3.Connection, script_id: int) -> str:
    from stocklab.plugin import store
    row = store.get_script(conn, script_id)
    if row is None:
        raise LookupError(f"脚本 {script_id} 不存在")
    return str(row["plugin_id"])


def _trading_days(conn: sqlite3.Connection, start: str,
                  end: str) -> list[str]:
    return [r[0] for r in conn.execute(
        "SELECT date FROM trading_calendar WHERE date BETWEEN ? AND ?"
        " ORDER BY date", (start, end))]
```

`REBALANCE_DAYS` 从 `plugin/sandbox.py` 导入会引入 `candidate → plugin` 依赖（那是对的：`candidate` 可以依赖 `plugin`）。在文件头加：

```python
from stocklab.plugin.sandbox import REBALANCE_DAYS
```

> ⚠️ 这会让 `replay.py` import `sandbox.py`，而 `sandbox.py` 又要通过注入调 `replay` —— **不是循环**（注入是参数，不是 import），但要在两边都写清。若你判断不合适，把 `REBALANCE_DAYS` 挪到一个中性位置（如 `stocklab/config/`）再各取所需，**并在报告里说明**。

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_candidate_replay.py`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add stocklab/candidate/replay.py tests/test_candidate_replay.py
git commit -m "feat(candidate): 回放 Δ 序列与训练/验证两段切分"
```

---

### Task 5: `sandbox` 接线 + `MIN_VALID_PERIODS`

**Files:**
- Modify: `stocklab/plugin/sandbox.py`
- Test: `tests/test_plugin_sandbox.py`、新建 `tests/test_sandbox_replay_wiring.py`

**Interfaces:**
- Consumes: 注入的 `replay` callable
- Produces:
  - `MIN_VALID_PERIODS: int = 120`（**替代** `MIN_VALID_DAYS`，不留别名）
  - `run_sandbox(conn, *, candidate_script_id, baseline_script_id, pool, window_start, window_end, now, replay=None) -> SandboxVerdict`
  - `ReplayFn = Callable[..., tuple[list[float], list[float]]]`

- [ ] **Step 1: 写失败的测试**

新建 `tests/test_sandbox_replay_wiring.py`：

```python
"""Task 5：sandbox 接受注入的 replay；verdict 只看验证段。"""

import pytest

from stocklab.plugin import sandbox, store
from stocklab.store.db import connect
from stocklab.store.migrate import init_db

NOW = "2026-09-20T16:00:00+08:00"
SCRIPT = ("def run(ctx):\n"
          "    return {'score': 50.0, 'pass_flag': True, 'reason': 'r',"
          " 'risk_list': []}\n")


@pytest.fixture
def conn(tmp_db):
    init_db(tmp_db)
    c = connect(tmp_db)
    for pid in ("0", "1", "2", "3", "4"):
        sid = store.insert_script(c, plugin_id=pid, version="1.0.0",
                                  source_text=SCRIPT, note=None, now=NOW)
        from stocklab.plugin import lifecycle
        lifecycle.record_submit(c, sid, actor="t", now=NOW)
        lifecycle.record_sandbox(c, sid, passed=True, reason="ok", now=NOW)
        lifecycle.approve(c, sid, actor="t", reason="ok", now=NOW)
    yield c
    c.close()


def test_constant_renamed(conn):
    assert sandbox.MIN_VALID_PERIODS == 120
    assert not hasattr(sandbox, "MIN_VALID_DAYS"), \
        "旧名不保留别名：留着会让「120 日」与「120 周期」两种读法并存"


def test_no_replay_stays_fail_closed(conn):
    """不注入 replay 时保持现状：样本 0 → INCONCLUSIVE。"""
    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2026-01-01",
                            window_end="2026-09-18", now=NOW)
    assert v.verdict == "INCONCLUSIVE"
    assert v.n_days == 0


def test_verdict_uses_validate_segment_only(conn):
    """验证段全正、训练段全负 → verdict 必须是 WIN（只看验证段）。"""
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([-0.01] * 80, [0.01] * 130)

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay)
    assert v.verdict == "WIN"
    assert v.n_days == 130, "n_days 必须是验证段周期数"


def test_insufficient_validate_periods_is_inconclusive(conn):
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([0.01] * 80, [0.01] * 119)

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay)
    assert v.verdict == "INCONCLUSIVE"
    assert "样本不足" in v.note


def test_first_version_still_inconclusive(conn):
    """首版无 baseline —— 即使注入了 replay 也不出结论。"""
    def boom(*a, **kw):
        raise AssertionError("首版不应调用 replay")

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=None,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW, replay=boom)
    assert v.verdict == "INCONCLUSIVE"


def test_train_segment_is_reported_in_detail(conn):
    """训练段的 Δ 均值要进 detail —— 过拟合标记要用它。"""
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([0.02] * 80, [0.001] * 130)

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay)
    assert v.detail["train_mean"] == pytest.approx(0.02)
    assert v.detail["validate_mean"] == pytest.approx(0.001)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_sandbox_replay_wiring.py`
Expected: FAIL —— `AttributeError: module 'stocklab.plugin.sandbox' has no attribute 'MIN_VALID_PERIODS'`

- [ ] **Step 3: 实现**

把 `stocklab/plugin/sandbox.py` 的常量与 `run_sandbox` 改为：

```python
from typing import Callable

#: 出结论所需的最少有效**调仓周期**数。
#:
#: ⚠️ **它比原来的 `MIN_VALID_DAYS = 120`（120 个交易日）严格得多** ——
#: 一个 60 日调仓周期含 60 个交易日，所以 120 个周期 ≈ 7200 个交易日 ≈ 29 年。
#: 这是「观测单元改成周期」的算术后果（设计文档 §2 D1），不是缺陷。
#: **不许为了让某个池能出结论而调低它** —— 那是改口径迁就结果。
MIN_VALID_PERIODS: int = 120

#: 注入的回放函数：`(conn, *, candidate_script_id, baseline_script_id, pool,
#: window_start, window_end, **kw) -> (训练段 Δ, 验证段 Δ)`。
#:
#: **为什么要注入而不是直接调**：回放要跑 `candidate/` 的打分内核，而
#: `plugin/` 不得 import `candidate/`（依赖方向）。注入保住分层，也沿用
#: 项目既有模式（`ingest_daily_bars` 的 `fetch` 同样注入）。
#: **不注入 → 保持 fail-closed**（空序列 → `INCONCLUSIVE`），绝不假装
#: 「没有回放」等于「没有差异」。
ReplayFn = Callable[..., tuple[list[float], list[float]]]
```

`run_sandbox` 的签名加 `replay: ReplayFn | None = None`，并把中间段改为：

```python
    if replay is None:
        train, validate = [], []
    else:
        train, validate = replay(
            conn, candidate_script_id=candidate_script_id,
            baseline_script_id=baseline_script_id, pool=pool,
            window_start=window_start, window_end=window_end)

    n_periods = len(validate)
    detail = {"rebalance_days": REBALANCE_DAYS[pool],
              "train_n": len(train), "validate_n": n_periods,
              "train_mean": (sum(train) / len(train)) if train else None,
              "validate_mean": (sum(validate) / len(validate))
                               if validate else None}

    if n_periods < MIN_VALID_PERIODS:
        return SandboxVerdict(
            verdict="INCONCLUSIVE", pool=pool, n_days=n_periods, delta=None,
            ci_low=None, ci_high=None, baseline_script_id=baseline_script_id,
            note=f"样本不足（{n_periods} 个有效调仓周期 < "
                 f"{MIN_VALID_PERIODS}），不构成结论", detail=detail)

    delta = sum(validate) / n_periods
    lo, hi = _bootstrap_ci(validate)
    verdict = "WIN" if lo > 0 else "LOSE"
    return SandboxVerdict(
        verdict=verdict, pool=pool, n_days=n_periods, delta=delta,
        ci_low=lo, ci_high=hi, baseline_script_id=baseline_script_id,
        note=f"验证段 Δ 周期均值 {delta:+.4%}，95% CI [{lo:+.4%}, {hi:+.4%}]，"
             f"周期数 n={n_periods}", detail=detail)
```

**删除** `_replay_daily_excess`（真回放在 `candidate/replay.py`，通过注入调用），并更新模块 docstring 里「首版没有 baseline」「有效交易日」等措辞为「周期」。

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_sandbox_replay_wiring.py tests/test_plugin_sandbox.py`
Expected: PASS

> `tests/test_plugin_sandbox.py` 里引用 `MIN_VALID_DAYS` 与 `n_days` 的断言需跟随改名与新语义更新 —— **这是预期的破坏**，不是「改测试迁就」。

- [ ] **Step 5: 全量测试**

Run: `.venv/bin/python -m pytest`
Expected: 全绿

- [ ] **Step 6: 提交**

```bash
git add stocklab/plugin/sandbox.py tests/test_sandbox_replay_wiring.py tests/test_plugin_sandbox.py
git commit -m "feat(plugin): sandbox 接受注入回放；门槛改为 120 个调仓周期，verdict 只看验证段"
```

---

### Task 6: `train_gap` 驱动的 `overfit_flag`

**Files:**
- Modify: `stocklab/plugin/sandbox.py`
- Test: `tests/test_sandbox_replay_wiring.py`

**Interfaces:**
- Produces: `OVERFIT_TRAIN_GAP: float = 0.005`；`overfit_flag(train_mean, validate_mean) -> str | None`

- [ ] **Step 1: 写失败的测试**

在 `tests/test_sandbox_replay_wiring.py` 末尾追加：

```python
# ---------- Task 6：train_gap 驱动 overfit ----------

def test_train_gap_threshold_constant():
    assert sandbox.OVERFIT_TRAIN_GAP == 0.005


def test_train_better_than_validate_is_suspected():
    """过拟合的形态：训练段比验证段好看。"""
    assert sandbox.overfit_flag(0.02, 0.001) == "suspected"


def test_validate_better_than_train_is_not_flagged():
    assert sandbox.overfit_flag(0.001, 0.02) is None


def test_small_gap_is_not_flagged():
    assert sandbox.overfit_flag(0.006, 0.005) is None      # gap=0.001 < 0.005


def test_gap_exactly_at_threshold_is_not_flagged():
    assert sandbox.overfit_flag(0.01, 0.005) is None       # gap=0.005


def test_missing_inputs_give_none():
    assert sandbox.overfit_flag(None, 0.01) is None
    assert sandbox.overfit_flag(0.01, None) is None
    assert sandbox.overfit_flag(None, None) is None


def test_old_ci_width_heuristic_is_gone():
    """旧的 CI 宽度启发式必须删除 —— 留着会让两种判据打架。"""
    import inspect
    src = inspect.getsource(sandbox)
    assert "ci_high - ci_low" not in src
    assert "2 * abs(delta)" not in src


def test_verdict_carries_overfit_flag(conn):
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([0.05] * 80, [0.001] * 130)

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay)
    assert v.detail["overfit_flag"] == "suspected"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_sandbox_replay_wiring.py -k overfit`
Expected: FAIL

- [ ] **Step 3: 实现**

把 `overfit_flag` 替换为：

```python
#: 训练段比验证段好看的容忍上限。
#:
#: ⚠️ **这个数是我定的**（设计文档 §8.3），来源是「两段差距到了半个
#: 百分点，多半不是噪声」这个直觉的数值化 —— 编不出来源。与 ADR-012
#: 的跌破线、ADR-016 的断更阈值同属**自创取值**，已列在 ADR-017 里。
OVERFIT_TRAIN_GAP: float = 0.005


def overfit_flag(train_mean: float | None,
                 validate_mean: float | None) -> str | None:
    """过拟合标记：**训练段比验证段好看**超过阈值即 `'suspected'`。

    方向固定 —— `train_gap = train_mean − validate_mean`，只有 **> 0**
    （训练段更好）才是过拟合的形态；验证段反而更好不标记。

    返回：
    - ``'suspected'`` — 命中，建议人工复查。
    - ``None``        — 输入缺失或未命中。两种「无标记」统一用 ``None``，
      避免下游真值判断把 ``'none'`` 当成阳性。
    """
    if train_mean is None or validate_mean is None:
        return None
    if train_mean - validate_mean > OVERFIT_TRAIN_GAP:
        return "suspected"
    return None
```

并在 `run_sandbox` 里把 flag 写进 `detail`（`detail["overfit_flag"] = overfit_flag(train_mean, validate_mean)`），两条返回路径**都要写**（含 `INCONCLUSIVE` 那条）。

**调用方**：`cli/plugin.py` 里原来传 `overfit_flag(v.delta, v.ci_low, v.ci_high)` 的地方改为读 `v.detail.get("overfit_flag")`。

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_sandbox_replay_wiring.py tests/test_cli_plugin.py`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add stocklab/plugin/sandbox.py stocklab/cli/plugin.py tests/test_sandbox_replay_wiring.py
git commit -m "feat(plugin): overfit_flag 改由 train_gap 驱动，删除 CI 宽度启发式"
```

---

### Task 7: 报告——双超额与 script_id 留痕

**Files:**
- Modify: `stocklab/candidate/replay.py`、`stocklab/plugin/sandbox.py`
- Test: `tests/test_sandbox_replay_wiring.py`

**Interfaces:**
- Produces:
  - `replay.benchmark_excess(conn, *, asof_dates, pool, plugin_overrides, benchmark="sh000300", costs=None) -> float`
  - `SandboxVerdict.detail["candidate_excess_index300"]` / `["baseline_excess_index300"]` / `["scripts"]`

- [ ] **Step 1: 写失败的测试**

在 `tests/test_sandbox_replay_wiring.py` 末尾追加：

```python
# ---------- Task 7：双超额与 script_id 留痕 ----------

def test_verdict_records_both_benchmark_excess(conn):
    """两版相对 index_300 的超额都要在 detail 里 —— 铁律要求「跑不赢就明说」。"""
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([0.01] * 80, [0.01] * 130)

    def fake_excess(conn, *, asof_dates, pool, plugin_overrides, **kw):
        return 0.05 if plugin_overrides else -0.05

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay, benchmark_excess=fake_excess)
    assert "candidate_excess_index300" in v.detail
    assert "baseline_excess_index300" in v.detail


def test_verdict_records_script_ids(conn):
    """回放时各插件的 script_id 要留痕 —— 否则同一份回放重跑对不上。"""
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([0.01] * 80, [0.01] * 130)

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay)
    assert v.detail["scripts"]["candidate"] == 1
    assert v.detail["scripts"]["baseline"] == 2
    assert v.detail["scripts"]["active"], "其余插件的 active 版本也要记"


def test_win_verdict_can_still_report_negative_excess(conn):
    """「新版跑赢旧版、但两者都跑输指数」必须能同时看见。"""
    def fake_replay(conn, *, candidate_script_id, baseline_script_id, pool,
                    window_start, window_end, **kw):
        return ([0.02] * 80, [0.01] * 130)

    def neg_excess(conn, *, asof_dates, pool, plugin_overrides, **kw):
        return -0.03

    v = sandbox.run_sandbox(conn, candidate_script_id=1, baseline_script_id=2,
                            pool="short", window_start="2015-01-01",
                            window_end="2026-09-18", now=NOW,
                            replay=fake_replay, benchmark_excess=neg_excess)
    assert v.verdict == "WIN"
    assert v.detail["candidate_excess_index300"] < 0
    assert v.detail["baseline_excess_index300"] < 0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_sandbox_replay_wiring.py -k "excess or scripts"`
Expected: FAIL —— `TypeError: run_sandbox() got an unexpected keyword argument 'benchmark_excess'`

- [ ] **Step 3: 实现**

**3a.** `sandbox.run_sandbox` 再加一个注入参数 `benchmark_excess: Callable | None = None`，并在返回前补齐 detail：

```python
    detail["scripts"] = {
        "candidate": candidate_script_id,
        "baseline": baseline_script_id,
        "active": {pid: active_script_id(conn, pid)
                   for pid in ("0", "1", "2", "3", "4")},
    }
    if benchmark_excess is not None:
        marks = rebalance_marks(conn, pool=pool, start=window_start,
                                end=window_end) if rebalance_marks else []
        detail["candidate_excess_index300"] = benchmark_excess(
            conn, asof_dates=marks, pool=pool,
            plugin_overrides={pid: candidate_script_id})
        detail["baseline_excess_index300"] = benchmark_excess(
            conn, asof_dates=marks, pool=pool,
            plugin_overrides={pid: baseline_script_id})
```

> **调仓日从哪来**：`sandbox.py` 不能 import `candidate/replay.py`（依赖方向），所以调仓日也走注入。`run_sandbox` 再加一个 `rebalance_marks: Callable | None = None` 参数（签名 `(conn, *, pool, start, end) -> list[str]`），`cli/plugin.py` 传 `replay.rebalance_marks`。未注入时 `marks=[]` → 两个超额都退化为 `0.0`，**不假装算过**。
>
> 若你判断四个注入参数（`replay` / `benchmark_excess` / `rebalance_marks`）太散，把 `replay` 与 `benchmark_excess` 收成一个对象由 CLI 传 —— 但**必须在报告里说明改了什么**。
>
> `candidate/replay.py` 要据此加一个公开薄封装（不要在 CLI 里拼内部函数）：
>
> ```python
> def rebalance_marks(conn: sqlite3.Connection, *, pool: str, start: str,
>                     end: str) -> list[str]:
>     """窗口内的调仓日序列。供 `sandbox` 经注入调用（它不能 import 本模块）。"""
>     days = _trading_days(conn, start, end)
>     return rebalance_dates(days, period=REBALANCE_DAYS[pool],
>                            start=start, end=end)
> ```

**3b.** `candidate/replay.py` 追加：

```python
#: 基准指数代码（腾讯口径，见 ADR-003 的指数源）。
BENCHMARK_CODE = "sh000300"


def benchmark_excess(conn: sqlite3.Connection, *, asof_dates: list[str],
                     pool: str, plugin_overrides: dict[str, int] | None = None,
                     benchmark: str = BENCHMARK_CODE,
                     costs: CostModel | None = None) -> float:
    """候选池相对基准指数的**超额收益**（同区间、同调仓日）。

    铁律要求「任何策略必须与 index_300 比较，跑不赢就明说」—— 所以这个
    数与版本 Δ **并列报告**，不是替代。
    """
    if len(asof_dates) < 2:
        return 0.0
    pool_r = period_returns(conn, asof_dates=asof_dates, pool=pool,
                            plugin_overrides=plugin_overrides, costs=costs)
    bench_r: list[float] = []
    for d0, d1 in zip(asof_dates, asof_dates[1:]):
        a, b = _close_on(conn, benchmark, d0), _close_on(conn, benchmark, d1)
        bench_r.append((b / a - 1.0) if (a and b) else 0.0)
    if not pool_r:
        return 0.0
    return sum(pool_r) / len(pool_r) - sum(bench_r) / len(bench_r)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_sandbox_replay_wiring.py`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add stocklab/plugin/sandbox.py stocklab/candidate/replay.py tests/test_sandbox_replay_wiring.py
git commit -m "feat(plugin): 沙盒报告两版相对 index_300 的超额与回放时的 script_id"
```

---

### Task 8: `plugin sandbox` CLI 接线

**Files:**
- Modify: `stocklab/cli/plugin.py`
- Test: `tests/test_cli_plugin.py`

**Interfaces:**
- Consumes: `replay.replay_period_deltas`、`replay.benchmark_excess`
- Produces: `cmd_plugin_sandbox`；子命令 `plugin sandbox <script_id> [--pool short|mid|long] [--window-start D] [--window-end D]`

- [ ] **Step 1: 写失败的测试**

在 `tests/test_cli_plugin.py` 末尾追加：

```python
# ---------- Task 8：plugin sandbox 子命令 ----------


def test_sandbox_cli_runs_and_reports(db, capsys):
    code, out = run(db, "plugin", "sandbox", "1", "--pool", "short",
                    "--window-start", "2015-01-01",
                    "--window-end", "2026-09-18", "--now", NOW, capsys=capsys)
    # 库里没有行情 → 回放得到空序列 → INCONCLUSIVE，但命令本身成功
    assert code == 0
    assert "INCONCLUSIVE" in out


def test_sandbox_cli_unknown_pool_fails(db, capsys):
    code, out = run(db, "plugin", "sandbox", "1", "--pool", "nope",
                    "--now", NOW, capsys=capsys)
    assert code != 0


def test_sandbox_cli_first_version_has_no_baseline(db, capsys):
    code, out = run(db, "plugin", "sandbox", "1", "--pool", "short",
                    "--now", NOW, capsys=capsys)
    assert code == 0
    assert "baseline" in out.lower() or "INCONCLUSIVE" in out
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_cli_plugin.py -k sandbox`
Expected: FAIL —— argparse 不认识 `sandbox` 子命令

- [ ] **Step 3: 实现**

在 `stocklab/cli/plugin.py` 加：

```python
def cmd_plugin_sandbox(args) -> int:
    from stocklab.candidate import replay
    from stocklab.plugin import sandbox

    conn = _open(args)
    try:
        sid = int(args.script_id)
        row = store.get_script(conn, sid)
        if row is None:
            print(f"❌ 脚本 {sid} 不存在", file=sys.stderr)
            return 1
        baseline = lifecycle.active_script_id(conn, row["plugin_id"])
        verdict = sandbox.run_sandbox(
            conn, candidate_script_id=sid, baseline_script_id=baseline,
            pool=args.pool, window_start=args.window_start,
            window_end=args.window_end, now=_now(args.now),
            replay=replay.replay_period_deltas,
            benchmark_excess=replay.benchmark_excess)
        print(f"verdict={verdict.verdict} pool={verdict.pool} "
              f"n_periods={verdict.n_days}")
        print(verdict.note)
        d = verdict.detail
        if d.get("candidate_excess_index300") is not None:
            print(f"候选版本相对 index_300 超额："
                  f"{d['candidate_excess_index300']:+.4%}")
            print(f"基线版本相对 index_300 超额："
                  f"{d['baseline_excess_index300']:+.4%}")
        if d.get("overfit_flag") == "suspected":
            print("⚠️ 疑似过拟合（训练段明显好于验证段）")
        print(f"回放时插件版本：{d.get('scripts')}")
        return 0
    finally:
        conn.close()
```

在 `build_parser` 的 `plugin` 子命令组追加：

```python
    pl_sandbox = pl_sub.add_parser(
        "sandbox", help="回放对比：该版本 vs 当前 active（离线只读）")
    pl_sandbox.add_argument("script_id")
    pl_sandbox.add_argument("--pool", default="short",
                            choices=["short", "mid", "long"])
    pl_sandbox.add_argument("--window-start", default="2015-01-01")
    pl_sandbox.add_argument("--window-end",
                            default=datetime.now(timezone.utc).date().isoformat())
    pl_sandbox.add_argument("--now", help="覆盖当前时刻（测试用）")
    pl_sandbox.add_argument("--db")
    pl_sandbox.set_defaults(func=cmd_plugin_sandbox)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_cli_plugin.py`
Expected: PASS

- [ ] **Step 5: 全量测试 + 验证脚本**

Run: `.venv/bin/python -m pytest` → 全绿
Run: `bash scripts/verify.sh` → `✅ 验证通过: all`

- [ ] **Step 6: 提交**

```bash
git add stocklab/cli/plugin.py tests/test_cli_plugin.py
git commit -m "feat(cli): plugin sandbox 子命令接上真实回放"
```

---

### Task 9: 历史补到上市首日

**Files:**
- Create: `scripts/extend_history.py`
- Create: `tests/test_extend_history.py`

**Interfaces:**
- Produces: `scripts/extend_history.py` —— 对 21 只逐个分页回溯到最早可得，写 `bars_daily`

- [ ] **Step 1: 写失败的测试**

新建 `tests/test_extend_history.py`（**离线**，用假 client）：

```python
"""Task 9：历史扩充脚本的分页与 fail-closed（离线，注入假 client）。"""

import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))

from extend_history import extend_one          # noqa: E402


class _FakeClient:
    """按 `end` 锚点回放：每次返回更早的一页，直到空。"""

    def __init__(self, pages):
        self.pages = pages          # list[list[dict]]，从新到旧
        self.calls = []

    def get_text(self, url, *, headers, source, cache_key):
        import json
        self.calls.append(cache_key)
        i = len(self.calls) - 1
        rows = self.pages[i] if i < len(self.pages) else []
        return json.dumps({"data": {"sz000333": {"qfqday": rows}}})
```

> 具体的解析入口以 `data/sources/tencent.py` 的实际形状为准；实现时按它调整，并在报告里写明。

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_extend_history.py`
Expected: FAIL —— `ModuleNotFoundError: No module named 'extend_history'`

- [ ] **Step 3: 实现**

新建 `scripts/extend_history.py`（**唯一允许联网的入口**，开头注明它不属于测试）：

```python
"""把种子标的的日K 分页回溯到**上市首日**（联网，手工运行，不属于测试）。

    .venv/bin/python scripts/extend_history.py [--db data/stocklab.db]

## 为什么要往更早拉

沙盒回放的观测单元是**调仓周期**，门槛 120 个周期。60 日调仓要凑够
120 个验证周期需约 24000 个交易日 —— 历史越深，越可能跨过门槛。

## ADR-003 的坑（必须遵守）

腾讯 qfq 上限 800 且 **801–2000 区间会静默降级回 640 且不报错** →
只采**不复权**（`adj_mode='none'`）；写入口对 `adj_mode != 'none'` 早已
硬拒绝，保持。用 `end` 锚点分页（`beg` 被服务端忽略）。

## fail-closed

翻页过程中任一页解析为 0 行但**未到最早可得日** → 抛错，不静默截断。
采完打印**每只的最早期与行数**，供报告核对是否真的回溯到了上市首日。
"""

from __future__ import annotations

import argparse
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from stocklab.candidate.seeds import SEED_CODES                # noqa: E402
from stocklab.config import paths                              # noqa: E402
from stocklab.data.http import HttpClient                      # noqa: E402
from stocklab.data.ingest import ingest_daily_bars             # noqa: E402
from stocklab.store.db import connect                          # noqa: E402
from stocklab.store.migrate import ensure_schema               # noqa: E402


def extend_one(conn, client, code: str, *, now: str) -> tuple[str, int]:
    """把一只标的的历史拉到最早可得，返回 `(最早期, 新增行数)`。

    分页能力**已内置**在 `data/fetch.py::fetch_daily_bars`（docstring：
    「翻页锚点从 `end` 开始，逐页向前推到返回空页或超出 `max_pages`」），
    所以这里**不另写翻页** —— 只是把 `start` 放到足够早（1990-01-01，
    A 股最早的开市年），让它一直翻到源站返回空页为止。

    ADR-003：`adj=""`（不复权）是唯一允许的口径；腾讯 qfq 在 801–2000
    区间会**静默降级**，所以绝不能传 `adj="qfq"`。
    """
    from stocklab.config.universe import DEFAULT_UNIVERSE, instrument_type
    from stocklab.data.fetch import fetch_daily_bars
    from stocklab.data.ingest import ingest_daily_bars
    from stocklab.data.models import Instrument as _I    # noqa: F401

    # 腾讯口径的代码（sz000333 / sh600690）
    inst = next(i for i in SEED_UNIVERSE if i.code == code)
    rows = fetch_daily_bars(client, code=inst.tencent_code,
                            start="1990-01-01", end=_today(), adj="")
    report = ingest_daily_bars(conn, client, None, [inst],
                               start="1990-01-01", end=_today(), now=now,
                               fetch=lambda _code: rows)
    earliest = min((b.date for b in rows), default="")
    return earliest, report.bars_written


def _today() -> str:
    return __import__("datetime").date.today().isoformat()


def main(argv=None) -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--db", default=str(paths.DB_PATH))
    args = ap.parse_args(argv)
    ...   # 逐只调 extend_one；打印每只的最早期与行数；结束时汇总
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_extend_history.py`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add scripts/extend_history.py tests/test_extend_history.py
git commit -m "feat(scripts): 把种子标的日K 分页回溯到上市首日（联网手工运行）"
```

---

### Task 10: 真实回放验收 + ADR

**Files:**
- Create: `docs/decisions/2026-09-20-ADR-017-沙盒回放口径.md`
- Create: `docs/plans/2026-09-20-沙盒真回放-验收记录.md`

- [ ] **Step 1: 跑历史扩充**

```bash
cp /tmp/fin-acceptance.db /tmp/replay.db
.venv/bin/python scripts/extend_history.py --db /tmp/replay.db
```

记录**每只的最早期与行数**（证明真的回溯了，而不是被静默截断）。

- [ ] **Step 2: 跑真实回放**

```bash
# 短期池（5 日调仓）
.venv/bin/python -m stocklab.cli.main plugin sandbox <候选script_id> \
  --pool short --db /tmp/replay.db
# 中期池（20 日）
.venv/bin/python -m stocklab.cli.main plugin sandbox <候选script_id> \
  --pool mid --db /tmp/replay.db
# 长期池（60 日）
.venv/bin/python -m stocklab.cli.main plugin sandbox <候选script_id> \
  --pool long --db /tmp/replay.db
```

- [ ] **Step 3: 写验收记录**

`docs/plans/2026-09-20-沙盒真回放-验收记录.md`，写**真实输出**：

- 历史扩充后的每只**最早期与行数**
- 三个池各自的：**总周期数 / 训练段 / 验证段 / verdict / Δ / CI**
- **两版相对 index_300 的超额**（两个数都要）
- 回放时各插件的 script_id
- 回放前后 `candidate_*` 三表**行数不变**的实测计数
- **必须写明**：长期池若 `INCONCLUSIVE`，原因是「60 日调仓在可得历史里独立周期不足 120」，**不是**回放有问题；**不许**为了让长期池出结论而调低门槛

**如实记录失败**。某只拉不到更早历史就说哪只、到哪年；某个池算不出来就说哪个、为什么。

- [ ] **Step 4: 写 ADR**

`docs/decisions/2026-09-20-ADR-017-沙盒回放口径.md`，照 ADR-011/016 的格式记：观测单元（调仓周期）、训练/验证 70/30、下游规则（等权/空池持现金/成本/涨跌停）、门槛单位（120 周期，及它比 120 交易日严格得多）、**自创阈值 `OVERFIT_TRAIN_GAP = 0.005`**、以及「长期池在可预见历史长度内无结论」这个已接受的后果。

- [ ] **Step 5: 全量验证**

Run: `.venv/bin/python -m pytest` → 全绿
Run: `bash scripts/verify.sh` → `✅ 验证通过: all`

- [ ] **Step 6: 提交**

```bash
git add "docs/decisions/2026-09-20-ADR-017-沙盒回放口径.md" \
        "docs/plans/2026-09-20-沙盒真回放-验收记录.md"
git commit -m "docs: 沙盒回放口径 ADR + 真实回放验收记录"
```

---

## 收尾检查

- [ ] `.venv/bin/python -m pytest` 全绿，总数只增不减
- [ ] `bash scripts/verify.sh` → `✅ 验证通过: all`
- [ ] 设计文档 §10 的测试组**每一组都能指出对应测试**
- [ ] 模块2 冻结：`git diff --name-only <本轮起点>..HEAD` 里没有 `predict/`/`verify/`/`chain/`/`paper/`/`portfolio/`
- [ ] 回放前后 `candidate_*` 三表行数不变（验收记录里有实测计数）
- [ ] 出现新教训 → 写进 `docs/errors/ERROR_DIARY.md`
