# 候选池页面（模块1 展示层）—— 实现与验收记录

- **日期**：2026-09-21
- **对象**：`docs/superpowers/specs/2026-09-21-候选池页面-design.md`（rev2）
  ＋ `docs/plans/2026-09-21-候选池页面设计评审.md` 的 9 条最小改动清单
- **代码基线**：`feat/financials`，起点 `83766b8`（本记录随实现一起提交）
- **定位**：实现完成、验证通过；**下文 §5 的三处偏离**需要你过一眼（都已写进代码注释与 ERROR_DIARY）

---

## 0. 结论

模块1 现在有页面了：`<base>/candidate`（导航栏「候选池」）—— 回答「**系统选出了什么、为什么**」。

- 评审的 9 条改动全部落地（W1 默认值 / W2 token 用 `ctx.lab.asof` / W3 `(asof, run_kind)` 组合 /
  W4 读 `instruments` 取名 / S1 未出现差集 / S2 机器名映射 / S3 UTC→+08 / S4 共享落报告 / S5+S6 写进设计）；
- **一行 CSS 没加**，复用 `layout` / `table.tbl` / `tr.empty` / `more` / `banner`；
- **三份既有 labweb 测试文件一条没改**（模块2 零回归）；
- 新增测试 **20 条**，全套 `pytest` 与 `bash scripts/verify.sh` 通过。

### 交付物

| 文件 | 说明 |
|---|---|
| `stocklab/labweb/cand_data.py`（新，≈210 行） | `CandLab`：只读三表 + `instruments` 取名；`SnapshotKey`；UTC→+08；`risk_json` 解码口径 |
| `stocklab/labweb/cand_render.py`（新，≈330 行） | 页面渲染：三池分区、淘汰分组、未出现差集、跑一次的写路径表单、快照选择器 |
| `stocklab/labweb/app.py` | `Context.cand`（**末位可选**）、`GET /candidate`、`POST /candidate/run`、`handle()` docstring |
| `stocklab/labweb/render.py` | `NAV_ITEMS` +1 行（`/candidate` 候选池）；渲染逻辑一行未动 |
| `stocklab/labweb/__init__.py` | 组成表补两行（模块1 取数 / 渲染分文件） |
| `stocklab/cli/main.py` | `cmd_lab_serve` 注入 `cand=CandLab(db)` + 启动横幅打印候选池 URL |
| `stocklab/candidate/snapshot.py` | `+ list_snapshot_keys(conn)`（唯一新写的 SQL，按分层落在数据层而不是页面里） |
| `stocklab/candidate/report.py` | `+ report_path()` / `write_report_file()`（**文件名规则只有这一处**） |
| `stocklab/cli/candidate.py` | 默认报告路径改走共享函数（`--out` 仍是显式文件覆盖） |
| `tests/test_labweb_candidate.py`（新，20 条） | 见 §3 |
| `docs/errors/ERROR_DIARY.md` | **#45**：写路径的线程模型教训（见 §5.1） |
| `docs/ops/cron-2026-09-21-待挂.md`（新） | 定时任务的**成品命令**（待拍板，一条未挂）：见 §7 第 4 条 |

---

## 1. 页面内容（与设计一致）

- **当前快照**块：快照 `#id` / `asof · run_kind` / 参数（种子 21 只 · TOPN 短6 中8 长5）/
  生成于（**+08，并同时标注库中 UTC 原值**）/ 成员行数 · 淘汰条数 / 报告路径；
- **快照选择器**：一条一个链接（`?asof=…&run_kind=…`），当前那条带 `aria-current`；
- **三池分区**：代码 / 名称 / 原始分 / 调整分 / 理由 / 风险 / 状态；表格按
  `load_snapshot` 的 `(pool, adj_score DESC, code)` 顺序，**页面不重排**；
- **淘汰记录**：按 `stage` 分组（盘前排雷 / 行业排雷 / 打分未过），条数在首屏、明细进 `<details>`；
- **未出现差集**：`SEED_CODES − 成员 − 淘汰`，附「这是差额不是原因」的说明；
- **诚实性**：`risk_json == []` → 「无」；解码失败 → 「风险明细解析失败（原文…）」；
  `plugin_id IS NULL` → 「未知」；空池 → 「本池为空」（不写 0）；无快照 → 「还没有跑过候选池」（不显示空表格）；
  请求的 `(asof, run_kind)` 不存在 → **回落到最新一条并明说**（不是一个走不到的 404）。

## 2. 写路径（`POST /candidate/run`）

1. `_token` **先于一切**：失败 403，库一个字节不动（有测试钉住）；
2. token 一律 `ctx.lab.asof` 签发/校验（§3.1）—— 用**快照 asof** 签的 token 有反向用例钉住必须 403；
3. 参数校验：`asof` 走 `date.fromisoformat`（严格 `YYYY-MM-DD`）、`run_kind ∈ {light, weekly, quarterly}`，
   非法 → **400 且不写库**；
4. `ensure_schema()`（P33）→ 起子进程跑 `candidate run`（见 §5.1）→ 读回 `snapshot_id`；
5. 303 回 `GET /candidate?asof=…&run_kind=…&ran=new|exists&sid=…`（PRG；刷新不重发），
   页面据 `ran` 给横幅：「已跑出新快照 #N」或「已有快照 #N，**未重跑**（幂等）」；
6. 失败（超时 / 非零退出码 / 退出码 0 却没落快照）→ **500 + 子进程原话**，不吞错。

## 3. 验证证据（全部本机实测）

### 3.1 测试

| 项 | 结果 |
|---|---|
| `tests/test_labweb_candidate.py` | **20 passed** |
| `tests/test_labweb_app.py` + `test_labweb_overview.py` + `test_labweb_actions.py` | **全部通过，零改动**（模块2 无回归） |
| 全套 `pytest` | 通过（无红项；汇总行被 `addopts -q` 吃掉，按 `scripts/verify.sh` 的规矩不叠 `-q`） |
| `bash scripts/verify.sh` | 通过（文档结构 / 错误日记 / shell 语法 / 测试 / 忽略路径 / 红线 / 工作区七项） |

对应用例（20 条覆盖评审 §5 的全部修正）：

- 渲染：三池都在 + 分数/名称/代码在表里 + **UTC 已转 +08**（同时保留库中 UTC 原值）；
- 渲染：空池写「本池为空」且该段**不出现 0**；
- 渲染：淘汰三环节分组、条数与库一致；**认不出的 stage 原样显示机器名**（另有一条单元测试钉 `stage_label`）；
- 渲染：未出现差集条数 = 种子 − 去重入池 − 淘汰，且入池/淘汰那几只**不在**差集块里；
- 渲染：`risk_json == []` → 「无」；`plugin_id IS NULL` → 「未知」；`risk_json` 坏 → 「解析失败」；
- 渲染：无快照 → 明说且**不出现空表格**；库不存在 → 200 + 「数据库文件不存在」（**直接调 `handle()`**，该分支正常跑不到）；
- 渲染：选择器列出每个 `(asof, run_kind)`；同 asof 两种 kind **各取各的**；
- 渲染：请求的组合不存在 → 回落 + 明说；
- 写：无 token → 403 且 `candidate_snapshots` 行数不变；**快照 asof 签的 token → 403**；
- 写：新跑 → 303 + 快照 +1 + 成员/淘汰行数 > 0；**也落 `reports/candidate/<asof>-<kind>.md`**；
- 写：幂等 → 只有 1 行，第二次响应里出现「未重跑 / 已有快照」；
- 写：坏参数（`2026-13-99` / `hourly`）→ 400 且不写库；
- **同源**：**两个独立库**、同一 asof，一个走 CLI、一个走页面 → 成员与淘汰**逐行相同**
  （不是同一个库跑两次 —— 那样比的是幂等加载器）；
- 守卫：`ctx.cand` 未注入 → 500 + 可读文案（不是 `AttributeError`）。

### 3.2 真实库端到端冒烟（真起服务 + 真 POST，跑在**库副本**上）

```
cp data/stocklab.db /tmp/labsmoke.db
python -m stocklab.cli.main lab serve --db /tmp/labsmoke.db --port 8799 --asof 2026-09-21
GET  /lab/candidate                    → 200（8532 bytes；三池都在；「未出现（9 只）」）
POST /lab/candidate/run asof=2026-09-10 weekly → 303（跟回列表页：19 行 / 淘汰 0）
       报告落盘 reports/candidate/2026-09-10-weekly.md ✅
POST 再来一次（同参数）                → 303 ran=exists（幂等，库里仍是一条）
POST 无 _token                         → 403
库中快照：[(1,2026-09-18,weekly), (2,2026-06-15,weekly), (3,2026-09-10,weekly)]
```

### 3.3 真实库的渲染对账（`data/stocklab.db`，只读）

2 条快照 / 38 行成员 / 淘汰 0 条 → 页面：短 6 / 中 8 / 长 5（快照 #2）、「未出现（9 只）」，
「淘汰记录」明说「一条都没有记录」并指向未出现块。

---

## 4. 库路径：`data/stocklab.db` 已建（可逆）

- 之前 `scripts/check_redlines.py` 的真实库红线**一直静默跳过**，因为 `data/stocklab.db` 不存在；
  唯一有完整数据的库是 `/tmp/replay.db`（会随系统清理）。
- 本次用 `sqlite3 /tmp/replay.db ".backup data/stocklab.db"`（**一致性备份**，不是 `cp`）建库：
  两份 `integrity_check=ok`、39 表 / 快照 2 / 成员 38 / 财报 1443 / K 线 92446 / 插桩 7 —— **逐项一致**。
- `/tmp/replay.db` **原样保留**（未删未写）。若你决定不迁，删掉 `data/stocklab.db` 即回到原状
  （`data/backups/replay-2026-09-21.db` 那份保护副本仍在）。
- ⚠️ **迁移的直接后果**：真实库红线第一次真的跑起来了，而且**是红的** —— 见 §6（那是旧账，不是本次改动）。

---

## 5. 与设计稿的三处偏离（都要你过一眼）

### 5.1 写路径改成**子进程跑 CLI**（必须改，否则按钮点不了）

设计稿写的是「请求线程里直接调 `run_candidate`」。**实测做不到**：插桩执行器的超时用
`signal.setitimer`，而 `signal` 只允许主线程调用（`stocklab/plugin/runtime.py` 的模块 docstring
早就写明这条约束），labweb 是 `ThreadingHTTPServer`，请求都在工作线程里 ——
实测第一个插桩调用就抛 `ValueError: signal only works in main thread`，
客户端只看到 `RemoteDisconnected`（服务端有完整 traceback，客户端啥也没有）。

改法：`POST` 起子进程 `python -m stocklab.cli.main candidate run --db … --asof … --run-kind … --out …`
（`cwd` = 仓库根，`PYTHONPATH` 显式给）。子进程的主线程满足 signal 前提，**沙盒一行不改**，
页面与命令行仍是同一段生产代码（§3.1 的「同源」用例就是钉这条）。附超时 300s（实测一次 <1 秒）。
已记入 `docs/errors/ERROR_DIARY.md` **#45**。

### 5.2 `write_report_file` 落在 `candidate/report.py`，不是 `cli/candidate.py`

设计 §4.2 写的是 `cli/candidate.py`，评审说「`candidate/run.py` 或 `cli/candidate.py` 都行」。
我放在 `candidate/report.py`：文件名规则与「渲染报告」同属报告模块，
且 `labweb` 不该依赖 `cli`（跨层依赖会绕回来）。CLI 与页面都调它，**产出物仍同源**。

### 5.3 快照选择器是**链接**，不是 `<select>`

原生 `<select>` 只能提交一个参数，而快照的唯一键是**两个**（`asof` + `run_kind`）——
要塞进一个控件就得自己发明分隔符并再解析一遍。链接既保住 §3.2 的 URL 契约
（`?asof=…&run_kind=…`），又零 JS。当前库里 2 条快照，一屏内可点。

> 另两处**与设计一致、但值得记一句**：「最新」= `created_at` 降序（同秒按 `(asof, run_kind)` 字典序）；
> 「已存在 / 新跑」在子进程方案下改为「跑之前查一次」（`RunResult.skipped` 跨进程拿不到）。

---

## 6. ⚠️ 顺带发现：真实库回归红线**是红的**（旧账，不是本次改动）

修好库路径后第一次真跑 `scripts/check_redlines.py`：

| 目标 | 期望 sha256（2026-09-16 基线） | 实际 sha256 |
|---|---|---|
| `predict_real_2026-09-14` | `b69afb711f60ec53…` | `2f623027d5e6ae5f…` |
| `backfill_real_2013-12-23_2026-09-14` | `18f2eeeff7168d41…` | `5b0d190e01f6c45a…` |

**归因实验已做**（基线文件要求的第①步）：在 `git worktree add /tmp/basecheck 83766b8`（**未含本次改动**）
＋ 同一份库副本上重跑，两个 sha **逐字节相同**（`2f623027…` / `5b0d190e…`）
⇒ **与本次改动无关**，是 09-16 之后「**库/标的清单变了**」：差异里
`n_bars_used 6062 → 7807`、`.evidence.000651.*` 整段新增（种子清单扩容）。

**没有** `--regen`（基线文件明确禁止「红了就重基线」）。要不要重新基线、以及新值怎么 append
到 `docs/plans/2026-09-15-p8-实验流水线.md` 与 `docs/experiments/README.md` 台账 —— 等你拍板。

---

## 7. 待你拍板

| # | 事项 | 我的建议 |
|---|---|---|
| 1 | `data/stocklab.db` 保留（库路径就此统一）还是删掉回原状 | **保留**：红线、`lab serve` 默认路径、其它会话的默认库都指着它；`/tmp` 那份随时会被清 |
| 2 | §6 的两条真实库红线怎么办（重新基线 / 先查为什么库变了 / 维持红） | 先确认「库变了」是不是你已知的扩容，再 `--regen` 并把新值 append 进台账 |
| 3 | 写路径用子进程（§5.1）你认不认；要不要另开一轮把沙盒换成子进程方案（设计文档方案 B），让页面能真正在进程内跑 | 本轮的子进程写法先用着（沙盒不动）；换沙盒是**独立一轮**，别混在这里 |
| 4 | 07 的四类定任务是否重新挂上（巡检 30m / 盘中 tick / 收盘 tick / 收盘链 / 日历刷新） | 先挂**每日收盘链（含库备份）+ 30 分钟巡检**（都带「前置产物未就绪就跳过」）；**成品命令已写好在 [`docs/ops/cron-2026-09-21-待挂.md`](../ops/cron-2026-09-21-待挂.md)**，你点头即可执行 |
| 5 | 成本口径 §5.1 的档位集合是否按新注册改成 {5万,10万,50万}（反向证伪保留） | 改，且**明确写成新注册**（不动旧记录） |
| 6 | 模块2 本轮是否开工 | 建议先不开：先把页面用起来、把 07 的调度挂上 |
| 7 | 商品② 304 组价（109.9/139.9/129.9/159.9/169.9）是否改 | 抖店的事，与本次无关，但还挂着 |

---

## 8. 复现命令

```bash
cd ~/Documents/Workspace/stock

# 新页面的测试
.venv/bin/python -m pytest tests/test_labweb_candidate.py -q

# 自己看页面（用库的副本；`lab serve` 起服务时会前滚 schema）
cp data/stocklab.db /tmp/mylab.db
.venv/bin/python -m stocklab.cli.main lab serve --db /tmp/mylab.db --port 8791 --asof $(date +%F)
# → http://127.0.0.1:8791/lab/candidate

# 端到端冒烟脚本（起服务 + 真 POST + 幂等 + 403）
.venv/bin/python /tmp/lab_smoke.py     # 脚本内容见本记录 §3.2 的命令序列

# 真实库红线（现在是红的，见 §6）
.venv/bin/python scripts/check_redlines.py
```
