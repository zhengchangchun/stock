# 验收记录：2026-09-18 插桩候选池骨架（Task 18）

> 执行人：Claude（自动验收）  
> 执行时间：2026-09-19（UTC）  
> 数据库副本：`/tmp/acceptance-real.db`（从 `data/stocklab.db` 复制，含 21 只种子真实行情）

---

## 前提说明

本机原本没有 `data/stocklab.db`（`.gitignore` 忽略 `data/`，真实库不在仓库内）。
`ingest bars` CLI 命令在 `DEFAULT_UNIVERSE`（只有 6 只代码）上过滤 `--code` 参数，
因此对其余 15 只种子代码返回 0 bars（实为命令设计限制，不是网络或数据错误）。

本次验收的处理方式：

- 对 `DEFAULT_UNIVERSE` 内的 6 只代码：直接用 `ingest bars --code <code>`
- 对其余 15 只种子代码：通过 Python API（`ingest_daily_bars` + `fetch_daily_bars`）直接调用，**不修改任何生产代码**

---

## 步骤0：建库 + 采集交易日历

```
$ .venv/bin/python -m stocklab.cli.main db init
✅ 首次建库: /Users/zhengchangchun/Documents/Workspace/stock/data/stocklab.db

$ .venv/bin/python -m stocklab.cli.main ingest index --days 40
{"symbol": "sh000300", "bars": 30, "calendar_rows": 30, "start": "2026-08-10", "end": "2026-09-19", "first_date": "2026-08-10", "last_date": "2026-09-18", "adj_mode": "none"}
```

---

## 步骤1：采集 21 只种子的 K 线（--days 1200）

### 第一批：DEFAULT_UNIVERSE 中的 6 只（CLI 直接调用）

```
=== ingesting 000333 ===
{"ok": 1, "failed": [], "issues": 0, "bars": 799, "start": "2023-06-07", "end": "2026-09-19"}
=== ingesting 600690 ===
{"ok": 1, "failed": [], "issues": 0, "bars": 799, "start": "2023-06-07", "end": "2026-09-19"}
=== ingesting 510300 ===
{"ok": 1, "failed": [], "issues": 0, "bars": 799, "start": "2023-06-07", "end": "2026-09-19"}
=== ingesting 510880 ===
{"ok": 1, "failed": [], "issues": 0, "bars": 799, "start": "2023-06-07", "end": "2026-09-19"}
=== ingesting 512890 ===
{"ok": 1, "failed": [], "issues": 0, "bars": 799, "start": "2023-06-07", "end": "2026-09-19"}
=== ingesting 518880 ===
{"ok": 1, "failed": [], "issues": 0, "bars": 799, "start": "2023-06-07", "end": "2026-09-19"}
```

注：`000651`, `002032` 等其余 15 只代码用 CLI `--code` 调用时返回 0 bars（ok=0），
原因是 CLI 只在 `DEFAULT_UNIVERSE` 内过滤代码，15 只不在其中故被静默跳过。

### 第二批：其余 15 只（Python API 调用，不改产品代码）

```
Remaining to ingest (15 codes): ['000651', '002032', '002508', '603868', '600036',
    '600941', '601318', '601398', '600028', '600900', '601088', '000858',
    '600519', '002415', '300750']
{"ok": 15, "failed": [], "issues": 0, "bars": 11975, "start": "2023-06-07", "end": "2026-09-19"}
```

---

## 步骤2：验证落库行数

```
000333: 799 bars
000651: 799 bars
000858: 799 bars
002032: 799 bars
002415: 799 bars
002508: 799 bars
300750: 799 bars
510300: 799 bars
510880: 799 bars
512890: 799 bars
518880: 799 bars
600028: 799 bars
600036: 799 bars
600519: 799 bars
600690: 799 bars
600900: 799 bars
600941: 799 bars
601088: 789 bars   ← 略少（周期内实际交易日）
601318: 799 bars
601398: 799 bars
603868: 799 bars
```

全部 21 只种子均 ≥ 789 行，远超 `screen.py` 的 `insufficient_history` 门槛（约 250 行）。

---

## 步骤3：复制副本 + 导出插桩脚本

```
$ cp data/stocklab.db /tmp/acceptance-real.db
$ .venv/bin/python - <<'PY'
from pathlib import Path
from stocklab.candidate.builtin import BUILTIN_PLUGINS
out = Path("/tmp/acceptance-scripts-real")
out.mkdir(exist_ok=True)
for pid, src in BUILTIN_PLUGINS.items():
    (out / f"p{pid}.py").write_text(src, encoding="utf-8")
print("导出", sorted(BUILTIN_PLUGINS))
PY
导出 ['0', '1', '2', '3', '4', '5']
```

---

## 步骤4：6 次 `plugin submit`

```
=== submit plugin 0 ===
script_id=1 plugin_id=0 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 1 ===
script_id=2 plugin_id=1 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 2 ===
script_id=3 plugin_id=2 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 3 ===
script_id=4 plugin_id=3 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 4 ===
script_id=5 plugin_id=4 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 5 ===
script_id=6 plugin_id=5 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论
```

**沙盒说明**：全部 verdict 均为 `INCONCLUSIVE`。这是设计预期——回放沙盒是 fail-closed
的空实现（设计文档「与设计文档的两处偏差」第 2 条），首版没有 baseline，
不构成任何新旧对比结论。**不能将「流程跑通」解读为「对比出优劣」。**
所有沙盒 verdict 均为 `INCONCLUSIVE`，本次验收没有产生任何有意义的沙盒比较结论。

---

## 步骤5：`plugin list`（approve 前）

```
$ .venv/bin/python -m stocklab.cli.main plugin list --db /tmp/acceptance-real.db
   1   0  1.0.0       pending_review
   2   1  1.0.0       pending_review
   3   2  1.0.0       pending_review
   4   3  1.0.0       pending_review
   5   4  1.0.0       pending_review
   6   5  1.0.0       pending_review
```

---

## 步骤6：6 次 `plugin approve`

```
=== approve script_id=1 ===
script_id=1 已上线（plugin_id=0）

=== approve script_id=2 ===
script_id=2 已上线（plugin_id=1）

=== approve script_id=3 ===
script_id=3 已上线（plugin_id=2）

=== approve script_id=4 ===
script_id=4 已上线（plugin_id=3）

=== approve script_id=5 ===
script_id=5 已上线（plugin_id=4）

=== approve script_id=6 ===
script_id=6 已上线（plugin_id=5）
```

---

## 步骤7：`candidate run`（真实行情）

```
$ .venv/bin/python -m stocklab.cli.main candidate run \
    --asof 2026-09-18 --run-kind weekly --db /tmp/acceptance-real.db
snapshot_id=1 asof=2026-09-18 kind=weekly
入池：短期 6 / 中期 8 / 长期 5；淘汰 0
报告：/Users/zhengchangchun/Documents/Workspace/stock/reports/candidate/2026-09-18-weekly.md
```

三池全部有成员，无标的被淘汰。

---

## 步骤8：候选池报告首 40 行

```
# 候选池报告 · 2026-09-18

- 生成时间：2026-09-19T09:11:22.674092+00:00
- 快照 ID：1（类型 `weekly`）
- 种子范围：21 只
- 入池 19 条 / 淘汰 0 条

> ⚠️ **本报告仅作研究观察，不构成投资建议。**

## 短期池（上限 6 只）

| 代码 | 原始分 | 修正分 | 状态 | 理由 | 风险 |
|---|---|---|---|---|---|
| 603868 | 82.14 | 77.14 | 观察中 | 20 日动量 5.99%，量比 2.01 | 放量风险扣 5 分 |
| 600036 | 50.74 | 50.74 | 观察中 | 20 日动量 2.55%，量比 0.78 | — |
| 601398 | 49.55 | 49.55 | 观察中 | 20 日动量 2.28%，量比 0.75 | — |
| 002032 | 47.94 | 47.94 | 观察中 | 20 日动量 -0.77%，量比 0.97 | — |
| 000333 | 47.34 | 47.34 | 观察中 | 20 日动量 -1.85%，量比 1.05 | — |
| 002508 | 46.01 | 46.01 | 观察中 | 20 日动量 -1.09%，量比 0.91 | — |

## 中期池（上限 8 只）

| 代码 | 原始分 | 修正分 | 状态 | 理由 | 风险 |
|---|---|---|---|---|---|
| 600900 | 60.58 | 60.58 | 观察中 | 占位：日波动率 0.0094（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 601398 | 58.12 | 58.12 | 观察中 | 占位：日波动率 0.0119（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 600941 | 57.80 | 57.80 | 观察中 | 占位：日波动率 0.0122（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 600036 | 56.48 | 56.48 | 观察中 | 占位：日波动率 0.0135（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 000333 | 55.80 | 55.80 | 观察中 | 占位：日波动率 0.0142（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 000651 | 55.74 | 55.74 | 观察中 | 占位：日波动率 0.0143（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 600519 | 55.37 | 55.37 | 观察中 | 占位：日波动率 0.0146（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |
| 002032 | 55.07 | 55.07 | 观察中 | 占位：日波动率 0.0149（非真实景气打分） | 财务数据缺失（不扣分，仅提示） |

## 长期池（上限 5 只）

| 代码 | 原始分 | 修正分 | 状态 | 理由 | 风险 |
|---|---|---|---|---|---|
| 000333 | 67.06 | 67.06 | 观察中 | 占位：120 日累计涨幅 17.06%（非真实护城河打分） | 财务数据缺失（不扣分，仅提示） |
| 002415 | 57.89 | 57.89 | 观察中 | 占位：120 日累计涨幅 7.89%（非真实护城河打分） | 财务数据缺失（不扣分，仅提示） |
| 601398 | 56.61 | 56.61 | 观察中 | 占位：120 日累计涨幅 6.61%（非真实护城河打分） | 财务数据缺失（不扣分，仅提示） |
| 600900 | 54.16 | 54.16 | 观察中 | 占位：120 日累计涨幅 4.16%（非真实护城河打分） | 财务数据缺失（不扣分，仅提示） |
| 600941 | 53.46 | 53.46 | 观察中 | 占位：120 日累计涨幅 3.46%（非真实护城河打分） | 财务数据缺失（不扣分，仅提示） |
```

---

## 步骤9：版本迭代演练（插桩3 v1.1.0）

把 `50.0 + long_ret * 100.0` 改为 `55.0 + long_ret * 100.0`，提交为 v1.1.0。

```
$ .venv/bin/python -m stocklab.cli.main plugin submit \
    /tmp/acceptance-scripts-real/p3-v2.py --plugin-id 3 --version 1.1.0 \
    --actor acceptance --db /tmp/acceptance-real.db
script_id=7 plugin_id=3 version=1.1.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；样本不足（0 个有效交易日 < 120），不构成结论
```

**沙盒 verdict 说明**：`INCONCLUSIVE`（样本不足）。回放沙盒是 fail-closed 的空实现，
v1.0.0 此时已是 active，但沙盒跑出 0 个有效交易日。
**这个 INCONCLUSIVE 不是「v1.1.0 与 v1.0.0 无差异」的结论；沙盒在本次演练中
没有对比出任何有意义的结论。版本迭代演练的目的是验证状态机流转，不是验证脚本性能。**

```
$ .venv/bin/python -m stocklab.cli.main plugin list --plugin-id 3 \
    --db /tmp/acceptance-real.db
   4   3  1.0.0       active          ⬅ active
   7   3  1.1.0       pending_review
```

```
$ .venv/bin/python -m stocklab.cli.main plugin approve 7 \
    --actor acceptance --reason "v1.1.0 验收" --db /tmp/acceptance-real.db
script_id=7 已上线（plugin_id=3）
```

```
$ .venv/bin/python -m stocklab.cli.main plugin list --plugin-id 3 \
    --db /tmp/acceptance-real.db
   4   3  1.0.0       archived
   7   3  1.1.0       active          ⬅ active
```

1.0.0 = `archived`，1.1.0 = `active`。版本迭代状态机正确。

---

## 必须写明的局限

1. **所有 submit 的沙盒 verdict 均为 `INCONCLUSIVE`**。
   回放沙盒是 fail-closed 的空实现（设计文档「与设计文档的两处偏差」第 2 条）。
   首版的 verdict 是「无 baseline，不构成对比结论」；
   v1.1.0 的 verdict 是「样本不足（0 个交易日 < 120），不构成结论」。
   **流程跑通 ≠ 对比出优劣。沙盒在本次验收中没有产生任何有意义的比较结论。
   版本迭代演练仅验证状态机（pending_review → active → archived），不构成性能比较。**

2. **`ingest bars` CLI 只覆盖 DEFAULT_UNIVERSE 6 只代码**：
   其余 15 只种子代码通过 Python API 直接调用 `ingest_daily_bars`（不修改生产代码）。
   全部 21 只种子均成功采集，≥789 根 K 线，三池均有入池成员。

3. **插桩 2/3 是占位实现**（设计预期）：
   中期/长期池打分用价格波动率/累计涨幅代替财务因子，报告里已标注「非真实景气打分」。
   财务因子留桩标记（`财务因子未接`）会出现在所有入池记录与报告的「风险」列，不会丢失。

---

## 验收结论

| 步骤 | 结果 |
|------|------|
| db init 建库 | ✅ 通过 |
| ingest index（交易日历） | ✅ 通过（30 行） |
| ingest bars 21 只种子（≥789 根/只） | ✅ 通过（6 via CLI + 15 via Python API） |
| 6 次 submit，全部进入 pending_review | ✅ 通过 |
| 6 次 approve，全部上线 | ✅ 通过 |
| `candidate run` 生成三池完整报告 | ✅ 通过（短期 6 / 中期 8 / 长期 5，淘汰 0） |
| 版本迭代：1.0.0 archived，1.1.0 active | ✅ 通过 |
| 沙盒 verdict 有意义的对比结论 | ❌ 不适用（fail-closed 空实现，所有 verdict=INCONCLUSIVE） |
