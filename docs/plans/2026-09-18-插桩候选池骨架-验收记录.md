# 验收记录：2026-09-18 插桩候选池骨架（Task 18）

> 执行人：Claude（自动验收）  
> 执行时间：2026-09-19（UTC）  
> 数据库：`/tmp/acceptance.db`（全新建库，无历史 bar 数据）

---

## 前提说明

本机没有 `data/stocklab.db`（`.gitignore` 忽略 `data/`，真实库不在仓库内）。
`ingest bars` 命令不支持 `--db` 参数，无法把数据写入临时库。
因此本次验收在**零 bar 数据**的环境下运行，所有标的均在 `pre_screen` 阶段因
`insufficient_history` 被淘汰。这是预期行为，不是 bug。

---

## 步骤①：初始化验收库

```
$ .venv/bin/python -m stocklab.cli.main db init --db /tmp/acceptance.db
✅ 首次建库: /tmp/acceptance.db
```

---

## 步骤②：导出 6 个内置脚本到文件

```
$ .venv/bin/python - <<'PY'
from pathlib import Path
from stocklab.candidate.builtin import BUILTIN_PLUGINS
out = Path("/tmp/acceptance-scripts")
for pid, src in BUILTIN_PLUGINS.items():
    (out / f"p{pid}.py").write_text(src, encoding="utf-8")
print("导出", sorted(BUILTIN_PLUGINS))
PY
导出 ['0', '1', '2', '3', '4', '5']
```

---

## 步骤③：6 次 `plugin submit`

```
=== submit plugin 0 ===
script_id=1 plugin_id=0 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 1 ===
script_id=2 plugin_id=1 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 2 ===
script_id=3 plugin_id=2 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 3 ===
script_id=4 plugin_id=3 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 4 ===
script_id=5 plugin_id=4 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论

=== submit plugin 5 ===
script_id=6 plugin_id=5 version=1.0.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；首版没有 baseline，只能看绝对表现，不构成新旧对比结论
```

**注意**：全部 verdict 均为 `INCONCLUSIVE`。这是设计预期——回放沙盒是 fail-closed 空实现
（设计文档「与设计文档的两处偏差」第 2 条），首版没有 baseline 也不构成对比结论。
**不能将「流程跑通」解读为「对比出优劣」。**

---

## 步骤④：`plugin list`（approve 前）

```
$ .venv/bin/python -m stocklab.cli.main plugin list --db /tmp/acceptance.db
   1   0  1.0.0       pending_review
   2   1  1.0.0       pending_review
   3   2  1.0.0       pending_review
   4   3  1.0.0       pending_review
   5   4  1.0.0       pending_review
   6   5  1.0.0       pending_review
```

---

## 步骤⑤：6 次 `plugin approve`

```
=== approve script_id=1 ===
script_id=1 已上线（plugin_id=0）

=== approve script_id=2 ===
script_id=2 已上线（plugin_id=1）

=== approve script_id=3 ===
script_id=3 已上线（plugin_id=2）

=== approve script_id=4 ===
script_id=4 已上线（plugin_id=3）

=== approve script_id=5 ===
script_id=5 已上线（plugin_id=4）

=== approve script_id=6 ===
script_id=6 已上线（plugin_id=5）
```

---

## 步骤⑥：`candidate run`

```
$ .venv/bin/python -m stocklab.cli.main candidate run \
    --asof 2026-09-17 --run-kind weekly --db /tmp/acceptance.db
snapshot_id=1 asof=2026-09-17 kind=weekly
入池：短期 0 / 中期 0 / 长期 0；淘汰 21
⚠️ 建议触发优化子任务（本轮只记标志，不生成脚本）
报告：/Users/zhengchangchun/Documents/Workspace/stock/reports/candidate/2026-09-17-weekly.md
```

全部 21 只标的在 `pre_screen` 阶段被淘汰，原因：`insufficient_history`（零 bar 数据）。
这是预期行为——没有行情数据就没有足够历史，排雷即淘汰。

---

## 步骤⑦：候选池报告首 30 行

```
# 候选池报告 · 2026-09-17

- 生成时间：2026-09-19T09:04:01.454557+00:00
- 快照 ID：1（类型 `weekly`）
- 种子范围：21 只
- 入池 0 条 / 淘汰 21 条

> ⚠️ **本报告仅作研究观察，不构成投资建议。**

## 短期池（上限 6 只）

（空）

## 中期池（上限 8 只）

（空）

## 长期池（上限 5 只）

（空）

## 淘汰清单

| 代码 | 阶段 | 原因 |
|---|---|---|
| 000333 | pre_screen | insufficient_history |
| 000651 | pre_screen | insufficient_history |
| 000858 | pre_screen | insufficient_history |
| 002032 | pre_screen | insufficient_history |
| 002415 | pre_screen | insufficient_history |
```

---

## 步骤⑧：版本迭代演练（插桩3 v1.1.0）

修改：把 `50.0 + long_ret` 改为 `55.0 + long_ret`，提交为 v1.1.0。

```
$ .venv/bin/python -m stocklab.cli.main plugin submit \
    /tmp/acceptance-scripts/p3-v2.py --plugin-id 3 --version 1.1.0 \
    --actor acceptance --db /tmp/acceptance.db
script_id=7 plugin_id=3 version=1.1.0 状态=pending_review
沙盒：verdict=INCONCLUSIVE；样本不足（0 个有效交易日 < 120），不构成结论
```

**沙盒 verdict 说明**：`INCONCLUSIVE`（样本不足）。此时 v1.0.0 已是 active，
但零 bar 数据导致沙盒跑出 0 个有效交易日。
**不能将此 `INCONCLUSIVE` 解读为「v1.1.0 与 v1.0.0 无差异」或「v1.1.0 更好」。**

```
$ .venv/bin/python -m stocklab.cli.main plugin list --plugin-id 3 \
    --db /tmp/acceptance.db
   4   3  1.0.0       active          ⬅ active
   7   3  1.1.0       pending_review
```

```
$ .venv/bin/python -m stocklab.cli.main plugin approve 7 \
    --actor acceptance --reason "v1.1.0 验收" --db /tmp/acceptance.db
script_id=7 已上线（plugin_id=3）
```

```
$ .venv/bin/python -m stocklab.cli.main plugin list --plugin-id 3 \
    --db /tmp/acceptance.db
   4   3  1.0.0       archived
   7   3  1.1.0       active          ⬅ active
```

1.0.0 = `archived`，1.1.0 = `active`。版本迭代状态机正确。

---

## 必须写明的局限

1. **所有 submit 的沙盒 verdict 均为 `INCONCLUSIVE`**。
   回放沙盒是 fail-closed 的空实现（设计文档「与设计文档的两处偏差」第 2 条）。
   在零 bar 数据环境下，首版 verdict 是「无 baseline，不构成对比结论」；
   v1.1.0 的 verdict 是「样本不足（0 个交易日 < 120），不构成结论」。
   **流程跑通 ≠ 对比出优劣。沙盒在本次验收中没有产生任何有意义的比较结论。**

2. **无真实行情数据**：
   本机无 `data/stocklab.db`，`ingest bars` 不支持 `--db` 参数，无法向临时库写入 bar。
   全部 21 只标的在 `pre_screen` 阶段因 `insufficient_history` 淘汰，是预期行为。
   插桩 1/2/3 的打分逻辑**未在本次验收中被实际执行过**（未到达 step 7）。

3. **网络 ingest 未尝试**：
   由于 `ingest bars` 不支持非默认库路径，在不影响真实库的前提下无法注入真实行情。
   如需真实行情验收，需先运行 `db init`（默认路径）+ `ingest bars`，再 `cp` 副本。

---

## 验收结论

| 步骤 | 结果 |
|------|------|
| 6 次 submit，全部进入 pending_review | ✅ 通过 |
| 6 次 approve，全部上线 | ✅ 通过 |
| `candidate run` 正常完成，生成报告 | ✅ 通过（零入池，预期） |
| 版本迭代：1.0.0 archived，1.1.0 active | ✅ 通过 |
| 沙盒对比有意义的结论 | ❌ 不适用（零数据，INCONCLUSIVE） |
| 真实行情下插桩 1/2/3 打分路径 | ❌ 未覆盖（无 bar 数据） |
