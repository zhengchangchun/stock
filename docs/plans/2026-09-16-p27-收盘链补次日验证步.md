# P27 · 收盘链补「次日验证」步 + 补跑缺口

> 状态：**已实施**（见文末「实施结果」）
> 日期：2026-09-16
> 预算：$6
> 前置：P26（`stocklab/chain/accuracy.py`）实测「实时段 n_days = 0」

---

## 0. 事实复核（先复核任务书的假设，不照抄）

任务书给的背景是「`verifications` 的 `max(target_date)` 只有 `2026-09-14`」。
**实测不是这个值**，两条数都有出入，如实记录：

| 判据 | 任务书写的 | 实测（`.venv/bin/python` 只读查库） |
|------|-----------|-----------------------------------|
| `verifications.max(target_date)` | `2026-09-14` | **`2026-09-15`**（该日已有 4 行） |
| `predictions` 中已到期未评分 | 「2 条 + 09-17 中已到期的部分」 | **2 条**，全部是 `asof_date=2026-09-15 → target_date=2026-09-16`（`pred_id` 6056 `000333` / 6057 `600690`，`created_at=2026-09-15T18:15:18+08:00`） |
| `bars_daily.max(date)` | `2026-09-16` | `2026-09-16` ✅ |
| 全部未评分预测 | — | 4 条（另 2 条 `target_date=2026-09-17`，**目标日未到**，不算缺口） |

**结论（任务书的定性判断成立、定量数字有一处过期）**：确实存在「已到期却从未被打分」的
预测；缺口是 **2 条**，不是 3 条。任务书里 `2026-09-14` 那个读数在写下时可能已过期
（库里 09-15 的 4 行是怎么来的：09-15 收盘链跑了 `verify`，但 09-16 那条链**没跑**
或没跑到验证步）。这属于 ERROR_DIARY #34 的同类现象——**引用别人的读数前先自己重测**。

---

## 1. 根因

收盘链（15:30，由 nanobot 侧 cron 维护）的步骤是：

```
ingest bars → 取 LATEST → session backfill-close → predict run → review daily → paper step → doctor
```

`predict run` 产出的是 **target_date = 次日** 的预测（`predictions` 表）。
**链上没有任何一步会给「今天到期」的预测打分并落库**：

- `verify run --target-date D` 能做这件事，但它**要求显式给出 D**，且 D 上没有预测时
  直接 `NoPredictions` 退出 2 —— 链上没人知道该给哪天；
- `review daily` 按设计**只读不写**（`cmd_review_daily` docstring + `build_review` 全程
  无写操作），所以它只会在报告里写「验证：0 条」，不会补。

于是每天的实时预测**永远不被评分**，`verifications` 里 LIVE 桶永远不增长 →
P26 的「实时段准确率」永远累积不起来。

---

## 2. 方案

### 2.1 新增 `stocklab/verify/pending.py`（纯读 + 一个判定）

两个函数，都**复用既有实现**，不另造口径：

```python
latest_closed_session(conn, now) -> (date | None, evidence: dict)
pending_predictions(conn, now)    -> dict
```

**「最新已收盘交易日」的判定（fail-closed）**：

```
日历侧 = session.tick.closed_through(cal, now)        # 复用既有实现（含 15:00 收盘判据）
行情侧 = MAX(bars_daily.date)，且必须满足
         session.tick.is_trade_date_closed(该日, now)  # 复用既有实现
latest_closed = max(日历侧, 行情侧)   # 两侧都取不到 → None
```

即任务书要的 `trading_calendar ∪ bars_daily`，两侧各自套 `is_closed_snapshot` 那一族的
**收盘时刻判据**（`>= 15:00`，解不出即 `False`）。fail-closed 的三个落点：

1. **未来日不算已收盘**：`is_trade_date_closed` 对 `d > today` 返回 `False`，
   所以即使库里混进未来日期的 bar，也不会被当成「已到期」；
2. **日历为空不报错、降级到行情侧**（复用 `load_calendar` 的既有降级语义）；
3. **两侧都判不出来 → `latest_closed = None`**，此时 `pending_predictions` 返回
   `n = None` + `reason`，**绝不返回 0** —— 0 会被读成「没有缺口」，而真相是「判不了」
   （ERROR_DIARY #36：每一个 0 旁边必须写出为什么是 0）。

**「待验证」的判定**：

```sql
SELECT p.* FROM predictions p
LEFT JOIN verifications v ON v.pred_id = p.pred_id
WHERE v.verification_id IS NULL
  AND p.status = 'ok'                 -- 与 verify_target 的选取条件逐字一致
  AND p.target_date <= :latest_closed
ORDER BY p.target_date, p.code
```

`status='ok'` 这一条**必须与 `verify_target` 一致**：否则会出现「待验证清单里有、
但打分器永远不碰」的行 → 永久假报警。实测库里 6059 行全是 `ok`，所以它今天是空操作，
但它是**不变量**而不是巧合，测试里钉住。

### 2.2 新增 CLI：`verify pending`（幂等 + append-only）

```
stocklab verify pending [--db ...] [--code ...]
```

语义：**把「已到期且没有验证行」的预测补齐**。

- 打分**完全复用** `stocklab/verify/service.verify_target`（不新造阈值/公式）；
- 逐 `target_date` 调用，且**只传该日待验证的 `codes`** —— 已评分的行一个字节都不碰
  （任务书硬禁「改既有行」；用 `codes` 把作用域限制在待验证集合上是最强的保证）；
- **幂等**：靠 `insert_verification` 的四态机 + `ux_verifications_pred` 唯一索引。
  第一次跑写入 N 行；第二次跑**选择集为空** → 报「无待验证」、零写入；
- **append-only**：全程只有 `INSERT`（以及 ADR-002 允许的 `unscorable → scorable` 补分），
  没有任何 `DELETE`，也不碰历史回放区间；
- 命令**自检**：处理完再查一次待验证集合，输出 `pending_after`；
  `pending_after != 0` → 退出码 1（自己证明自己没干完）；
- 退出码：`0` 完成（含无待验证）/ `2` 判不出最新已收盘交易日或库不存在 / `1` 有冲突或没补干净。

### 2.3 缺步检测（自动发现）

`build_review` 增加一个键 `pending`（由 `pending_predictions` 提供），因此：

- `reports/YYYY-MM-DD-review.md` 里多一段「**到期未验证**」，`n > 0` 时写明
  「跑 `stocklab verify pending` 补」；
- `review daily` 的 stdout JSON 多一个顶层字段 `pending_verifications`
  （`n`，判不出时为 `null`）与 `pending_verifications_detail`；
- **不新起 cron、不动 nginx、不动 nanobot 的收盘链命令**。

`review daily` 的 `now` 锚点沿用既有写法 `f"{date}T23:59:59+08:00"`
（该命令的语义就是「这一天走完了，回头看」）→ `build_review` **保持无时钟依赖**，
「同输入两次运行逐字节一致」这条既有性质不受影响。

### 2.4 该插在收盘链的哪一步

```
ingest bars → 取 LATEST → session backfill-close → predict run
    → ★ verify pending ★        ← 新增（打分「今天到期」的预测）
    → review daily              ← 这一步现在能真读到验证行，且能报 pending
    → paper step → doctor
```

理由：必须在 `predict run` **之后**（否则当天新预测还没入库）、在 `review daily`
**之前**（否则复盘读到的还是「验证 0 条」）。**cron 命令由 nanobot 维护，本轮不动。**

---

## 3. 验收（逐条自证）

| # | 判据 | 证据 |
|---|------|------|
| a | 复核 SQL（修前 `verifications.max(target_date)` + 未评分条数明细） | 本文件 §0 + 提交里的 SQL 输出 |
| b | `verify pending` 连跑两次 → 第二次报「无待验证」、零写入 | 两次 stdout |
| c | 修后 `max(target_date)` = 最新交易日，已到期未评分 = 0 | 同一段 SQL |
| d | `chain accuracy` 实时段 `n_days` 从 0 → ≥1 | 报告里对应几行 |
| e | `.venv/bin/python -m pytest` 全绿且只增不减（基线 1536 passed） | pytest 汇总行 |
| f | `bash scripts/verify.sh` → `✅ 验证通过: all`；4c 红线仍全绿 | verify.sh 输出 |
| g | 每个子任务 commit | `git log` |

## 4. 反过拟合 / 准确性纪律自查

- 本轮**不改任何预测数字、口径、`MODEL_VERSION`**；
- 本轮**不改任何既有 `predictions` / `verifications` 行**（只 `INSERT` 缺失的 2 行）；
- 补出来的 2 行**样本量是 1 个交易日** → 按 CLAUDE.md 度量纪律 3，**样本严重不足**，
  本轮**不据此下任何准确率结论**，只是让账本不再丢数据；
- `chain accuracy` 的实时段仍会显示 `样本不足（n=1 < 120）` —— 这是**正确**的输出，
  不许为了让验收好看去动门槛。

---

## 实施结果

状态：**已完成**（验收 a–g 逐条自证，证据如下）

### 根因修正：任务书「链上缺一步」的定性对，但机制不是那样

复核时发现 `session tick` **本来就有**验证逻辑（步骤③，2026-09-15 11:50 起就在），
所以「链上缺一步」这个说法需要更精确：

| 证据 | 值 |
|------|----|
| `trading_calendar` 的 `2026-09-16` 行 `created_at` | `2026-09-16T15:32:56+08:00` |
| 当天最后一次 `session_tick` | `2026-09-16T15:23:35+08:00` |
| 那次 tick 的 `verify_inserted` | `0` |

`session tick` 步骤③ 的 cutoff **只来自日历**（`closed_through`），而日历行由
`ingest index` 在 **15:30+** 才写 —— **比当天最后一次 tick 晚 9 分钟**。于是那次 tick
看到的 cutoff 只能是昨天，它把昨天那 4 行重算了一遍（全 `identical`）→ `inserted=0`；
「今天到期」的预测**永远轮不到它**。而 15:30 的收盘链跑的是 `session backfill-close`
（只回填 amount/turnover），**不跑 `session tick`**。

∴ 缺口是**验证步的证据源单一且天生滞后一天**，不是「没人做这件事」。

同时 **任务书给的一个读数已过期**：它写 `verifications.max(target_date) = 2026-09-14`，
实测是 **`2026-09-15`**（该日已有 4 行）。这条按 #34 的教训记下：引用别人的读数前先自己重测。

### 改动

| 文件 | 改动 |
|------|------|
| `stocklab/verify/pending.py` | **新增**。`latest_closed_session` 取 `trading_calendar ∪ bars_daily`；`pending_predictions` 的选取条件与 `verify_target` 逐字一致（含 `status='ok'`） |
| `stocklab/cli/main.py` | **新增** `verify pending`；`review daily` 输出加 `pending_verifications` / `pending_verifications_detail` |
| `stocklab/session/tick.py` | 步骤③ 改用同一份判据（根因修复） |
| `stocklab/session/review.py` | `build_review` 加 `pending`；正文加「到期未验证」一行 |
| `tests/test_verify_pending.py` | **新增 12 条**测试 |

### 验收证据（逐条）

**(a) 修前复核 SQL**

```
verifications: {'max_target_date': '2026-09-15', 'n': 6055}
bars_daily 最新: 2026-09-16
已到期未评分条数: 2
   {'pred_id': 6056, 'code': '000333', 'asof_date': '2026-09-15', 'target_date': '2026-09-16', 'created_at': '2026-09-15T18:15:18+08:00'}
   {'pred_id': 6057, 'code': '600690', 'asof_date': '2026-09-15', 'target_date': '2026-09-16', 'created_at': '2026-09-15T18:15:18+08:00'}
```

**(b) 幂等：连跑两次**

```
第 1 次：pending_before=2, inserted=2, targets={"2026-09-16":{"pending":2,"inserted":2}}, pending_after=0   RC=0
第 2 次：pending_before=0, inserted=0, targets={}, note="无待验证的到期预测", pending_after=0              RC=0
```

**(c) 修后 SQL**

```
verifications: {'max_target_date': '2026-09-16', 'n': 6057}
bars_daily 最新 = 2026-09-16 → 已到期未评分 = 0
补出的两行（**真分数，不是 DATA 缺口**）：
   verification_id=6056 pred_id=6056 target=2026-09-16 code=000333 actual_close=85.0   actual_pct=-0.025565 hit_direction=0 total_score=0.682088 attribution_auto=UNDETERMINED
   verification_id=6057 pred_id=6057 target=2026-09-16 code=600690 actual_close=20.76  actual_pct=-0.019367 hit_direction=0 total_score=0.687524 attribution_auto=UNDETERMINED
```

`attribution_auto=UNDETERMINED` 是**正确**的：程序只判得了 `DATA`，其余四类留人工
（总纲 §9）。两支都 `hit_direction=0`（未命中方向）。

**(d) `chain accuracy` 实时段**

```
## 实时段（真实运行期）
- **区间**：2026-09-16 → 2026-09-16
- **该来源已写入的预测**：4 条；其中**已被验证打分**：2 条（差 2 条 = 目标日还没到 / 还没打分，**不是**「没有预测」）
- **样本量**：有效交易日数 **n_days = 1**，行数 2
⚠️ **样本不足（n=1 < 120），不构成准确率结论**
```

`n_days` **0 → 1** ✅。⚠️ 注意：**1 个交易日不是「准确率」**，按度量纪律 3 这里仍然
不给任何结论，报告也如实只印样本量与「样本不足」。

**(e) pytest**

```
1548 passed in 137.17s (0:02:17)
```
基线 1536 → **+12**，只增不减，零回归。

**(f) `scripts/verify.sh`**

```
--- 4c. 回归红线（P25 / 错误日记 #34）---
  ✅ predict_synthetic                    sha256 20023d531eb24342… == 基线；叶子 175 项一致
  ✅ predict_real_2026-09-14              sha256 b69afb711f60ec53… == 基线；叶子 175 项一致
  ✅ backfill_real_2013-12-23_2026-09-14  sha256 18f2eeeff7168d41… == 基线；叶子  89 项一致
✅ 回归红线全部成立
✅ 验证通过: all
```

红线**数字一个都没变**（与预期一致：只补未评分的行，回放区间止于 2026-09-14，
本次补的是 2026-09-16 的两行，落在回放区间之外）。**没有跑 `--regen`。**

**(g) commit**：`032d079`（代码 + 测试 + 本计划）。

**(附) 缺步检测实测**

正常态（已补齐）：
```
pending_verifications = 0
正文：- 到期未验证：0 条（判据：`target_date <= 2026-09-16` 且无验证行）
```
有缺口时（专门造的库，不是改真库的行 —— append-only 触发器会 `ABORT`）：
```
pending_verifications = 1
正文：- ⚠️ **到期未验证：1 条**（`2024-07-15`×1）—— 这些预测的账本正在丢数据，
      跑 `stocklab verify pending` 补（幂等、append-only；判据：…）
```

### 遗留与限制（不藏）

1. **实时段样本量 1 天**，距 120 天门槛极远 → 本轮**不产生任何准确率结论**，
   `chain accuracy` 也如实只印「样本不足」。
2. `status='ok'` 这个不变量今天是**空操作**（库内 6059 行全 `ok`）。若将来出现
   `status != 'ok'` 的预测，它会同时被 `verify_target` 和本清单跳过 —— 这是**设计**
   （避免永久假警），但没有测试覆盖真实非 ok 行（造不出来）。已用测试钉住两条
   选取条件必须一致（`test_cli_verify_pending_leaves_existing_verification_rows_untouched`
   的反面 + 代码注释）。
3. `origin` 判据的已知边界（`created_at` 是入库时刻）不变，见
   `chain/accuracy.py` 的 `ORIGIN_LIMITS`。**补出的两行确实进 LIVE 桶**：
   `classify` 比的是**预测的** `created_at[:10] == asof_date`，即
   `"2026-09-15" == "2026-09-15"` → `live` ✅（补分时的
   `verifications.created_at = 2026-09-16T22:23:51` **不参与**这个判据，
   参与的是 `predictions.created_at = 2026-09-15T18:15:18`）。
   —— 本条**曾被我写错一次**（把两者混为一谈，写成「有出入、要查」），
   复核后改正：这正是 ERROR_DIARY #33「理由文字没有与实测值连线」的同型，
   落文档前必须把字段名与取值对到同一行上。

### 收盘链的插入位置（**cron 由 nanobot 维护，本轮未动**）

```
ingest bars → 取 LATEST → session backfill-close → predict run
    → ★ verify pending ★      ← 新增（打分「今天到期」的预测）
    → review daily            ← 现在能真读到验证行，且能报 pending
    → paper step → doctor
```

必须在 `predict run` **之后**、`review daily` **之前**。

