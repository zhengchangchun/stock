# 需求澄清录（`stock/00`–`07`）

日期：2026-09-21 ｜ 依据：`docs/plans/2026-09-21-整体需求评审.md`（同一批核对证据）
做法：**只改三类东西** —— ① 与代码对不上的**事实**；② 文档之间/内部的**矛盾**；③ **空缺**（没数字、没写完、没归属）。
**不改**：业务取向、人群与边界、候选池数量建议、优先级排序 —— 那些是你的判断，不是我的。

> 每条都有编号（D-n），可逐条回退。原文用引号、改动落在需求文档里。

---

## 1. 逐条留痕

### D-1 「AI」的所指 → 外部编码 agent + 人工复核

| | |
|---|---|
| **原文** | 00 §1「AI仅允许修改沙盒插桩脚本」；01 §AI优化子流程 第3步「**AI生成**新版本插桩脚本」 |
| **问题** | 现实里新脚本由 **Claude Code + 你** 产出，不是系统内部自动调用模型。两种读法差一整块未开工的核心（自动生成 = 尚未实现的 AI 能力）。 |
| **改后** | 00 新增《术语与现状口径》第1条 + 01 第3步就地标注：「此步由**外部编码 agent + 人工**完成，系统内不实现自动生成」。 |
| **依据** | 09-18 之后的实际流程：脚本经 `plugin submit → 沙盒回测 → 人工 approve` 上线，生成环节在系统之外。 |
| **影响** | 01 §AI优化子流程 只剩 4/5/6 三步需要系统实现，且**三步都已完成**（`plugin sandbox` / 报告 / `approve` 人工 CLI）。 |

### D-2 过拟合：**标记 ≠ 拦截**（03 §4 整节按现状重写）

| | |
|---|---|
| **原文** | 03 §4.1「`overfit_flag` 是占位返回值……replay 是 fail-closed 空实现……**没有代码层面的过拟合检测**」；05 §已知风险2「依赖过拟合检测模块**拦截**」 |
| **问题** | ① 03 描述的是 09-20 之前的状态，**已过期**（真回放 + 真 `overfit_flag` 已落地）；② 03 与 05 **互相矛盾**（一个说没有检测，一个说有拦截）；③ 即便按实现，也是**标记**不是**拦截**。 |
| **改后** | 03 §4 改标题为《过拟合**标记**（不是拦截）》，补 4.1 现状表（回放引擎 / 70-30 切分 / `overfit_flag` / CLI）；05 风险2 改为「依赖 沙盒 70/30 切分 + `overfit_flag` **标记** + 人工复查」，并注明两处口径已统一。 |
| **依据** | `stocklab/plugin/sandbox.py::overfit_flag`（真逻辑：`train − validate > 阈值` → `'suspected'`）；ADR-017；`docs/plans/2026-09-20-沙盒真回放-验收记录.md`。 |
| **未改** | 四条红线（CLAUDE.md）**仍未代码化** —— 这条事实保留在原处，别让读者以为已经自动拦住了。 |

### D-3 版本状态枚举：**以实现的 5 态为主口径**

| | |
|---|---|
| **原文** | 02 §存储「草稿｜沙盒回测中｜等待人工审核｜**验证中**｜**策略冻结**｜策略失效归档｜**已回滚**」（7 态）；06「草稿/沙盒回测中/等待审核/线上/归档」（5 态） |
| **问题** | 加上实现共**三套**口径，任何「按状态查线上版本」的实现都会分叉。 |
| **改后** | 02 与 06 统一为实现口径：`draft / pending_review / rejected / active / archived`（由 `plugin_audit` 事件流**折叠**得出，脚本表本身无状态列）。「验证中 / 策略冻结」标注为**模块2 新增态、尚未实现**；「已回滚」不是独立状态（**回滚 = 对 `archived` 版 `approve` → 回到 `active`**）。01 §存储 同步加注。 |
| **依据** | `stocklab/plugin/lifecycle.py::STATES` + `_ACTION_TO_STATE`。 |

### D-4 提前熔断阈值：**主干默认值 = 验证期内自峰值回撤 ≥ 10%**

| | |
|---|---|
| **原文** | 02 §提前熔断保护「账户回撤超过**主干设定阈值**」—— 全文没有这个数 |
| **问题** | 00 §1 规定主干参数 AI 不可改；没数就等于让实现自己编一个 —— 那正是「AI 改主干」。 |
| **改后** | 02 补：「验证期内账户净值**自峰值回撤 ≥ 10%** → 标记策略失效」，并写明该值放主干常量、**只能由你改**。 |
| **状态** | 这是我给的**建议默认值**，请复核（改它只需改 02 这一行 + 将来 `stocklab/config/`）。 |

### D-5 06 的定位：**核心对象摘要，不是全表清单**

| | |
|---|---|
| **原文** | 06 只列 4 张表 |
| **问题** | 实际库 **39 张表**。当「摘要」可以，当**开发输入**会漏表（Claude Code 会以为表就这么点）。 |
| **改后** | 06 顶部加边界声明：全量以 `stocklab/store/schema.sql` 为准，并列出主要表族。 |

### D-6 00 的两条硬约束**没有承载字段** → 06 补记

| | |
|---|---|
| **原文** | 00 §5「冻结最长有效期 90 天」、§6「连续 **3 次**优化验证失败强制归档」 |
| **问题** | 06 的「策略评估记录」里**没有**失败计数、没有冻结到期时间 —— 自定的硬约束，数据字典撑不住。 |
| **改后** | 06 写明必须补 `连续优化失败次数` 与 `冻结到期时间`，并注明缺失时那两条约束**无法执行**。 |

### D-7 07 补《和原有系统集成关系》+ 修正调度约束 + 记录执行面现状

| | |
|---|---|
| **原文** | 07 末节标题「## 和原有系统集成关系」**正文空白**；§调度约束1 要求「低优先级排队等待，禁止并发」 |
| **问题** | ① 缺的正是最容易做重的部分（跟现有 `session/`（P11 调度链）的关系）；② 「优先级队列」在 nanobot cron 上**不可实现**（cron 与用户会话共用一条会话，是抢占式排队）。 |
| **改后** | 末节补写：**包一层、不重写**（任务2 = 收盘链的「到期验证」步，同一件事）；调度约束 1 改为「**自查前置产物 + 未就绪则跳过 + 幂等重跑**」；每日任务新增固定一步**库备份**。另附**执行面现状**：四类任务当前**一条都没挂**，且**不是被删的**——日志（起于 09-18 16:55）无删除记录，`Cron: added job` 全表只有重挂后的 5 条 → 是随 09-18 网关重置丢的。 |

### D-8 `04` 文件名订正

`# 04_P1_P2_扩展组件需求.md` → **`04_P1_P2_P3_扩展组件需求.md`**
理由：文件名带 Markdown 标题字符 `# `（命令行必须加引号）；且正文含 **P3** 档而文件名只写 `P1_P2`（03 又另叫 `P0`）。

### D-9 「基准对标」指定唯一归属

| | |
|---|---|
| **原文** | 04 P1-1「基准对标系统：AI模拟不仅要跑赢人工，同时要对标市场基准」 |
| **问题** | 模块1 的沙盒回放**已经**产出「候选 vs 基线相对 `index_300` 的超额」（ADR-017）—— 同一件事两个位置，会做出两套「跑赢沪深300」。 |
| **改后** | 04 顶部写明：模块1 只出「插桩版本对比」的相对超额；**模块2** 才做「AI 模拟 vs 人工镜像 vs 基准」三方对标。 |

### D-10 标注「插桩5 已定义未接线」

| | |
|---|---|
| **事实** | `plugin_scripts` 里有插桩5（v1.0.0），契约也在；但 `candidate run` 只走插桩 0/1/2/3/4，**插桩5 只有 CLI 能调用**。 |
| **改后** | 01 插桩清单下加注，避免它被当成「已完成」。 |

### D-11 `03` §2.3 的 TOTAL_EQUITY 从「待观察探针」升格为「已定性」

原文把它列为「若长期不过再查」的悬案；`docs/plans/2026-09-20-财报数据层设计评审.md` 已定性：DMSK 的 `TOTAL_EQUITY` = 总资产 − 总负债（**含少数股东权益**），**不是归母权益** → `roe` 与 `dupont` 口径打架且不报错。已按定性后的口径改写，并指向评审 §E 的处置。

### D-12 数据库落点（**未改文档，列待拍板**）

事实：`paths.py` 指向 `data/stocklab.db`（**不存在**），真正在用的是 **`/tmp/replay.db`**（16 MB / 39 表 / 财报 1443 行）。
本轮**只做了可逆的保护动作**：备份到 `data/backups/replay-2026-09-21.db`。
**没有**切换库路径 —— 因为开发中可能有其他会话正指向 `/tmp`，中途换路径会造成两份库分叉。切换时机与方式留给你拍板。
（**后记**：同日晚上已切到 `data/stocklab.db`，`/tmp/replay.db` 保留不删。）

### D-13 … D-22 在别的文件里

编号是全局的，但留痕按题目分放在最相关的那份文档 ——
D-13/D-14/D-15（成本口径：名义仓位 / 滑点如实计入）在
`docs/plans/2026-09-21-回放成本口径-验收记录.md`；D-16…D-22（智能体动态编排臂）在
`docs/plans/2026-09-21-智能体动态编排对照臂.md`。本节只补本节主题的条目。

### D-23 `07` 的**调度载体**从「Nanobot 定时能力」改为**系统定时能力（macOS launchd）**（2026-09-22）

| | |
|---|---|
| **原文** | 07 概述后一行：「调度载体：Nanobot 定时能力（支持每30分钟、每日、每周、季度调度）」 |
| **问题** | ① 载体与判定分在两个版本库：判据改一次要同时改项目代码和 nanobot 任务正文，而正文一改就换 id（旧 id 还得手动删）——09-22 已实测过一次「调度器说任务 ok、数据链说异常」（#53）；② 网关不常驻：它不跑，三条定时任务静默停摆且无从报警（ 09-18 网关重置就丢过任务正文）；③ 结论只落在会话里，回看「上周三跑到第几步」只能翻聊天记录 |
| **改后** | 概述后那行改为：调度载体 = **系统定时能力（macOS launchd）**，并在末节《和原有系统集成关系》同步「落地方式 = 系统定时器调用现有 CLI」。实现：`stocklab ops schedule generate\|install\|uninstall\|status\|kickstart`（plist 由纯函数生成）＋ 页面 `/lab/ops` |
| **依据** | 用户 2026-09-22 原话「其实不需要 nanobot 定时任务，全部在系统里，到时候跑完页面会有展示」；决策与四个备选见 **ADR-020**，操作手册见 `docs/ops/2026-09-22-launchd-定时任务.md` |
| **不受影响** | D-7 修的三件事（自查前置产物 / 不承诺优先级队列 / 每日备份）全部保留 —— 它们说的是「任务自己怎么保证正确」，与载体无关 |

---

## 2. 明确**没动**的（你的判断，不是我的）

- 候选池数量建议（短3-6 / 中4-8 / 长3-5）—— 与实测一致，不动。
- 人群、店铺、边界、免责、优先级排序（P1/P2/P3）—— 不动。
- 00 §全局硬性约束 1–8 的**实体条款** —— 只补「AI 指谁」，不改条款。
- 模块2 是否本轮开工 —— 不替你决定，见 §4。

---

## 3. 顺带修的

- `docs/README.md`：挂上本批 4 份文档（整体需求评审、本澄清录、两份设计评审）的索引。
- 8 份需求文档此前**全部 untracked**（一次 clean 就没），本轮已 `git add` 纳管（**未提交**，等你过一眼）。

---

## 4. 按澄清后的口径，开发的下一步

澄清之后，需求侧只剩**模块2** 未开工；模块1 的待办是**具体的、已设计过的**两件：

| 顺序 | 事项 | 设计稿 | 状态 |
|---|---|---|---|
| 1 | **回放成本口径 · 显式仓位假设**（C1–C6：名义仓位常量、费用改用 `fees()`、**滑点如实计入**） | `docs/superpowers/specs/2026-09-21-回放成本口径-仓位假设-design.md`（已 rev2） | 未落代码（现仍用 `LOT=100` + `costs.total()`） |
| 2 | **候选池页面**（模块1 展示层） | `docs/superpowers/specs/2026-09-21-候选池页面-design.md`（已 rev2） | 未落代码 |
| 3 | 模块2 开工 | `stock/02` | 需要你先拍板 |

---

## 5. 待你复核

| # | 事 | 我的建议 |
|---|---|---|
| 1 | D-4 熔断阈值 10% | 先按 10% 写；你若有偏好直接改 02 那一行 |
| 2 | D-12 库路径是否迁回 `data/stocklab.db` | 建议迁（现在只有 `/tmp` 一份 + 一份备份） |
| 3 | 07 的四类任务是否**重新挂上** | ~~建议挂「每日收盘链 + 每 30 分钟巡检」两条起步~~ → **已做**：载体改系统 launchd（D-23 / ADR-020），三条命令与 plist 已就绪，待 TCC 授权；nanobot 三条等 launchd 自证后删 |
| 4 | 模块2 是否本轮开工 | 建议**先不开**：模块1 的样本外证据还没产出（`predictions` / `verifications` 均 0 行） |

---

## 6. 模块2 开工前的补充澄清（D-24 – D-33，2026-09-22）

> 背景：用户 2026-09-22 拍板「终极目的＝系统的 AI 模拟调整后能跑赢大盘」并定下协作口径
> （CC 按 superpowers 开发 / nanobot 审计文档 / 进度定期报告）。完整需求书与任务序列见
> `docs/plans/2026-09-22-模块2-需求书.md`；本节只固化**对需求 02/04/05 原文的澄清**，
> 逐条可单条回退。

| # | 原文/歧义 | 改成（生效口径） | 依据 |
|---|---|---|---|
| **D-24** | 02 通篇写「AI 模拟选股」但没说 AI 是谁 | A1/A2/A3/B1 是**插桩脚本**，走既有 plugin 契约 + 沙盒 + 人工审核闸门；「AI 选股」＝外部编码 agent 产出脚本后经人工 `approve` 上线 | D-1 + 00 §2 |
| **D-25** | 02 §通路B「系统生成【镜像账户】复刻人工操作」 | 镜像＝**按人工交易流水复刻**（date/code/side/price/qty/fee），复用既有 `arm-now` 实现；**不新建第二套镜像代码** | ADR-006/ADR-010 |
| **D-26** | 04 §P2-2「多策略并行隔离」粒度未定 | 每策略版本 = 一个 `paper_accounts` 行 + 独立 NAV，**不共享现金池** | 04 §P2-2 |
| **D-27** | 02 §提前熔断「主干设定阈值」 | **验证期内账户净值自峰值回撤 ≥ 10%** → 标记本轮策略失效、终止本轮、记事件；落主干常量，AI 不可改；**不改写历史台账** | D-4 + 05 §运维约束 2 |
| **D-28** | 02 §5「轮次≥2-3、总时长≤30天」 | 常量化：轮次 ∈ [2,3]、总时长 ≤ 30 天、冻结最长 90 天；不满足 → **拒绝并要求重新生成**（不 clamp） | 02 §5 + 参数静默 clamp 教训 |
| **D-29** | 02 §存储列出 7 态 | 主干 5 态 + 新增 `validating`（验证中）/`frozen`（策略冻结），仍由 `plugin_audit` 折叠；**「已回滚」不设独立态**（回滚 = 对 `archived` 版 `approve`） | D-3 |
| **D-30** | 04 §P1-1 与模块1 的「基准对标」重叠 | 模块1 只出「插桩版本对比相对 `index_300` 的超额」；**模块2 才做三方对标**（AI 模拟 vs 人工镜像 vs 基准）；两处口径不得混用 | 04 §D-9（原文） |
| **D-31** | 04 §P1-2「自动区分错误来源」（大盘冲击/行业黑天鹅/个股突发利空/因子失效） | 代码判不了 → 只做「程序给候选标签 + **人工确认**」的结构位，**禁止自动填充** | 总纲 §9 不造假归因 |
| **D-32** | 04 §P3-2「全局参数可视化配置」 | 配置中心 = **只读展示**主干常量与当前生效策略参数 + 来源；**网页端不提供主干常量的写入口** | 05 §业务边界 3 |
| **D-33** | 模块2 四支插桩如何编号 | `m2_a1` / `m2_a2` / `m2_a3` / `m2_b1`，**不占用既有「插桩 0–5」编号** | 0–5 已被需求 01 与实现钉住 |

### 6.1 任务序列（依赖顺序）

P44 主干（状态机/常量/验证周期台账）→ P45 四支插桩契约 → **P47** 双通路与镜像 →
**P48** 三方对标与预测校验（依赖 P41 绩效指标集）→ **P49** 自评估与熔断 → **P50** 误差归因与配置视图。
任务书目录：`docs/tasks/2026-09-22-p44…p50-*.md`。

> ⚠️ **2026-09-22 晚间编号重排**（append-only：旧号不删，只标失效）：模块2 规划里的
> `P46 双通路与镜像` 与当晚落地的 `P46 patrol 收盘抢跑与预测定型闸门`
> （[`tasks/2026-09-22-p46-patrol收盘抢跑与预测定型闸门.md`](../tasks/2026-09-22-p46-patrol收盘抢跑与预测定型闸门.md)）
> **撞号**。**P46 归当晚那个修复站**（它已交付：`eadbe8f`），模块2 剩下四站顺序不变、编号各 +1：
> 双通路与镜像 = **P47**、三方对标与预测校验 = **P48**、自评估与熔断 = **P49**、误差归因与配置视图 = **P50**。
> 同日文档里凡写「模块2 P46/P47/P48/P49」的，一律按本表读。

### 6.2 与旧结论的关系

- §5「待你复核」第 4 条「模块2 建议先不开」**已被 2026-09-22 的用户决定取代**：
  现在开，但**按 P44…P50 串行推进**（模块2 的 P46+P47+P48+P49 已于 2026-09-22 晚编号各 +1，原因见 §6.1），
  且每站交付后由 nanobot 独立审计（需求书 §5 清单）。
- §4「下一步」第 3 条同理作废。
