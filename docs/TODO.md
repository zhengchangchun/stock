# stock-lab 待办清单（维护者：nanobot · 生成 2026-09-16 10:0x）

> 口径：**所有数字都能当场复现**。命令与文件出处写在每条后面；拿不出出处的标「未验证」。
> 状态：✅ **自动任务已于 2026-09-16 14:30 恢复运行、14:50 完成一轮优化**（用户指令「按照最新的开始弄」+「自动化任务你看下优化下」）。巡检改为 5 次/日（09:45/11:45/13:45/15:45/19:45）并新增「当天链路体检+缺步补跑」；收盘链修正 `LATEST` 取值顺序（详见 §E）。§C1 已开跑（claude 任务 P20）。

---

## A. 等你拍板才能动（3 条 · 最高优先）

| # | 事项 | 卡在哪 | 选项 |
|---|---|---|---|
| A1 | 要不要采「估值」与「资金流」两类数据 | 库里 `valuation_daily` 0 行、`money_flow_daily` 0 行（实测：`select count(*)`），没有采集口径我做不了；不做就只能长期只玩价量类策略 | (a) 给我口径就做 (b) 明确不做，把相关策略（高股息/资金流跟随）永久下架 |
| A2 | 交易日历只到 2026-09-15（实测 `select max(date) from trading_calendar`），之后按「工作日近似」推算 | 会把节假日当交易日，导致预测目标日算错 | (a) 每天自动刷新日历 (b) 你人工维护 (c) 维持近似、但每份报告都标注 |
| A3 | 决策表里有 4 行历史重复记录（`experiment_decisions` 共 24 行） | 守 append-only 我默认不回删 | (a) 保留（默认） (b) 清理 |

## B. 开发队列（你不发话我不起）

| # | 事项 | 现状 |
|---|---|---|
| B1 | ~~模拟盘（三臂对照）~~ | ✅ 已完成：引擎 + CLI + ADR-010，真实库 5 个账户 / 4 笔成交 / 5 条净值，最新 2026-09-15 |
| B2 | 界面视觉残项（对账单版式） | 上次预算触顶中断，功能全绿、观感未定稿；你看了截图不满意再迭代 |
| B3 | 模拟盘「等时间」 | 只有 1 个交易日，<120 交易日不算结论 —— 这条不需要开发，只需要日历往前走 |
| B4 | 报告文本缺陷（小） | P20 复核出三处：JSON `counts.test_rows` 报 7（实际 1962）、md 里判据 (a) 的理由文字写反、§6 口径自检两套行集未说明差异。已进巡检缺口队列 P21，预算 ≤$2 |
| B5 | ~~收盘回填的快照时刻校验（中）~~ | ✅ **已完成（2026-09-16 20:1x，提交 `e6155f9`）**：新增 `is_closed_snapshot(ts)`，快照时刻 <15:00:00 或解析失败即跳过该标的（fail-closed）、记 `skipped_stale_snapshot` + `system_events` warn，一个值都没补上时 `reason="stale_snapshot"`。验证：`pytest -q` **1497 passed** / `verify.sh` all ✅；真实数据副本复现「只有 09:35+14:30 快照 → 一行未写」，补 15:18 快照后正常写入 2546180000.0 / 0.43 |
| B6 | 开发通道被 API 额度打断 | 2026-09-16 19:47 P22 任务被 commandcode 429（5 小时额度，limit 14）中断，$4.07/23 turns 未提交；同日 18:52 P21c 亦为 `error_max_budget_usd`（$6.02/32 turns，成果已提交）。额度窗口 21:11 恢复 |

## C. 实验队列（我自己的活）

| # | 实验 | 状态 / 结果 |
|---|---|---|
| C1 | 趋势状态命中率（`trend-state-hit-rate`） | ✅ **已跑完（2026-09-16，P20，$6.13 触顶但成果全保）**：**判据 (b) 成立 / 判据 (a) 否证**。validate 652 天：UP 延续率 53.57% vs 无条件 18.61%（Δ=+33.66pp，CI [+27.7,+39.8]）、DOWN 73.04% vs 38.50%（+32.04pp）；**方向增量仍为负**（UP 命中率 32.42% vs 同日基线 37.87%，Δ=−3.26pp）。test 段（654 天）达标打开一次，(b) 仍成立。→ **采纳为标签层（状态描述 / 风险-波动条件化），不采纳为交易信号**。台账：`docs/experiments/2026-09-16-trend-state-hit-rate-正式版.md` |
| C2 | P8 下半口径修正与第二批变体准入 | 已登记未运行；堵「拿不可交易子群数字当能力」的口子 |
| C3 | 报告文本缺陷（P21，白） | 三处文字/计数错（见 §B4）；不影响结论，但证据文件必须自洽 |

> 已否证的 4 条轴（**不许重跑**）：漂移方向 μ、指数动量、σ 状态缩放、区间残差分位；
> 另：**趋势状态当方向信号也已否证**（C1 判据 a 两段皆否）——「趋势会延续 ≠ 趋势能预涨」。
> 硬红线：样本外才算数 / 按日聚类 / <120 交易日不算数 / 扣成本 / 对照 index_300。

## D. 你的持仓相关（系统只给数据，不给建议）

| # | 事实 | 出处 |
|---|---|---|
| D1 | 美的 000333 一手（100 股）占总资产约 43.5%，高于你自己定的 ≤40% 上限 | `paper` 纪律判定实测 43.53%–43.56% |
| D2 | 按整手规则，100 股只能「全清或不动」，减 10% 这种动作不可执行 | ADR-010 §已知限制 |
| D3 | 实盘样本：只有 1 笔成交、LIVE=0，没有实盘准确率可谈 | `real_trades` 1 行 |

## E. 定时任务现状（2026-09-16 14:50 · 已恢复 + 已优化）

| 任务 | 计划 | 现状 |
|---|---|---|
| 巡检（**每 30 分钟**） | 工作日全天 `every 30m` | ✅ 已挂（id `9e8554c4`，2026-09-16 16:0x 起替代 5 次/日的 `070073b7`；用户指令「每半个小时对项目进度排空并报告」） |
| 盘中采集 tick | 交易日 09:35 / 11:35 / 13:35 | ✅ 已挂（`35 9,11,13 * * 1-5`，id `3fde77c1`） |
| 收盘采集 tick | 交易日 15:05 | ✅ 已挂（`5 15 * * 1-5`，id `e8fb0023`） |
| 收盘链（采集→预测→复盘→模拟盘） | 交易日 15:30 | ✅ 已挂（`30 15 * * 1-5`，id `b0630843`） |
| heartbeat / dream | 系统内部：记忆整理 | 系统托管，不可移除；不跑开发任务、不碰数据 |

**本轮优化三处（都有实测依据）**：

1. **巡检 7 次/日 → 5 次/日**。原时段 09:45–21:45 每 2 小时，其中 17:45 / 19:45 / 21:45 三次落在非交易时段，此时数据链已停、几乎无新信息；实测每轮巡检要跑 2–17 分钟（来源：备份文件里 6 次 runHistory：129s / 1033s / 391s / 282s / 200s / 243s）。现改为 5 次，晚间只留 19:45 一次。
2. **巡检新增「当天链路体检 + 缺步补跑」（第 0 优先级）**。原来巡检只「看」不「修」；现在每天四查（今日 bars / 明日预测 / 当日复盘报告 / 模拟盘净值），缺哪步就按收盘链顺序补跑该步。
3. **修正收盘链的顺序 bug**：原脚本在 `ingest bars` **之前**取 `LATEST`，而 15:30 时当日 K 线尚未入库（实测依据：2026-09-15 15:38 `system_events`「快照有数据但 bars_daily 无当日行，回填跳过」）→ 会把「昨天」当 asof，导致**明日预测不生成**（库里 2026-09-16 的预测实际是 2026-09-15 18:15 手动跑的，不是链路产物）。已改为 ingest 之后再取，并在汇报里强制写出 asof。

4. **巡检 5 次/日 → 每 30 分钟**（2026-09-16 16:0x，用户指令「每半个小时对项目进度排空并报告」）：每轮含「当天链路体检 + 缺步补跑」，成本约 ×5。原 5 次/日 job `070073b7` 已删。

- 原始任务定义（含完整命令）：`docs/ops/cron-backup-2026-09-16.json`（**已被本轮改动超越，以本表为准**）
- ⚠️ 停跑期缺口：2026-09-16 盘中快照只跑了 09:35（11:35 / 13:35 两次因任务已被移除而缺）；当天 15:05 收盘 tick **排队未执行**（见下），由 nanobot 15:18 手工补采，15:30 收盘链起正常。
- ⚠️ **已知行为（重要）**：cron 任务与用户会话**共用同一条会话**，会话忙时任务会停在 `queued`（实测：15:05 收盘 tick 到 15:2x 仍未跑）。后果：收盘链可能被推到 15:45 之后才跑——数据本身不受影响（幂等），但巡检看到「缺步」时须先分辨真失败还是排队。
- 待用户拍板：① 巡检 5 次/日够不够（你原话是「每 30 分钟」，成本约 ×5）；② 收盘链日报要不要主动推送（当前 `deliver=false`，只在会话里留记录）。
