# stock-lab 待办清单（维护者：nanobot · 生成 2026-09-16 10:0x）

> 口径：**所有数字都能当场复现**。命令与文件出处写在每条后面；拿不出出处的标「未验证」。
> 状态：⛔ **自动任务已于 2026-09-16 全部停止**（清单见 §E），恢复方式见 §E 末尾。

---

## A. 等你拍板才能动（3 条 · 最高优先）

| # | 事项 | 卡在哪 | 选项 |
|---|---|---|---|
| A1 | 要不要采「估值」与「资金流」两类数据 | 库里 `valuation_daily` 0 行、`money_flow_daily` 0 行（实测：`select count(*)`），没有采集口径我做不了；不做就只能长期只玩价量类策略 | (a) 给我口径就做 (b) 明确不做，把相关策略（高股息/资金流跟随）永久下架 |
| A2 | 交易日历只到 2026-09-15（实测 `select max(date) from trading_calendar`），之后按「工作日近似」推算 | 会把节假日当交易日，导致预测目标日算错 | (a) 每天自动刷新日历 (b) 你人工维护 (c) 维持近似、但每份报告都标注 |
| A3 | 决策表里有 4 行历史重复记录（`experiment_decisions` 共 24 行） | 守 append-only 我默认不回删 | (a) 保留（默认） (b) 清理 |

## B. 开发队列（你不发话我不起）

| # | 事项 | 现状 |
|---|---|---|
| B1 | ~~模拟盘（三臂对照）~~ | ✅ 已完成：引擎 + CLI + ADR-010，真实库 5 个账户 / 4 笔成交 / 5 条净值，最新 2026-09-15 |
| B2 | 界面视觉残项（对账单版式） | 上次预算触顶中断，功能全绿、观感未定稿；你看了截图不满意再迭代 |
| B3 | 模拟盘「等时间」 | 只有 1 个交易日，<120 交易日不算结论 —— 这条不需要开发，只需要日历往前走 |

## C. 实验队列（我自己的活 · 2 条预注册未跑，来自 `docs/experiments/README.md` 状态列）

| # | 实验 | 预期与用途 |
|---|---|---|
| C1 | 趋势状态命中率（`trend-state-hit-rate`） | 趋势「延续率」68–74% 大概率成立，但**对未来涨跌没有增量** → 只当风险/仓位标签，不当买点 |
| C2 | P8 下半口径修正与第二批变体准入 | 已登记未运行；用来堵「拿不可交易子群数字当能力」的口子 |

> 已否证的 4 条轴**不许重跑**：漂移方向 μ、指数动量、σ 状态缩放、区间残差分位。
> 硬红线：样本外才算数 / 按日聚类 / <120 交易日不算数 / 扣成本 / 对照 index_300。

## D. 你的持仓相关（系统只给数据，不给建议）

| # | 事实 | 出处 |
|---|---|---|
| D1 | 美的 000333 一手（100 股）占总资产约 43.5%，高于你自己定的 ≤40% 上限 | `paper` 纪律判定实测 43.53%–43.56% |
| D2 | 按整手规则，100 股只能「全清或不动」，减 10% 这种动作不可执行 | ADR-010 §已知限制 |
| D3 | 实盘样本：只有 1 笔成交、LIVE=0，没有实盘准确率可谈 | `real_trades` 1 行 |

## E. 定时任务现状（2026-09-16 已全停）

| 任务 | 原计划 | 现状 |
|---|---|---|
| 巡检（2h） | 工作日 09:45–21:45 每 2 小时 | ⛔ 已停 |
| 盘中采集 tick | 交易日 09:35 / 11:35 / 13:35 | ⛔ 已停 |
| 收盘采集 tick | 交易日 15:05 | ⛔ 已停 |
| 收盘链（预测 + 复盘 + 模拟盘推进） | 交易日 15:30 | ⛔ 已停（今天 15:30 不会跑） |
| heartbeat / dream | 系统内部：记忆整理 | 系统托管，不可移除；不跑开发任务、不碰数据 |

- 原始任务定义已备份（含完整命令）：`docs/ops/cron-backup-2026-09-16.json`
- 恢复：让我按备份重新挂上即可（一条指令，不需要重新设计）。
- ⚠️ **代价（必须知道）**：停的每一天，采集/验证/模拟盘净值都**不会补**——历史可以回补，但「当天盘中看到过什么」只有当天采才有；`amount` 目前只有 2 行数据，停跑就不再增长。
