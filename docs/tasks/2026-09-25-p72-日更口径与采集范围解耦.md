# P72 日更口径与采集范围解耦（`universe sync` 落 `active=0` ＋ 四条 ingest 支持 `--universe`）

- 立项人：nanobot（2026-09-25 00:5x，P71 独立复核时发现）
- 上游：P71 宇宙扩容实现站（`d9a0828` feat ＋ `c90cda4` docs）＋ nanobot 对 P71 的独立复核（本档 §0.1）
- 下游：真库 `universe sync csi300-500` 与 B1–B5 分批采集（nanobot 跑）

## 0. 一句话

`universe sync` 往 `instruments` 新增成员时**落 `active=0`**（研究池不进日更口径），并给
`ingest bars / valuation / moneyflow / actions` 加 `--universe <id>`（显式指定 ⇒ 采该宇宙
**全部成员、不受 `active` 过滤**）；**不传新参数的默认路径逐位不变**。

## 0.1 为什么：P71 复核发现的缺口（**本站唯一的存在理由**）

`instruments.active=1` 在本仓是**全局「日更口径」开关**，不是装饰位。实测读它的地方：

| 位置 | 含义 |
|---|---|
| `stocklab/predict/service.py:79,402` | **预测标的集合** = 库内 active 股票（现在 17 只） |
| `stocklab/verify/replay.py:73` | 回放/验证的标的集合 |
| `stocklab/experiments/runner.py:70` | 实验的标的集合 |
| `stocklab/session/review.py:255` | 复盘 / 滚动准确率的标的集合 |
| `stocklab/cli/main.py:114,189` | `_bars_universe` / `_stock_universe`（`ingest bars`/`actions` 的默认集合） |
| `stocklab/cli/main.py:554,1502` | 其它默认集合 |
| `stocklab/cli/main.py:1674` | `_tick_universe`（`session tick` 的标的集合） |
| `stocklab/ops/chain.py:87-93`、`ops/patrol.py:95-101` | `ops close` / `ops patrol` 的 `ingest *` 步骤**不带任何范围参数** |

而 P71 的 `_sync`（`stocklab/cli/universe.py:118-122`）对新成员**硬写 `active=1`**：

```python
"INSERT INTO instruments (code, name, market, board, type, sector,"
" active, added_at) VALUES (?,?,?,?,?,?,1,?)",
```

⇒ 一旦在**真库**跑 `universe sync csi300-500`，**当天立刻**：

1. 预测集合 **17 → 817 条/日**；`verify pending`、`review daily`、`session tick`、
   `experiments` 的标的集合同步放大；
2. `ops close` / `ops patrol` 的 `ingest bars/valuation/moneyflow/actions --days 30`
   从 **21 只 → 821 只**（`CLOSE_STEPS` 整轮预算 **900 s**，`ops/chain.py:70`；
   仅 `ingest moneyflow --days 30` 一项 ≈ 821 × ~0.5 s ≈ 7 min）⇒ **日链天天 exit 2**；
3. 月度链的 `ingest_financials`（不带参数）同样放大到 817 只。

**P70 设计稿与 P71 任务书都没覆盖这一项**：设计稿全文**零次**出现 `active`；P71 §6 判据 5 只在
`/tmp` 副本上跑 sync，**看不见日更口径**（副本不跑 close/predict）。⇒ 这是「真源文件 → 投影」
这条链上唯一漏掉的消费方，必须在**真库 sync 之前**补。

## 0.5 拍板点（nanobot 定，站不许另选；有异议写 §7.7 偏离）

| # | 锁定值 | 依据 |
|---|---|---|
| **F1** | `universe sync` 新成员落 **`active=0`**；既有行**一字不动**；**不加** `--activate` 开关 | 日更口径要显式；把 800 只塞进日更口径＝「隐式换宇宙」，与 D2 冲突 |
| **F2** | 四条 `ingest`（bars/valuation/moneyflow/actions）加 `--universe <id>`：**显式指定 ⇒ 采集集合＝该宇宙全部成员（含 `active=0`）**；**不带 ⇒ 逐位维持现状**（`_bars_universe` 等一个字不改） | 采集要能把研究池的行情灌进库，但不能顺手改日更口径 |
| **F3** | `--universe` 一律 **fail-closed**（文件缺失/坏 ⇒ exit 2），**不得回退** | 与 P71 `load_universe` 同一条线 |
| **F4** | `ops/chain.py` / `ops/patrol.py` / `CLOSE_STEPS` / `patrol` 的步骤参数**一字不动** | 本站目标就是「日链零变化」，改它反而引风险 |
| **F5** | 红线**不 regen**；**真库不许写**（sync/`--universe` 实测只在 `/tmp` 副本） | 同 P71 §2 |
| **F6** | `universe doctor` 的「instruments 覆盖」判据＝「成员**存在于 `instruments` 表**」（**不看 `active`**），输出里**逐条报 active 计数** | sync 之后 `active=0` 是**预期**，doctor 不能因此报红；但两边分布要可见 |

## 1. 任务

### T1 `universe sync` 落 `active=0`

- `stocklab/cli/universe.py::_sync` 的 INSERT 由 `…,1,…` 改 `…,0,…`。
- 既有行的 `UPDATE`（补空 `sector`）**不许**碰 `active`。
- 重新 sync 同一个 id 必须**幂等**：第二次跑 `added=0`、`instruments` 行数不变。
- `repo.log_event` 的 context 里加一个可查字段（如 `instruments_added_inactive: true`），
  让「新增的是非 active 成员」在事件流里一眼可见。

### T2 四条 `ingest` 支持 `--universe <id>`

- `cli/main.py`：`ingest bars` / `ingest valuation` / `ingest moneyflow` / `ingest actions`
  各加 `--universe`（与 P71 已加的 `ingest financials` **同一形状与文案**）。
- 取集合处：显式给了 `--universe` ⇒ 用 `load_universe(id).members`；不带 ⇒ 走既有函数
  （`_bars_universe` / `_stock_universe` …），**逐位不变**。
- **`--code` 既有语义不变**：`--code` 与最终集合仍走 `_reject_unknown_codes`；不在集合内的代码
  **报错退出**（不静默跳过，ERROR_DIARY #44）。
- `ingest bars` 的「ETF 也采」、`ingest actions` 的「ETF 不进复权链」（ADR-008）两条既有事实
  **不许被 `--universe` 路径破坏**。
- `ingest financials`（P71 已加）若与本站形状不一致 ⇒ 对齐到同一 helper。

### T3 `universe doctor` 口径

- 「instruments 覆盖」= 成员在表里存在（`SELECT code FROM instruments`），**不看 `active`**。
- 输出加一行：active 成员数 / 非 active 成员数（只报数，不改判据）。
- `--universe seed21`（21 只全 active）与 `--universe csi300-500`（21 active ＋ 780 inactive）都 exit 0。

### T4 测试

- 改：`tests/test_universe_cli.py` —— sync 后断言 `active` 分布（新增成员全 0、既有 21 只仍 1）。
- 新增：`tests/test_ingest_universe_scope.py` —— 四条命令的 `--universe` 路径（**假 fetcher 注入**，
  照抄 `tests/test_ingest_financials.py` 的做法）：
  - 不带 `--universe` 时采集集合与改前**逐位相同**；
  - 带 `--universe` 时集合 = 宇宙成员（**含 `active=0` 成员**）；
  - 文件缺失 ⇒ exit 2；
  - `--code` 不在集合内 ⇒ 报错。
- 一条**逐位对账判据**：`/tmp` 副本上 sync `csi300-500` 后
  `SELECT count(*) FROM instruments` = **821** 且 `WHERE active=1` = **21**。

### T5 文档

1. `docs/decisions/` 新增 ADR-027（或 ADR-026 追加「修正」段）：记 F1/F2 与「`active` 是全局日更口径」。
2. `docs/errors/ERROR_DIARY.md` 新增 **#78**：症状＝「往共享表插投影行时顺手写了默认 `active=1`，
   把研究池灌进了日更口径」；教训＝**插行时的默认值就是口径**，要按消费方逐个核。
3. 本任务书 §7 实施记录（含 §7.7 偏离、§7.8 未验证口子）。
4. **不写**「扩池后准确率会提高」（P70 §7.3 判据 E1–E5 仍然适用）。

### T6 判据（站自跑，逐条贴原始输出）

见 §3。

## 2. 硬约束（违反即返工）

1. **不许联网**：测试一律假 fetcher／fixture。
2. **不许写真库**：sync/采集实测只在 `/tmp` 副本；真库一律 `mode=ro`
   （`sqlite3.connect('file:/abs/path/data/stocklab.db?mode=ro&immutable=1', uri=True)`）。
3. **`ops/chain.py`、`ops/patrol.py`、`stocklab/predict/`、`stocklab/m2/`、`stocklab/paper/`
   零改动**（若判「必须改」⇒ 停手写 §7.7，别自己扩面）。
4. 默认路径（不传新参数）行为**逐位不变**：这是本站唯一的「不许动」红线。

## 3. 判据（站自跑，逐条贴原始输出）

1. `.venv/bin/python -m pytest -o addopts=""` **全绿**（开工基线 3147 passed / 2 skipped，
   本站后应 ≥ 3160；0 failed）。
2. `bash scripts/verify.sh` **exit 0**。
3. 红线三目标 sha == `0acf35c9…` / `73ffbfb1…` / `ddac0a88…`，**未 regen**。
4. `/tmp` 副本：`universe sync --universe csi300-500` 后 `count(*) instruments` = **821**、
   `WHERE active=1` = **21**（贴两条 SQL 与输出）；再跑一次 ⇒ `added=0`（幂等）。
5. `/tmp` 副本：`universe doctor --universe csi300-500` exit 0 且输出含 active 计数。
6. 假 fetcher 下：四条 `ingest … --universe csi300-500` 的采集集合 = **800**；
   不带 `--universe` 时 = **21**（逐位对账）。
7. 真库 `shasum -a 256 data/stocklab.db` 跑前 == 跑后。

## 4. 进度

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | `_sync` 落 `active=0` ＋ 幂等 | ✅ | `test_sync_lands_new_members_inactive_and_keeps_existing_active` / `test_sync_second_run_adds_nothing_and_keeps_active_zero`；§7.J4 副本实测 821/21 ＋ 二次 `added=0` |
| T2 | 四条 `ingest --universe` | ✅ | `tests/test_ingest_universe_scope.py` 16 用例绿；§7.J6 假 fetcher 实跑 800 vs 21/21/21/17 |
| T3 | `universe doctor` 口径 ＋ active 计数 | ✅ | `test_doctor_exit_0_when_members_are_inactive`；§7.J5 副本 doctor exit 0 ＋ `成员 active 0 / 非 active 800` |
| T4 | 测试（1 改 ＋ 1 新增） | ✅ | `tests/test_universe_cli.py` ＋3（未删改既有）＋ 新文件 `tests/test_ingest_universe_scope.py` 16 条；全量 3166 passed / 2 skipped |
| T5 | ADR ＋ ERROR_DIARY #78 ＋ §7 | ✅ | `docs/decisions/2026-09-25-ADR-027-…（active=0）.md`、`docs/errors/ERROR_DIARY.md` #78、本档 §7 |
| T6 | §3 七条判据逐条自跑 | ✅ | §7.J1–§7.J7，逐条贴原始输出 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

## 5. 反目标自查（完成时逐条贴证据）

| 反目标 | 证据 | 状态 |
|---|---|---|
| `git diff` 不碰 `ops/`、`predict/`、`m2/`、`paper/` | `git diff --stat -- stocklab/ops stocklab/predict stocklab/m2 stocklab/paper` **空** | ✅ |
| 真库 sha/mtime 跑前跑后一致 | §7.0 vs §7.J7：`e393f76d…dc53b` 两次相同，mtime/size 也一字未变 | ✅ |
| 没有 regen 红线 | §7.J3：`git status --porcelain docs/baselines/redlines.json` 空 | ✅ |
| 没有写「准确率会提高」 | `grep -rn "准确率会提高" docs/ stocklab/ tests/` 只命中**既有**「**不写**…」的否定句；新 ADR / #78 / §7 无此类承诺（§7.8-8 是明确否认） | ✅ |
| 默认路径逐位不变 | §7.J6：21/21/21/17 与改前相同；`git diff` 里三条默认分支（`_bars_universe` ×2、`_stock_universe`）一字未改 | ✅ |

## 7. 实施记录（站自跑，原始输出）

### 7.0 开工基线

- **HEAD**：`4d39691355b063a936cdeec536cbdb458f846e47`
  （`docs(p73): 真实成分表收口任务书…` —— 我的任务书是它前一笔 `417acf3`）。
  开工时工作区**干净**（`git status --porcelain` 空）。
  ⚠️ 本轮是**并行站**：P73（`docs/tasks/2026-09-25-p73-真实成分表收口.md`）同时在跑，
  它改 `stocklab/config/universes.py` 的板别表与 fixture；本站**不碰**那个文件。
- **真库**（只读读数，不是我的写入）：`data/stocklab.db`
  sha256 `e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b`，
  mtime `2026-09-24T20:30:29`，size `197779456` bytes。
  只读读数：`instruments` **21** 行 / `active=1` **21** / `active=1 AND type='stock'` **17**
  —— 最后这个 17 就是 §0.1 说的「预测标的集合（现在 17 只）」。
  记忆里的纪律：真库 sha 变了**不等于**我违规（有并发写者 + WAL checkpoint）；
  我所有写路径都指向 `/tmp/p72_copy2/` 与 `/tmp/p72_scope_data/`。
- **pytest collect**：`3149 tests collected`（基线 3147 passed ＋ 2 skipped）。
- **`check_redlines.py` 三目标**：见 §7.J3（改动后的工作区上跑，三条全绿）。

### 7.J1 判据 1：全量 pytest

```
$ .venv/bin/python -m pytest -o addopts="" -q
3166 passed, 2 skipped in 196.28s (0:03:16)
EXIT=0
```

3166 ≥ 3160，**0 failed**。增量 ＋19 ＝ 新文件 `tests/test_ingest_universe_scope.py` 16 条
＋ `tests/test_universe_cli.py` 3 条（T1 active 分布/幂等 2 条 ＋ T3 doctor active 计数 1 条）；
**没有删改任何既有用例**（只往 `test_universe_cli.py` 追加）。

### 7.J2 判据 2：`scripts/verify.sh`

```
$ bash scripts/verify.sh > /tmp/p72/verify_p72.log 2>&1; echo "EXIT=$?"
EXIT=0
$ grep -E "passed|failed|回归红线|验证通过" /tmp/p72/verify_p72.log
  ▶ .venv/bin/python -m pytest
3166 passed, 2 skipped in 198.15s (0:03:18)
  ✅ .venv/ 已忽略
  ✅ data/ 已忽略
  ✅ reports/ 已忽略
  ✅ predict_synthetic
  ✅ predict_real_2026-09-14
  ✅ backfill_real_2013-12-23_2026-09-14
✅ 回归红线全部成立
  ⚠️  有未提交改动：      ← 提交前的预期状态（下一步就提交）
✅ 验证通过: all
```

`verify.sh` 内部第 4 步跑了全量 pytest、第 4c 步跑了 `check_redlines.py`（见 §7.J3），均绿。

### 7.J3 判据 3：红线三目标未 regen

```
$ .venv/bin/python scripts/check_redlines.py; echo "EXIT=$?"
==============================================================================
🔍 回归红线检查（all）  baseline=docs/baselines/redlines.json
==============================================================================
  ✅ predict_synthetic
       sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致（leaves_sha256 a59e8c1906bc4ce2…）
  ✅ predict_real_2026-09-14
       sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致（leaves_sha256 3af6008903703950…）
  ✅ backfill_real_2013-12-23_2026-09-14
       sha256 ddac0a889c623d75… == 基线；叶子 171 项一致（leaves_sha256 06bf461ef1234b4e…）
==============================================================================
✅ 回归红线全部成立
==============================================================================
EXIT=0

$ git status --porcelain docs/baselines/redlines.json
（空 —— 未 regen）
```

三目标 sha 与基线逐位相同：`0acf35c9…`（叶 175）/ `73ffbfb1…`（1330）/ `ddac0a88…`（171）。

### 7.J4 判据 4：`/tmp` 副本 sync ⇒ 821 / active=1 = 21，再跑一次 `added=0`

⚠️ **取数范围**（一处一行，见 §7.7 偏离 1）：真源 `config/universes/csi300-500.*`
**不在仓里**（联网 build 归 nanobot，P71 §0.5.1），而 P71 既有用例
`test_sync_missing_universe_file_exits_2_and_writes_nothing` **钉的就是「这个文件不存在」**
⇒ 往仓里放会打红它、并污染并行站 P73。所以本站用**假 fetcher** 把 csi300-500 现建到
`/tmp/p72_uni`，再把 `cli/universe.py` 的 `load_universe` 默认 root 指过去
（driver `/tmp/p72/uni_driver.py`）；**repo 的 `config/universes/` 一字不碰**。

副本：`mkdir -p /tmp/p72_copy2 && cp data/stocklab.db /tmp/p72_copy2/stocklab.db`（新目录）。

```
$ .venv/bin/python /tmp/p72/uni_driver.py universe sync --universe csi300-500 --db /tmp/p72_copy2/stocklab.db
[driver] built /tmp/p72_uni/csi300-500.csv / /tmp/p72_uni/csi300-500.meta.json（UNIVERSE_DIR 未使用）
universe=csi300-500 投影=800 行 新增 instruments=800 只 补空 sector=0 只 sha=ae1ca1e6603d5019b007635e8813da4729d86e56b45380738b500c93566a6033
exit=0
```

两条 SQL：

```
$ SELECT count(*) FROM instruments
821
$ SELECT count(*) FROM instruments WHERE active=1
21
（附：WHERE active=0 -> 800；universe_memberships WHERE universe_id='csi300-500' -> 800）
```

**幂等**（同一 id 再跑一次）：

```
$ .venv/bin/python /tmp/p72/uni_driver.py universe sync --universe csi300-500 --db /tmp/p72_copy2/stocklab.db
universe=csi300-500 投影=800 行 新增 instruments=0 只 补空 sector=0 只 sha=ae1ca1e6…a6033
exit=0
$ （复核）count(*)=821  active=1=21
```

`added=0`、行数不变、`active=1` 仍是 21 —— 既有的 21 只**一个字节没动**。

### 7.J5 判据 5：`universe doctor --universe csi300-500` 在副本上 exit 0 且报 active 计数

```
$ .venv/bin/python /tmp/p72/uni_driver.py universe doctor --universe csi300-500 --db /tmp/p72_copy2/stocklab.db
universe=csi300-500 文件成员=800 文件 sha=ae1ca1e6603d5019b007635e8813da4729d86e56b45380738b500c93566a6033
表投影行=800；instruments 覆盖 800/800
成员 active 0 / 非 active 800（**只报，不判红**：研究池成员 `active=0` 是 `sync` 的预期结果）
sector 非空 300/800（**只报，不判红**：seed21 在真库上本来就是 0/21，属 P53 回填的历史欠账）
✅ 一致：文件 sha == 表投影 sha，instruments 覆盖完整
exit=0
```

「覆盖 800/800」不看 `active`（F6）：这 800 只**全部**是 `active=0`，仍判一致、exit 0。
`active` 分布单独一行可见（这里 0/800 —— 副本里的 21 只种子不在这个宇宙里）。

### 7.J6 判据 6：假 fetcher 下四条 `ingest` 的采集集合（800 vs 21）

driver `/tmp/p72/scope_driver.py`：**假 fetcher**（绝不真起 `HttpClient` 请求）、
`paths.DB_PATH` 重指到 `/tmp/p72_scope_data/stocklab.db`（已 sync 过的副本）；
每一次 `run()` 打印**实际被 fetch 的 code 集合大小**。

```
$ .venv/bin/python /tmp/p72/scope_driver.py
ingest bars（不带 --universe）                           rc=0 采集集合=21
ingest bars --universe csi300-500                    rc=0 采集集合=800
ingest valuation（不带 --universe）                      rc=0 采集集合=21
ingest valuation --universe csi300-500               rc=0 采集集合=800
ingest moneyflow（不带 --universe）                      rc=0 采集集合=21
ingest moneyflow --universe csi300-500               rc=0 采集集合=800
ingest actions（不带 --universe）                        rc=0 采集集合=17
ingest actions --universe csi300-500                 rc=0 采集集合=800
instruments 行数 = 821 / active=1 = 21
```

逐位对账：

- 不带 `--universe` ⇒ 21/21/21 —— 与**改前**逐位相同（这三条走 `_bars_universe`，
  `git diff` 里那一行一个字没改：`universe = scoped if scoped is not None else _bars_universe(conn)`）；
- `ingest actions` 不带 `--universe` ⇒ **17**（不是 21）：这是 `_stock_universe` 的
  `type='stock'` 口径 —— **ETF 不进复权链（ADR-008）**，`git diff` 里这条分支同样一字未改。
  判据里写的「= 21」只适用于 bars/valuation/moneyflow（它们连 ETF 一起采）。
- 四条带 `--universe` ⇒ **800**（该宇宙全部成员，含 `active=0`）。
- 跑完 `instruments` 仍是 **821 / active=1 = 21** —— 显式宇宙**没有**顺手把 800 行写成
  `active=1`（F2；若 upsert，这里会变成 1621/821）。

### 7.J7 判据 7：真库 sha256 跑前 == 跑后

跑前（§7.0）：`e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b`
（mtime `2026-09-24T20:30:29`，size `197779456`）

跑后（全量 pytest ＋ `verify.sh` ＋ `/tmp` 副本 sync×2 / doctor / 四条 ingest 之后）：

```
$ shasum -a 256 data/stocklab.db
e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b  data/stocklab.db
$ stat -f '%Sm %z bytes' -t '%Y-%m-%dT%H:%M:%S' data/stocklab.db
2026-09-24T20:30:29 197779456 bytes
```

sha **逐位相同**，mtime / size 也一字未变（比「sha 相同」更强：连 WAL checkpoint 都没发生）。
所有写路径都指向 `/tmp/p72_copy2/`（判据 4/5）与 `/tmp/p72_scope_data/`（判据 6）。

### 7.7 偏离（一处一行）

1. **`/tmp` 判据用假 fetcher 现建的宇宙**（`load_universe` 默认 root 重指到 `/tmp/p72_uni`），
   不把真源文件放进仓：它不在仓里（联网 build 归 nanobot，P71 §0.5.1），而 P71 既有用例
   `test_sync_missing_universe_file_exits_2_and_writes_nothing` **钉的就是它不存在**；
   放进去会打红该用例、并污染并行站 P73。生产姿势（文件在 `config/universes/`）路径相同、
   只有 root 不同。
2. **`--universe` 路径不调 `repo.upsert_instruments`**（任务书未点名这一处）。理由：
   `instruments.active` 的 schema 默认值是 `1`，upsert 会把库里还没有的成员插成
   `active=1` —— 正是 F2 禁止的「顺手改日更口径」；成员登记是 `universe sync` 的职责
   （ADR-026）。这与 P71 的 `ingest financials --universe` **同形**（它本来就不 upsert）。
   判据 `test_explicit_universe_does_not_pollute_daily_scope`（跑完 `instruments` 不变）。
3. **`ingest bars` 的 `--code` 未知检查保留内联**（没改成 `_reject_unknown_codes`）：
   T2 写「`--code` 与最终集合仍走 `_reject_unknown_codes`」，但 bars **原本就没用它**
   （它把 `--code` 与集合求交并内联点名）。改成 helper 会改**默认路径**的 stderr 文案，
   与硬约束 4（逐位不变）冲突 ⇒ **硬约束优先**；语义（点名 ＋ exit 1）一致，既有用例
   `test_ingest_bars_cli_rejects_unknown_code` 绿。
4. **`ingest financials` 的 `--universe` help 文案改为共用 `UNIVERSE_SCOPE_HELP`**：
   T2 允许「对齐到同一 helper」「同一形状与文案」⇒ 五条命令现在共用一份文案。
   行为零变化，只有 `--help` 文本变。
5. **判据 4/6 的 `821` / 「不带 = 21」隐含「宇宙与既有 `instruments` 零交集」**：真实
   csi300-500 与 21 只种子**重叠 16 只**（只读实测 nanobot 自己的 dump
   `/tmp/p73/csi800_rows.json`：`000333/000651/000858/002032/002415/002508/300750/
   600028/600036/600519/600690/600900/600941/601088/601318/601398`）⇒ 真名单 sync 应得
   **805**（`active=1` 仍 21），不是 821。本站按判据给的数字取证，故假名单**刻意零交集**
   （`605xxx`/`003xxx` ⇒ 增量正好 800）。
6. **真名单含 `302132`（中航成飞），而 `_BOARD_BY_PREFIX` 尚未收录 `302`** ⇒ 用真名单
   `universe build/load` 会 `UniverseError`（nanobot 已在 P73 §0.1 实测）。该缺口归 P73（G1），
   本站假名单规避 `302` ⇒ **P73 必须先落**，nanobot 才能 build/sync 真名单。

### 7.8 没做成的 / 未验证口子（如实写，别抹掉）

1. **真名单 `csi300-500` 的真实 `build` / `sync` 未跑**：联网与写真库由 nanobot 执行
   （P71 §0.5.1）；本站只跑了「假名单 800 只」的同一代码路径。
2. **`/tmp` 判据里的宇宙 sha `ae1ca1e6…` 是假名单的 sha**，不是真名单（P73 记的
   `eff7b478…c302`）—— 两者不可互相印证。
3. **真库上「sync 之后 `ops close` 的出口码」未实测**（本站不写真库、不跑日链）。
   「日链零变化」的依据是 `ops/` **零改动** ＋ 默认集合仍 **21 只**（§7.J6 的实跑读数），
   不是一条 `ops close` 的实跑读数。
4. **`active` 的「谁有权写」目前只是约定**（`sync` 只写 `0`、要进口径得人工 `UPDATE`）：
   **没有守卫**阻止将来别的写入方写 `active=1`。要做成硬防线得加触发器/写入口断言，
   本站**未做**（F1 明确不加 `--activate`，但也没要求加反向守卫）。
5. **`ingest … --universe` 在「没有先 sync」的库上**会把行情落进 `instruments` 里没有的
   code（本站选择不 upsert）：该状态下 `doctor` / 下游是否报红**未实测**。
6. **`_default_fetcher()`（生产抓取路径）零测试覆盖**（P71 §7.8-7 的口子照旧）。
7. **`--code` 在 `--universe` 路径上的报错文案仍是「不在库 instruments」**（bars）：
   集合来源此时是宇宙文件不是库，措辞略偏；语义（点名 ＋ exit 1）正确。
   与偏离 3 同源（保默认路径逐位不变），**未改**。
8. **扩池后准确率**：本站**没有**也**不能**给「扩池会提高准确率」的结论 ——
   P70 §7.3 的 E1–E5 判据仍然适用，未跑。

### 7.10 提交（逐笔 sha ＋ message）

| # | 内容 | sha | message |
|---|------|-----|---------|
| 1 | 代码 ＋ 测试（4 文件：`cli/main.py`、`cli/universe.py`、`test_universe_cli.py`、`test_ingest_universe_scope.py`） | `bec4e0d179103b04ebe791f59bd5cfe917593b46` | `feat(p72): 日更口径与采集范围解耦（sync 落 active=0 ＋ 四条 ingest 支持 --universe）` |
| 2 | 文档（本记录 ＋ ADR-027 ＋ ERROR_DIARY #78） | 本记录所在的 `docs(p72):` 提交 | `docs(p72): ADR-027 ＋ ERROR_DIARY #78 ＋ 任务书 §7 实施记录` |

- 两笔**分开**（代码/测试一笔、`docs/` 一笔），均只用**显式路径** `git add <path>`，
  **未用** `git add -A` / `git add .`；未提交 `data/` 下任何东西
  （所有写路径都在 `/tmp/p72_copy2/`、`/tmp/p72_scope_data/`）。
- 文档引用第 1 笔的 sha —— 同一提交里写自己的哈希做不到，故分两笔（同 P71）。
- 提交前跑过 `bash scripts/verify.sh`（exit 0）与全量 `pytest`（3166 passed / 2 skipped），
  见 §7.J1 / §7.J2；红线三目标见 §7.J3。

