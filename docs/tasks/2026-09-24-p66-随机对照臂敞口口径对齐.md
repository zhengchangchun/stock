# P66 随机对照臂的敞口口径对齐（扣掉「不在 picks 里的存量持仓」）

创建：2026-09-24（nanobot 需求澄清，用户当日拍板「按你推荐的来」＝修）｜ 实现：Claude Code（superpowers 全流程）
来源：`docs/tasks/2026-09-24-p65-杠杆禁令落到执行层.md` **§7.7-1（存疑项 1）** ＋
`docs/errors/ERROR_DIARY.md` **#73**（同型缺陷的原始案例）。
上一站：`docs/tasks/2026-09-24-p64-A1排除买不起一手的标的.md`（`dc9bbde`）、P65（`5e1770f`）。

---

## 0. 一句话

P65 给 **AI 臂**（插桩 `m2_a1` v1.0.3）加了口径「Σw 上限先扣掉**不在本轮 picks 里**的
存量持仓占比」；**随机对照臂**（`paper/agent_decide.py::random_payload`）**没跟上** ——
它仍按 `total_assets × w` 定目标市值。本站把两条臂的口径**对齐**，让 docstring
里那句「**同护栏、同成本**」重新成立。

> ⚠️ 这是**改控制臂**（D-19 的归因基线），属口径变更 ⇒ 必须留生效时点、必须不重写历史。
> 用户 2026-09-24 已就本条明确授权（原话：「按照你推荐的来」，即「修，小任务书」）。

## 1. 事实：口径不对称的实测证据

**代码层面**（两条臂的分母不同）：

| 臂 | 目标市值 | 上界 |
|---|---|---|
| AI 臂 `m2_a1` v1.0.3 | 组合口径 `total_assets × w` | `Σw ≤ (100 − CASH_FLOOR) − reserved`，`reserved` ＝ **不在 picks 里**的存量持仓占比 |
| 随机臂 `random_payload` | 组合口径 `total_assets × w` | **没有**：`exposure = uniform(0, 100)`，只看「Σw + cash_pct == 100」这条载荷自洽 |

**后果**（执行层 P65 的闸门会整轮拒绝）：随机臂抽到高敞口 ⇒ 目标市值合计 > 可用现金 ⇒
`CashShortfall` ⇒ 整日事务回滚 ⇒ **该臂那天没有净值行**，且 `paper agent run` 返回 2。

**真库状态下的实测读数**（nanobot 2026-09-24 只读探针，`mode=ro`、零写入；账户
`arm-agent-random` / asof 2026-09-23：现金 ¥11,320、存量 `000333`×100 ＝ 总资产的 **42.15%**、
可用现金占比 **57.85%**）：

```
seeds=200   被闸门拒=31（15.5%）   通过=169（84.5%）   其它错=0
被拒者的目标敞口：62.87% / 65.44% / 68.45% / 70.02% / …（全部 > 57.85% 可用现金）
通过者的目标敞口：0.12% / 0.44% / 1.36% / 1.57% / 4.31% / …
```

> P65 §7.7-1 写的「200 个种子里 91 个」是**更粗的判据**（只看「目标市值合计 > 可用现金」，
> 没算「被抽中的存量票卖出会释放现金」）；31/200 是走完真实路径
> （`rebase_payload` → `execute_decision`）的拒绝率。**本站的判据以 31/200 这条路为准。**

**为什么这是缺陷而不只是「运气差」**（三条，缺一条就不用修）：

1. 种子是 `(arm, asof, seed)` 决定的 ⇒ **哪一天被拒是确定的**，不是随机缺失；
2. 被拒的都是**高敞口日** ⇒ 留存下来的随机臂样本**系统性地低敞口、低波动**；
3. ⇒ 用这种对照臂算 `delta_vs_random`，等于拿「被修剪过运气的对照组」比 AI 臂，
   **D-19 的结论（AI 是选对了，还是同预算下多试了几次）从根上不成立**。

**存量确认**（2026-09-24 核）：`paper_accounts` 里 `arm-agent-random` 的
`params.executor = agent_decision` ⇒ 它**在飞**、每天都该有读数；`paper_agent_decisions`
现有 2 行（`decision_id=1` 随机臂 / asof 2026-09-23；`decision_id=2` ds-v1 / 同日）
—— **这两行是 append-only，本站一个字都不许改**。

## 2. 口径（本站唯一的算法，实现必须逐条对上）

**新口径（口径 v2）**：随机臂的**敞口上界**先扣掉「不在本轮 picks 里的存量持仓当前占比」。

```
reserved = Σ weight_pct(holdings whose code ∉ picks)        # 单位：%
cap      = round((100 − RANDOM_CASH_FLOOR) − reserved, 2)    # RANDOM_CASH_FLOOR = 10.0
exposure = round(rng.uniform(0.0, max(0.0, cap)), 2)         # ← 唯一改动的抽取区间
```

其余**一个字不改**：`k` 只数仍取 `rng.randint(*RANDOM_N_CODES)`、
`picks = sorted(rng.sample(usable, k))`、权重仍 `round(exposure × rand/Σrand, 2)`、
余项仍进 `cash_pct`、种子材料仍 `sha256(f"{arm}|{asof}|{seed}")` ⇒ **逐字节可复现**。

`weight_pct` 的来源与 A1 完全一样：`hold_qty[code] × mark / total_assets × 100`
（**不许**新增契约字段、不许改 `agent_context`）。

**退化口径**（必须与旧版**逐位相同**）：
- `total_assets is None` 或 `<= 0` ⇒ `reserved = 0.0`、`exposure = uniform(0, 90)`；
- 无持仓（`held_qty` 为空） ⇒ `reserved = 0.0`；
- 池内没有可定价标的 ⇒ 仍走既有的「全现金 `cash_pct = 100`」早退分支。

**为什么这么选（写进 docstring，不要只写在任务书里）**：
- 它**逐字复刻** A1 v1.0.3 的上界公式 ⇒ 「同护栏」可验证，而不是靠形容词；
- 它保证 **成交后现金 ≥ 10% × 总资产**（`cash_after ≥ 10 + net_consumed − Σw 取整残差`，
  取整残差 < 0.005n ≤ 0.02%，被 10% 的下限吃掉）⇒ 手续费有地方出，不会踩 `CASH_TOL = 0.005`；
- 与 A1 的差别只有一处、且是**故意**的：A1 是等权 `w = cap/n`，随机臂是随机权重 —— 随机性保留。

**口径切换的可见性（硬要求）**：
- `random_payload` 的 `rationale` 里必须出现口径标识（如 `"口径 v2（P66）"`）与
  `reserved`/`cap` 的**实际取值**；
- **不重写历史**：`decision_id=1`（2026-09-23，旧口径）原样留在台账里，一个字不删不改；
- 生效时点 ＝ 本站合入日；实施记录必须写「**上一版口径的最后一天**」是 2026-09-23。

## 3. 任务列表

### T1 随机臂敞口上界扣存量（`stocklab/paper/agent_decide.py` ＋ `paper/config.py`）

- **描述**：按 §2 的口径改 `random_payload`；新增主干常量
  `RANDOM_CASH_FLOOR: float = 10.0`（放 `paper/config.py` 紧邻 `RANDOM_N_CODES`，
  注释写明「镜像 `m2_a1` v1.0.3 的 `CASH_FLOOR`，为了让两条臂同护栏」）。
- **实现步骤**：
  1. 先写测试（TDD）：同 `(arm, asof, seed)` 两遍逐字节相同；`reserved` 越大 `cap` 越小；
     `total_assets is None` / 空持仓两条退化路径与旧版逐位相同；
  2. 改 `random_payload`：算 `reserved` → `cap` → 在 `[0, cap]` 上抽 `exposure`；
     在 docstring 里写清「为什么这么选」与「口径 v2 的生效时点」；
  3. 在 `rationale` 里带上口径标识与 `reserved` / `cap` 的实际值。
- **验证方式**：
  - [ ] `tests/test_paper_agent_decide.py`（或既有随机臂用例所在文件）新增用例全绿；
  - [ ] 逐字节复现：同 seed 连跑两遍，`canonical_payload` 相同；
  - [ ] 退化路径与**改动前**的输出逐位相同（用固定的旧期望值钉住，不是「重算一遍看着对」）。

### T2 实测「零拒绝」（**判据的核心，不许用公式推**）

- **描述**：在真库**副本**上对 `seed 0..199` 走真实路径（`random_payload` →
  `rebase_payload` → `execute_decision`），**被拒数必须是 0**。
- **实现步骤**：
  1. `cp` 真库到 `/tmp`（真库**只读**或只读连接；`ingest *`／写库动作一律不许碰真库）；
  2. 用真库现有的 `arm-agent-random` 状态（现金 ¥11,320 / 存量 `000333`×100 / 总资产 ¥19,567）
     跑 200 个种子；
  3. 记录：通过数、**每个种子成交后的 `cash` 与 `cash / total_assets`**、目标敞口区间
     （min / max）。
- **验证方式**：
  - [ ] 200/200 通过、`CashShortfall` 计数 **0**（原文输出贴进实施记录）；
  - [ ] 每个种子 `cash ≥ 10% × total_assets − 1 元`（实测，不是推理）；
  - [ ] 报出 `max(exposure)` ≤ `cap` 的逐 seed 最大 `reserved` 那一例的读数。

### T3 既有用例的对偶化（**不许删用例**）

- **描述**：P65 留了 `tests/test_m2_builtin.py::test_p65_random_payloads_can_overdraw_and_the_gate_rejects_them`
  ——「随机臂会透支、闸门会拒」这条读数**当时是真的**。本站修好之后它必须**变成对偶断言**：
  「同一条口径下**不再有**可构造的透支载荷」，而不是被删掉或改弱。
- **实现步骤**：
  1. 保留原用例名（或改名成 `test_p66_random_payloads_never_overdraw`）并在 docstring 里
     写清「P65 时它是红的读数，P66 起它是对偶断言」；
  2. 新增一条**反向自检**：手工构造一个「按旧口径（`uniform(0,100)`）」的载荷喂给
     `_gate_cash`，**必须仍然被拒** —— 证明闸门本身没被本站改弱。
- **验证方式**：
  - [ ] 两条用例都在；`_gate_cash` 的**任何一行**都没改（`git diff` 里 `_gate_cash` 区块零改动）；
  - [ ] 反向自检用例会在「把闸门删掉」的变异下判红。

### T4 退出码与文档对账（A6「文件在说谎」）

- **描述**：`paper agent run` 的退出码 2 现在有**两种**成因：① 库的状态坏了
  （未知 `executor` / 无认领账户）；② **某个账户被资金闸门整轮拒**（`CashShortfall`）。
  现有文档只写了 ①。
- **实现步骤**：
  1. 改 `stocklab/cli/main.py::cmd_paper_agent_run` 的 docstring（退出码三栏）；
  2. 改 `docs/ops/2026-09-23-AI操盘手-日更循环.md` §1 的退出码表与 §3 的失败处置表：
     加一行「`run` 退出码 2 且 stderr 带 `code=cash` ⇒ 该臂当天没有净值行（**这是设计**），
     处置＝检查该臂载荷的目标敞口，**不要**去改执行层闸门」；
  3. 一并写明本站之后「**两臂口径同源**」这句话的依据（引用 `random_payload` 的新公式）。
- **验证方式**：
  - [ ] 两处文档的退出码栏与代码里的 `return` 值逐条对得上（人工对账记录写进实施记录）。

### T5 全量回归与提交

- **验证方式**：
  - [ ] `.venv/bin/python -m pytest` 全绿，**收集数只增不减**（开工先记基线数）；
  - [ ] `bash scripts/verify.sh` exit 0；
  - [ ] 红线三目标 sha == 基线（`0acf35c9…`/175、`73ffbfb1…`/1330、`ddac0a88…`/171，**未 regen**）；
  - [ ] 真库 `data/stocklab.db` **零写入**（开工/收工 sha 相同，或差异能归因到 launchd 的 patrol/close）；
  - [ ] 提交只用**显式路径**（禁 `git add -A`）：`stocklab/paper/agent_decide.py`、
    `stocklab/paper/config.py`、`stocklab/cli/main.py`、`tests/<新增或改动的用例文件>`、
    `docs/tasks/2026-09-24-p66-*.md`、`docs/ops/2026-09-23-AI操盘手-日更循环.md`、
    如需 `docs/errors/ERROR_DIARY.md`。

## 4. 反目标（越界即违站）

- ❌ **不动执行层闸门**：`agent_decide._gate_cash` 一字不改（修口径，不修闸门）；也不许改成缩单/夹紧
  （P65 已否掉，理由见 `_gate_cash` 的 docstring）；
- ❌ 不动 A1（`m2/builtin/a1_pick.py`）／不动 `m2/` 任何文件；
- ❌ 不改 `_ALLOWED_BUILTINS`、成本口径（`paper/rules.py`）、整手/停牌规则、主干成本常量；
- ❌ 不重写 `paper_agent_decisions` 的任何历史行、不删任何既有测试；
- ❌ 真库 `data/stocklab.db` **只读**，实测只在 `/tmp` 副本；
- ❌ 不做参数搜索、不为了让某天的读数好看而挑 seed 区间。

## 5. 判据汇总（可证伪）

1. 200 个种子在真库账户状态上**零拒绝**（实测输出）；
2. 每个种子成交后现金 ≥ 10% × 总资产（实测）；
3. 同 seed 两遍逐字节相同；退化路径与旧版逐位相同；
4. `_gate_cash` 零改动、既有 P65 用例**对偶化而非删除**；
5. 全量 pytest 只增不减 + `verify.sh` exit 0 + 红线三目标 == 基线 + 真库零写入。

## 6. 实施记录（由实现站填写）

> 逐条贴实测输出（命令 + 原文），**不许**出现「应该可行」「大概够」。

## 7. 进度

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | 随机臂敞口上界扣存量（含 `RANDOM_CASH_FLOOR`） | ⬜ 未开始 | |
| T2 | 真库副本上 200 种子零拒绝（实测读数） | ⬜ 未开始 | |
| T3 | P65 用例对偶化 + `_gate_cash` 零改动自检 | ⬜ 未开始 | |
| T4 | 退出码/文档对账（`run` exit 2 的两种成因） | ⬜ 未开始 | |
| T5 | 全量回归 + 显式路径提交 | ⬜ 未开始 | |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败
