# P77 · 候选池打分改走 PIT 复权价（因子侧）· 涨跌停/停牌判定保留未复权价

日期：2026-09-25 ｜ 提出：nanobot（用户 2026-09-24 授权「只要能提高 ai 模拟的准确率，就都按照你想的优化」）
｜ 实现：Claude Code ｜ 上游证据：本文件 §0.2、`docs/experiments/2026-09-25-xsec-topn-csi300-500.md` §5

---

## 0. 背景

### 0.1 一句话

候选池打分（插桩 1/2/3/4 看到的 `ctx["bars"]`）现在喂的是**未复权价**，而项目其它所有价格路径
都走 ADR-004 的复权链。本任务把**因子侧**的输入价改为 **PIT 复权价**，
同时把**涨跌停/停牌判定**（`screen.py`）保留在**未复权价**上 —— 这两类用途的口径不同，不能一起换。

### 0.2 实测证据（2026-09-25，只读探针，脚本与逐日读数留档
`~/.nanobot/workspace/artifacts/stock/2026-09-25-p77-adjprice-quant.md` §1）

`stocklab/candidate/run.py::_load_bars` 直接读 `bars_daily`（真库全表 `adj_mode='none'` = **未复权**），
`score.build_ctx` 把它原样放进 `ctx["bars"]`。插桩1（短期池）的公式是
`score = 50 + 20日动量×200 + (量比−1)×20` ⇒ 20 日动量在**除权日当天会被除权缺口污染**。

对 `csi300-500`（800 只）逐调仓日比对「未复权动量」vs「复权动量」：

| 调仓日 | 20 日动量 |Δ| > 1pp 的标的占比 | 单只最大 |Δ| | 例（raw → adj） |
|---|---|---|
| 2018-06-29 | 11.7% | 56.1pp | `002429` −61.43% → −3.28% |
| 2020-06-30 | 10.2% | 65.1pp | `000661` −35.12% → +29.96% |
| 2022-06-22 | 14.6% | 47.2pp | `300432` −5.68% → +41.48% |
| 2024-06-28 | 12.3% | 35.4pp | `300308` −12.33% → +23.07% |
| 2026-06-24 | 9.8% | 40.7pp | `002831` −5.34% → +35.36% |
| 2026-09-23 | 3.2% | 30.0pp | `300803` −33.39% → −3.42% |

⇒ 分红送转季（6 月）**每 8–10 只里有 1 只**的动量被污染，且**方向会反**（raw 大跌 / adj 大涨）。
打分是「动量×200」线性放大的 ⇒ 这些标的的**排序被系统性搞错**，而排序就是通路 A 选股的唯一依据。

### 0.3 为什么这是「漏网」而不是「有意的口径」

- 本项目**所有**其它价格路径都走 `data/adjust.py::load_bars_adjusted`：`predict/service.py`、
  `verify/service.py`、`risk/rules.py`、`strategies/evaluate.py`、`plugin_review/inputs.py`、`paper`（经 rules）；
- 既有测试 `tests/test_verify_*` 里有 `test_score_uses_adjusted_not_raw_prices`（针对**验证器**）——
  「用复权价」是本项目的既定口径；
- `docs/decisions/` 29 篇 ADR 里**没有任何一篇**写过「候选池打分故意用未复权价」；该路径的 docstring
  也没有提这件事。
- ⇒ 判定：**未记载的口径缺口**。修它不需要改任何策略参数、不改任何插桩公式。

### 0.4 与已归档实验的关系（必须知道）

2026-09-25 归档的 `docs/experiments/2026-09-25-xsec-topn-csi300-500.md`（Δ=+0.3132%、
CI 跨 0）与 P60 的 `2026-09-24-xsec-topn.*` 都是在**旧（未复权）口径**下跑的 ⇒
**它们与「本任务之后」的读数不可同时引用**。本任务不改写它们，只要求在 §7 记下这件事。

---

## 0.5 锁定口径（按顺序执行；要改先回来说，不要自作主张）

- **D1（因子侧换价）**：喂给插桩的 `ctx["bars"]` 必须是 **PIT 复权价**
  （`data/adjust.py::load_bars_adjusted`，`as_of` 与 `_load_bars` 的 `asof` 同一个值，
  **不缩窗**：起止日期与同一天的 `_load_bars` 逐位对齐）。**不许**自己实现一份复权。
- **D2（判定侧不换价）**：`candidate/screen.py` 的输入**保持未复权价**。理由：涨跌停按**前收**判定，
  除权日未复权价会自然跳空（这正是「跌停」与「除权」的区别），换成复权价会把除权误判成跌停、
  也会漏掉真实跌停。`screen.py` 是主干，**一行都不许改**。
- **D3（ETF 与缺因子标的的显式口径）**：
  - ETF：`load_bars_adjusted` 按 ADR-008 抛 `EtfChainUnsupported` ⇒ 该标的风控/打分**回退到未复权价**，
    并计入 `n_adj_fallback`（不许静默）；
  - 个股但复权链为空（真库现有 333 只 `candidate/ingest actions` 未采的标的，补采任务在跑）⇒
    因子恒 1、`load_bars_adjusted` 的既有语义即「价 = 未复权价」，同样计入 `n_adj_fallback`。
  - **fail-open 的理由**：这是打分路径不是下单路径；一条标的缺因子不该让整轮 `candidate run` 死掉，
    但**必须可见**（快照参数 + 报告计数）。**不许**把 `n_adj_fallback` 当成静默。
- **D4（口径可追溯、历史不改写）**：`candidate_snapshots.params_json` **只增键**：
  `scoring_price_mode = "adjusted_factor_side+raw_screen"`、`n_adj_fallback`（整数）。
  历史快照行**一个字节都不许改**；新口径的生效点写一篇新 ADR（`docs/decisions/2026-09-25-ADR-029-候选池打分价格口径.md`）。
  老口径下同一天已有快照时**会幂等命中**（`(asof, run_kind)`）⇒ 在 §7 如实写清「口径切换点之后
  才能靠新快照区分」这件事，**不要**为了让它生效去删历史行。
- **D5（不许动的）**：`screen.py` 判据/阈值；`stocklab/candidate/builtin/p1_short.py` 的公式
  （已上线的插桩源文本，改它要走 `plugin submit`）；`predict/ verify/ paper/ m2/ ops/` 四个路径；
  `bars_daily` 的采集口径（**不要在库里存复权价**，复权是读取层的事 —— ADR-004）。
- **D6（性能与确定性）**：复权读比裸读慢，扫描会变慢，**可接受**（记读数即可）。若要加缓存：
  必须只读、逐位可复现、且**不许**改变任何数值结果；不满足就不要加。

---

## 1. 任务列表

### T1 拆分两条取数：未复权（判定用）／复权（因子用）

- **描述**：`candidate/run.py` 里把「给 `screen` 的 bars」与「给 `ctx` 的 bars」显式分开。
- **实现步骤**：
  1. 保留 `_load_bars`（未复权）语义与签名**逐位不变**，仅补 docstring 说清它的用途（判定侧）。
  2. 新增 `_load_bars_adjusted(conn, code, asof)`：薄封装 `adjust.load_bars_adjusted`，
     **不缩窗**、`as_of=asof`；ETF/无因子异常的处理放调用方（见 T2）。
  3. `score_pipeline` 主循环：`screen.screen(...)` 仍收 `_load_bars` 的结果；
     `score.build_ctx(...)` 收复权结果。两次取数的**日期集合必须覆盖**（缺失的日期由复权函数自己决定，
     不许在调用方手工补/删）。
- **验证方式**：
  - [ ] 新增测试：同一天 `_load_bars` 与 `_load_bars_adjusted` 返回的 `date` 序列**逐位相同**（非 ETF、
    有因子行的标的，取 ≥1 个真库样本构造 fixture）
  - [ ] `screen.py` 的 diff **为 0 行**（`git diff --stat` 自证）

### T2 ETF / 缺因子标的口径（D3）

- **描述**：显式回退 + 计数 + 可见。
- **实现步骤**：
  1. 调 `_load_bars_adjusted` 时捕获 `EtfChainUnsupported`（ADR-008）与「复权链为空」两类情形，
     回退到未复权 bars，计数 +1。
  2. 计数进 `PipelineResult.params`（或等价位置）→ 落 `candidate_snapshots.params_json.n_adj_fallback`，
     并在既有报告（`candidate` 报告 / `labweb` 候选池页）里**可见**（至少 params 里能看到）。
  3. `stocklab/candidate/builtin/p1_short.py` 的 `SOURCE` **零改动**（`git diff` 自证）。
- **验证方式**：
  - [ ] 构造「无因子行的个股」与「ETF」两个样本，`score_pipeline` 不抛异常且返回与旧口径
        **逐位相同**的成员（因为因子恒 1 ⇒ 价 = 未复权价 ⇒ 结果必须与改前一致 —— 这条是**反证**：
        如果不等，说明你顺手改了别的东西）
  - [ ] 快照 `params_json` 里出现两个新键，且都是新增（老键名/取值不变）

### T3 红-绿自检（口径真的生效了）

- **描述**：证明「除权不再污染动量」，并证明「跌停仍按未复权判」。
- **实现步骤**：
  1. 造 fixture：一只标的在窗口内有 `corp_actions`/`adj_factors` 事件（`cqr = asof`），
     未复权价在该日跳水、复权价连续。
  2. 断言 ①：插桩1 的动量输入（`ctx["bars"]` 的收盘价序列）在除权日**不跳变**；
     断言 ②：同一 fixture 下，若未复权价构成跌停，`screen.screen` 仍判跌停（`screen` 吃未复权）。
  3. 断言 ③：同一天两次 `score_pipeline(universe=...)` 结果**逐位相同**（确定性；`sorted` 键不变）。
- **验证方式**：
  - [ ] 三个断言各一条测试，且**先红后绿**（把 T1 的改动临时回退，测试必须红 —— 在 §7 记下红的那一行输出）

### T4 真库副本影响量化（只读真库、只写 `/tmp` 副本）

- **描述**：给出「换价之后，选股到底变了多少」的读数。
- **实现步骤**：
  1. `cp -c data/stocklab.db /tmp/p77/audit.db`（APFS 克隆）；探针里 `patch` `paths.DB_PATH`
     指向副本（`paths.DB_PATH` 在**调用时**读取，patch 有效）。
  2. 取 ≥6 个调仓日：`2018-06-29`、`2020-06-30`、`2022-06-22`、`2024-06-28`、`2026-06-24`、
     `2026-09-23`（前五个是分红季、末一个是平时）。
  3. 对每个日子，在**改前**（`git stash` 或 detached worktree）与**改后**各跑一次
     `score_pipeline(conn, asof=day, universe=<csi300-500 的 members>)`，输出：
     `short` 池 top-6 的交集数、成员 `adj_score` 的最大差、`eligible["short"]` 计数差、
     `n_adj_fallback`。
- **验证方式**：
  - [ ] 6 个日子 × 4 个读数的表进 §7（缺一项就写「未测」并说原因）
  - [ ] 真库 sha 跑前 == 跑后（只读自证）

### T5 全量测试与红线

- **验证方式**：
  - [ ] `.venv/bin/python -m pytest -o addopts="" -q` 全绿，且**用例数只增不减**（基线 3214 passed / 2 skipped）
  - [ ] `bash scripts/verify.sh` exit 0
  - [ ] 红线三目标 sha 与叶子数 == `docs/baselines/redlines.json`（**未 regen**）：
        `predict_synthetic` `0acf35c9…`/175、`predict_real_2026-09-14` `73ffbfb1…`/1330、
        `backfill_real_2013-12-23_2026-09-14` `ddac0a88…`/171
  - [ ] 真库 `data/stocklab.db` sha 跑前 == 跑后

### T6 文档

- **描述**：口径变更必须留痕。
- **实现步骤**：
  1. 新 ADR：`docs/decisions/2026-09-25-ADR-029-候选池打分价格口径.md`（背景 / 决策 D1–D6 /
     为什么不换 `screen` 的价 / 与 P60&扩池实验读数的关系）+ 在 `docs/decisions/README.md` 加一行。
  2. 任务书 §7 实施记录：逐条勾 T1–T5、贴命令与读数、写清「旧口径读数不可与新口径混引」。
- **验证方式**：
  - [ ] ADR 文件存在且进 README 目录
  - [ ] §7 每条判据都有实测输出（命令 + 结果），没有「应该没问题」

### T7（独立小修，单独一笔提交）产物文件名带宇宙 id

- **描述**：`stocklab/research/xsec.py` 的报告文件名 `stem = f"{end}-xsec-topn"` **不含宇宙 id** ⇒
  2026-09-25 的扩池重跑把 P60 的 `reports/research/2026-09-24-xsec-topn.{md,json}` **覆盖**了（已不可恢复）。
- **实现步骤**：`stem` 改为含宇宙 id（如 `{end}-xsec-topn-{universe_id}`），**默认路径（seed21）也带上
  宇宙 id**（与旧名不同是**有意的**：旧名本身就是碰撞源）；同步改既有测试的期望文件名。
- **验证方式**：
  - [ ] 新增/更新测试：给定 `universe=seed21` 与 `universe=csi300-500` 两个 stem 不同
  - [ ] `grep -rn "xsec-topn" stocklab/ tests/` 里没有残留的旧拼接写法

---

## 2. 风险点（先读 ERROR_DIARY）

- **#47**：`ingest` 类命令不接受 `--db`；本任务**不要**写真库（只读 + `/tmp` 副本）。
- **#55/#56**：CLI 用例必须注入假 runner；本任务不涉及 `ops/chain.py`，别顺手改。
- **#59/#72/#74/#77**：口径类改动最容易「顺手改到别处」⇒ T1–T3 都要求 **diff 自证**。
- **#81**：只读探针必须 `conn.row_factory = sqlite3.Row`（裸 tuple 会误报）。
- **性能**：800 只 × 571 天的扫描本来 ≈96 min（P60 实验）；换复权读后会变慢 ——
  **不要**为了跑快而缩窗/抽样，读数要逐位可复现。
- **幂等**：同一天已有旧口径快照时 `candidate run` 会命中幂等 ⇒ 别去删历史行（D4）。

## 3. 提交纪律

- 只用**显式路径** `git add <file…>`，**禁** `git add -A`；
- commit message 用 `-F <临时文件>`，**禁反引号**（bash 命令替换会把消息打残）；
- 分笔：① ADR/文档 ② 实现（T1–T3）③ T7 小修（独立一笔）。

---

## 7. 实施记录（站填）

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | 拆分两条取数 | ✅ 完成 | 真样本 date 序列逐位相同 + 机械 spy 钉住 screen 吃未复权；`screen.py` diff = 0 |
| T2 | ETF/缺因子回退与计数 | ✅ 完成 | 反证逐位相同；两个新键进快照、老行不动；`p1_short.py` diff = 0 |
| T3 | 红-绿自检 | ✅ 完成 | 三条断言各一条用例；回退 T1 ⇒ 8 failed（红行见下），另补 ②/③ 的定点红证 |
| T4 | 真库副本影响量化 | ✅ 完成 | 6 日 × 4 读数齐全（真库只读、只写 `/tmp` 副本） |
| T5 | 全量测试与红线 | ⚠️ 4/5 达成 | pytest 3224/2、verify.sh exit 0、红线三目标逐位相同；**「真库 sha 前后相等」未达成**（并发补采，归因见 §7.5） |
| T6 | 文档（ADR-029） | ✅ 完成 | ADR + README 索引各一行 |
| T7 | 报告文件名带宇宙 id | ✅ 完成 | 新用例先红后绿；`grep` 无残留旧拼接 |

改动面（`git status --porcelain` 全量输出）：

```
 M docs/decisions/README.md
 M stocklab/candidate/run.py
 M stocklab/research/xsec.py
 M tests/test_research_xsec.py
 M tests/test_research_xsec_universe.py
?? docs/decisions/2026-09-25-ADR-029-候选池打分价格口径.md
?? tests/test_candidate_run_p77.py
```

禁改路径的 diff（**空 = 0 行**）：

```
$ git diff --stat -- stocklab/candidate/screen.py stocklab/candidate/builtin/ \
      stocklab/predict stocklab/verify stocklab/paper stocklab/m2 stocklab/ops \
      stocklab/data/adjust.py stocklab/config
（无输出）
$ git diff --stat -- stocklab/candidate/run.py
 stocklab/candidate/run.py | 87 +++++++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 84 insertions(+), 3 deletions(-)
```

### 7.1 T1 拆分两条取数

- `_load_bars`：签名与语义**逐位不变**（只补 docstring 定位为判定侧）；
- 新增 `_load_bars_adjusted(conn, code, *, asof, raw)`：薄封装
  `adjust.load_bars_adjusted(conn, code, asof)`（**不缩窗**、`as_of=asof`），
  返回 `(bars, fallback)`；回退时**原样返回调用方刚读的 `raw`**（不重复读库）；
- `score_pipeline` 主循环：`screen.screen(inst, raw_bars)` / `build_ctx(..., bars)`。

判据（真库样本 `000333` = 2014-04-30 `10派20元转15股`，见用例里 `REAL_BARS` 的取数 SQL）：

```
$ .venv/bin/python -m pytest -o addopts="" -q tests/test_candidate_run_p77.py
.........                                                                [100%]
9 passed in 0.55s
```

- `test_p77_real_sample_date_sequences_are_identical`：`[b.date for b in adj] ==
  [b.date for b in raw]`（同一天、非 ETF、该标的**有因子行** ⇒ `assert_blackout_current`
  的核对路径也走到），且价格序列**不相等**（否则这条判据不能说明复权生效）；
- `test_p77_pipeline_feeds_screen_with_raw_bars`：spy 抓 `screen.screen` 收到的 bars，
  与 `_load_bars` 的输出逐位相等、`adj_mode` 全为 `none`（机械钉住 D2）；
- `screen.py` diff = 0 行（上面的 `git diff --stat` 自证）。

### 7.2 T2 ETF / 缺因子回退与计数（D3）

```
tests/test_candidate_run_p77.py::test_p77_fallback_members_are_bytewise_identical_to_old_caliber PASSED
tests/test_candidate_run_p77.py::test_p77_fallback_is_counted_and_visible_in_params PASSED
tests/test_candidate_run_p77.py::test_p77_snapshot_params_has_two_new_keys_and_history_untouched PASSED
```

- **反证**：旧口径用 `monkeypatch` 把 `_load_bars_adjusted` 换成 `(raw, None)`
  （= 改前那条「一次取数喂两边」的路径）⇒ 无因子行的个股 `600519` 与 ETF `510300`
  的 `members`（含 `raw_score`/`adj_score`/`risk_json`）/`rejects`/`eligible`
  **逐位相同**（因子恒 1 ⇒ 价 = 未复权价 ⇒ 必须相同）；
- 计数：两者都进回退 ⇒ `params["n_adj_fallback"] == 2`；真样本（有因子行、复权成功）
  ⇒ `== 0`（计数**不是**恒等于 universe 大小）；
- `params_json`：`scoring_price_mode == "adjusted_factor_side+raw_screen"`、
  `n_adj_fallback`（整数），老键 `seed_count`/`topn`/`universe_id`/`members_sha256`
  取值不变；先塞一行**旧形状**快照（`{"seed_count":2,"topn":...}`）再跑
  `run_candidate` ⇒ `skipped=True`、那一行的 `params_json` **字节不变**（D4）。
- `p1_short.py` diff = 0 行（上面的 `git diff --stat` 自证）。

> **两处如实记的偏差**
> 1. **D3 点名的两类不够用**：实测 `csi300-500` 800 只里 **148 只**会在
>    `load_bars_adjusted(无 start)` 上抛 `MissingFactor`（窗口跨过不可定价事件，
>    例 `000001`：`窗口 [1991-01-02, 2026-09-23] 跨越了无法定价的除权事件
>    ['1993-05-24','2000-11-06']`）。只捕获任务书点名的 `EtfChainUnsupported` +
>    「链为空」⇒ 这 148 只让整批 abort，与 D3 自己的 fail-open 理由直接冲突。
>    本站按「理由优先」多捕 `MissingFactor` / `StaleFactorTable`（都属于「数据侧
>    不可用」这一类），裸 `AdjustError` 仍原样抛（ERROR_DIARY #81 的分档教训）。
>    理由与计数进 ADR-029 §D3。
> 2. **T2.2 的「报告里可见」只做到 `params_json`**：`candidate/report.py` 与
>    labweb 页面都不在硬约束 1 的允许改动清单里，所以没有加报告行；
>    `candidate_snapshots.params_json` 是报告与候选池页的共同数据源。

### 7.3 T3 红-绿自检

三条断言各一条用例（`tests/test_candidate_run_p77.py`）：

| 断言 | 用例 | 观测点 |
|---|---|---|
| ① 除权日不跳变 | `test_p77_t3a_plugin1_sees_continuous_adjusted_prices` | 插桩1 的 `ctx["bars"]` 收益序列 = `raw_score` ⇒ 1.0（未复权会是 0.95） |
| ①（真样本） | `test_p77_t3a_real_sample_ex_div_jump_is_removed` | `000333` 2014-04-30：未复权 −63.25% → 复权 **−4.03%** |
| ② 跌停仍按未复权判 | `test_p77_t3b_screen_still_judges_limit_down_on_raw` | 3 个「10送2股」除权日 ⇒ 未复权天天 −16.7%（判 `consecutive_limit_down`）、复权后逐日收益 0 |
| ③ 确定性 | `test_p77_t3c_two_runs_are_bytewise_identical` | 两次 `score_pipeline` 的 members/rejects/eligible/params 全等 |

**红（任务书指定的做法：临时回退 T1 的改动）**

```
$ git stash push -- stocklab/candidate/run.py
$ .venv/bin/python -m pytest -o addopts="" -q tests/test_candidate_run_p77.py
>       assert a.params["n_adj_fallback"] == b.params["n_adj_fallback"]
E       KeyError: 'n_adj_fallback'
FAILED tests/test_candidate_run_p77.py::test_p77_real_sample_date_sequences_are_identical
FAILED tests/test_candidate_run_p77.py::test_p77_fallback_members_are_bytewise_identical_to_old_caliber
FAILED tests/test_candidate_run_p77.py::test_p77_fallback_is_counted_and_visible_in_params
FAILED tests/test_candidate_run_p77.py::test_p77_snapshot_params_has_two_new_keys_and_history_untouched
FAILED tests/test_candidate_run_p77.py::test_p77_t3a_plugin1_sees_continuous_adjusted_prices
FAILED tests/test_candidate_run_p77.py::test_p77_t3a_real_sample_ex_div_jump_is_removed
FAILED tests/test_candidate_run_p77.py::test_p77_t3b_screen_still_judges_limit_down_on_raw
FAILED tests/test_candidate_run_p77.py::test_p77_t3c_two_runs_are_bytewise_identical
8 failed, 1 passed in 1.87s
$ git stash pop          # 恢复后 9 passed
```

绿的那条是 `test_p77_pipeline_feeds_screen_with_raw_bars` —— **它本来就该在改前也是绿的**
（D2 是「不许被顺手改掉」，不是「改前是错的」）。

**②③ 的定点红证**（「回退 T1」对这两条不构成红：②在改前后都吃未复权，③与口径无关。
用一次性临时用例注入对应的错误实现，跑完即删、**不进提交**）：

```
② 把复权价喂给 screen：
E  AssertionError: 喂复权价后 screen 判不出跌停：rejects={}、
   members=['600690', '600690', '600690']        （3 次都当成通过排雷）
③ 注入随机 tie-break（8 只同分）：
E  AssertionError: 确定性断言在注入随机 tie-break 后应变红：
   ['600101','600106','600107','600104','600102','600103'] vs
   ['600103','600107','600105','600106','600104','600102']
2 failed in 0.29s
```

### 7.4 T4 真库副本影响量化（只读真库、只写 `/tmp` 副本）

```
$ cp -c data/stocklab.db /tmp/p77/audit.db          # APFS 克隆，0.31 s
$ P77_ROOT=/tmp/p77/base P77_TAG=before .venv/bin/python /tmp/p77/probe_t4.py   # 基线 = HEAD 的 detached worktree
$ P77_ROOT=<工作树> P77_TAG=after  .venv/bin/python /tmp/p77/probe_t4.py
```

探针要点：`paths.DB_PATH = /tmp/p77/audit.db`（调用时取属性）、
`conn.row_factory = sqlite3.Row`（ERROR_DIARY #81）、宇宙 `csi300-500`（800 只、
`members_sha256 = eff7b478ea9a…`）、`score_pipeline(asof=day, universe=members)` 现算。

| 调仓日 | 扫描耗时 s（前→后） | short top-6 交集 | 共同成员 / 其中 adj_score 变 | `eligible["short"]` 差 | `n_adj_fallback` |
|---|---|---|---|---|---|
| 2018-06-29 | 8.48 → 13.19 | 5/6 | 18 / 0 | 324 → 324（+0） | 106 |
| 2020-06-30 | 9.72 → 14.80 | 4/6 | 17 / 0 | 373 → 373（+0） | 133 |
| 2022-06-22 | 11.54 → 17.42 | 5/6 | 18 / 0 | 460 → 460（+0） | 203 |
| 2024-06-28 | 13.19 → 19.85 | 6/6 | 19 / 1（max\|Δ\|=3.225749） | 518 → 518（+0） | 248 |
| 2026-06-24 | 14.76 → 22.55 | 5/6 | 18 / 0 | 533 → 533（+0） | 260 |
| 2026-09-23 | 15.16 → 22.78 | 6/6 | 19 / 0 | 532 → 532（+0） | 259 |

- **`eligible["short"]` 6/6 天差 0**、**中/长期池成员 6/6 天逐位相同**（插桩 2/3 只读财报）；
- 短池成员进出**只发生在分红送转季**，且进入者的插桩1 得分（未复权 → 复权）：
  `300604` 51.72→100.0、`002475` 65.22→100.0、`300014` 5.43→100.0、
  `002568` 46.56→100.0、`002831` 57.15→100.0；退出者自身得分不变（100.0→100.0，
  是被挤出去的）。例：`300604` 2018-06-29 的 20 日动量 **−37.98% → +18.13%**
  （事件 `10派1.5元转9股`），插桩1 的 `50 + 动量×200` 因此从 0 直接跳到上限 100。
- 耗时 **+55%～+85%**（同一天同宇宙；多一次全历史 K 线读 + 一次事件链构建）。
- ⚠️ 上表「共同成员 / 变化数」是**共同成员内**的统计；退出成员的得分差不能按
  「缺失记 0」去算（那会得到「100 → 0」的假读数 —— 第一次算表时踩到过，已改）。

### 7.5 T5 全量测试与红线

```
$ .venv/bin/python -m pytest -o addopts="" -q
3224 passed, 2 skipped in 199.90s (0:03:19)
```
（基线 3214 passed / 2 skipped ⇒ **+10**，正是本次新增的 9 + 1 条用例，只增不减。）

```
$ bash scripts/verify.sh; echo "exit=$?"
  ▶ .venv/bin/python -m pytest
  3224 passed, 2 skipped in 198.11s (0:03:18)
--- 4c. 回归红线（P25 / 错误日记 #34）---
🔍 回归红线检查（all）  baseline=docs/baselines/redlines.json
  ✅ predict_synthetic
       sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致（leaves_sha256 a59e8c1906bc4ce2…）
  ✅ predict_real_2026-09-14
       sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致（leaves_sha256 3af6008903703950…）
  ✅ backfill_real_2013-12-23_2026-09-14
       sha256 ddac0a889c623d75… == 基线；叶子 171 项一致（leaves_sha256 06bf461ef1234b4e…）
✅ 回归红线全部成立
==========================================
✅ 验证通过: all
exit=0
```

红线三目标与 `docs/baselines/redlines.json` **逐位相同、未 regen**：
`predict_synthetic` `0acf35c9…`/175、`predict_real_2026-09-14` `73ffbfb1…`/1330、
`backfill_real_2013-12-23_2026-09-14` `ddac0a88…`/171。

#### ⚠️ 「真库 sha 跑前 == 跑后」**未达成** —— 归因：后台补采任务，与本站无关

```
跑前（本任务开工时 23:30）：83f80e8c2edd3b23da74062bb7e49340d6785b033a751d9d2e361688766ff75c
跑后（verify.sh 通过后 23:49）：1a0eb6db61511d4fbf539b7489f1143b1845f5fa82a7984cbda9dbf8dd6fe9a9
文件 2030665728 → 2055868416 字节（+25.2 MB），mtime 23:49
```

**为什么这不是本站造成的**（三条独立证据）：

1. **本站全程没碰真库**：所有实测的 `paths.DB_PATH` 都被 patch 到
   `/tmp/p77/audit.db`（`probe_t4.py` 开头），那是 **inode 不同的另一个文件**
   （`data/stocklab.db` = 193216112、副本 = 201677830），且副本自身的 sha 至今稳定；
   `check_redlines.py` 走 `shutil.copy`（源码自述「真实库零写入」）。
2. **库内计数指向补采**：真库只读计数 `corp_actions 10272 → 11330`、
   `adj_factors 2637616 → 2874335`，而 `bars_daily 3137732 → 3137732` **一根没动**
   —— 正是 `ingest actions` + 因子重算的签名（补采任务在跑，任务书 §硬约束 3 已点名）。
3. **进程在位**：`ps` 查到 `ingest actions --universe csi300-500 …` 持续运行
   （先 pid 71253、后 pid 85482），文件 mtime 与它的写入节奏一致。

⇒ 本判据在「真库被另一个任务并发写」的前提下**不可能成立**（任务书 §硬约束 3 自己写了
「此刻后台正在写真库」）。本站按「硬约束优先」处理：**只读真库**，把可复现的读数全部
落在 `/tmp` 副本上，并把差异如实记在这里，**不**宣称这条判据通过。

### 7.6 T6 文档

- `docs/decisions/2026-09-25-ADR-029-候选池打分价格口径.md`：背景（含探针证据）/
  备选 4 案 / 决策 D1–D6（含 D3 的四类回退与「多捕两类」的偏差说明）/ 后果
  （实测影响表 + 代价与后续注意）；
- `docs/decisions/README.md` 索引末尾加一行。

### 7.7 T7 报告文件名带宇宙 id（独立一笔提交）

`stocklab/research/xsec.py::write_report`：`stem = f"{end}-xsec-topn-{universe_id}"`
（默认 `seed21` 也带上 —— 旧名本身就是碰撞源）；同步改既有用例期望的文件名
（`2016-06-30-xsec-topn-seed21.{json,md}`），新增
`tests/test_research_xsec_universe.py::test_write_report_stem_carries_the_universe_id`。

```
$ .venv/bin/python -m pytest -o addopts="" -q tests/test_research_xsec.py tests/test_research_xsec_universe.py
38 passed in 5.99s
```

先红后绿（把 `stem` 改回老拼接式）：

```
E  AssertionError: assert '2026-09-24-xsec-topn.json' == '2026-09-24-x...n-seed21.json'
1 failed in 0.16s
```

`grep -rn "xsec-topn" stocklab/ tests/` 的残留检查：只剩**实验名**（`EXPERIMENT`、
`summary:` 行、CLI 子命令名）与**历史预注册文件路径**
（`docs/experiments/2026-09-24-xsec-topn.md`，台账 append-only 不许改名）——
**没有**任何残留的文件名拼接写法。

### 7.8 必须写明的一句

**旧口径（未复权）下的实验读数不可与本 ADR 之后的读数混引**：
`docs/experiments/2026-09-25-xsec-topn-csi300-500.md`（Δ=+0.3132%、CI 跨 0）与 P60 的
`2026-09-24-xsec-topn.*` 都是在改前口径下跑的。本任务**不改写**它们（append-only 台账），
只在这里记下「不可同时引用」。另：真实库里同一天已有旧口径快照时 `candidate run`
会幂等命中 ⇒ **口径切换点之后**才能靠新快照区分（D4）。

## 8. 实施记录（nanobot 独立复核）

**站读数**（`/tmp/p77sta/station.log` 尾部 JSON）：`is_error=false` / `subtype=success` /
`terminal_reason=completed` / `num_turns=123` / `total_cost_usd=6.8308` /
`duration_ms=1469927`（≈24.5 min）/ `session_id=691ea8eb…`。看门狗（cron `1a0ae064`）
到点查 `ps -p 43583` 已退出 ⇒ 走「已退出」分支。

### 8.1 三笔提交落地（`git log --oneline`）

| # | 内容 | sha |
|---|---|---|
| ① | ADR-029 + `docs/decisions/README.md` 索引 + 任务书 §7 | `df1f4a8` |
| ② | 实现 T1–T3：`candidate/run.py` + `tests/test_candidate_run_p77.py` | `4bc7cb9` |
| ③ | T7 小修：`research/xsec.py` + 2 个 xsec 测试 | `6e1b9a4` |

工作区 `git status --porcelain` = **空**（clean）。

### 8.2 改动面（`git diff --name-only 4dfb6bb..HEAD`）

8 个文件：`stocklab/candidate/run.py`、`stocklab/research/xsec.py`、
`tests/test_candidate_run_p77.py`、`tests/test_research_xsec.py`、
`tests/test_research_xsec_universe.py`、ADR-029、`docs/decisions/README.md`、本任务书。

禁改路径（`candidate/screen.py`、`candidate/builtin/`、`predict/ verify/ paper/ m2/ ops/`、
`data/adjust.py`、`config/`）的 `git diff --name-only` 输出**为空** —— 与站自报一致，我逐条核过。
`run.py` 的改动 +84/−3（`git show --stat 4bc7cb9`）。

### 8.3 独立复核（自跑，不采信自报；全部在真库静止后）

- **全量测试**：`.venv/bin/python -m pytest -o addopts="" -q` →
  **3224 passed / 2 skipped, rc=0**（203.84 s）。开工基线 3214/2 ⇒ **+10**＝新增 9+1 条，只增不减。
- **`bash scripts/verify.sh` → exit 0**；红线三目标逐位 == `docs/baselines/redlines.json`（**未 regen**）：
  `predict_synthetic` `0acf35c9…`/叶子 175、`predict_real_2026-09-14` `73ffbfb1…`/1330、
  `backfill_real_2013-12-23_2026-09-14` `ddac0a88…`/171。verify.sh 的「工作区干净」项亦通过。
- **真库 sha**：跑前 == 跑后 ==
  `5d5c3c38057a6429c2a7d4e286ea72a96643007959ca0081bc00bb787a65c9de`
  （2,079,801,344 B，mtime 00:05 未动）⇒ **本轮复核零写库**。
- **代码审计**：`_ADJ_UNAVAILABLE = (EtfChainUnsupported, MissingFactor, StaleFactorTable)`
  三类在 `stocklab/data/adjust.py` 均存在（第 54/58/66 行）；
  `adjust.load_bars_adjusted(conn, code, as_of, *, start=None)` 签名与调用位一致（第三参数＝`as_of`）。
  **未捕获 `UnpriceableTerms` 是安全的**：该异常只在 `build_chain` 循环体内抛出、并被**同处**的
  `except UnpriceableTerms` 就地接住（`adjust.py:248` / `:256`），逃不出 `load_bars_adjusted`
  ⇒ 不存在「漏一种异常就 abort 整批」的余口。

### 8.4 §7.5 那条「真库 sha 跑前 == 跑后」未达成 —— 归因成立，且现已反证

站自报 `83f80e8c…` → `1a0eb6db…`，归因「并发补采」。**归因正确**：并发写者是
我 2026-09-25 23:28 起的 **333 只 `ingest actions --universe csi300-500`** 补采
（pid 39641、`/tmp/p77act/`、17 批全 `rc=0` / `failed=0`、**2026-09-26 00:05:40 收工**）。
写前后签名吻合：

| 表 | 补采前 | 补采后 |
|---|---|---|
| `corp_actions` | 8,439（468 code） | **12,283（781 code）** |
| `adj_factors` | 2,140,016（468 code） | **3,120,921（801 code）** |
| `bars_daily` | 3,137,732 | 3,137,732（**一根没动**） |

「有事件却无因子行」的 code 数 ＝ **0** ⇒ B5 遗留的 333 只缺口**已全部关闭**。
补采收工、库静止后我重跑该判据 ⇒ **成立**（见 8.3）。⇒ 这条未达成项是**时序**
（真库被另一个我起的任务并发写），不是本站缺陷；判据的前提在任务书 §硬约束 3 里本就被否掉了。

### 8.5 其余如实记的偏差 —— 我的判断

1. **D3 点名的两类不够用**：真库 `csi300-500` 800 只里 **148 只**抛 `MissingFactor`
   （窗口跨不可定价事件），只捕任务书点名的 `EtfChainUnsupported` + 「链为空」会让
   **18.5% 标的 abort 整批**。站按「理由优先」多捕 `MissingFactor`/`StaleFactorTable`，
   裸 `AdjustError` 仍原样抛，并写进 ADR §D3 与本文件 §7.2。
   **判断：正确，且比我写的任务书更准** —— 我的点名清单漏了真库上最大的一类。
2. **T3 的红只对断言①**：回退 T1 得 8 failed（红行见 §7.3），②③ 的「红」在语义上不可能
   由回退 T1 产生（②改前后都吃未复权、③与口径无关）。站另做**定点红证**（②把复权价喂给
   `screen` ⇒ `rejects={}`、members 出现 3 次；③注入随机 tie-break ⇒ 两次 short 列表不同），
   临时用例跑完即删、未进提交。**判断：可接受**。
3. **T2.2 的「报告里可见」只做到 `params_json`**：`candidate/report.py` 与 labweb 不在
   硬约束 1 的允许改动清单内 ⇒ 未加报告行；`candidate_snapshots.params_json` 是报告与
   候选池页的共同数据源。**判断：符合任务书**，代价（可见性弱于报告行）已记进 ADR §D4；
   后续若要报告行，另立任务书。
4. **T7 承认的覆盖事故**：`reports/research/2026-09-24-xsec-topn.{md,json}`（P60 产物）
   已被 09-25 扩池重跑覆盖、**不可恢复**；本笔把 `stem` 加宇宙 id 修掉碰撞源。
   **判断：如实、修得对**（旧名本身就是碰撞源；append-only 台账文件不在此列）。

### 8.6 一句结论

**P77 交付成立**：改动面最小（1 个生产文件 + 1 处文件名）、口径切换可追溯（ADR-029 + 快照两个新键）、
回退可见且有计数；唯一未达成项（真库 sha）已归因到并发补采，并在库静止后由我反证成立。
⚠️ 引用纪律照 §7.8：**旧（未复权）口径**下的 `2026-09-25-xsec-topn-csi300-500.md`（Δ=+0.3132%、CI 跨 0）
与 P60 的 `2026-09-24-xsec-topn.*` **不可与本 ADR 之后的读数混引**。
