# P49 模块2 · 自评估与熔断

创建：2026-09-22（nanobot 需求澄清）｜ 实现：Claude Code（superpowers 全流程）
需求书：[`docs/plans/2026-09-22-模块2-需求书.md`](../plans/2026-09-22-模块2-需求书.md) §3（本行）
上游需求：`stock/02_模块 2_…需求.md` §策略优化两类工作内容 / §提前熔断保护（D-4/D-27/D-28）；
边界文档 `stock/05_边界_风险_约束文档.md`（主干硬约束不可被 AI 改）
前站：[`P44` 状态机与主干常量](2026-09-22-p44-模块2-主干-状态机与常量.md)（三张台账表＋常量＋触发器已落地）、
[`P41` 绩效指标集](2026-09-22-p41-模块2-绩效对比指标集.md)（读数真源）、
[`P48` 三方对标与预测校验](2026-09-22-p48-模块2-三方对标与预测校验.md)（读数与错判案例真源）
口径：[`ADR-021`](../decisions/2026-09-22-ADR-021-模块2状态枚举与主干常量.md)、ADR-023、ADR-024、ADR-025；
澄清录 §6（D-27/D-28/D-29/D-31）＋ §8.4（**D-44**：三分支的输入/输出与执行权归属，本站新增）

---

## 0. 一句话

把「**N 轮验证周期 → 每轮读数 → 三分支判定（冻结 / 优化 / 微调）→ 提前熔断**」这条主干接起来：
判定只出**建议＋依据**（人 `approve` 才执行），边界越界**拒绝并要求重生成**（不 clamp），
熔断**可复现、幂等、只追加**。

> ❗ 本站**不自动改策略、不自动上线、不自动填归因**——那三条分别属人工闸门（D-1/D-24）与 P50（D-31）。

## 1. 判定的输入与输出（D-44，已拍板）

**输入**（全部只读，已有真源，**不许另造读法**）：

| 输入 | 来源 |
|---|---|
| 周期声明（轮次、单轮天数、参数集、**判据原文**） | `validation_cycles`（P44 已建） |
| 每轮读数 | `validation_rounds.metrics_json`，口径 = **P41 指标集**（P48 已接同一取数函数） |
| 净值/回撤 | `paper_nav_daily` + 既有 `paper/engine.py::drawdown`（**不另写回撤算法**） |
| 错判案例 | P48 的错判案例集（只读引用，**归因字段仍恒空**，D-31） |
| 边界常量 | `stocklab/config/limits.py`（`VALIDATION_ROUNDS_MIN/MAX`、`CIRCUIT_BREAKER_DRAWDOWN`、时长/冻结上限）——**AI 不可改** |

**输出** = 一条**建议**（三个分支之一）＋ 依据清单（哪几条读数、哪个案例、算到哪一步）：

| 分支 | 适用（口径） | 落地动作（**要人 `approve` 才发生**） |
|---|---|---|
| **冻结** `freeze` | 读数未达标**且**没有可归因的改进方向（案例集给不出方向） | 该版本停用，`frozen` 态，**冻结期 ≤ 90 天**；台账一行不删 |
| **优化** `optimize` | 有明确改进方向**且需要改脚本逻辑**（A1/A2 编排或因子层） | 出**新 script 版本**（走既有 plugin 生命周期 + 人工 `approve`）；**新账户 = 新策略版本**（D-26），新周期 |
| **微调** `tune` | 只动**参数**、不动脚本逻辑（因子权重/估值公式/阈值/概率模型超参） | 同 script 版本下**新参数集** ⇒ **必须新开验证周期**，把新 `params_json` 与**判据原文**一并落库 |

**纪律**：
1. 建议**不是执行**：三个分支一律只写「建议 + 依据」到台账/页面；执行路径仍走人工（`approve` 只能人工 CLI，既有护栏不许放松）。
2. **判据原文落库**（`criteria_text`，P44 已建）：事后换口径 = 这一站的典型作弊方式，禁止。
3. **越界即拒**：轮次 ∉ [2,3]、总时长 > 30 天、冻结 > 90 天 ⇒ 拒绝并要求重新生成，**不许 clamp**、不许「取最近的合法值」。
4. **样本门禁沿用 120**：轮次读数不足时不许出「优化/冻结」的**结论性**判定，只写「证据不足」＋现有读数。

## 2. 提前熔断（D-27）

- 判据：**验证期内账户净值自峰值回撤 ≥ `CIRCUIT_BREAKER_DRAWDOWN`（0.10）** ⇒
  立即标记本轮策略失效、**终止本轮验证**、写 `validation_events(kind='circuit_breaker')`，
  `at_value`（实测回撤）与 `threshold`（判据阈值）**分开存**，`criteria_text` 记判据原文。
- **不改写历史台账**：终止 = 追加事件 + 追加一轮/周期收尾行，绝不 UPDATE 既有行。
- **可复现**：同一历史输入重放必须得到同一判定（同一天、同一份净值 ⇒ 同一 `at_value`）；
  不许读墙上时钟、不许读「当前最新净值」以外的输入。
- **幂等**：同一 `(cycle_id, asof)` 重复检查**不重复写事件**（结构性防线优先：唯一索引或等价的 append-only 幂等键）。
- 熔断触发后：**账户不再下单**（通路侧的行为由 P47 的通路读取「周期已终止」状态决定，本轮只需把状态与判据暴露出去）。

## 3. 错判案例回流模块1（D-31 的边界）

- 回流 = **只读汇总**：把某周期内 P48 的错判案例按 `code`/区间聚成「待复核清单」，写进台账/页面；
- **归因字段恒空**、**不许自动填**、**不许把回流清单当结论**展示；
- 回流产物必须带**来源指纹**（`input_sha256`/`script_version`）与**生成时间**，便于事后对账。

## 4. 入口与页面

- CLI：`m2 cycle start|round|judge|fuse-check|status`（名字可调，**写进任务书 §7 进度表**）；
  退出码语义与既有 CLI 一致：成功 0、输入不合法 2、**冲突/已存在 1**（照抄既有惯例，不发明新语义）。
- 页面：判定结果与熔断事件展示在 **`/lab/m2`**（P48 建的页）新增一节，
  **只读、不提供任何写入口与「触发判定」按钮**（ADR-021 的「Web 零写入口」判据不许破）。

## 5. 测试（先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | **边界越界即拒** | 轮次 1 / 4、总时长 31 天、冻结 91 天 ⇒ 拒绝 + 明确文案；**断言库里没有半截行**（拒绝了就不许写） |
| T2 | **不 clamp** | 与 T1 同输入，断言**没有**被改成合法值的行出现（对比「越界值原样落库」与「被改写」两种实现的差别） |
| T3 | **建议 ≠ 执行** | 判定后策略版本状态/账户/参数**全部未变**；只有人工 `approve` 路径能改（静态扫描：判定模块不得调用 approve/写入路径） |
| T4 | **熔断可复现** | 同一历史输入重放两次 ⇒ `at_value`/`threshold` 逐位相同；构造一份回撤 9.99% 与 10.00% 的净值，断言判定分界与阈值一致 |
| T5 | **熔断幂等** | 同 `(cycle_id, asof)` 重复检查 ⇒ 事件数不增（结构性防线：唯一索引/幂等键） |
| T6 | **判据原文不可事后改写** | 台账里的 `criteria_text` 在判定前后逐字节相同；触发器拦 UPDATE/DELETE（既有护栏回归） |
| T7 | **错判回流不填归因** | 回流清单里归因字段恒 `NULL`/空；带来源指纹与生成时间；页面上无「原因」文案 |
| T8 | **页面零写入口** | 源码扫描 `labweb/` 对判定/熔断写路径**零引用**；`/lab/m2` 的 HTML 里没有表单/按钮提交（含反向自检） |
| T9 | 全量 | `.venv/bin/python -m pytest -q` exit 0 ＋ `bash scripts/verify.sh` exit 0（红线三目标；红了先归因，**禁止 regen**） |

## 6. 反目标

- ❌ 不做归因自动填充/配置视图/旁路触发（P50）；❌ 不改 P41/P48 的读数口径。
- ❌ 不自动 `approve`、不自动生成脚本、不自动上线（D-1/D-24）。
- ❌ 不改主干常量（阈值/边界/门禁），不提供网页写入口。
- ❌ 不用未来数据、不读墙上时钟做判定（PIT 纪律）。
- ❌ 不 `git commit`、不建分支/worktree（提交由 nanobot 拆批）。

## 7. 环境与收尾

- 解释器 `.venv/bin/python`（3.12）；测试全离线 fixture；真库 `data/stocklab.db` **只读**，
  写实测在 `/tmp` 副本上做（用完删）。
- 判据＝`pytest -q` exit 0 ＋ `bash scripts/verify.sh` exit 0；红线三目标
  `0acf35c90f54ab6a…`/175、`73ffbfb136e8620d…`/1330、`ddac0a889c623d75…`/171 不变。
- 交付物：代码 + 测试 + 页面小节 +（若需）ADR；§8 进度表逐条打勾 + 文末「实施记录」（实测命令与关键输出行）。

## 8. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | 周期主干：start / round / 收尾（三张台账表的唯一写入口） | ✅ 已验证 |
| T2 | 三分支判定（建议 + 依据，不执行）＋ 边界拒绝（不 clamp） | ✅ 已验证 |
| T3 | 提前熔断（可复现 + 幂等 + 只追加） | ✅ 已验证 |
| T4 | 错判案例回流模块1（只读汇总、不填归因） | ✅ 已验证 |
| T5 | CLI + `/lab/m2` 小节（零写入口） | ✅ 已验证 |
| T6 | §5 的 T1–T9 测试 + `pytest`/`verify.sh` | ✅ 已验证 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

### 8.1 CLI 命令名（§4 「名字可调，写进任务书 §7 进度表」的落地）

```
stocklab m2 cycle start       # 开周期（越界即拒：轮次 ∉ [2,3] / 天数 ∉ [1,30]）
stocklab m2 cycle round       # 落一轮读数（读数由 P41/P48 既有函数算出）
stocklab m2 cycle end         # 收尾（追加 validation_end，不 UPDATE 既有行）
stocklab m2 cycle judge       # 三分支判定（只写建议 + 依据，**不执行**）
stocklab m2 cycle fuse-check  # 提前熔断检查（只追加、可复现、幂等）
stocklab m2 cycle status      # 只读现状（轮次 / 事件 / 判定 / 主干边界）
```

退出码沿用既有语义：`0` 成功（含幂等返回 `already`）／`1` 冲突
（周期已存在、已收尾、轮次已有行）／`2` 输入不合法（越界、日期格式、未知分支、
表未前滚）。**没有新语义**。

---

## 实施记录（P49 · Claude Code）

> 记录顺序按任务书要求：**先写本节与 §8 打勾（含当时的实测），最后再跑一遍全量
> `pytest` / `verify.sh` 并把最终数字补进 9.1** —— 上一站（P48）在最后一跳 403
> 失联、任务书里一个字都没留下，审计只能全靠重跑。

### 9.1 交付物与文件

| 文件 | 变更 | 说明 |
|---|---|---|
| `stocklab/m2/selfeval.py` | 新增 | **纯判定**：三分支规则表 + 熔断判定 + 主干边界只读视图。零写库、零写路径 |
| `stocklab/m2/cycle.py` | 新增 | 周期主干唯一写入口：`start_cycle` / `add_round` / `end_cycle` / `fuse_check` / `judge` / `status` |
| `stocklab/store/schema.sql` | 追加 | `m2_judgements`（append-only，`UNIQUE(cycle_id,asof_date)`）+ `validation_events` 上两条**部分唯一索引** |
| `stocklab/store/validation.py` | 追加 | `insert_event(commit=False)`（熔断 + 收尾同事务）、`find_event` |
| `stocklab/m2/store.py` | 追加 | `find_judgement` / `insert_judgement` / `list_judgements` |
| `stocklab/m2/config.py` | 追加 | 三分支枚举与中文名、`fix_kind` 白名单、熔断判据**原文**常量、判定表名、归因字段名 |
| `stocklab/labweb/m2_data.py` | 追加 | `cycle_readings` / `case_summary` / `review_backlog` / `cycle_panel`；`panel` 多一段 `cycles`；`miss_cases` 的案例多带 `score_id`/`forecast_id` |
| `stocklab/labweb/m2_render.py` | 追加 | `/lab/m2` 新增「自评估判定与熔断」一节 + `cycles_text`（`m2 report` 同步） |
| `stocklab/cli/main.py` | 追加 | `m2 cycle …` 六个子命令；`_m2_conn` 的表守卫从 3 张扩到 4 张 |
| `tests/test_p49_selfeval.py` | 新增 | 56 条用例（T1–T8） |
| `tests/test_store_schema.py` / `tests/test_m2_channels.py` | 一行 | 表清单与 INSERT 白名单**只增**一项（`m2_judgements`） |

### 9.2 逐条判据的实测命令与关键输出行

| 判据 | 命令 | 关键输出（原文） |
|---|---|---|
| T1 越界即拒 | `.venv/bin/python -m pytest -q tests/test_p49_selfeval.py -k "t1"` | `4 passed`（`--rounds 1` → `exit 2`；stderr：`验证轮次 1 不在 [2, 3] 内 —— 拒绝，请重新生成方案（不做 clamp）`；`state of validation_cycles/rounds/events/m2_judgements = 0/0/0/0`） |
| T1 冻结越界 | `-k "test_t1_freeze_beyond_the_limit"` | `exit 2`；`冻结天数 91 不在 [0, 90] 内 —— 拒绝，请重新生成方案（不做 clamp）`；`m2_judgements=0 and validation_events=0` |
| T2 不 clamp | `-k "t2"` | `3 passed`；`written == []`（原值不回写、也不被夹到 2/3/30） |
| T3 建议≠执行（静态） | `-k "test_t3_the_judge_module_has_no_execution_path or test_t3_cycle_module"` | `selfeval.py`：执行路径调用 `[]`、SQL 写语句 `[]`；`cycle.py`：只出现 `insert_cycle/insert_round/insert_event/insert_judgement` |
| T3 反向自检 | `-k "planted_violation"` | 喂 `lifecycle.approve(...)` + `UPDATE plugin_scripts …` ⇒ `["approve@3"]`、`["UPDATE@4"]` |
| T3 行为对照 | `-k "changes_nothing_outside"` | 判定前后 8 张表（插桩/审计/账户/净值/成交/周期/轮次/事件）逐行相等；`m2_judgements == 1` |
| T4 分界 | `-k "threshold_boundary"` | 谷底 19802.2（峰值 22000）⇒ `tripped=False`；谷底 19800.0 ⇒ `tripped=True`；`threshold=0.1` |
| T4 可复现 | `-k "bit_identical"` | 两次 `at_value/threshold` 逐位相同；第二次 `status="already"`；`abs(at_value-0.1) < 1e-9` |
| T5 结构幂等 | `-k "unique_index_is_the_defense"` | 绕接口裸插第二条熔断 ⇒ `sqlite3.IntegrityError` |
| T5 幂等 | `-k "repeat_checks"` | 连查三次 ⇒ `validation_events == 2`（熔断 + 收尾） |
| T6 判据原文 | `-k "t6"` | 判定前后 `criteria_text` 逐字节相同；6 条 UPDATE/DELETE 全部被触发器拒 |
| T7 归因恒空 | `-k "t7"` | 清单 `n_cases > 0`；每条明细 `item["attribution"] is None`；`generated_at` 非空；页面 `"原因" not in html` |
| T8 零写入口 | `-k "t8"` | `POST /lab/m2 → 404`；HTML 无 `<form` / `<button` / `<input` / `method="post"`；`labweb/` 对 9 个写入口名零引用（反向自检判红） |
| T8 读数同源 | `-k "round_metrics_come_from"` | 轮次 `metrics_json.metrics` 与 `paper_data.performance` 五指标**逐字段相等**；`sample_gate` 相等 |
| 全量 | `.venv/bin/python -m pytest -q` | 见 9.3 |
| 红线 | `bash scripts/verify.sh` | `sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致`、`sha256 ddac0a889c623d75… == 基线；叶子 171 项一致`、`✅ 回归红线全部成立` / `✅ 验证通过: all`（exit 0） |

### 9.3 判据先红后绿（变异测试）

不只「跑绿了」，而是**确认判据有区分力** —— 往被测实现里注入 3 个缺陷，
对应用例必须判红（脚本化：改文件 → 跑用例 → 还原）：

| 注入的缺陷 | 期望 | 实测 |
|---|---|---|
| `limits.check_validation_plan` 改成 **clamp** 而不是抛错 | T1/T2 判红 | ✅ 8 条 `test_t1_plan_out_of_bounds_is_refused_with_no_rows[...]` / `test_t2_no_clamped_row_is_written[...]` 全红 |
| 去掉 `uq_validation_events_circuit_breaker` **唯一索引** | T5 判红 | ✅ `test_t5_the_unique_index_is_the_defense` 红（裸插不再被拒） |
| 让 `selfeval.py` 出现 `approve(...)` + `UPDATE plugin_scripts` | T3 判红 | ✅ `test_t3_the_judge_module_has_no_execution_path` 红 |

> **诚实备注**：第 2 条变异只让「绕接口裸插」那条用例判红，`test_t5_repeat_checks_do_not_add_rows`
> 仍绿 —— 因为 `fuse_check` 里还有一道 `find_event` 预读。也就是说幂等实际由
> **预读 + 唯一索引**两道共同保证，其中「结构防线」这半边的证据是第一条用例给的。
> 两条都留，不合并成一条「幂等」用例（合并会让「防线是不是结构性的」这个问题无从回答）。

### 9.4 真库 `data/stocklab.db`（任务书 §7 / 施工须知 9）

- 开工前：`shasum -a 256` = `3dcb62ab7e0810eaf6404609d14b38e27582a7c5f6f100f9188f8d7836a5ef82`
- 收工：`fab07dc91d549ac855d8c24cdb220c51338dbfd83cd20b0176b8afefce6c395a` —— **变了**。
- **归因（不是本站写入的数据）**：变的只有**结构**。`scripts/verify.sh` 第 4c 步
  `check_redlines.py` 的 `predict` / `backfill` 目标**依赖本机真实库**（脚本注释自己写明
  「不假装 hermetic」），它们打开写库入口 → `ensure_schema` 前滚（ADR-015）→
  建出 `m2_judgements` 与两条部分唯一索引。实测该库所有模块2 / 验证台账表**行数仍为 0**：

  ```
  validation_cycles 0 / validation_rounds 0 / validation_events 0
  m2_channel_runs 0 / m2_forecasts 0 / m2_forecast_scores 0 / m2_judgements 0
  plugin_scripts 7 / paper_accounts 5        # 均非本站所写
  idx: ['uq_validation_events_circuit_breaker', 'uq_validation_events_end']
  ```

  同一时刻红线三目标（按**叶子**比对）全部与基线一致 ⇒ 事实数据未变。
- **口子（点名）**：`ensure_schema` 会在任何写库入口前滚真库，因此「真库只读」在
  **跑过 `verify.sh` 之后就自动不成立**（这是 P44 起就有的既有行为，不是本站引入的）；
  本站只做到「不改动任何既有行、不新增任何数据行」。若要求 verify.sh 全程不碰真库，
  需要上一站改 `check_redlines.py`（不在本站范围）。

### 9.5 未验证 / 存疑（点名）

1. **`decide_branch` 的规则表是本站的落地解读**：D-44 说「冻结＝读数未达标且无可归因方向」，
   但「未达标」的机械判据（相对基准超额 ≤ 0）与「需要改脚本逻辑」的声明方式
   （`--fix-kind`）在 D-44 里没有逐字规定。本站的做法是**把无法从数据推出的那一格
   交给调用方声明、判定只核对自洽性**，并写进 `selfeval.py` 的模块 docstring。
   **这不是 D-44 的原文，是一个需要复核的解读** —— 若口径要改，改的是规则表而不是判据。
2. **`insufficient` 是第 4 个枚举值**（不是第四个「分支」）：任务书 §1 的表只列了三分支，
   但纪律 4 要求门禁不过时「只写证据不足」，所以它必须有落库的形状（否则「没判定」与
   「判定为冻结」在库里分不开）。`m2_judgements.branch` 的 CHECK 因此是 4 值。
3. **页面上的「生成时间」**取的是来源行的**落库时间上界**（不读墙上时钟，为了可复现）；
   页面顶部的「页面生成于 …」仍是墙上时钟。两者名字不同、语义不同，容易被读成同一个 —— 已在页面上写明。
4. **`review_backlog` 的周期内案例是 P48 最近 50 条的子集**（复用 `miss_cases` 的取数与
   上限，不另开筛选口径）。周期内错判超过 50 条时只看得到最近那一段 —— 已把
   「上限」与「窗口内总量」都返回，但没有做到「周期内全量」。
5. **熔断只暴露状态，不拦下单**：`fuse_check` 之后账户不再下单，要靠 P47 的通路读
   「周期已终止」—— 本轮**没有**去改通路侧的这一步（任务书 §2 明说「本轮只需把状态与
   判据暴露出去」）。真实交易路径上「熔断后不再下单」因此**尚未端到端验证**。
6. **`m2_judgements` 是本站新增的表**：真库上的前滚只在本机 verify.sh 的一次运行里发生过，
   未在「老库（未前滚）」上单独演练 `ensure_schema` 的建表路径。
7. **判定与轮次都不写 `plugin_audit`**：策略版本的 `validating` / `frozen` 状态迁移仍无
   生产调用者（ADR-021 的「未验证口子 1」原样留给下一站），本站的 T3 只证明「判定没有动它」。

### 9.6 全量结果（最后一次运行，2026-09-23）

- `.venv/bin/python -m pytest -q` → **exit 0，全绿（输出里没有 `F`/`E`/`FAILED`）**。
  用例数：`--collect-only` 采集 **2711** 项；基线采集 2655 项（= 2653 passed + 2 skipped），
  本站 **+56**（`tests/test_p49_selfeval.py`），**只增不减**。
  > 注：`pyproject.toml` 的 `addopts = "-q"` 与命令行的 `-q` 叠加成 `-qq`，pytest 因此
  > **不打印** `N passed` 汇总行（实测：末行是进度条而非统计）。所以这里的「绿」以
  > 退出码 + 无失败标记为准，数字来自 `--collect-only` 的求和。
- `bash scripts/verify.sh` → `✅ 验证通过: all`，**exit 0**；红线三目标与基线一致：
  `sha256 0acf35c90f54ab6a…/175`、`sha256 73ffbfb136e8620d…/1330（leaves 3af6008903703950…）`、
  `sha256 ddac0a889c623d75…/171（leaves 06bf461ef1234b4e…）`，`✅ 回归红线全部成立`。
  **未 regen 任何基线**（红了先归因，本站没有红）。

---

## 10. nanobot 独立复核（2026-09-23，审计记录）

> 判据**全部自跑**，不采信 §9 的自报数字；写实测只在 `/tmp` 副本上做（副本已删）。

### 10.1 判据逐条结果

| 判据 | 我的命令 | 实测 |
|---|---|---|
| 全量测试 | `bash scripts/verify.sh` | `2709 passed, 2 skipped in 165.67s`（采集 2711 = 基线 2655 + 本站 56，**无 `F`/`E`/`FAILED`**）→ `✅ 验证通过: all`，**exit 0** |
| 本站用例单跑 | `.venv/bin/python -m pytest -q tests/test_p49_selfeval.py` | `56 passed`（进度点 56 个），exit 0 |
| 红线三目标 | 同上（`scripts/check_redlines.py`） | `0acf35c9…`/叶子 175（leaves a59e8c19…）、`73ffbfb1…`/1330（leaves 3af60089…）、`ddac0a88…`/171（leaves 06bf461e…）**全部 == 基线** ⇒ `✅ 回归红线全部成立`；`docs/baselines/redlines.json` 未被改动（**未 regen**） |
| 工作区范围 | `git status --porcelain` | 恰好 §9.1 那 11 改 + 3 新，**无多余改动**；未碰 `ops/chain.py`、P48/P50 范围文件 |
| 真库 | `shasum -a 256 data/stocklab.db` | 审计前 `17e28b92…`、**审计后同值**；模块2/验证台账 7 张表行数全 0（`validation_cycles/rounds/events`、`m2_channel_runs/m2_forecasts/m2_forecast_scores/m2_judgements`） |
| 页面 | 副本上 `m2_data.panel` + `m2_render.m2_page`；`handle("POST", "/lab/m2")` | `POST → 404`；HTML 无 `<form>`/`<button>`/`<input>`/`method="post"`；含「自评估判定与熔断（P49）」小节、判据原文、判定表、逐条「算到哪一步」 |
| CLI 端到端 | 副本上自跑 15 条（退出码逐个捕获） | 见 10.2 |

### 10.2 CLI 演练的退出码（副本 `/tmp/p49-copy2.db`，真库未参与）

| 调用 | exit | 库内副作用 |
|---|---|---|
| `start --rounds 1` / `--days 31` | **2** / **2** | 四表**零行**（拒绝了就不写，无半截行） |
| `start`（合法） | 0 | `validation_cycles` +1 |
| `start`（同版本同账户未收尾） | **1** | 不增行 |
| `round --round-no 1` | 0 | `validation_rounds` +1 |
| `round --round-no 1`（重放） | **1** | 不增行 |
| `round --round-no 4` / `asof` 早于起跑 / `2026-9-22`（非规范写法） | **2** / **2** / **2** | 不增行 |
| `fuse-check`（窗口最深回撤 0.020988 vs 0.1） | 0 | **不写事件**（未触发就不留行） |
| `fuse-check`（同一 `(cycle, asof)` 重放） | 0 | 不增行 |
| `judge --fix-kind tune`（门禁未过，无案例） | **2** | 不写行 |
| `end` / `end`（重放） | 0 / 0 | 追加 1 行 `validation_end` / 返回 `already` |

### 10.3 两处新发现（不阻断交付，建议收口）

1. **F1 · 冻结天数越界在「证据不足」路径上不校验**：全新 `(cycle, asof)` + `--fix-kind none --freeze-days 91` + 门禁未过 ⇒
   **exit 0**，且 `evidence.freeze_days = 91` 落进台账（该行 `branch=insufficient` / `conclusion=false` / `actions=[]`）。
   `FREEZE_MAX_DAYS` 只在真要走 `freeze` 分支时才校验 ⇒ §5 的 T1「冻结 91 天 ⇒ 拒绝」**只在门禁达标那一种场景成立**，
   §9.2 那一行没注明前提。**风险**：越界值进了 append-only 台账，与「越界即拒」的纪律读起来矛盾（不会被执行，但会被读到）。
   **建议**：把 `--freeze-days` 的边界校验前移到 `judge()` 入口（与分支无关），或 `insufficient` 分支不把 `freeze_days` 写进依据。
2. **F2 · 幂等优先 ⇒ 越界入参静默得到 exit 0**：同一 `(cycle, asof)` 已有判定行时，后来的 `judge`（哪怕 `--freeze-days 999`）
   返回 `status=already` + **exit 0**，**不校验本次入参**。**建议**：`already` 返回里加一句「本次入参未使用（返回的是已存行）」，
   免得调用方读成「我的请求被接受」。

> 两条都属于「判定层」的口子，**没有执行路径**（`actions=[]`、执行仍要人工 `approve`），故**不阻断**本站交付、也不影响 P50 派站。

### 10.4 真库 sha 与 §9.4 的差额（我这一侧的归因）

§9.4 记的开工前值 `3dcb62ab…` 与我审计时看到的 `17e28b92…` 不同，差额**不是本站写的业务行**：
`job_runs` 显示 09-23 由 launchd `ops patrol` 触发的 `ingest_actions`（17/17 ok ⇒ `adj_factors` +17）、
`ingest_valuation`、`session_tick`（`snapshots +17`）都会写真库数据行；模块2 / 验证台账 7 张表**行数仍为 0**，
红线三目标（按叶子比对）与基线逐项相同 ⇒ **事实数据未变**。§9.4 的「口子」结论成立且比它写的更广：
真库在「跑过 `verify.sh`」与「launchd 定时任务跑过」之后都会变，「真库只读」只能按**行级**判（有无本站写入的数据行），
不能按 sha 判。

### 10.5 采信与未验证

- §9.5 的七条口子**我全部采信**（逐条读过代码：`insufficient` 确为第 4 个枚举值；`fuse_check` 只暴露状态、通路侧未接；
  `review_backlog` 复用 `miss_cases` 的 50 条上限；判定/轮次都不写 `plugin_audit`）。其中第 5 条（熔断后不再下单未端到端验证）
  与第 7 条（状态迁移无生产调用者）**留到后续站**，本站不补。
- 未独立复核的部分：§9.3 的三条变异注入是我读代码判断合理性、未逐条重放（重放的代价大于收益）；其余判据均已自跑。
- CC 花费：`/tmp/cc-p49.json` = `is_error=false` / `subtype=success` / `terminal_reason=completed` /
  `num_turns=128` / `duration_ms=1625343`（10:23→10:50）/ **`total_cost_usd=7.628513`**。

