# P11 任务拆分 · 调度链（task 45–52）

计划：`docs/plans/2026-09-15-p11-调度链.md`

| # | 任务 | 验证方式 |
|---|------|----------|
| 45 | `quote_snapshots` 表 + 两个 append-only 触发器 | `pytest tests/test_session_store.py`；`sqlite3 .schema` |
| 46 | `fetch_quotes`（腾讯快照，GBK，白名单，**不入 raw_cache**） | `tests/test_data_fetch_quotes.py`（离线 fixture 回放） |
| 47 | `session/quotes.py` 行构造 + `session/store.py` 四态幂等 | 同上 + `tests/test_session_store.py` |
| 48 | `insert_bars` 改 `COALESCE`（重采不抹回填值） | `tests/test_store_repo.py` 新增一条 |
| 49 | `session/close.py` 收盘回填（只补 NULL / 只当日 / adj_mode 硬拒绝） | `tests/test_session_close.py` |
| 50 | `session/tick.py` 编排 + JSON 摘要（到期判据 / 异常留痕） | `tests/test_session_tick.py` |
| 51 | `session/review.py` 报告（口径分列 / 按日聚类 + CI） | `tests/test_session_review.py` |
| 52 | CLI 接线（`session tick` / `session backfill-close` / `review daily`）+ `doctor` 抽函数 | `tests/test_cli_session.py` |

## 开工前查错误日记（必做）

| 日记条目 | 对本次的风险 |
|----------|--------------|
| #10 `_canonical()` 未规约 → 第二次跑必冲突 | 快照 hash 前必须 `float()/int()` 归一 |
| #16 分支按「开关」而非「事实」取值 → 报告谎报 | `provenance` 分支必须按**实测行数**取值，不按「有没有实盘开关」 |
| #17 幂等键取整份产物 → 改错别字多判一条 | 快照键取 `(code, trade_date, ts)` 语义，不含 `fetched_at` |
| #18 判据无落点 → 只能插桩复算 | 报告的每个关键词都要有 JSON 字段落点 |
| 2026-09-14「取不到就回退」 | 接口失败**不许**降级/造数，必须显式报错 |
