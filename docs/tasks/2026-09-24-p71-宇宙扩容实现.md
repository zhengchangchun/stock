# P71 宇宙扩容实现（**代码站**：载体 + 消费方 + 预注册字段；**不联网、不写真库**）

- 立项人：nanobot（2026-09-24 23:0x）
- 上游：`docs/tasks/2026-09-24-p70-种子宇宙扩容-设计先行.md`（设计先行站，已完成）
  ＋ `docs/plans/2026-09-24-p70-宇宙扩容-设计稿.md`（Q1–Q6 结论、A/B/C 对比、一页拍板点 D1–D8）
- 上游复核：设计站 §8「nanobot 独立复核」已落（commit `6ecf18e`），设计稿不必重写、结论不必重测。

## 0. 一句话

让「宇宙」从**写死的常量**变成**带 id 与 sha 的显式对象**：新增 `config/universes/` 真源文件 ＋
`universe build/sync/doctor` 三条命令 ＋ 8 处消费方的 **`--universe` 参数（默认值一个都不改）**
＋ 预注册新增 `universe` 字段；**默认路径（老的 21 只）逐位不变**。

## 0.5 拍板点已锁定（nanobot 定，站不许另选；有异议写在 §7.7 偏离里）

| # | 锁定值 | 依据 |
|---|---|---|
| **D1** | 只做「现成分」；报告一律打 `non_pit=true` + 设计稿 §2.2 的 N1/N2/N3 限定句 | 历史成分与退市名单都拿不到（设计稿 §2.1） |
| **D2** | **`SEED_UNIVERSE` 语义与取值一字不动**；扩宇宙**只能**走新增 `--universe <id>`，**不得隐式回退** | 这是任务书 §0.6 ③ 点名的「必须回对话」项 —— 拍板结果＝**选不动**，所以不需要回对话；另一条路（把 `SEED_UNIVERSE` 降级为历史宇宙之一）**保持开放**，本档不碰 |
| **D3** | **C ＝ repo 文件＝真源、DB 表＝投影**（＋ `universe doctor` 只读对账） | 真源要能被 `git log` 追溯；下一站要 SQL join |
| **D4** | `stocklab/m2/context.py:113` 硬守卫**一字不动** | 放宽到表 ≈ 不改变被拒集合，却把防线从常量级降到数据级 |
| **D5** | `sector` 为空 ⇒ 名称推断的**兜底保留** | 删了会让 15/21 只输出变化且**测试抓不到** |
| **D6** | 采集分批顺序 `universe+sector → bars → valuation → financials → moneyflow → actions`；moneyflow 单独一批、可跨天 | 详见 §0.5.1 |
| **D7** | 扩池版 `xsec-topn` **必须新预注册**，且 `research/xsec.py::PREREG_FIELDS` **新增 `universe`** | 不加字段 ⇒ 同一份预注册换宇宙能跑出两个结论（静默换宇宙） |
| **D8** | 红线基线**不 regen**；扩池后库指纹大量「新增」差异是**预期** | `instruments 21→821`、`bars code 23→823` |

### 0.5.1 站点边界（**重要**：D6 的采集不在本站）

设计稿 §9.3 已定：**B0–B5 的联网写库动作由 nanobot 执行，CC 侧一律假源**。
⇒ 本站**只交付代码、测试、文档**；`universe build`（联网抓成分）与 `universe sync`（写真库）
的**真实执行由 nanobot 另跑**。站内测试一律注入假 fetcher，**不许联网**。

## 1. 任务

### T1 宇宙载体（D3＝C）

- 新增 `stocklab/config/universes.py`：
  - `Universe` 数据类：`universe_id` / `members: tuple[Instrument, ...]` / `meta: Mapping` /
    `members_sha256`（csv 文本的 sha256，**canonical、与行序绑定**）/ `path`。
  - `load_universe(universe_id, *, root=paths.UNIVERSE_DIR)` —— **fail-closed**：
    文件缺失 / CSV 列不合规 / 行数与 `meta.n_members` 不符 / `members_sha256` 与 `meta` 不符
    / 代码重复 ⇒ 抛 `UniverseError`，**绝不回退到 `SEED_UNIVERSE`**。
  - `build_universe(source, *, out_dir, fetcher=None)` —— `fetcher` **可注入**（默认走
    `stocklab.data` 的既有 `fetch_*`；离线测试用假 fetcher 返回固定行）。`source` 取值
    `seed21`（离线可建）与 `csi300+csi500`（联网；本站只在假 fetcher 下测）。
- 新增 `config/universes/seed21.csv` ＋ `config/universes/seed21.meta.json`：
  `seed21` 由 `SEED_UNIVERSE` **离线派生**（本站可直接生成、可逐位对账），
  `meta` 字段按设计稿 §3.2 问 1：`source` / `built_at` / `pit`（`false`）/ `n_members` / `members_sha256`。
- 真实 `csi300-500` 文件**不要求本站生成**（需联网）；但 `build --source csi300+csi500`
  的**代码路径与测试必须在**（假 fetcher 两页 300+500 去重成 800）。

### T2 三条 CLI（`stocklab/cli/main.py` 新增 `universe` 子命令组）

| 命令 | 行为 | 何时由谁跑 |
|---|---|---|
| `universe build --source <s> --out <dir>` | 抓成分 ⇒ 写 `config/universes/<id>.csv` ＋ `.meta.json` | 联网；**nanobot** 跑 |
| `universe sync --universe <id>` | 把成员表投影进 DB（新表 `universe_memberships`）＋ 追加 `instruments` 行 ＋ 补 `sector` | 写真库；**nanobot** 跑（站内只在 `/tmp` 副本测） |
| `universe doctor --universe <id>` | **只读**对账：文件 sha vs 表投影 sha、`instruments` 覆盖、`sector` 非空率；不一致 ⇒ **exit 2** ＋ 逐条打印差异 | 任何时刻都只读 |

- `universe sync` 的投影必须**幂等**（按 `universe_id` 整体重写该 id 的行；重入不新增行）。
- 老 21 行 `instruments` 与任何历史表行**不动**（只允许新增行、只允许给 `sector` 之类**空列**补值）。

### T3 消费方改造（设计稿 §4 的 9 条逐条落地）

| # | 位置 | 改法 |
|---|---|---|
| 1 | `candidate/run.py:98` `_cross_section_map` | 加**显式**参数 `universe=None`（`None ⇒ SEED_UNIVERSE`） |
| 2 | `candidate/run.py:135` `score_pipeline` 主循环 | 同上、参数透传（横截面分位必须与扫描集合同集合） |
| 3 | `candidate/run.py:188` | **新增** `universe_id` ＋ `members_sha256`，**保留** `seed_count` |
| 4 | `m2/context.py:53` `INSTRUMENTS` | **不改**（D4） |
| 5 | `m2/context.py:109-119` 硬守卫 | **不改**（D4） |
| 6 | `cli/main.py:452,462,467` `ingest financials` | 加 `--universe <id>`；**默认仍是 `SEED_UNIVERSE`** |
| 7 | `scripts/extend_history.py:35,65,120` | 加 `--universe <id>`（或 `--codes-file`）；**不带参数时行为逐位不变，含 `StopIteration` 语义** |
| 8 | `labweb/cand_data.py:47,150,151,209` | 改读快照 `params_json` 的 `universe_id`/`members_sha256`；**取不到则走旧路径并标注** |
| 9 | 硬断言 21 的测试（见 §1 T5） | 断言对象改成 `SEED_UNIVERSE` 派生量；新增 `--universe` 路径用例 |

> **「行为零变化」的准确边界**（照设计稿原文）：改 1/2/6/7 的默认参数后，不传新参数时字节级行为不变；
> 但第 3 条**会**多两个键，那是**有意的口径增量**。判据＝「老键逐位不变 ＋ 成员集合逐位不变」，
> **不是**「快照行 sha 不变」。

### T4 预注册新增 `universe`（D7）

- `stocklab/research/xsec.py::PREREG_FIELDS` 新增 `"universe"`。
- **兼容裁决（nanobot 定，别自己发明）**：老预注册文件
  `docs/experiments/2026-09-24-xsec-topn.md` **一字不改**（台账 append-only），
  其**缺** `universe` 字段 ⇒ 语义**视为 `seed21`**（写进 docstring ＋ 报告一行）；
  `validate_prereg` 仍按 `universe` 比对命令行实参 ⇒ **命令行传 `--universe csi300-500`
  而预注册推得 `seed21` 即拒跑（exit 2）**。这样既堵「静默换宇宙」又不破坏老实验原样重跑。
- 新增预注册**模板/示例**文件（可在本档建，若不便则由 nanobot 建）：`docs/experiments/` 下新文件，
  **不许改**老文件。

### T5 测试

- 新增：`tests/test_universe_config.py`（载体、fail-closed、sha、幂等 build）、
  `tests/test_universe_cli.py`（三条命令退出码；`sync`/`doctor` 只在副本库上跑）、
  `tests/test_research_xsec_universe.py`（`universe` 字段缺失/不符 ⇒ exit 2；缺失 ⇒ `seed21` 语义）。
- 修改（硬断言 21）：`tests/test_candidate_seeds.py`、`tests/test_research_xsec.py:92`、
  `tests/test_labweb_candidate.py:117,266`、`tests/test_ingest_financials.py:92`、
  `tests/test_extend_history.py:12,115`（**先核实际行号**，任务书给了设计稿的记法）。
- 覆盖一条**逐位对账判据**：`--universe seed21` 与默认路径的候选池快照
  `params_json` 除新键外相等、成员集合相等。

### T6 文档

1. `docs/decisions/2026-09-24-ADR-026-宇宙配置载体（文件为真源表为投影）.md` —— 记 D3＝C、
   D2＝不动、D4/D5＝不该动，含「什么条件下推翻」。
2. `docs/plans/2026-09-24-p71-实现计划.md` —— 实现计划（站点自拟结构，一屏能读完）。
3. 本任务书 §7 实施记录（含 §7.7 偏离、§7.8 未验证口子）。
4. **不写**「扩池后准确率会提高」这类无判据的话（设计稿 §7.3 已给定可否证的判据 E1–E5）。

### T7 判据（站自跑，逐条贴原始输出）

见 §6。

## 2. 硬约束（违反即返工）

1. **不许联网**：所有测试用假 fetcher／fixture；`HttpClient` 的真实调用只在 nanobot 侧发生。
2. **不许写真库**：任何 `sync`/写路径实测只在 `/tmp` 的库副本上；真库一律 `mode=ro`。
   只读打开姿势：`sqlite3.connect('file:/abs/path/data/stocklab.db?mode=ro&immutable=1', uri=True)`
   （**必须绝对路径**）。
3. **默认路径逐位不变**：`paper/**`、`m2/**`、`plugin/**` 零行为变化；红线三目标
   `0acf35c9…`（叶 175）/ `73ffbfb1…`（1330）/ `ddac0a88…`（171）逐位不变、**不许 regen**。
4. **不改主干常量语义**：`SEED_UNIVERSE` / `SEED_CODES` / `DEFAULT_UNIVERSE` / `ETF_WHITELIST` 取值不动。
5. **不混站**：不动 `select_top` 取前 N 的策略问题（另立任务书）；不做 PIT 历史成分（拿不到）。
6. **提交纪律**：只用**显式路径**提交（禁 `git add -A`）；`docs/` 与代码分开提交；
   不提交 `data/`、`config/universes/*.csv` 之外的生成物需先确认是否该进 git（**CSV 与 meta 要进 git**，这是 D3 的前提）。
7. **不删不改历史行**：append-only 语义照旧；不删任何既有测试用例（只允许改断言对象）。

## 3. 已经测过的硬数字（**直接引用，不许重测**）

- K 线源站吞吐：`sh600030` 4024 根 0.90 s／`sz000001` 3994 根 0.73 s／`sh601899` 4025 根 0.97 s ⇒ **0.87 s/只**。
- 真库现状（只读）：`instruments` 21 行（`sector` 非空 **0**）；`bars_daily` 97323 行 / 23 code。
- 成分可得性：东财 `push2` clist `fs=b:BK0500`=300 行、`fs=b:BK0701`=500 行；
  `RPT_INDEX_TS_COMPONENT`（`TYPE=1`→300 / `TYPE=3`→500）带 `WEIGHT` ＋ `INDUSTRY`（800/800 非空，两页 1.11 s）。
- ⚠️ `RPT_INDEX_CONSTITUENT` **不是**历史成分（`count` 恰 300，`TRADE_DATE` 跨 2005→2026）—— 别当历史来源用。
- 中证指数官网**拿不到**（且 `csindex.com.cn` 不在 `ALLOWED_HOSTS` 白名单）—— 别把「拿不到」写成事实之外的东西。
- `eligible_n`（`seed21`、`short` 池、571 边界）：P50 18、max 20；`n_periods_arms_identical = 0/571`。

## 4. 交付物

1. 代码：`stocklab/config/universes.py`、`stocklab/cli/main.py`（`universe` 组）、
   §1 T3 的 6 处消费方改动、`stocklab/research/xsec.py`（`PREREG_FIELDS` ＋ 校验）。
2. `config/universes/seed21.csv` ＋ `seed21.meta.json`（进 git）。
3. 表 `universe_memberships` 的建表/迁移（`stocklab/store/migrate.py` 里加一条**幂等**迁移）。
4. 测试（T5 的 3 个新文件 ＋ 6 个改动文件）。
5. 文档（T6 的 ADR ＋ 实现计划 ＋ 本任务书 §7）。

## 5. 反目标（跑完自查，逐条贴证据）

| 反目标 | 怎么证 |
|---|---|
| 没有动 `SEED_UNIVERSE` 取值/语义 | `git diff` 里 `candidate/seeds.py` 只允许新增注释级内容，常量块逐行不变 |
| 没有动 `m2/**` 行为 | `git diff --stat -- stocklab/m2` 为空 |
| 没有 regen 红线 | `docs/baselines/redlines.json` 不在 diff 里；三目标 sha == §2.3 基线 |
| 没有联网 | 站内 `grep -rn "HttpClient(" tests/` 只出现在既有 fixture；新测试全假 fetcher |
| 没有写真库 | 真库 sha256 跑前跑后一致（跑前后各记一次并贴出） |
| 没有承诺「准确率会提高」 | 全文检索无此类表述 |

## 6. 判据（站自跑，逐条贴输出）

1. `.venv/bin/python -m pytest -o addopts=""` **全绿**（记录 passed/skipped 数字；基线 3005 passed / 2 skipped，
   本站新增用例后应 ≥ 3020）。
2. `bash scripts/verify.sh` **exit 0**。
3. 红线三目标 sha（`check_redlines.py` 或 `verify.sh` 路径）== `0acf35c9…` / `73ffbfb1…` / `ddac0a88…`，**未 regen**。
4. `--universe seed21` vs 默认路径：候选池快照 `params_json` 老键逐位相同、成员集合相同（新键除外）。
5. `universe doctor --universe seed21` 在**副本库** exit 0；把副本 sha 改坏后 exit 2 且打印差异。
6. `xsec.py`：预注册缺 `universe` ⇒ 视为 `seed21`；命令行传 `csi300-500` ⇒ exit 2（贴命令与输出）。
7. 真库 sha256 跑前 == 跑后（贴两次 `shasum -a 256 data/stocklab.db`）。

## 7. 进度

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | 宇宙载体 `config/universes.py` ＋ `seed21` 文件 | ✅ | `tests/test_universe_config.py` 26 用例绿；`seed21.csv` sha `b77cd49e…` 由 `SEED_UNIVERSE` 离线派生、可重生成（`test_rebuilding_seed21_offline_reproduces_the_committed_file`） |
| T2 | `universe build/sync/doctor` 三条 CLI ＋ 迁移 | ✅ | `tests/test_universe_cli.py` 14 用例绿；§6 判据 5 在 `/tmp` 副本上实测（见下） |
| T3 | 消费方 8 处（4/5 明确不改） | ✅ | `git diff --stat -- stocklab/m2` **空**；§6 判据 4 逐位对账绿 |
| T4 | `PREREG_FIELDS` 新增 `universe` ＋ 兼容裁决 | ✅ | `tests/test_research_xsec_universe.py` 9 用例绿；§6 判据 6 实测 exit 2 |
| T5 | 测试（3 新 ＋ 6 改） | ✅ | 新增 3 文件 ＋ `test_candidate_seeds.py` 改断言对象；**实测只有 1 个既有文件需要改**（见 §7.7 偏离 3）；另加 `test_candidate_run.py` 判据 4 用例 |
| T6 | ADR-026 ＋ 实现计划 ＋ §7 实施记录 | ✅ | `docs/decisions/2026-09-24-ADR-026-宇宙配置载体（文件为真源表为投影）.md`、`docs/plans/2026-09-24-p71-实现计划.md` |
| T7 | §6 七条判据逐条自跑 | ✅ | 见 §7.J1–§7.J7，逐条贴原始输出 |

### 7.0 开工基线

- **HEAD**：`758fa9c1878d7caf493096ffeea31cffbed7d2fe`（`docs(p71): 宇宙扩容实现任务书…`）。
  ⚠️ 本轮是**续跑**：上一轮把 T1–T5 的代码与测试写进工作区、未提交、§7 未写。
  开工时工作区＝任务书列出的 14 个 `M` ＋ 6 个 `??`（`config/`、`cli/universe.py`、
  `config/universes.py`、3 个测试文件）；本轮另加 `tests/test_candidate_run.py`（判据 4 用例）。
- **真库**（只读读数，不是我的写入）：`data/stocklab.db`
  sha256 `e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b`，
  mtime `2026-09-24T20:30:29`，size `197779456` bytes。
  ⚠️ 记忆里的纪律：真库 sha 变了**不等于**我违规（有并发写者 + WAL checkpoint）；
  我所有写路径都指向 `/tmp/u_copy2/`。
- **pytest collect**：`3148 tests collected`（`.venv/bin/python -m pytest -o addopts="" --collect-only -q`）。
- **`check_redlines.py` 三目标**（跑在改动后的工作区上）：

```
  ✅ predict_synthetic
       sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致（leaves_sha256 a59e8c1906bc4ce2…）
  ✅ predict_real_2026-09-14
       sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致（leaves_sha256 3af6008903703950…）
  ✅ backfill_real_2013-12-23_2026-09-14
       sha256 ddac0a889c623d75… == 基线；叶子 171 项一致（leaves_sha256 06bf461ef1234b4e…）
✅ 回归红线全部成立
```

### 7.J1 判据 1：全量 pytest

```
$ .venv/bin/python -m pytest -o addopts="" -q
3147 passed, 2 skipped in 191.74s (0:03:11)
```

（本次提交前的**冻结树**上又跑了一次：`3147 passed, 2 skipped in 199.08s` —— 与上同。
两次之间只改了一处测试 docstring 的章节引用，无任何可执行代码差异。）

3147 passed ≥ 3020，0 failed（**第一次跑有 1 条红**：`tests/test_store_schema.py::
test_schema_creates_all_tables` 的 `EXPECTED_TABLES` 缺新表 —— 已把
`universe_memberships` 加进该集合，**只改断言对象、用例未删**；上面的数字是修好后的）。

### 7.J2 判据 2：`scripts/verify.sh`

```
$ bash scripts/verify.sh > /tmp/verify_p71.log 2>&1; echo "EXIT=$?"
EXIT=0
$ grep -E "passed|failed" /tmp/verify_p71.log | tail -3
3147 passed, 2 skipped in 199.60s (0:03:19)
...
✅ 验证通过: all
```

`verify.sh` 内部除 pytest 外还跑了 `check_redlines.py`（第 4c 步）—— 见 §7.J3。

### 7.J3 判据 3：红线三目标未 regen

见 §7.0 的 `check_redlines.py` 原始输出：三目标 sha 与基线逐位相同，
`docs/baselines/redlines.json` **不在** `git status` 里（未 regen）。

### 7.J4 判据 4：`--universe seed21` vs 默认路径逐位对账

由用例 `tests/test_candidate_run.py::test_explicit_seed21_matches_default_path_bytewise` 覆盖：

```
$ .venv/bin/python -m pytest -o addopts="" -q tests/test_candidate_run.py
14 passed in 1.52s
```

断言内容：`params` 的老键（`seed_count` / `topn`）逐位相同；新键 `universe_id` 两条路径都
是 `"seed21"`、`members_sha256` 相等；`members` 与 `rejects` 的 code 序列逐位相同。

### 7.J5 判据 5：`doctor` 在副本库 exit 0；改坏 sha 后 exit 2

副本：`mkdir -p /tmp/u_copy2 && cp data/stocklab.db /tmp/u_copy2/stocklab.db`（**新目录**，
不用 `rm -rf`）。

```
$ .venv/bin/python -m stocklab.cli.main universe sync --universe seed21 --db /tmp/u_copy2/stocklab.db
universe=seed21 投影=21 行 新增 instruments=0 只 补空 sector=0 只 sha=b77cd49ebee8e0b74fd7114ce47b3efe081e6168598aee379825966c45f330f3
exit=0

$ .venv/bin/python -m stocklab.cli.main universe doctor --universe seed21 --db /tmp/u_copy2/stocklab.db
universe=seed21 文件成员=21 文件 sha=b77cd49ebee8e0b74fd7114ce47b3efe081e6168598aee379825966c45f330f3
表投影行=21；instruments 覆盖 21/21
sector 非空 0/21（**只报，不判红**：seed21 在真库上本来就是 0/21，属 P53 回填的历史欠账）
✅ 一致：文件 sha == 表投影 sha，instruments 覆盖完整
exit=0
```

改坏一行 sha（`WHERE universe_id='seed21' AND code='000333'` → `'deadbeef'`）后：

```
❌ 不一致（1 条）：
  - 表投影 sha ['b77cd49ebee8e0b74fd7114ce47b3efe081e6168598aee379825966c45f330f3', 'deadbeef'] ≠ 文件 sha b77cd49ebee8e0b74fd7114ce47b3efe081e6168598aee379825966c45f330f3
exit=2
```

注：`sync` 报「新增 instruments=0 只、补空 sector=0 只」与真库现状（21 行、`sector` 非空 0）
一致 —— **只补空列**这条纪律在副本上实测成立（既有行一个字没动）。

### 7.J6 判据 6：预注册缺 `universe` ⇒ `seed21`；命令行换宇宙 ⇒ exit 2

```
$ .venv/bin/python -m stocklab.cli.main research xsec-topn --pool short --start 2015-01-01 \
    --prereg docs/experiments/2026-09-24-xsec-topn.md --out /tmp/xsec_out \
    --universe csi300-500 --db /tmp/absent.db
预注册校验失败：预注册不一致：universe 预注册='seed21' 命令行='csi300-500'（预注册没写该字段时语义为 'seed21'） —— exit 2，零输出、不跑回放：**换宇宙必须新预注册**，否则同一份预注册能跑出两个结论（静默换宇宙）
exit=2

$ .venv/bin/python -m stocklab.cli.main research xsec-topn --pool short --start 2015-01-01 \
    --prereg docs/experiments/2026-09-24-xsec-topn.md --out /tmp/xsec_out --db /tmp/absent.db
打不开库（只读）：库不存在：/tmp/absent.db
exit=2
```

第二条证明「卡住它的不是宇宙校验，而是库不存在」—— 即老预注册（→ `seed21` 语义）＋默认
路径**通过**了校验。`/tmp/xsec_out` 未被创建（零输出）。

### 7.J7 判据 7：真库 sha256 跑前 == 跑后

- 跑前（§7.0）：`e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b`
  （mtime `2026-09-24T20:30:29`，size `197779456`）
- 跑后（全量 pytest ×2 ＋ `verify.sh` ×2 ＋ `/tmp` 副本 sync/doctor 之后）：

```
$ shasum -a 256 data/stocklab.db
e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b  data/stocklab.db
$ stat -f '%Sm %z bytes' -t '%Y-%m-%dT%H:%M:%S' data/stocklab.db
2026-09-24T20:30:29 197779456 bytes
```

sha **逐位相同**，且 mtime / size 也一字未变 —— 比「sha 相同」更强（说明**根本没有过写动作**，
连 WAL checkpoint 都没发生）。所有写路径都指向 `/tmp/u_copy2/`（§7.J5）。

### 7.7 偏离（一处一行）

1. **N4 补进非 PIT 项**：任务书 §0.5 D1 写「设计稿 §2.2 的 N1/N2/N3 限定句」，
   而设计稿 §2.2 在「**必须写进报告的「非 PIT 项」（逐条，不许含糊）**」标题下实际列了
   **N1–N4**（N4＝指数只到 800 只、更大的 `TYPE` 指数名未核实）。按**上游优先**取四条，
   `NON_PIT_UNIVERSE_ITEMS` 落 N1–N4；任务书点名的 N1/N2/N3 一条不少。
   副作用：N4 文案原稿含「中证2000」，撞了既有守卫
   （`tests/test_research_xsec.py::test_research_module_does_not_borrow_paper_or_m2_calibers`
   的 `assert "2000" not in src`，它防的是 `bootstrap_n=2000` 被手抄）⇒ 改成
   「某个更大的宽基指数名」表述，**没有**改那条既有测试。
2. **报告 §3 的「种子只有 21 只」改成按宇宙陈述**：任务书未要求改这句，但扩池（800 只）
   时它与命令行实参**直接矛盾**，正是 D7 要堵的那类「读数与实参不符」。
   改法：`_seed_scope_note` / `_selection_bias_note` 两个小函数，`seed21` 分支**逐字返回**
   原常量（`SEED_SCOPE_NOTE` / `SELECTION_BIAS_SENTENCE`）⇒ 默认路径输出逐字不变
   （由新用例 `test_seed21_default_markdown_is_byte_identical_to_the_old_wording` 钉住）。
3. **T5 列的「6 个改动文件」实测只有 1 个要改**：`test_research_xsec.py` /
   `test_labweb_candidate.py` / `test_ingest_financials.py` / `test_extend_history.py`
   里的 `21` **不是**硬断言（是日期 `2026-09-21`、行情数列、或 `NON_PIT_ITEMS` 文案），
   四个文件**零改动**即全绿。只改了 `tests/test_candidate_seeds.py` 的断言对象。
   （任务书原话「先核实际行号」—— 核完确实对不上。）
4. **`board` 派生表比 §9 裁决 2 多认一个 `301` 前缀**：`300*`→`gem` 之外把 `301*` 也归 `gem`。
   理由：深市创业板注册制后 `301xxx` 是创业板（±20% 涨跌停），中证500 里就有这类标的；
   按裁决原文归 `main` 会**算错涨跌停**。已写成显式前缀行 ＋ 注释点名。
5. **T5 未列但必须改的两个既有文件**：① `tests/test_candidate_run.py` —— 加判据 4 的
   逐位对账用例（该文件有现成的 `_seed_db` 最小可跑库），**新增**用例、未改既有断言；
   ② `tests/test_store_schema.py` —— `EXPECTED_TABLES` 缺新表 `universe_memberships`
   （全量跑时 1 条红），**只把新表名加进集合**、用例未删未改逻辑。
6. **`ensure_schema` 之前先 `resolve_universe`**（`cli/candidate.py` 与
   `cmd_ingest_financials`）：用法错误（未知 `--universe`）必须**零写入**，
   而 `ensure_schema` 会写库 ⇒ 顺序固定为「先解析宇宙、再碰库」。任务书未规定顺序。

### 7.8 没做成的 / 未验证口子（如实写，别抹掉）

1. **东财 `RPT_INDEX_TS_COMPONENT` 的 code/name 列名未实测**：P70 只逐条记录了
   `WEIGHT` / `INDUSTRY` / `MAXTRADEDATE`。`parse_index_components` 按候选键
   （`SECURITY_CODE`/`SECUCODE`/`F12`/`CODE` 等）依次取。**真实联网 build 未被本站执行**；
   若列名与候选集都不符 ⇒ code 取不到 ⇒ `build_universe` 因「0 行」fail-closed 抛错
   （**不会**静默写出一个空宇宙），但「能不能抓成」这件事**未验证**。
2. **`csi300-500` 的真实 `build` / `sync` 未跑**：按 D6/§0.5.1，联网与写真库由 nanobot 执行。
   站内只验证了「两页 300+500 去重成 800」的**代码路径**（假 fetcher，见
   `test_csi_build_dedupes_two_pages_into_800`）。
3. **扩池后真库的「新增」差异未实测**：D8 预期 `instruments 21→821`、`bars code 23→823`，
   本站未制造也未核对这些数字（写库属 nanobot）。
4. **`doctor` 的 `sector` 非空率在扩池宇宙上未验证**：只在 `seed21`（0/21）上跑过，
   而该读数**只报不判红**，所以谈不上「验过扩池的 sector 覆盖率」。
5. **`immutable=1` 忽略 WAL 的窗口**（`cli/universe.py` 模块 docstring 已点名）：
   若别的进程正持有 WAL 句柄，`doctor` 可能读到略旧的一份投影。它只做对账报告、
   不据此写任何东西，但**「读到的就是最新投影」未验证**。
6. **扩池报告里 `NON_PIT_ITEMS` 第 ③ 条仍是旧文案**（「种子宇宙是事后挑选的 21 只白马蓝筹」）：
   既有测试 `test_report_carries_universe_and_incomparability` 明确要求「口径不许被替换」，
   本轮**没有**按宇宙改写它。⇒ 非 `seed21` 宇宙的报告里这一条与实参不符（新的 N1–N4 是
   **追加**，不是替换）。要改属设计口径变更，留给 nanobot 定。
7. **`_default_fetcher()`（生产抓取路径）零测试覆盖**：站内一律注入假 fetcher，
   真实 `HttpClient` 分支只在 nanobot 侧跑到。

### 7.10 提交（逐笔 sha ＋ message）

| # | 内容 | sha | message |
|---|------|-----|---------|
| 1 | 代码 ＋ 测试（23 文件） | `d9a0828c4eefc75c8206b683b10751f6e41048cf` | `feat(p71): 宇宙从常量变为带 id 与 sha 的显式对象（载体＋三条 CLI＋消费方 --universe）` |
| 2 | 文档（本记录 ＋ ADR-026 ＋ 实现计划） | 本记录所在的 `docs(p71):` 提交 | `docs(p71): ADR-026 ＋ 实现计划 ＋ 任务书 §7 实施记录` |

- 两笔**分开**（代码/测试一笔、`docs/` 一笔），均只用**显式路径** `git add <path>`，
  **未用** `git add -A` / `git add .`。
- 文档引用第 1 笔的 sha —— 同一提交里写自己的哈希做不到，故分两笔（不是「留待补」）。
- 提交前跑过 `bash scripts/verify.sh`（exit 0）与全量 `pytest`（3147 passed / 2 skipped），
  见 §7.J1 / §7.J2。


## 8. nanobot 独立复核（不采信自报，逐条自跑）

（本节由 nanobot 填）

## 9. 已知的**接口裁决**（我定的口径，站照此实现，别自造）

1. **CSV 列**（固定顺序，表头必写）：
   `code,name,market,board,asset_type,org_type,sector,index_membership`
   —— 对齐 `stocklab/config/universe.py::Instrument` 的字段（注意它是 `market` **不是** `exchange`；
   `asset_type` 用该模块的 `ASSET_STOCK`/`ASSET_ETF` 常量值；**先读该文件确认再动手**，
   若与本节不一致以代码为准并在 §7.7 记一行）。
2. **`board` 派生必须是确定性规则**（按代码前缀：`300*`→`gem`、`688*`/`689*`→`star`、`8*`/`4*`→`bse`、
   其余→`main`），**不许猜、不许查表**；未知前缀 ⇒ 抛错，不静默归 `main`。
3. **`org_type` 未知时不许猜**（它决定东财 F10 报表名 `G/B/I`）：扩池行的 `org_type` 无法从数据源确定时
   ⇒ CSV 该列**留空**、载入为 `None`（`Instrument.org_type` 的 dataclass 默认值 `"通用"` 是「不破坏既有调用点」的
   兼容默认，扩池路径**不许**借它兜底），并在 `financials` 取数路径上**显式**跳过 F10 公告日
   （退回法定披露截止日 ＋ `notice_date_source`，即 P53 设计 §E 的口径）。除本档的 `seed21`（全部已知）外，
   任何「留空却被当成通用」的静默路径都算返工。
4. **`doctor` 的**「一致」判据＝文件 `members_sha256` == 表里该 `universe_id` 行的 canonical sha；
   表缺行也是 exit 2（不是「跳过」）。
