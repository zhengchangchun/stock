# P58 插桩5（定期复盘分析）接线 ＋ v1.1.0 源文本

创建：2026-09-23（nanobot 需求澄清）｜ 实现：Claude Code（superpowers 全流程）
来源：`stock/01_模块 1_候选池筛选系统需求.md` 插桩清单第 5 行（「定期复盘分析脚本：AI优化子任务内部，计算因子胜率、回测指标」）
＋ 同文件 2026-09-21 的核对批注「**已定义未接线** …… 不要当成已完成」；口径留痕 `docs/plans/2026-09-21-需求澄清录.md` **D-10**。
上游：`P45`（插桩契约）、`P38`（`ops monthly`）、`P49`（模块2 错判案例回流，只读）、`P57`（脚本走 `submit` 人工闸门的先例）。

---

## 0. 一句话

把「插桩5」从「契约在、没人调」变成**有一条真调用路径**：
新增 `candidate review --asof <日>`（复盘**不是**每日动作 ⇒ **不进 `candidate run` 的 12 步主干、不进 `ops close`**），
输入只读历史快照 ＋ 回测台账 ＋ 模块2 回流错判案例，输出落 **append-only 台账** ＋ 报告；
同时交出一版 **v1.1.0 复盘脚本源文本**（真算因子胜率与回测指标），走既有生命周期停到 `pending_review`。

## 1. 口径与判据

1. **调用点**（文档 01 明写它在「AI优化子流程内部」）：
   ① `candidate run` 的 12 步主干 **一个字不动**；② `CLOSE_STEPS` **一个字不动**；
   ③ `candidate review` 独立可跑，并作为**非阻断步骤**加进 `MONTHLY_STEPS`（月度链）——
   它失败不许把整链拖红（照该元组里非阻断步骤的既有语义写）。
2. **active 脚本缺席必须显式**：只认 `plugin/lifecycle.py` 的 active 版本（plugin_id=`5`）；
   **没有 active 版本 ⇒ exit 2** 且点名「插桩5 没有 active 版本」，不静默跑空。
   现役 v1.0.0 是**桩**（返回 `{"analysis_result": {"status": "not_implemented"}, "bad_case_list": []}`）
   ⇒ 命令 **exit 0**、台账照记，但**报告第 1 行就写**「复盘脚本尚未实现（script_id=6 / v1.0.0 桩），本报告只有输入侧事实」。
   **不许把桩的输出包装成结论**，也不许为让报告好看而造假数据。
3. **输入必须是事实，PIT 只读**（T0 先只读探测形状，把探测到的列名写进实施记录）：
   - 历史快照：`candidate_snapshots` / `candidate_members`（只取 `asof <= 目标日`）；
   - 回测台账：`plugin_backtests`（各脚本版本已落库的回放读数；**不自己重跑回放**）；
   - 模块2 回流：`labweb/m2_data.py` 的**错判案例只读汇总**（P49 / D-31：「回流是『看』，不写表」）——
     若它现在只在页面层可用，就抽成**可复用的只读函数**（放 `m2/` 或新模块皆可），
     **不许复制第二份口径**（模块2 需求书 §5 A5 的判据）。
4. **因子胜率的定义**（写死、写进报告抬头）：以历史快照里的候选为样本（带池别与 `adj_score`），
   对照其在**其后 N 个交易日**的方向/收益（N 用**常量**写死，改它要改代码）；只用 `<= asof` 的数据。
   样本不足 ⇒ `None` ＋ `na_reasons`（**不许给 0.0 冒充「胜率 0%」**，照 `m2` 既有做法）。
5. **落库**：新增 **append-only** 表（候选名 `plugin_reviews`；CC 可换名但要写进实现记录），
   幂等键 `(asof, script_id)`；触发器拦 `UPDATE`/`DELETE`；迁移登记进 `_KNOWN_MARKERS` / `_pending_column_migrations`，
   **可重入**、**对老库是空动作**。真库迁移由 nanobot 做（§3）。
6. **报告落点**：`reports/plugin-review/<asof>.md`（新目录）。
   ⚠️ **禁止**叫 `reports/<asof>-review.md` —— 那是 `session review` 的地盘，`ops patrol` §⑤ 按这个名字判存亡，
   撞名会让 patrol 读到错的文件。**加测试钉住 patrol 的判据不受影响**，并遵守 `conftest` 的报告目录夹具（错误日记 #51）。
7. **闸门零放松**：脚本走 `plugin/runtime.py` 的受限执行；**不改** `plugin/` 下的
   `contract.py` / `guard.py` / `lifecycle.py` / `sandbox.py`（一个字都不许松）。
8. **v1.1.0 源文本**（`stocklab/candidate/builtin/review.py`，形状照 `stocklab/candidate/builtin/`）：
   `SOURCE: str` ＋ `BUILTIN_PLUGINS` 风格映射；纯函数、不 import、不做 IO、不取时间、不用随机；
   真算 ①因子胜率（按池别与分数分桶）②回测指标读数（从 `ctx` 里已有的台账条目算）③`bad_case_list`（来自 `ctx`）。
   最少样本数等上限**写死在源码里**，**不许做成可配置**；契约形状就是
   `analysis_result: dict` ＋ `bad_case_list: list`（`SHAPES["5"]`）。
   真库 `submit` 由 nanobot 做 ⇒ 本站产物停在 `pending_review`（D-24 人工闸门）。

## 2. 测试（先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | `candidate review --asof` 形状与退出码 | 夹具库里 active 脚本为桩 ⇒ exit 0；输出含 `script_id` / `script_version` / `asof` / `report_path` / 台账行 id |
| T2 | 无 active 版本 ⇒ 点名报错 | 副本上把 plugin 5 置为无 active（`reject` 或空表夹具）⇒ exit 2、**零写入**、stderr 含 `插桩5` |
| T3 | 桩不伪装 | 桩输出时报告首行含「尚未实现」；台账 `analysis_result["status"] == "not_implemented"` 原样落库（不重写） |
| T4 | 幂等 | 同 `(asof, script_id)` 跑两次 ⇒ 第二次不增行、exit 0；人为改一个字段重跑 ⇒ 不覆盖（append-only） |
| T5 | 触发器 | 直接 `UPDATE` / `DELETE` 新表 ⇒ 被拒（用 `PRAGMA recursive_triggers=ON` 的既有做法） |
| T6 | PIT | 塞一条 `asof+1` 的快照/行情 ⇒ 不进样本、不进报告（或显式报错） |
| T7 | 报告路径不撞 patrol | 跑 `ops patrol` 的夹具用例（或 patr ol §⑤ 的判据函数）⇒ 仍读 `reports/<L>-review.md`，新目录不影响它 |
| T8 | 源文本 | `guard.check_source(SOURCE)` 过；`runtime.load_script(SOURCE, plugin_id="5")(PROBE_CTX)` 过 `validate_return`；同一 `ctx` 两遍**逐字节相同**；源码里出现 `random`/`time`/`datetime`/`open(` 判红 |
| T9 | 月度链非阻断 | `ops monthly` 夹具：`candidate review` 步失败 ⇒ 整链语义照非阻断步骤既有口径（不把 exit 0 变成 exit 2） |
| T10 | 全量 | `.venv/bin/python -m pytest -q` exit 0（用例数只增不减）＋ `bash scripts/verify.sh` exit 0（红线三目标 sha 不变） |

## 3. 真库动作（nanobot 在验收后做，写进回执）

1. `data/backups/` 先备份（`store/migrate.py::backup_db`）；
2. 真库迁移（新表）＋ `candidate review --asof <上月末交易日>` 走**现役桩** ⇒ 看 exit 0 与报告抬头；
3. `plugin submit` v1.1.0 源文本 ⇒ 核对 `pending_review` 并记 `script_id`；
4. **`approve` 由用户点头后执行**（人工闸门）；
5. approve 后再跑一次 `candidate review` ⇒ 看真读数（因子胜率/回测指标原文照贴）＋ 报告交付。

## 4. 反目标

- ❌ 不改 `candidate run` 的 12 步主干、不改 `CLOSE_STEPS`、不改 `MONTHLY_STEPS` 里既有 5 步的行为；
- ❌ 不改 `plugin/` 的四个闸门文件；`approve` 仍只能人工（源码扫描测试钉住）；
- ❌ 不复制模块2 回流口径（只抽一份可复用只读函数）；
- ❌ 报告不落 `reports/<asof>-review.md`；不许把桩输出包装成结论；
- ❌ 脚本里不许参数搜索/自学习/联网/读文件；上限不许可配置；
- ❌ 不 `git commit`、不建分支/worktree（提交由 nanobot 做）；真库 `data/stocklab.db` **只读**。

## 5. 实测前提（nanobot 已核，2026-09-23 23:2x）

- `plugin_scripts` 里 plugin_id=`5` 只有一条：`script_id=6 / v1.0.0 / active`，源文本 172 字符，
  返回 `{"analysis_result": {"status": "not_implemented"}, "bad_case_list": []}`；
  注释原文：「本轮 AI 优化子流程未实现（设计文档 §3「不做」），故返回空结果。」
- 契约：`SHAPES["5"] = (("analysis_result", "dict"), ("bad_case_list", "list"))`。
- `MONTHLY_STEPS` 现 5 步：`calendar_holidays_fetch` / `ingest_bars_long` / `ingest_index_500` /
  `ingest_financials` / `doctor`。
- `reports/` 现状：session review 占 `<日期>-review.{md,json}`（2026-09-21/22/23 各有）；
  候选池报告在 `reports/candidate/`；`ops/` 回执在 `reports/ops/`。
- 相关表已存在：`candidate_snapshots` / `candidate_members` / `candidate_rejects` /
  `plugin_backtests` / `m2_judgements` / `m2_attributions`。
- 相关坑：ERROR_DIARY #51（夹具不许往仓库 `reports/` 写）、#48（升版是写侧+读侧一对动作）、#52（死代码卫生）。

## 6. 交付物

- `stocklab/review/`（新包）或等价落点：输入装配（PIT 只读）＋ 服务层（走插件生命周期）＋ 报告渲染；
- `stocklab/candidate/builtin/review.py`（v1.1.0 `SOURCE`）；
- 新表 + 迁移（登记进 `store/migrate.py`）；
- `stocklab/cli/main.py` 的 `candidate review` 子命令；
- `stocklab/ops/chain.py` 的 `MONTHLY_STEPS` 加一步（非阻断）；
- 测试（T1–T10）；本任务书 §7 打勾 ＋ 文末「实施记录」。

## 7. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | T0 只读探测（表/列/既有回放台账形状）＋ 实现计划 | ✅ 已验证 |
| T2 | `candidate review` 命令 ＋ 新 append-only 表 ＋ 迁移 ＋ 报告落点 | ✅ 已验证 |
| T3 | 月度链接线（非阻断） | ✅ 已验证 |
| T4 | v1.1.0 复盘脚本源文本（因子胜率 / 回测指标 / 错判案例） | ✅ 已验证 |
| T5 | T1–T10 测试（含 PIT、幂等、触发器、路径不撞 patrol、确定性） | ✅ 已验证 |
| T6 | 全量 pytest ＋ `verify.sh`（红线三目标 sha 不变） | ✅ 已验证 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

---

## 8. 实施记录（Claude Code，2026-09-23 23:23–23:46）

计划稿：`docs/plans/2026-09-23-p58-插桩5复盘接线.md`。

### 8.1 T0 只读探测（`file:data/stocklab.db?mode=ro`，真库只读，未写一个字节）

探测到的形状（**实施按这些列名落地**）：

| 表 | 列（实测） | 行数 |
|---|---|---|
| `candidate_snapshots` | `snapshot_id, asof, run_kind, params_json, created_at` | 2（asof `2026-09-18` / `2026-06-15`，均 `weekly`） |
| `candidate_members` | `snapshot_id, code, pool, raw_score, adj_score, reason, risk_json, status, entered_at` | 38 |
| `plugin_backtests` | `backtest_id, candidate_script_id, baseline_script_id, pool, window_start, window_end, metrics_json, verdict, overfit_flag, report_sha256, created_at` | 7 |
| `plugin_scripts` | `script_id, plugin_id, version, source_text, source_sha256, created_at, note` | 11 |
| `plugin_reviews`（**我新加的表**） | 见 §8.2 | 0（**已由 nanobot 的 P62 运行建出，见 §8.6**） |

插桩5 的真实现役形态（与任务书 §5 一致，一处措辞修正）：

```
script_id=6  plugin_id='5'  version='1.0.0'（任务书写的是 v1.0.0，库里无前缀 v）
source_text 长度=172  source_sha256=a36e9c5b803450723c1409c47cd3fd1ad7987f57726d3bce5eb720758a2cafcc
审计链：audit_id 16/17/18 = submit→sandbox_pass→approve（actor=acceptance，2026-09-20）
```

其余相关事实：`trading_calendar` 最大日 `2026-09-23`；`bars_daily` 23 个标的；
`m2_forecast_scores` 0 行（所以模块2 回流现在必然是空集，不是异常）。

### 8.2 落点（与任务书 §6 的对应）

| 交付物 | 落点 |
|---|---|
| 输入装配（PIT 只读） | `stocklab/plugin_review/inputs.py` |
| 服务层 | `stocklab/plugin_review/service.py` |
| 报告渲染 | `stocklab/plugin_review/render.py` |
| 台账读写 | `stocklab/plugin_review/store.py` |
| v1.1.0 源文本 | `stocklab/candidate/builtin/review.py`（`SOURCE` ＋ `BUILTIN_PLUGINS={"5": SOURCE}`；`p5_review.py` 现役桩未动） |
| 新表 + 迁移 | `stocklab/store/schema.sql` ＋ `stocklab/store/migrate.py` |
| 命令 | `stocklab/cli/candidate.py::cmd_candidate_review` ＋ `stocklab/cli/main.py` 的 `candidate review` |
| 月度链 | `stocklab/ops/chain.py::MONTHLY_STEPS`（新增 `candidate_review`，`blocking=False`）＋ `stocklab/ops/runner.py`（`Step.blocking`） |
| 模块2 回流口径 | `stocklab/m2/cases.py`（从 `labweb/m2_data.py` **原样搬出**，页面侧改为 import 转出，口径仍只有一份） |
| 测试 | `tests/test_plugin_review.py`（24 条）＋ `tests/test_ops_close.py`（+3 条） |

### 8.3 任务书未写清、本实施**自行定死**的口径（点名）

| # | 口径 | 落地 | 理由 / 后果 / 需要谁拍板 |
|---|---|---|---|
| D1 | **包名** | `stocklab/plugin_review/`（任务书建议 `stocklab/review/`，属「等价落点」） | 本任务的红线之一是「别跟 session review 撞名」；包名与报告目录名（`plugin-review`）保持一致，歧义不必靠记忆消除。**需要拍板**：若 nanobot 侧按 `stocklab/review/` 找文件，请告知改回。 |
| D2 | **退出码 1** | active 版本在、但脚本**跑不出来**（guard 预检 / 超时 / 契约校验不过）⇒ exit 1、**不落台账、不写报告** | 任务书只定了 0 与 2。1 沿本仓库既有语义（「有版本但跑了不可用」≠「没有版本」）。后果：月度链上这类失败被当作「非阻断」，只在 steps/anomalies 里可见。**需要拍板**：是否要让它更响（例如 2）。 |
| D3 | **非阻断的实现** | `Step` 增一个**默认 True** 的 `blocking` 字段；`run_steps` 不因非阻断步骤的非 0 退出码中止整链；`worst_code` 不把它的退出码计入链的退出码（异常仍进 `steps` / 摘要 `bad=` / `anomalies`） | 任务书说「照该元组里非阻断步骤的既有语义写」，但实测 `MONTHLY_STEPS` 原 5 步**全是阻断步**、`runner` 里也没有非阻断概念 ⇒ 无先例可抄。既有 5 步与巡检/收盘链行为逐字节不变（新字段有默认值）。跑不起来（`exit_code is None`）**仍算致命** —— 那是链级故障。**需要拍板**：默认值选 True 而非 False，是为了「不改既有行为」。 |
| D4 | **`_pending_column_migrations` 的判据** | 只有「表在但 append-only 触发器缺」才算 pending；**表不存在不算** | 与既有约定一致（表不存在 ⇒ `executescript` 按新 shape 建出，无需迁移/备份）。若把「表不存在」算成 pending，每次写库入口都会给老库做一次备份。后果：老库上 `plugin_reviews` 由 `schema.sql` 直接建出（**空动作**照字面成立）。**需要拍板**：登记进 `_pending_column_migrations` 的是「漂移修复」这一条分支。 |
| D5 | **`FORWARD_DAYS = 5` 落在服务层**（`inputs.py`，常量），脚本经 `ctx["n_days"]` 拿到并写进 `analysis_result` | 前视收益的 PIT 边界只有读数据的那一层能保证（SQL/日历），把它做进脚本就得把 K 线喂进沙盒。脚本侧「写死」的是 `MIN_SAMPLES=5` / `MIN_BACKTESTS=3`（不出现在任何配置文件里）。 | 后果：改窗口要改 `inputs.py`（是代码，不是配置）。**需要拍板**：若要求 N 也长在脚本内，则 ctx 需改为携带逐日复权序列。 |
| D6 | **报告第 1 行**的形态 | `# 插桩5 复盘 <asof> —— 复盘脚本尚未实现（script_id=6 / 1.0.0 桩），本报告只有输入侧事实` | 既满足「第 1 行就写」与「首行含『尚未实现』」，又保留 markdown 标题。 |
| D7 | **`bad_case_list` 原样转交** | 逐条 `dict(case)` 复制 `ctx["bad_cases"]["cases"]`（含 D-31 的 `attribution: None`） | 任务书 §1.8 的原文就是「`bad_case_list`（来自 `ctx`）」；不在这里重算口径，也不新增字段。 |
| D8 | **复权口径** | 前视收益走 `data/adjust.py::load_bars_adjusted(code, as_of, start=t0)`；算不出的标的（ETF / 链有缺口）**逐条剔除并留原因**，不退回 `bars_daily.close` | ADR-004/ADR-008：不复权价跨除权会凭空多出一截假跌幅，且数值看着正常。后果：ETF 候选（如 510300 若入池）在前视读数里**缺席**，但缺席是**可见的**（报告里有「剔除的原因」一节）。 |
| D9 | **报告正文不含墙上时钟** | `now` 只进台账 `created_at` | 给定同一份 ctx 与同一版脚本，报告逐字节相同 —— 「报告变了」永远意味着「输入或脚本变了」（与 `m2_data.review_backlog` 的同一纪律）。 |

### 8.4 测试证据（命令与输出）

```
$ .venv/bin/python -m pytest                          # 冻结点（文档落盘之后再跑）
2908 passed, 2 skipped in 164.67s (0:02:44)          # exit 0
$ .venv/bin/python -m pytest --collect-only -q | awk -F': ' '/^tests\//{s+=$2} END{print s}'
2910                                                  # = 2908 passed + 2 skipped（ERROR_DIARY #68 的数法）

$ .venv/bin/python -m pytest tests/test_plugin_review.py
24 passed in 1.45s

$ bash scripts/verify.sh
  ✅ predict_synthetic        sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致
  ✅ predict_real_2026-09-14  sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致
  ✅ backfill_real_2013-12-23_2026-09-14
                              sha256 ddac0a889c623d75… == 基线；叶子 171 项一致
✅ 回归红线全部成立
✅ 验证通过: all
```

**用例数只增不减**：基线 2881 = 2908 − 27；新增 = `tests/test_plugin_review.py` 24 条
＋ `tests/test_ops_close.py` 3 条（该文件 29 → 32，`--collect-only` 实测）。
`tests/test_store_schema.py` 只把新表加进 `EXPECTED_TABLES`（不加用例）。

T1–T10 与本文件 `tests/test_plugin_review.py` 的对应：

| 判据 | 用例 |
|---|---|
| T1 形状与退出码 | `test_t1_stub_run_exits_zero_and_prints_the_shape`、`test_t1_rejects_a_bad_asof_before_touching_anything` |
| T2 无 active ⇒ 点名 + 零写入 | `test_t2_no_active_version_is_named_and_writes_nothing`、`test_t2_empty_plugin_table_is_the_same_refusal` |
| T3 桩不伪装 | `test_t3_stub_report_says_so_on_the_first_line` |
| T4 幂等 / append-only | `test_t4_same_key_twice_adds_no_row`、`test_t4_existing_row_is_never_overwritten`、`test_t4_a_new_script_version_gets_its_own_row` |
| T5 触发器 | `test_t5_append_only_triggers_reject_update_and_delete`（2 参）、`test_t5_insert_or_replace_cannot_bypass_the_trigger` |
| T6 PIT | `test_t6_future_rows_do_not_leak_into_the_readings`、`test_t6_unfinished_window_is_dropped_with_a_reason` |
| T7 不撞 patrol | `test_t7_patrol_keeps_reading_the_session_review_file` |
| T8 源文本 | `test_t8_source_passes_guard_and_the_probe_context`、`test_t8_source_has_no_banned_constructs`（4 参）、`test_t8_source_is_byte_deterministic_on_the_same_ctx`、`test_t8_source_computes_real_readings` |
| T9 月度链非阻断 | `test_monthly_candidate_review_is_not_blocking`、`test_monthly_review_step_gets_the_month_asof`、`test_monthly_a_blocking_step_failure_is_still_fatal` |
| T10 全量 | 见上（2908 passed ＋ verify.sh 全绿） |

`SOURCE` 的实测读数（`_sample_ctx`，6 条样本 4 涨 2 跌）：`overall.win_rate = 0.6667`、
`by_pool["mid"].win_rate is None`（无样本 ⇒ None ＋ `na_reasons`）、
`backtests.win_rate = 0.6667`（WIN/LOSE/WIN）、`n_overfit_suspected = 1`。

### 8.5 反目标核对

- ✅ `candidate run` 的 12 步主干**未动**；`CLOSE_STEPS` **一个字未动**；`MONTHLY_STEPS` 原 5 步的 `Step` 字段与 argv 未变（有测试钉住）；
- ✅ `plugin/` 的 `contract.py` / `guard.py` / `lifecycle.py` / `sandbox.py` **一个字节未改**（`git status` 里没有它们）；`approve` 仍只能人工；
- ✅ 模块2 回流口径**只抽了一份**（`m2/cases.py`），页面侧改为 import 转出，没有第二份 SQL；
- ✅ 报告落 `reports/plugin-review/<asof>.md`，测试断言 `reports/<asof>-review.md` **不存在**；patrol ⑤ 判据未改且加了反向测试；
- ✅ 脚本无参数搜索 / 自学习 / 联网 / 读文件；上限是源码常量（`MIN_SAMPLES` / `MIN_BACKTESTS`），无任何配置项；`PROBE_CTX`（不改 `contract.py`）下全量 `na_reasons`、无 KeyError；
- ✅ **未 `git commit`、未建分支/worktree**；真库 `data/stocklab.db` **只读**（下面的 §8.6 是只读探测的观察，不是我写它）。

### 8.6 一条必须点名的观察：真库里已经有 `plugin_reviews` 了（**不是我写的**）

只读探测发现真库已有新表（0 行）＋两个 append-only 触发器。时序证据：

```
23:23:29  stocklab/store/schema.sql                ← 我加的新表（本任务）
23:26:10  docs/tasks/2026-09-23-p62-AI臂净值未结算修复.md   ← nanobot 起 P62
23:27:58  data/backups/stocklab.pre-p62-navfix.20260923T152824Z.db   ← P62 迁移前备份
```

即：**nanobot 的 P62 运行（23:27）带了 `ensure_schema`，把我 23:23 已经写好的
`schema.sql` 一并前滚进了真库** —— 新表的真库迁移因此**已经完成**（§3.2 那一步
事实上被 P62 顺带做掉了，且是由 nanobot 这个正确执行者做的）。我本人对真库的动作
只有 `mode=ro` 探测（未写；`-shm` 是 WAL 读取方的书签，不是数据写入）。

⚠️ 由此产生两条**要人看一眼**的影响：

1. §3.2 的「真库迁移（新表）」现在**无需再做**，但 §3 的备份步骤仍建议照做一次留痕；
2. 并行 actor 共用同一个真库：**我的 schema.sql 改动会在别人下一次 `ensure_schema` 时生效**。本次是好事（提前前滚），但同一个机制也可能把未审完的 schema 带进真库。需要拍板：是否要求「schema 改动等验收后再落」或「真库迁移只由指定步骤触发」。

### 8.7 未做（按任务书留给 nanobot / 用户）

- 真库 `plugin submit` v1.1.0（`stocklab/candidate/builtin/review.py::SOURCE`，`version="v1.1.0"`）—— 本站产物停在代码里；
- `plugin approve`（人工闸门，必须用户点头）；
- 真库 `candidate review --asof 2026-09-23` 实跑（会写台账与报告）；按 §8.1 的日历与快照推算：`asof=2026-09-18` 那份快照的窗口只走完 3 个交易日 ⇒ 会整条按「窗口未走完」剔除并留痕，`asof=2026-06-15` 那份才出样本 —— 这也是一条可供验收时对照的预期。

