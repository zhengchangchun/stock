# P62 AI 臂净值行写错：`execute_decision` 未结算状态（**生产实测 bug，修 + 修数据**）

创建：2026-09-23（nanobot 在生产实跑中发现）｜ 实现：Claude Code（superpowers 全流程）
来源：P56 首次循环实跑（asof `2026-09-23`）之后的净值复核。**这是 P56 的验收阻断项**：
AI 臂当日的净值行与它自己的成交重放**对不上**，读数凭空少了 ¥8,233.68。

---

## 0. 一句话（症状）

`paper agent run --asof 2026-09-23` 给 `arm-agent-ds-v1` 写下的净值行是**执行前**的状态：
现金没记卖出所得、持仓却已被改动 ⇒ 净值 `11320.0`（**−43.4%**），而按它自己的成交行重放应当是
`19553.68`（**−2.23%**）。

## 1. 硬证据（nanobot 2026-09-23 23:4x 实测，真库只读 + 库副本）

决策（台账 `decision_id=2`）：`arm=arm-agent-ds-v1`、`asof=2026-09-23`、`model_id=deepseek/deepseek-v4-pro`、
`context_sha256=3aa30255d19b…`；载荷＝卖 `000333`→0%、买 `600900` 14.35% / `603868` 15.98% /
`002415` 16.92% / `000651` 19.61%、`cash_pct=33.14`。

成交行（`paper_trades`，当天**只有 1 笔**）：

```
trade_id=12  arm-agent-ds-v1  000333  sell  100 股  ref 82.47  fill 82.4288
             fee_total 9.2（commission 5.0 / stamp 4.12 / transfer 0.08）
```

写下的净值行（`paper_nav_daily`）：

```
cash=11320.0  market_value=0.0  nav=11320.0  cum_cost=9.2  cum_return=-0.434
positions_json=[{"code": "000333", "qty": 0}]      ← 零股条目（不该存在）
```

同一副本上按成交重放得到的状态（`engine._ledger_arm_state` + `mark_to_market`）：

```
cash=19553.68  positions={}  →  nav 应为 19553.68（cum_return -0.0223）
```

⇒ **差额 = ¥8,233.68**（卖出所得 8,242.88 − 手续费 9.2）。**AI 臂这条读数作废**，
且它会被 `paper show` / `/lab/paper` / 报告当作当天事实读走（`cum_return` 差 41 个百分点）。

## 2. 根因（已定位到函数与行）

`stocklab/paper/agent_decide.py::execute_decision`（约 593–616 行）的 docstring 写着
「**在给定状态上原地结算**（现金 / 持仓）」，但实现只做了 `plan_orders(...)`，
**原样返回入参**：

```python
orders, evals = plan_orders(decision=decision, cash=cash, positions=positions, ...)
...
return cash, positions, [replace(d, reason=...) for d in orders], evals   # ← 入参，不是结算后的状态
```

`engine._step_all` 拿到这个返回值后**直接**用它写净值（`cash` / `positions` / `nav = cash + mv`），
所以净值行记的是**执行前**的现金。而 `positions` 被 `plan_orders` 路径**原地改过**
（`000333` 被置 0 而不是 pop），于是出现「现金是旧的、持仓是新的」的混合状态 —— 正是上面那行。

对照：纪律臂走的是 `_plan_steps`，它返回的是**结算后**的状态 ⇒ 五条静态臂的净值行与重放一致
（nanobot 在副本上逐条核过 `arm-discipline-05/10/15` / `arm-hold` / `arm-now`，全部一致）。
**所以这是决策驱动路径独有的缺陷。**

## 3. 修法要求（口径）

1. **`execute_decision` 必须结算**：把 `orders` 逐笔应用到 `cash` / `positions` 上再返回，
   语义与 `_ledger_arm_state` 的成交重放**逐字段相同**（买：`cash -= fill*qty + fee`、卖：`cash += fill*qty - fee`；
   持仓 `qty==0` 的键**pop 掉**，不留零股条目）。
   **不许**为实现它而复制第二份费用公式：复用既有 `CostModel` / `_fee_parts` / `store.insert_trade` 的口径 ——
   目标是「重放 = 写入」这一条不变量，不是再写一遍算术。
2. **不变量必须被测试钉住**（这是本站最重要的判据）：
   对**任一** AI 臂、任一天，`paper_nav_daily` 的行必须等于
   「`initial_cash` + 当日及之前全部成交按上面口径重放（+ 当日收盘价 mark-to-market）」，
   即已有的 `engine._ledger_arm_state` + `mark_to_market`。
   写成**属性测试**：造 3–4 天、含买/卖/部分成交的日子，逐日断言 `nav == replay_nav`（容差 1e-6）。
3. **零股条目不许出现**：净值行 `positions_json` 里不得有 `qty <= 0` 的键（加断言；`mark_to_market` 也会因此更干净）。
4. **P52 那条判据为什么没拦住（写进实施记录）**：T4 当时写的是「与 `paper step` 同口径算出的值一致」，
   而 AI 臂根本不走 `paper step` 的路径 ⇒ 那条判据是空转的。本站要把它替换成 §3.2 的不变量。
   **不要**把「测试是绿的」当理由反驳 —— 绿是因为期望值是从实现反推的。
5. **修历史行（append-only 纪律下的修复）**：`paper_nav_daily` 有 `no_update`/`no_delete` 触发器
   ⇒ 不许直接 UPDATE/DELETE。走**迁移式修复**（照 `store/migrate.py` 里
   `migrate_p56_agent_arms_executor` 的既有先例：事务内摘触发器 → 只删/重写**点名的那一行**
   → 重新挂回触发器 → 核对行数与其余行逐字节不变），并：
   - 修的是 **`(arm-agent-ds-v1, 2026-09-23)` 这一行**（先探测有几行属于这类缺陷，
     把清单打印出来；**超出该清单的行一行都不许动**）；
   - `paper_trades` **一行都不删**（成交是真的，赎回来的只是净值行）；
   - 重跑 `paper agent run --asof 2026-09-23` 让它按修好的代码重写（跑之前先删掉错行）；
   - 在 `system_events`（或项目既有的同类台账）留一条可查的修复记录：日期、账户、旧值、新值、原因、依据文件名。
   - **真库执行由 nanobot 做**（§5）。
6. **验证口径**：修完后重跑本站属性测试；对真库同日重新核一遍「净值行 == 重放」（nanobot 复核）。

## 4. 测试（先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | 复现：卖出当天 `cash` 未记所得 | 夹具：按 §1 的形状造一天（持有 100 股、决策清仓）⇒ **先红**（当前实现 cash 不变） |
| T2 | 买入当天 `cash` 已扣、`mv` 已含 | 同上，买入 2 手的一例 |
| T3 | 不变量：`nav == replay` | 多日多点（买/卖/部分成交/零成交）逐日断言，容差 1e-6 |
| T4 | 零股条目 | 清仓日 `positions_json` 不含 `000333`（或任何 `qty<=0`） |
| T5 | 纪律臂不受影响（防回归） | 既有 `test_paper_engine.py` / `test_cli_paper.py` 全绿；再补一条「静态臂 nav == replay」 |
| T6 | 修复迁移 | 可重入；只改点名行；其余行逐字节不变；触发器事后仍在（用副本实测） |
| T7 | 全量 | `.venv/bin/python -m pytest -q` exit 0（用例数只增不减）＋ `bash scripts/verify.sh` exit 0（红线三目标 sha 不变） |

## 5. 真库动作（nanobot 在验收后做）

1. `store/migrate.py::backup_db` 备份；
2. 在**副本**上跑修复迁移两次（可重入）并核对清单；
3. 真库：修复迁移 → `paper agent run --asof 2026-09-23` 重写该行 → 复核 `nav == replay`；
4. 核对其余 7 条账户与其余日期的净值行**逐字节未变**；
5. 把修复记录贴回本任务书 §7。

## 6. 反目标

- ❌ 不改 `plan_target_weight` 的「整手向下取整」语义（那是**设计**：宁可差一点也不越权）；
- ❌ 不改成本公式、不改主干常量、不改红线基线；
- ❌ 不删 `paper_trades` 任何一行、不动清单外的净值行；
- ❌ 不为这次修复放松任何 append-only 触发器（只允许「迁移式摘/挂」这一种既有先例）；
- ❌ 不 `git commit`、不建分支/worktree；真库 `data/stocklab.db` 在修复动作前**只读**。

## 7. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | 在夹具里复现（先红）＋ 把 §1 证据抄进实施记录 | ✅ 已验证（§9.4：5 条用例在旧实现上逐条红，输出原文在 §9.4） |
| T2 | `execute_decision` 结算（复用既有费用口径，不复制算式） | ✅ 已验证（§9.2） |
| T3 | 不变量属性测试（`nav == replay`）＋ 零股条目断言 | ✅ 已验证（`tests/test_paper_agent_nav_settlement.py` T3/T4） |
| T4 | 历史行修复迁移（可重入、只改点名行、留台账） | ✅ 已验证（`tests/test_store_migrate_p62.py` 4 条；真库**副本**全流程实测见 §9.5） |
| T5 | 全量 pytest ＋ `verify.sh` | ✅ 已验证（§9.7） |
| T6 | 真库修复与复核（nanobot） | ✅ 已完成（2026-09-24 01:2x 实测，记录见 §10；未结算行 1 → 0） |

## 8. 顺带点名（**不属于本站**，留给用户拍板）

那四笔买入**一笔都没成交**：AI 给的权重恰好每笔≈1 手（含滑点后 99.9 股 → 向下取整 0 手），
执行层如实写了「目标未达成」。这不是 bug（整手向下取整是设计），但会让「AI 的意图」与
「实际组合」差很远。两条可选口径（**推荐 A**）：
**A** 提示词 v2 明确要求「每个目标权重至少 1 手（含滑点），给出 `手数 = floor(权重% × 总资产 / (价 × 1.0009) / 100)`」，
并作为 `arm-agent-ds-v2` 重新预注册（换提示词 = 换版本账户，D-48）；
**B** 执行层对「未达成」回执再加一个显式计数（现在只在 `evaluation` 里）。

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

---

## 9. 实施记录（Claude Code，2026-09-24）

> 说明：任务书请求把它叫「§8 实施记录」，但本文件 §8 已是「顺带点名」，故顺延为 §9。

### 9.1 改动文件清单

**修改**

| 文件 | 改了什么 |
|---|---|
| `stocklab/paper/agent_decide.py` | `plan_orders` 去掉**原地改 `positions`** 的副作用（docstring 早写着「纯函数」，实现却在改调用方的状态 —— P62 的病灶）；新增 `_settle`；`execute_decision` 调 `_settle` 后返回**结算后**的状态；docstring 点名不变量与它防的那次事故 |
| `stocklab/store/migrate.py` | 新增 `p62_defective_agent_nav_rows`（只读探测，三分类）＋ `migrate_p62_agent_nav_settlement`（点名行删除 + 台账 + 可重入）；`_KNOWN_MARKERS` 登记 `p62_agent_nav_settled`（**不**登记进 `_pending_column_migrations` / `_apply_schema`，理由见 §9.3-②） |
| `docs/errors/ERROR_DIARY.md` | 追加 #71（判据与实现同源 ⇒ 绿也是空转；探测器不分类 ⇒ 漏修或越界） |

**新增**

| 文件 | 内容 |
|---|---|
| `tests/test_paper_agent_nav_settlement.py` | T1–T5。夹具走**正规链**：`paper agent decide` → `paper agent run`（AI 臂）+ `paper step`（静态五臂），4 天含买/卖/整手未达目标/零成交 |
| `tests/test_store_migrate_p62.py` | T6。夹具**照抄真库那一行**（含 `params_json` 全文、`trade_id=12` 那笔成交）：可重入、只删点名行、其余行逐字节不变、触发器挂回、台账可查、清单外未结算行 ⇒ 抛错零改动 |

**没改**（反目标）：`plan_target_weight` 的整手向下取整（设计）；`config/costs.py` 与 `_fee_parts`（费用口径唯一真源）；`engine.py` 主干与红线基线；`paper_trades` 一行未删。

### 9.2 修法

```python
# execute_decision
orders, evals = plan_orders(...)          # 纯函数：只产出订单
cash, positions = _settle(cash, positions, orders)   # ← P62 补上的那一步
```

`_settle` 里**一行算术都没有另写**：

- 持仓：懒 import `engine._apply`（`engine` 顶部 import 本模块，反向 import 会成环；仓里
  `comparison.py:110` / `candidate/replay.py:168` 是同款先例）—— 用的就是
  `_ledger_arm_state` 重放时调的**同一个函数**（「减到 0 就 pop」与超卖守卫都是它的）；
- 现金：`fill_price × qty ± fee_total`，其中 `fill_price` / `fees["total"]` 就是
  `store.insert_trade` 写进成交行的那两个数 ⇒ 与重放式**逐字相同**。

**为什么不用 `Decision.amount`**（它看起来更「复用」）：`amount = round(fill × qty ± fee, 2)`
**先舍到分**，而重放用的是 `round(fill, 4) × qty` 不舍 ⇒ 每笔差 ≤ ¥0.005。实测：本仓夹具里
AI 臂两天各差 ¥0.01/¥0.02，真库静态臂上也是同一档（§9.5）。净值行是给「重放」读的读数，
所以跟重放走。**它没有引入第二个费用公式** —— 参与运算的两个数仍然取自 `_fee_parts`。

顺带：`m2/channel_a.py` 用的是**同一个** `execute_decision`（`A1`/`A2` 两次调用后拿返回值
算 `market_value` / `nav`），所以它此前的日终净值也是「执行前的现金」。真库里没有 m2 账户
（`paper_accounts` 里 9 条全是 `arm-*`）⇒ 无历史行要修；测试确认 `m2` 通路无回归。

### 9.3 自行定死、需要拍板的口径

① **「未结算」的判据 = 持仓集合不等 **或** 现金差 > ¥1.00**（`_P62_SETTLEMENT_BAR`）。
   真库扫出来 3 类，混成一类会出事：`未结算` 1 行（¥8,233.68 量级）、`cent_drift` 5 行
   （¥0.01，`Decision.amount` 先舍到分造成，**既有**）、`valuation` 3 行（¥1.70/3.40/4.60，
   现金与持仓都对、只有净值差 ⇒ 写入之后市值口径或价格源漂移过，**原因未查**）。
   后两类**只报告不动**（改它们 = 改主干口径/查另一条线，都不在本站授权内）。
   若你认为「未结算」应当改成别的界（例如持仓精确 + 现金 > ¥0.01），说一声即可。

② **修复迁移不挂「任一写库入口」**：其余迁移是结构前滚，跑多少次都不动数据；本条**删数据行**。
   挂进 `_pending_column_migrations` / `_apply_schema` 等于某次 `paper predict` 顺手删历史净值。
   所以只登记 `_KNOWN_MARKERS`（doctor 只读报告），迁移**显式调用**。

③ **点名清单硬编码** `_P62_NAMED_ROWS = (("arm-agent-ds-v1", "2026-09-23"),)`；清单之外的
   **未结算**行 ⇒ 抛错、零改动（人来拍板）。分币级/估值级两类不触发抛错（它们不是本站的靶子）。

④ **真库重跑会多出一条 `arm-agent-ds-v2` 的平盘行**（§9.6-③）—— `paper agent run` 没有
   「只推一个账户」的开关，而 ds-v2 的 `start_date` 是 2026-09-15（继承）⇒ 引擎认为它在飞。
   ①推荐接受（`n_trades=0`、与 `arm-agent` 同日同值 19567.0，且现有行一行未动）；
   ②若你要的是「不许出现」，那是一条**单独点名**的行，得有人明确点头，我不擅自删。

⑤ **T5 对静态臂放行 `¥0.01 × 当日笔数` 的分币级差**（把既有漂移**限死**在这一档，不掩盖也不
   顺手修）。AI 臂那一侧仍是精确 1e-6。

### 9.4 T1–T5：先红后绿（红是真的红）

红：把 `stocklab/paper/agent_decide.py` 换回 `HEAD` 版本（`git show HEAD:... > 文件`，跑完立刻
从临时备份还原；**未 commit、未 stash**）：

```
$ .venv/bin/python -m pytest tests/test_paper_agent_nav_settlement.py -q
FAILED tests/test_paper_agent_nav_settlement.py::test_t1_sell_proceeds_are_in_the_nav_row
FAILED tests/test_paper_agent_nav_settlement.py::test_t2_buy_cash_is_deducted_and_market_value_included
FAILED tests/test_paper_agent_nav_settlement.py::test_t3_nav_equals_replay_on_every_day
FAILED tests/test_paper_agent_nav_settlement.py::test_t4_no_zero_qty_positions_in_any_nav_row
FAILED tests/test_paper_agent_nav_settlement.py::test_t5_all_arms_obey_the_same_invariant
E   AssertionError: arm-agent-t1/2026-09-15 的持仓与重放不符
E     assert {'000333': 0,...'510880': 500} == {'510300': 400, '510880': 500}
E       Left contains 1 more item: {'000333': 0}
```

最后那三行就是真库那一行的**同型**：`{"000333": 0}`（零股条目）+ 现金未结算。绿：修复后 5 条全过。
真库数字对得上任务书 §1：探测算出 `cash 11320.0 → 19553.68`、`nav 差 8233.68`、重放持仓 `{}`。

### 9.5 真库副本全流程（`cp data/stocklab.db /tmp/p62_copy.db`，真库**只读**未动）

```
probe: checked=21 defective=1 cent=5 valuation=3 skipped=12
  UNSETTLED arm-agent-ds-v1 2026-09-23 {'cash': {'got': 11320.0, 'replay': 19553.68},
    'positions_json': {'got': {'000333': 0}, 'replay': {}}, 'nav': {'got': 11320.0, 'replay': 19553.68}}
migration run 1 → ['arm-agent-ds-v1/2026-09-23: 删除未结算净值行（nav 11320.0 → 待重写）', +8 条 ⚠️ 只报告]
migration run 2 → []                      # 可重入；report-only 行仍 8 条（重申，不改动）
nav rows 33 -> 32；changed other rows: []
paper agent run --asof 2026-09-23 →
  arm-agent        already  nav=19567.0
  arm-agent-ds-v1  ran      wrote_nav=True n_trades=1 nav=19553.68
  arm-agent-ds-v2  ran      wrote_nav=True n_trades=0 nav=19567.0   ← §9.3-④
  arm-agent-random already  nav=19567.0
trades: 12 -> 12；new trades: []          # 重跑**没有**补出那 4 笔买入（含滑点后仍不足 1 手）
REPAIRED ROW: cash=19553.68 mv=0.0 nav=19553.68 positions=[] cum_cost=9.2 cum_return=-0.022316
post-repair probe: checked=22 defective=0 cent=5 valuation=3
system_events: 1 条（module=paper_nav_daily, level=warn, 含旧值/diff/依据文件名）
```

`nav 19553.68` / `cum_return -0.022316` 与任务书 §1 的期望（`19553.68` / `-0.0223`）**逐位相同**。
`arm-discipline-*` 的 5 条分币级行与 3 条估值漂移行**未被改动**。

### 9.6 需要 nanobot 执行的真库动作（对应 §5）

1. `store/migrate.py::backup_db` 备份（现库 197 MB，`data/backups/`）；
2. 先在**副本**上跑两次 `.venv/bin/python -c "..."`（见下）核对清单 —— 本站已跑过，清单同 §9.5；
3. 真库：调 `migrate_p62_agent_nav_settlement` → `paper agent run --asof 2026-09-23 --db data/stocklab.db`
   （注意退出码是 **1**：`arm-agent` 与 `arm-agent-ds-v2` 当日缺决策，属既有回执，不是失败）；
4. 核对：`paper_nav_daily` 行数 33 → 34（**-1 +2**：被删的点名行重写回来 + ds-v2 的平盘行，
   见 §9.3-④）；除 `(arm-agent-ds-v1, 2026-09-23)` 与新增的 `(arm-agent-ds-v2, 2026-09-23)`
   之外**逐字节未变**；`paper_trades` 仍是 12 行；`p62_defective_agent_nav_rows` ⇒ `defective=0`；
5. 把修复记录（含 §9.4/§9.5 两份输出）贴回本任务书 §7 的 T6。

调用片段（与 `docs/plans/2026-09-22-财报数据层v2-设计.md:288` 的写法同款）：

```python
from stocklab.store.db import connect
from stocklab.store.migrate import p62_defective_agent_nav_rows, migrate_p62_agent_nav_settlement
conn = connect("data/stocklab.db")
print(p62_defective_agent_nav_rows(conn))          # 先看清单（只读）
print(migrate_p62_agent_nav_settlement(conn))      # 再动手
```

### 9.7 判据（T7 口径）

```
$ .venv/bin/python -m pytest --collect-only -q | awk -F': ' '/^tests\//{s+=$2} END{print s}'
2919                                   # 基线 2910（2908 passed + 2 skipped）→ +9，只增不减
$ .venv/bin/python -m pytest -q        # exit 0，无 F / E（末行是进度条，见 ERROR_DIARY #68）
$ bash scripts/verify.sh               # exit 0，红线三目标 sha 未变
```

### 9.8 P52 那条判据为什么没拦住（§3.4）

P52 的 T4 写的是「与 `paper step` 同口径算出的值一致」。**AI 臂根本不走 `paper step`**：
它被 `external_executor` / `external_executor`⇒`agent_claim_accounts` 认领，`paper step` 里
`claim_handover` 直接 `continue` 让出，日终由 `paper agent run` 落。于是那条判据比对的是
**另一条路径**的产物 —— 它绿，是因为 AI 臂那一侧**没有任何东西被检查**。

更深一层：当时那条测试的期望值是从**实现**反推的（`round(d.amount, 2)` 那一支），
两边同源 ⇒ 实现错、期望一起错。本站把它换成**外部不变量**：
`nav == initial_cash + 全部成交按 `_fee_parts` 口径重放 + 当日收盘 MTM`，重放与市值都调既有
实现（`engine._ledger_arm_state` / `mark_to_market`），测试里不另算一份净值。

**教训写进 ERROR_DIARY #71**：判据与被测对象同源 ⇒ 绿也是空转；判据的**路径**必须点名
（哪个入口写的，就从哪个入口验），容差不能把性质不同的两类问题混成一类。

---

## 10. nanobot 独立审计与真库动作（2026-09-24）

### 10.1 审计（不采信自报）

| 判据 | 读数 |
|---|---|
| 代码站提交 | `b0f34ed fix(paper): P62 …`（显式路径 4 文件：`paper/agent_decide.py`、`store/migrate.py`＋2 个新测试） |
| 全量 | `.venv/bin/python -m pytest` **exit 0**；`--collect-only` **2919**（基线 2910 ⇒ +9，只增不减，＝新增 5＋4 条） |
| 红线 | `bash scripts/verify.sh` **exit 0**；`0acf35c9…`/175、`73ffbfb1…`/1330、`ddac0a88…`/171 **== 基线**（叶子 sha 亦一致） |
| 反目标 | `git diff --name-only` 无 `config/costs.py` / `paper/engine.py` / `schema.sql` / 红线基线；`plan_target_weight` 零改动；迁移只 `DELETE FROM paper_nav_daily`，`paper_trades` 不在其 SQL 里；触发器文本与 `schema.sql` 语义同文，跑完在副本上实测 `DELETE`/`UPDATE` 仍被 `paper_nav_daily is append-only` 拦下 |
| 独立复现 | 真库**只读**（`mode=ro`）探针：`checked=21 defective=1`，点名行 `cash/nav got 11320.0 → replay 19553.68`、`positions got {"000333": 0} → replay {}`；与 §1 逐位相同 |

### 10.2 真库动作（§5 五步，逐条留痕）

1. 备份 `data/backups/stocklab.p62-navfix.20260923T172737Z.db`（197,636,096 B），另存副本 `/tmp/p62_nanobot_copy.db`；
2. 副本：迁移第 1 次 = `['arm-agent-ds-v1/2026-09-23: 删除未结算净值行（nav 11320.0 → 待重写）', +8 条 ⚠️ 只报告]`、
   第 2 次 = `[]`（可重入）、净值行 33 → 32、其余行逐字节未变、`paper_trades` 12 行；
   副本上重跑 `paper agent run --asof 2026-09-23` ⇒ `ds-v1 ran / wrote_nav=True / n_trades=1 / nav=19553.68`；
3. 真库：同一迁移（33 → 32）→ `paper agent run --asof 2026-09-23 --db data/stocklab.db`
   **exit 1**（`anomaly_missing_decision=2`：`arm-agent`、`arm-agent-ds-v2` 当日无决策行 —— 既有回执，非本站失败）；
   `ds-v1 ran wrote_nav=True n_trades=1 nav=19553.68`；
4. 复核（真库读回）：`(arm-agent-ds-v1, 2026-09-23)` = `cash 19553.68 / mv 0.0 / nav 19553.68 / positions [] /
   cum_cost 9.2 / cum_return −0.022316` —— 与 §1 期望逐位相同；净值行 33 → **34**（−1 点名行 ＋1 重写 ＋1 `ds-v2`，见 §9.3-④）；
   **除该两行外其余 32 行逐字节未变**；`paper_trades` 12 → 12 **逐字段相同**；`p62_defective_agent_nav_rows` ⇒
   `checked=22 defective=0`（`cent_drift` 5 / `valuation` 3 仍在，未改动）；`system_events` 1 条（含旧值、diff、依据文件名）；
   两条触发器在；真库 sha `e626cdfa…` → `35c8e80e3f4cf3979680681e5d8683ae76e80bfe0c97514ace234fb442536845`；
5. 本节即第 5 步的贴回；§7 的 T6 已改 ✅。

### 10.3 遗留（需用户拍板，本站未动）

- `arm-agent-ds-v2 / 2026-09-23` 多出一条平盘行（`nav 19567.0`、`n_trades=0`）—— `paper agent run` 没有
  「只推一个账户」的开关，而 `ds-v2` 的 `start_date` 继承 2026-09-15 ⇒ 引擎认为它在飞。现按 §9.3-④① 接受；
  若要「不许出现」，那是一条**单独点名**的行。
- §9.3 的五条口径（未结算判据 ¥1.00 / 迁移不挂写库入口 / `_settle` 跟重放而不跟 `Decision.amount` /
  点名清单硬编码 / 静态臂放行分币级差）与 `cent_drift` 5 条、`valuation` 3 条（2026-09-22，原因未查）仍待拍板。
