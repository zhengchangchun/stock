# P51 杂项收口（连接泄漏 · 文案误导 · 生产验证核对点）

创建：2026-09-22（nanobot 需求澄清）｜ 实现：Claude Code
来源：P37 验收记录 §6、P38 验收记录 §5、P46 任务书 §9.4

> 本站**只做两件小改 + 一条验证动作**。改行为的两条要先红后绿；不许顺手重构。

## T1 · `cmd_paper_spec_set` 的两条早退路径没关连接（读码确认）

`stocklab/cli/main.py::cmd_paper_spec_set`：

- `2441` 冲突分支：`return _spec_conflict(...)` —— **没有** `conn.close()`；
- `2449` 幂等分支：`return 0` —— **同样没有** `conn.close()`；
- 同一函数的另外三条出口（`DecisionConflict` / `SpecViolation` / 成功）**都**显式 close。

**影响**：CLI 进程随即退出 ⇒ 现实伤害≈0；但它与本文件其他出口风格不一致，
一旦将来在同进程里复用（例如 labweb 走子进程之前先调一次）、或 `paper spec set` 被别处 import 调用，
就是真的写锁/句柄泄漏。**判据**：改成 `try/finally`（首选，一处收口）或给两条早退补 close；
新增一条**机械护栏**（源码扫描或调用计数），不许只靠人看。

- 反目标：不动退出码语义（1 冲突 / 2 输入不合法）、不动 `_spec_conflict` 的 JSON 字段。
- 证据口径：本条是**静态读码**结论（`stocklab/cli/main.py:2395-2475`），没有运行时句柄计数证据；
  实现时若拿不到可观测的泄漏（例如 `sqlite3` 直接 GC），**如实写「无法机械观测」**，不要编一个数字。

## T2 · patrol 计划 §① 的 reason 文案误导（P46 §9.4 只记录不改）

`stocklab/ops/patrol.py` 计划里 §① 在 `amount_null_rows>0` 时声称「当日快照的 `amount` 仍是 NULL」，
但实测那几行恰恰是**没有快照**的标的。**判据**：文案与实测一致（说清「没有快照的标的」），
**判定逻辑不许动**（单变量原则：这一条不是判据 bug，是文案 bug）。

## T3 · 生产验证核对点（**不属本任务**，由 nanobot 做）

2026-09-23 15:30 自然槽之后核对两件事：
1. `reports/ops/latest-close.json` → `exit_code == 0`（P46 的定型闸门生效、`predict_run` 不再冲突）；
2. 2026-09-23 的 `job_runs` 里 patrol 的 `plan.skipped` 含「`predict run --asof 今天`」，
   且不含任何 `predict_run` 补步。
> 在核对完成前，**不得声称「#60 已修复」**（P38 验收记录 §5.1）。

## 环境与收尾

- `.venv/bin/python`；`pytest -q` exit 0（用例数只增不减）＋ `bash scripts/verify.sh` exit 0（红线三目标不变）。
- 交付物：代码 + 测试 + 实测命令与输出；进度表打勾。
- ❌ 不 `git commit`、不建分支/worktree。

## 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | `cmd_paper_spec_set` 两条早退路径补 `close` + 机械护栏 | ⬜ 未开始 |
| T2 | patrol §① reason 文案与实测对齐（逻辑不动） | ⬜ 未开始 |
| T3 | （nanobot）2026-09-23 收盘后核对闸门生效 | ⬜ 未开始 |
