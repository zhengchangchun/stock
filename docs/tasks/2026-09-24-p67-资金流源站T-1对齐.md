# P67 资金流采集与判据的「源站 T-1」对齐（省钱 ＋ 去掉每天的假警）

创建：2026-09-24（nanobot 需求澄清＋实测；用户当日要求「你自己看下需要怎么优化」）｜ 实现：Claude Code（superpowers 全流程）
来源：2026-09-24 15:30 生产 `ops close` 的读数（整轮 exit 1，唯一非绿项＝⑦ `money_flow_daily max=2026-09-23`）
＋ nanobot 当日三组实测（见 §1）。相关：ERROR_DIARY #53（调度器 ok ≠ 数据链 ok）、#54（预算用尽曾报绿码）、#55（`ingest *` 不接受 `--db`）。

---

## 0. 一句话

资金流的源站（新浪 MoneyFlow）是 **T-1 发布**：当天收盘后拿不到当天数据。
现在这条链路为此付了两笔**不必要的代价**，本站各修一处：

1. **采集**：`fetch_money_flow_daily` 为了拿最后 1 行，**每天把 21 只标的 2010 年至今的整段历史翻一遍**
   （≈882 次请求 / **269 s** / 写入 0 行）；改成「翻到 `start` 边界即停」目标 ≈21 次请求 / ~5 s。
2. **判据**：⑦ 把「第三方发布节奏」当成自己的 SLA ⇒ **每天 15:35 固定挂一条 stale**，
   整轮 `ops close` 常年 exit 1。改成三档 `ok / lag / stale`，其中 `lag`（恰好差一个交易日、
   且有「该行是收盘后才拿到的」证据）**不计异常、但仍排补步**。

**这一条也不许放松**：`lag` 只容许**恰好一个交易日**；差 ≥2 个交易日、或证据显示源站当天发布过
却拿不到 ⇒ 仍判 `stale`（真缺口照旧报红）。**不许**把 ⑦ 改成「永远 ok」。

## 1. 实测证据（nanobot 2026-09-24 自跑，真库只读或纯网络、零写入）

**① 源站是 T-1（`money_flow_daily` 的「日期 vs 写入时刻」实测）**

| `date` | 行数 | 首次写入 `fetched_at` |
|---|---|---|
| 2026-09-22 | 21 | **2026-09-23T09:01:02** |
| 2026-09-23 | 21 | **2026-09-24T09:00:57** |

⇒ 每天的资金流行都在**次日早间**才入账；15:30 收盘链跑时当天数据**尚未发布**。
补它的是**早间 patrol 的 `--fix`**（`job_runs` run_id=119 / 09:00:04：
`fixed=ingest_actions,ingest_valuation,ingest_moneyflow,session_tick,session_backfill_close`，exit=0）。
**这条自愈路径必须保持可用**（见 §2 T2 的排步要求）。

**② 采集成本（纯网络探针，单页请求）**

```
page=1 用时 0.27s 行数=100 日期 2026-05-06 … 2026-09-23
page=2 用时 0.17s 行数=100 日期 2025-12-01 … 2026-04-30
page=3 用时 0.33s 行数=100 日期 2025-07-04 … 2025-11-28
PAGE_SIZE = 100
```

**③ 今天的实际开销**：`ops close` 的 `ingest_moneyflow` 步 **duration_s = 269.647**（其余 13 步合计 ≈60s）。
按 ① 的口径，它**一行都没写**（`money_flow_daily` 当日 0 行）。数据侧：每只标的 **4019 行**
（2010-03-01→2026-09-23）⇒ 41 满页 + 1 短页 ≈ **42 请求/只 × 21 只 = 882 请求**
—— 与 269s / 单请求 0.17–0.33s 对得上。而 `--days 30` 只需要**第 1 页**（覆盖 4.7 个月）。

**④ 假警**：15:30 那轮 `reports/ops/latest-close.json` 的 `after.anomalies` 只有一条
（⑦ `money_flow_daily max=2026-09-23`），整轮 `exit_code=1`；而 14 步本身**全 exit 0**。

## 2. 口径（实现必须逐条对上）

### T1 采集：翻页到 `start` 边界即停

- `stocklab/data/fetch.py::fetch_money_flow_daily`：页按 `opendate` **降序**（`asc=0`）⇒
  某页的**最老一行 `<= start`** 时，再往后翻只会拿到 `< start` 的行 ⇒ **立即停止翻页**。
- 其余一个字不改：`len(rows) > page_size` 的源站口径拒绝、`len(rows) < page_size` 的到头判定、
  `[start, end]` 过滤、按日期升序返回、`MAX_PAGES` 安全阀、raw cache 的 key 形状。
- **输出必须逐条不变**：同 `(code, start, end)` 下，新旧两版返回的行**逐条相同**（用对拍用例钉住，
  不是「看着一样」）。
- 边界：`min(page dates) == start` **可以停**（该页已含 `start` 那天的行，更老的必然 `< start`）。

### T2 判据：⑦ 拆子项，`lag` 不计异常但照旧排补步

- ⑦ 现在把 `valuation_daily` / `money_flow_daily` / `adj_factors` 三件事压成一个 `status`。
  改成**子项各自有状态**（键只增不减，旧键保留：`valuation_max` / `money_flow_max` /
  `adj_factors_rows` / `required` / `reason` 照旧）：
  - `valuation`：`ok` / `stale`（照旧，当日已能拿到——实测 2026-09-24 已入库）；
  - `money_flow`：**三档**
    - `ok`：`max >= required`；
    - `lag`：`max` 恰好等于 `required` 的**前一个交易日**，
      **且**该行的首次写入时刻 `> max` 那天的收盘（15:00）—— 这条是**从数据里读出来的证据**，
      不是「假设源站 T-1」（样本：`max=2026-09-23` 的 `fetched_at=2026-09-24T09:00:57`）；
    - `stale`：其它全部情形（差 ≥2 个交易日；**或**恰好差 1 天但证据显示源站当天就发布过
      —— 那是我们**采集中断**，不许读成 `lag`）；
  - `adj_factors`：`ok` / `stale`（照旧）。
  - ⑦ 汇总 `status`：任一子项 `stale` ⇒ `stale`；否则任一为 `lag` ⇒ `lag`；否则 `ok`。
  - 另给两个**只增**的键：`required_money_flow`（`lag` 时＝`max`，即「按源站节奏该有的那一行」）、
    `lag_sessions`（`required` 与 `max` 差几个交易日）。
- **`lag` 不进 `ANOMALY_STATUSES`**（⇒ 不影响 `verdict` / 整轮 exit code），
  与 ② 的 `unknown`「不计异常但补步仍然会去采一次」同款（`verdict()` 的 docstring 已把这个先例写死）。
- **但 `plan()` 仍要排补步**，且**按子项排**：`money_flow` 落后 ⇒ 排 `ingest_moneyflow`；
  `valuation` 落后 ⇒ 排 `ingest_valuation`；`adj_factors` 空 ⇒ 排 `ingest_actions`。
  （今天是一条 stale 就三样全排；按子项排更准，也少白跑。）
- 报告/页面：`lag` 的 `reason` 必须**人话点名**，例如
  「资金流源站 T-1 发布：最近一行 2026-09-23（2026-09-24T09:00:57 写入），
  2026-09-24 的行按同一节奏在下一交易日早间补落 —— **不算缺口，但要补**」。

### T3 `job_runs` 的时长不再恒为 0（顺带，A6「文件在说谎」）

- 实测：`ingest_moneyflow` 的 `started_at == finished_at`（如 `2026-09-24T15:31:03`），
  因为整条命令共用一个启动时刻 `now` 传给 `finish_job`。
- 修：`stocklab/data/ingest.py`（bars）、`stocklab/cli/main.py`（actions＋valuation/moneyflow 两处）
  在**收尾时取一个新的时刻**写 `finished_at`（`datetime.now(TZ)`），`started_at` 不动。
- 判据：一条**慢**的假采集（注入假 client，人为 sleep 或用假时钟）在 `job_runs` 里
  `finished_at > started_at`，差值反映真实耗时；并**实测一次真库外的对照**（副本或纯 fetch）。
- 注意：`job_runs` 里 49/52/72 三条永久 `running` 是 2026-09-22 的历史残留，**不要动**。

## 3. 反目标

- ❌ 不改 `MAX_PAGES`（=50 是安全阀；本站只加「越界即停」）。**但要在实施记录里点名**：
  全历史窗口 4019 行 ⇒ 42/50 页、余量仅 8 页，触发线＝「任一标的行数 > 4800」。
- ❌ 不放松任何闸门：`len(rows) > page_size` 的拒绝、`MAX_PAGES` 的 fail-closed、PIT 首写保留都不动。
- ❌ 不改 `ingest *` 不接受 `--db` 的现状（ERROR_DIARY #55），不新增真库写路径。
- ❌ 不动 ①②③④⑤⑥ 的判据与 `verdict()` 里 `unknown` 的待遇；不动 `CLOSE_STEPS`（`ingest_moneyflow` **保留在收盘链**里，T1 之后它只值几秒）。
- ❌ 真库 `data/stocklab.db` **只读**：真库上只跑**不带 `--fix`** 的 `ops patrol`（`ro_connect`，只读）；
  `--fix`、`ingest *`、任何写库动作一律只在 `/tmp` 副本上做。
- ❌ 不许为了让 ⑦ 变绿而放宽判据（例如「money_flow 差一天一律 ok」＝把采集中断也一起放行）。

## 4. 判据汇总（可证伪）

1. **T1 对拍**：同 `(code, start, end)`，新旧两版返回的行**逐条相同**（用例＋真库只读各抽 2 只标的实测）；
2. **T1 请求数**：`--days 30` 窗口下 21 只标的的**总请求数** ≤ 21（旧：≈882）——
   用带计数器的假 client 或对 fetch 层计数（**纯 fetch、不写库**）；并给「21 只逐只 fetch 的墙钟耗时」实测；
3. **T2 三档**：夹具库上 `lag` ⇒ `verdict.exit_code == 0` **且** `plan.steps` 仍含 `ingest_moneyflow`；
   `stale`（差 2 个交易日）⇒ `exit_code == 1`；「恰好差 1 天但证据说源站当天发布」⇒ `stale`（用例逐条钉住）；
4. **T2 真读数**：真库上跑一次**不带 `--fix`** 的 `ops patrol --now 2026-09-24T15:35:00`，
   ⑦ `status == "lag"`、`verdict.exit_code == 0`、`plan.steps` 含 `ingest_moneyflow`（原文贴进实施记录）；
5. **T3**：慢采集的 `job_runs` 读数里 `finished_at > started_at`；
6. 全量 `.venv/bin/python -m pytest -o addopts=""` 只增不减（开工先记基线；P65 后基线＝收集 2975 / 2973 passed / 2 skipped，
   **若 P66 先合入，以 P66 交付后的读数为基线并写明**）；`bash scripts/verify.sh` exit 0；
   红线三目标 == `0acf35c9…`/175、`73ffbfb1…`/1330、`ddac0a88…`/171（**未 regen**）；
   真库 sha 与开工一致或差异能归因到 launchd 的 patrol/close；
7. 提交只用**显式路径**（禁 `git add -A`）：`stocklab/data/fetch.py`、`stocklab/ops/patrol.py`、
   `stocklab/data/ingest.py`、`stocklab/cli/main.py`、`tests/<改动或新增的用例>`、
   `docs/tasks/2026-09-24-p67-*.md`、`docs/errors/ERROR_DIARY.md`（加一条）。

## 5. ERROR_DIARY 要加的一条（本站必须写）

> 题目自拟，但必须覆盖两句教训：
> ① **「为了拿最后一行，把整段历史翻一遍」**——分页方向与过滤时机不一致（先翻到底再按 `start` 过滤）
>    会让「增量窗口」在请求数上完全失效；实测 882 请求/269s 换 0 行新数据。
> ② **判据把第三方的发布节奏当成了自己的 SLA**——于是每天都稳定地报一条假警，
>    而「每天都有、每天都一样」的告警会被读成背景噪音（与 #53「调度器 ok ≠ 数据链 ok」同族，
>    但方向相反：这次是**数据链正常而调度判据说谎**）。修法是让判据**从数据里读出**滞后证据。

## 6. 实施记录（由实现站填写）

> 实现站：Claude Code ｜ 分支 `feat/financials` ｜ 日期 2026-09-24 ｜ TDD：每条判据先写失败用例、
> 看它因「功能缺失」失败，再写最小实现（RED→GREEN 的失败原文见各小节）。

### 6.0 开工状态

```
$ git status --short          # （空 —— 工作区干净）
$ git log --oneline -3
6a1e5c5 docs(tasks): P67 资金流源站 T-1 对齐（…）
18bb41d docs(p66): 实施记录 §6-§8 ＋ 运维手册退出码两栏 ＋ ERROR_DIARY #75
f0f555a feat(paper): 随机对照臂敞口上界扣掉「不在 picks 里的存量持仓」（口径 v2 / P66）
```

**P66 已合入**（`f0f555a`）⇒ 基线按 P66 交付后的**实测**读数算，不沿用 P65 的 2975/2973/2（见 §6.7）。

> 会话内工作区被**另一个 actor**（nanobot）动过一次：开工时 HEAD 是 `6a1e5c5`
> （P67 任务书本身），本站进行中 nanobot 又落了 `c52b75f`（P68 任务书，
> `git show --stat` = **1 file changed, docs/tasks/**，**不含任何测试文件**）。
> 故 §6.7 的基线导出实际拿到的是 `c52b75f`，**用例基数不受影响**。
> 本站未触碰该提交及其文件。

真库（开工，只读）：

```
$ shasum -a 256 data/stocklab.db
a7c68feb2a41c253e5f0f23094e58469d45f990eef30609a2db714fd0e79edfc  data/stocklab.db
$ SELECT MAX(run_id) FROM job_runs          ⇒ 137
$ SELECT COUNT(*) FROM job_runs WHERE job_name='patrol'   ⇒ 29
```

改动落地前先核对了 §1 的三条实测前提（真库只读）：

```
trading_calendar max=2026-09-24 ｜ bars_daily max=2026-09-24
money_flow_daily max=2026-09-23（21 行，MIN(fetched_at)=2026-09-24T09:00:57+08:00）
valuation_daily max=2026-09-24 ｜ adj_factors=80512 行
```

⇒ §1① 的「T-1 次日早间入账」在库中**逐字成立**：`2026-09-23` 那 21 行的首次写入
时刻是次日 `09:00:57`。§1④ 的假警也复现：旧判据下 `required=2026-09-24 > max=2026-09-23`。

---

### 6.1 判据 1：T1 对拍 —— 输出**逐条不变**

**用例**（`tests/test_data_fetch_p28.py`，新增 **8** 个：对拍 3 个参数化 + 边界 4 个 + `--days 30` 1 个）：
对拍基准是**旧口径本身的独立复写**
（`_ref_fetch_moneyflow`：先翻到底再按 `[start,end]` 过滤），在同一个假源上比
「三元组逐条相同」（含 `resp_sha256` / `cache_key`），不是「看着一样」。

RED（先看它失败）：

```
$ .venv/bin/python -m pytest -o addopts="" tests/test_data_fetch_p28.py -q -k moneyflow
7 failed, 4 passed, 4 deselected in 5.14s
E       assert (82 * 21) <= 21          # 旧口径：82 请求/只（=41 页×2 只）
```

GREEN：

```
$ .venv/bin/python -m pytest -o addopts="" tests/test_data_fetch_p28.py -q
15 passed in 0.30s
```

覆盖：`page_size∈{3,5,10}` 三档对拍（窗口跨到第 2 页才收口 ⇒ 也钉住「该翻的没少翻」）、
`min(该页日期) == start` 可停、页最老一行仍 `> start` 时必须继续翻、
`start` 比整段历史都老时不触发边界（照旧翻到短页）、
`start` 落在最新一页内时 1 请求、`--days 30` 窗口 21 只总请求 ≤ 21。

**真库只读抽 2 只标的实测**（真网络、纯 fetch、`cache=None`、不写库，脚本 `/tmp/p67_t1_probe.py`）：

```
$ .venv/bin/python /tmp/p67_t1_probe.py
# 真库 instruments：21 只（只读连接读出来的）
# 窗口 start=2026-08-25 end=2026-09-24（= CLI `--days 30` 的口径）

## ① 对拍（抽 2 只标的：新口径 vs 旧口径，逐条相同？）
  000333 (sz000333): 新=23 行/1 请求；旧=23 行/32 请求；逐条相同=True
     行日期 2026-08-25 … 2026-09-24
  603868 (sh603868): 新=23 行/1 请求；旧=23 行/26 请求；逐条相同=True
     行日期 2026-08-25 … 2026-09-24
```

（抽的是 `instruments` 排序后的**首只**（000333）与**末只**（603868），非人工挑样本。）

---

### 6.2 判据 2：T1 请求数 ≤ 21 ＋ 21 只墙钟耗时

```
## ② 请求数：21 只标的同窗口的总请求数（新口径）
  000333 (sz000333): 1 请求, 23 行, 0.29s
  000651 (sz000651): 1 请求, 23 行, 0.35s
  000858 (sz000858): 1 请求, 23 行, 0.35s
  002032 (sz002032): 1 请求, 23 行, 0.35s
  002415 (sz002415): 1 请求, 23 行, 0.39s
  002508 (sz002508): 1 请求, 23 行, 0.32s
  300750 (sz300750): 1 请求, 23 行, 0.35s
  510300 (sh510300): 1 请求, 23 行, 0.36s
  510880 (sh510880): 1 请求, 23 行, 0.36s
  512890 (sh512890): 1 请求, 23 行, 0.36s
  518880 (sh518880): 1 请求, 23 行, 0.36s
  600028 (sh600028): 1 请求, 23 行, 0.36s
  600036 (sh600036): 1 请求, 23 行, 0.36s
  600519 (sh600519): 1 请求, 23 行, 0.37s
  600690 (sh600690): 1 请求, 23 行, 0.36s
  600900 (sh600900): 1 请求, 23 行, 0.35s
  600941 (sh600941): 1 请求, 23 行, 0.31s
  601088 (sh601088): 1 请求, 23 行, 0.40s
  601318 (sh601318): 1 请求, 23 行, 0.31s
  601398 (sh601398): 1 请求, 23 行, 0.40s
  603868 (sh603868): 1 请求, 23 行, 0.32s

  合计请求数 = 21（判据 ≤ 21；旧口径 ≈ 42×21 = 882）
  21 只逐只 fetch 墙钟总耗时 = 7.38s（最慢一只 600028 0.42s）
```

**新旧代价对账**（同一进程、同一窗口、同一客户端，各跑一遍全量）：

```
## ③ 旧口径的总代价（21 只逐只翻到底）—— 与 §1③ 的 882 请求 / 269s 对账
  000333 (sz000333): 32 请求, 过滤后 23 行
  000651 (sz000651): 41 请求, 过滤后 23 行
  …（ETF 4 只各 5 请求，000333/603868/600941 各 32/26/12，其余 41）
  旧口径合计请求数 = 641；墙钟总耗时 = 226.37s
  新口径合计请求数 = 21；墙钟总耗时 = 7.38s
```

⇒ **请求数 641 → 21（−96.7%）、墙钟 226.37s → 7.38s（−96.7%）**，而过滤后**每只都是 23 行**
（与 ① 的「逐条相同 = True」互为印证）。§1③ 报的 269.647s 与这里的 226.37s 同量级
（差在当天网络与缓存），**口径一致**。

**为什么 641 而不是 882**（§1③ 的估算是 `4019 行 ÷ 100 = 42 页 × 21 只`，偏大）：

- 4 只 ETF 的历史短，各只要 5 页（`4019` 是**股票**的行数，不是 ETF 的）；
- `000333` 只翻到 32 页 —— 不是网络打嗝，而是**源站这段历史就到这儿**：直接探第 31–34 页：

```
page=31 raw_type=list 行数=100 日期 2014-05-21 … 2013-12-20
page=32 raw_type=list 行数=60  日期 2013-12-19 … 2013-09-18
page=33 raw_type=list 行数=0   日期 None … None
page=34 raw_type=list 行数=0   日期 None … None
```

（第 32 页 60 行 ⇒ 触发「`len(rows) < page_size` 到头判定」；第 33 页确实是空数组。
`000333` 在库里 4019 行是历史多源累积的结果，与源站当前可回溯到的 2013-09-18 不矛盾。）

### 6.3 判据 3：T2 三档（夹具库，用例逐条钉住）

RED（先看它失败）：`KeyError: 'valuation_status'`（子项键不存在 ⇒ 功能缺失，不是笔误）

```
$ .venv/bin/python -m pytest -o addopts="" tests/test_ops_patrol.py -q -k "lag or stale or pit or adj_factors"
8 failed, 2 passed, 44 deselected in 1.28s
E       KeyError: 'valuation_status'
```

GREEN：

```
$ .venv/bin/python -m pytest -o addopts="" tests/test_ops_patrol.py -q
54 passed in 1.27s
```

新增 7 个用例覆盖的口径（夹具 `L=2026-09-21`，其前一交易日 `=2026-09-18`）：

| 夹具 | `money_flow_status` | `exit_code` | `plan.steps` |
|---|---|---|---|
| `max=09-18`，`fetched_at=09-21T09:00:57`（次日早间） | `lag` | **0** | `["ingest_moneyflow"]` |
| `max=09-17`（差 2 个交易日） | `stale` | **1** | `["ingest_moneyflow"]` |
| `max=09-18`，`fetched_at=2026-09-18T14:50`（**当天收盘前**） | `stale` | **1** | `["ingest_moneyflow"]` |
| 日历没覆盖 `required`（判不出前一交易日） | `stale` | — | — |
| `valuation=09-18` + 资金流 `lag` | 汇总 `stale` | 1 | `["ingest_valuation","ingest_moneyflow"]` |
| `adj_factors` 空 | 汇总 `stale` | 1 | `["ingest_actions"]` |
| `lag` 的 reason | 含日期、写入时刻、「不算缺口…要补」 | — | — |

配套断言：`lag` 时 `anomalies == []`、`ok is True`、`summary_line` 出 `⑦lag … anomalies=0`；
`required_money_flow == max`、`lag_sessions == 1`（差 2 天那档为 `2`）。

### 6.4 判据 4：T2 真库读数（`ops patrol`，**不带 `--fix`**）

```
$ .venv/bin/python -m stocklab.cli.main ops patrol --now 2026-09-24T15:35:00
{
  "after_exit_code": 0,
  "anomalies": [],
  "calendar": { "error": null, "max_date": "2026-09-24", "reason": null,
                "required": "2026-09-24", "status": "ok" },
  "checks": {
    "bars": { "amount_null_rows": 6, "latest_date": "2026-09-24", "reason": null,
              "required": "2026-09-24", "rows": 23, "snapshots_for_date": 34,
              "status": "ok" },
    "paper_nav": { "accounts": 10, "claimed_accounts": ["arm-agent", "arm-agent-ds-v1",
                    "arm-agent-ds-v2", "arm-agent-random"], "expected_accounts": 6,
                   "latest_date": "2026-09-24", "reason": null,
                   "required": "2026-09-24", "rows_for_required": 6, "status": "ok",
                   "unknown_executors": [] },
    "pit_tables": {
      "adj_factors_rows": 80512,
      "adj_factors_status": "ok",
      "lag_sessions": 1,
      "money_flow_fetched_at": "2026-09-24T09:00:57+08:00",
      "money_flow_max": "2026-09-23",
      "money_flow_status": "lag",
      "reason": "资金流源站 T-1 发布：最近一行 2026-09-23（2026-09-24T09:00:57+08:00 写入），2026-09-24 的行按同一节奏在下一交易日早间补落 —— **不算缺口，但要补**",
      "required": "2026-09-24",
      "required_money_flow": "2026-09-23",
      "status": "lag",
      "valuation_max": "2026-09-24",
      "valuation_status": "ok"
    },
    "predictions": { "asof": "2026-09-24", "model_version": "pit-rw-v1.0.2", "n": 17,
                     "reason": null, "status": "ok", "target_date": "2026-09-25" },
    "review": { "path": "…/reports/2026-09-24-review.md", "reason": null,
                "required": "2026-09-24", "status": "ok" },
    "snapshot": { "rows": 18, "rows_after_close": 14, "status": "ok",
                  "trade_date": "2026-09-24" },
    "verifications": { "by_target_date": {}, "n": 0, "status": "ok" }
  },
  "db": "…/data/stocklab.db",
  "exit_code": 0,
  "fix": false,
  "job": "patrol",
  "journal": { "report": "…/reports/ops/latest-patrol.json", "run_id": 138,
               "status": "ok" },
  "latest_closed_session": { "date": "2026-09-24", "evidence": { "bars_max_date":
               "2026-09-24", "bars_side": "2026-09-24", "calendar_error": null,
               "calendar_range": { "first": "2007-01-15", "last": "2026-09-24" },
               "calendar_side": "2026-09-24", "now": "2026-09-24T15:35:00" } },
  "now": "2026-09-24T15:35:00",
  "ok": true,
  "plan": {
    "exit_code": 0,
    "reasons": [
      "① 2026-09-24 已有 34 行当日快照（时点见 ②），另有 6 行 amount/turnover 为 NULL —— 那几行正是**没有当日快照**的标的（快照只覆盖了部分标的）→ 回填补上",
      "⑦ 资金流源站 T-1 发布：最近一行 2026-09-23（2026-09-24T09:00:57+08:00 写入），2026-09-24 的行按同一节奏在下一交易日早间补落 —— **不算缺口，但要补**"
    ],
    "skipped": [],
    "steps": [ "ingest_moneyflow", "session_backfill_close" ]
  },
  "session_day": { "is_trading_day": true, "why": "trading_calendar" },
  "steps": [],
  "today": "2026-09-24",
  "verdict": { "exit_code": 0, "reasons": [] }
}
patrol: exit=0 latest_closed=2026-09-24 calendar=ok ①ok ②ok ③ok ④ok ⑤ok ⑥ok ⑦lag fixed=- anomalies=0
EXIT=0
```

逐条对上 §4.4：

| 要求 | 实测 |
|---|---|
| ⑦ `status == "lag"` | `"status": "lag"`（`money_flow_status: "lag"`，`valuation/adj_factors: ok`） |
| `verdict.exit_code == 0` | `"verdict": {"exit_code": 0, "reasons": []}` |
| `plan.steps` 含 `ingest_moneyflow` | `["ingest_moneyflow", "session_backfill_close"]` |
| 整轮退出码 | `EXIT=0`（**旧版这里是 1** —— §1④ 的假警消除） |

`plan.steps` 里同时出现 `session_backfill_close` 与 ⑦ 无关：那是 ① 的
`amount_null_rows=6 且当日有快照`（P51 T2 的既有规则），**不是本站引入的**。

> **`ops patrol` 本身写什么**（如实记）：它是 §2「真库只读」那条纪律点名的**唯一例外**
> （不带 `--fix`）。体检走 `mode=ro`；回执按 `journal.seal` 的既有设计另开读写连接，
> 写两样东西：`job_runs` 一行（本次 `run_id=138`）与 `reports/ops/latest-patrol.json`
> （后者未纳入版本控制）。真库 sha 因此由 `a7c68feb…` 变为 `331a7efe…`，
> **可归因**：新增行 `run_id=138 / job_name=patrol / started_at=finished_at=2026-09-24T15:35:00 / ok`
> （写入标记，不是靠 sha 反推）。

### 6.5 判据 5：T3 —— 慢采集的 `job_runs` 读数

RED（先看它失败，失败原文**就是**真库实测的那个缺陷）：

```
$ .venv/bin/python -m pytest -o addopts="" tests/test_job_runs_timing.py -q
5 failed in 4.94s
E       AssertionError: ingest_moneyflow: finished_at(2026-09-24T16:07:17+08:00)
        不晚于 started_at(2026-09-24T16:07:17+08:00) —— 时长恒为 0，正是本站要修的读数
E       assert '2026-09-24T16:07:17+08:00' > '2026-09-24T16:07:17+08:00'
```

（假采集人为 `sleep(1.1)`，而两字段仍**逐字相同** —— 与真库
`ingest_moneyflow 269.647s / started_at == finished_at` 是同一个病。）

GREEN：

```
$ .venv/bin/python -m pytest -o addopts="" tests/test_job_runs_timing.py -q
5 passed in 4.89s
```

覆盖 §2 T3 点名的**三处**（`stocklab/data/ingest.py` 的 bars；
`stocklab/cli/main.py` 的 actions 与 `_cmd_ingest_series`（valuation/moneyflow 共用））：

| 用例 | 断言 |
|---|---|
| `ingest bars`（CLI） | `finished_at > started_at`，差 ≥ 1s（秒级截断的保证下界） |
| `ingest_daily_bars`（单元） | `started_at == 注入的 now`（起点不动）；`finished_at` 落在调用前后的**真墙钟**区间内、且 `!= now` |
| `ingest actions`（CLI） | 同上 |
| `ingest valuation`（CLI） | 同上 |
| `ingest moneyflow`（CLI） | 同上（就是真库那一步） |

**真库对照**（只读；§2 T3 的「修前」读数）：

```
$ SELECT run_id, job_name, started_at, finished_at FROM job_runs WHERE job_name='ingest_moneyflow' …
ingest_moneyflow | 2026-09-24T15:31:03 | 2026-09-24T15:31:03      # 修前：逐字相同（假读数）
```

修后同一处的状态只能由**下一次真实采集**写入（今天 15:30 的收盘链已跑过）；本站在
`/tmp` 副本之外未对真库做任何写操作，故真库内该行仍是修前读数 —— 修后的行为由上面
5 个用例（含真墙钟对照）与 `/tmp` 副本外的纯 fetch 路径证明。

> `job_runs` 里 49/52/72 三条永久 `running` 是 2026-09-22 的历史残留，**按 §2 T3 未动**。

### 6.6 反目标点名：`MAX_PAGES` 余量（§3 第一条要求写明）

- 任务书口径：全历史窗口 4019 行 ⇒ **42 页 / 上限 50 页，余量仅 8 页**；
  触发线 =「**任一标的行数 > 4800**」。
- 本站实测（§6.2 ③）：源站当前单只最多只给到 **41 页**（4100 行）就返回空数组；
  `000333` 更是 32 页止于 2013-09-18。⇒ 余量比任务书口径**更宽**，但**判据不放宽**：
  `MAX_PAGES` 仍是 50、仍是 fail-closed，本站只加「越界即停」，**没有动它**。
- 其余反目标逐条确认未碰：`len(rows) > page_size` 的拒绝、PIT 首写保留、
  `ingest *` 不接受 `--db`、①②③④⑤⑥ 与 `verdict()` 里 `unknown` 的待遇、
  `CLOSE_STEPS`（`ingest_moneyflow` **仍在收盘链**里）——均未改动。

### 6.7 判据 6：全量回归 + `verify.sh` + 红线三目标

**基线（P66 已合入 ⇒ 按 P66 交付后的实测读数，不沿用 P65 的 2975/2973/2）。**
用 `git archive HEAD` 把当时的 HEAD **原样**导出到 `/tmp/p67_base_head` 后跑
（不 stash、不动工作区）：

```
$ git archive HEAD | tar -x -C /tmp/p67_base_head
$ (cd /tmp/p67_base_head && ln -sfn <repo>/.venv .venv && .venv/bin/python -m pytest -o addopts="")
1 failed, 2981 passed, 2 skipped in 189.68s

$ (cd /tmp/p67_base_head && .venv/bin/python -m pytest -o addopts="" --collect-only -q | tail -1)
2984 tests collected in 0.73s

$ ls /tmp/p67_base_head/docs/tasks/ | tail -3      # 反查导出到的是哪个提交
2026-09-24-p67-资金流源站T-1对齐.md
2026-09-24-p68-存量持仓可部署性口径.md
TASK_TEMPLATE.md
```

⇒ 导出的是 **`c52b75f`**（`2026-09-24T16:04:38`，P68 任务书），**不是开工时的 `6a1e5c5`**
—— nanobot 在本站进行中又落了这个 docs-only 提交（见 §6.0）。两者只差这一个文档文件：

```
$ git show --stat --oneline c52b75f
c52b75f docs(tasks): P68 敞口上界不许假设「picks 里的存量能减仓」（…P66 残余的整手洞）
 1 file changed, 171 insertions(+)     # docs/tasks/ 一个文件，无测试文件
```

⇒ **用例基数与 `6a1e5c5` 相同**（`--collect-only` 不受文档影响），基线读数有效。

那 1 个 failed 是**导出副产物、不是基线失败**：`tests/test_redline_baseline.py::
test_baseline_file_exists_and_is_git_tracked` 的 `_git_tracked()` 要求仓库里
有 `.git`（`git archive` 不带它）。补上软链后原样复跑：

```
$ (cd /tmp/p67_base_head && ln -sfn <repo>/.git .git && .venv/bin/python -m pytest -o addopts="" tests/test_redline_baseline.py -q)
23 passed in 1.90s
```

⇒ **基线 = 收集 2984 / 2982 passed / 2 skipped**。

**本站改动后**：

```
$ .venv/bin/python -m pytest -o addopts=""
3002 passed, 2 skipped in 187.05s (0:03:07)
$ .venv/bin/python -m pytest -o addopts="" --collect-only -q | tail -1
3004 tests collected in 0.72s
```

| | 收集 | passed | skipped | failed |
|---|---|---|---|---|
| 基线（`git archive c52b75f`，P66 交付后；与开工 `6a1e5c5` 用例基数相同，见上） | 2984 | 2982 | 2 | 0（见上） |
| 本站 | 3004 | 3002 | 2 | 0 |
| 差 | **+20** | **+20** | 0 | 0 |

`+20` ＝ 本站新增用例总数，**逐条对得上**：T1 `8`（`test_data_fetch_p28.py`：
对拍 3 个参数化 + 边界 4 个 + `--days 30` 1 个）＋ T2 `7`（`test_ops_patrol.py`）
＋ T3 `5`（`test_job_runs_timing.py`）。**只增不减，无新增失败。**

`scripts/verify.sh`：

```
$ bash scripts/verify.sh; echo "VERIFY_EXIT=$?"
…
--- 4. 项目测试 ---
3002 passed, 2 skipped in 182.98s (0:03:02)
--- 4c. 回归红线（P25 / 错误日记 #34）---
  ✅ predict_synthetic                 sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致
  ✅ predict_real_2026-09-14           sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致
  ✅ backfill_real_2013-12-23_2026-09-14  sha256 ddac0a889c623d75… == 基线；叶子 171 项一致
✅ 回归红线全部成立
…
✅ 验证通过: all
VERIFY_EXIT=0
```

红线三目标逐条对上 §4.6：`0acf35c9…`/**175**、`73ffbfb1…`/**1330**、`ddac0a88…`/**171**，
**全部 == 基线、未 regen**。

**真库 sha**：开工 `a7c68feb…` → 现在 `331a7efe…`，**差异可归因**（§6.4）：
本站对真库的全部动作只有那一次 §4.4 点名的、不带 `--fix` 的 `ops patrol`，
它按既有设计写回执 ⇒ `job_runs` 新增 `run_id=138`。除此之外真库只被只读连接读过
（`file:…?mode=ro`）。期间 launchd 没有新的 patrol/close 落库
（`job_runs` 尾部仍是 `134 ingest_valuation / 135 ingest_moneyflow / 136 session_tick /
137 close(15:30, failed) / 138 patrol`，无 139+）。

### 6.8 判据 7：提交（只用显式路径）

**全程 `git add` 只给显式路径，未使用 `git add -A` / `git add .`**（`git add` 与
`git status` 的输出见 §7 的「提交」一栏）。分两个提交：代码＋用例一个、文档一个 ——
文档 §6.8 要引用代码提交的哈希，自引用在同一个提交里做不到。

```
$ git add stocklab/data/fetch.py stocklab/ops/patrol.py stocklab/data/ingest.py \
          stocklab/cli/main.py tests/test_data_fetch_p28.py tests/test_ops_patrol.py \
          tests/test_data_ingest.py tests/test_job_runs_timing.py
$ git status --short
M  stocklab/cli/main.py
M  stocklab/data/fetch.py
M  stocklab/data/ingest.py
M  stocklab/ops/patrol.py
M  tests/test_data_fetch_p28.py
M  tests/test_data_ingest.py
A  tests/test_job_runs_timing.py
M  tests/test_ops_patrol.py
（docs/ 两个文件此时仍未暂存 —— 是**故意**分开的，见上）
```

**提交 A（代码＋用例）**：

```
$ git commit -m "feat(p67): 资金流翻页越界即停 ＋ ⑦ 拆子项 lag 不计异常但排补步 ＋ job_runs 完成时刻"
[feat/financials 1eac824] feat(p67): 资金流翻页越界即停 ＋ ⑦ 拆子项 lag 不计异常但排补步 ＋ job_runs 完成时刻
 8 files changed, 600 insertions(+), 23 deletions(-)
 create mode 100644 tests/test_job_runs_timing.py
commit 1eac82450444f7bbebf8e5d6cac3f48ddd7c3ee3
Author: 郑长春 <1056921980@qq.com>
Date:   Thu Sep 24 16:24:26 2026 +0800

    feat(p67): 资金流翻页越界即停 ＋ ⑦ 拆子项 lag 不计异常但排补步 ＋ job_runs 完成时刻

    T1 `fetch_money_flow_daily`：页按 opendate 降序 ⇒ 某页最老一行 <= start 时立即停止翻页
    （旧口径先翻到底再按 [start,end] 过滤，增量窗口对请求数完全失效）。实测真库同窗口：
    旧 641 请求 / 226.37s → 新 21 请求 / 7.38s，返回行逐条相同（对拍基准＝旧口径独立复写）。

    T2 ⑦ 拆子项：valuation/adj_factors 照旧，money_flow 三档 ok/lag/stale。`lag` 要求
    「恰好落后一个交易日」且「该行首次写入时刻 > 它自己那天的收盘」这条从数据里读出的证据，
    不进 ANOMALY_STATUSES（不影响 verdict/退出码），但 plan() 仍按子项排补步。
    差 ≥2 个交易日、或恰好差 1 天而源站当天就发布过 ⇒ 仍判 stale（采集中断不放行）。

    T3 bars/actions/valuation/moneyflow 四处 `finished_at` 改为收尾时另取时刻，
    不再与 started_at 逐字相同（真库实测 ingest_moneyflow 269.647s 而两字段相同）。

    真库只读：唯一动作是 §4.4 点名的不带 --fix 的 ops patrol（新增回执 run_id=138）。
    全量 3002 passed / 2 skipped（基线 2982/2，+20 全为新增用例）；verify.sh exit 0；
    红线三目标 == 基线未 regen。

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>

 stocklab/cli/main.py          |   8 +-
 stocklab/data/fetch.py        |  12 ++-
 stocklab/data/ingest.py       |   9 ++-
 stocklab/ops/patrol.py        | 127 ++++++++++++++++++++++++++++---
 tests/test_data_fetch_p28.py  | 164 ++++++++++++++++++++++++++++++++++++++++
 tests/test_data_ingest.py     |  10 +-
 tests/test_job_runs_timing.py | 170 ++++++++++++++++++++++++++++++++++++++++++
 tests/test_ops_patrol.py      | 123 +++++++++++++++++++++++++++++-
 8 files changed, 600 insertions(+), 23 deletions(-)
```

**提交 B（文档）**：`docs/tasks/2026-09-24-p67-资金流源站T-1对齐.md`（本文件）
＋ `docs/errors/ERROR_DIARY.md`（#76），同样只用显式路径 —— 提交信息
`docs(p67): 实施记录 §6-§8 ＋ ERROR_DIARY #76`。

```
$ git add docs/tasks/2026-09-24-p67-资金流源站T-1对齐.md docs/errors/ERROR_DIARY.md
$ git status --short
M  docs/errors/ERROR_DIARY.md
M  "docs/tasks/2026-09-24-p67-资金流源站T-1对齐.md"
$ git commit -m "docs(p67): 实施记录 §6-§8 ＋ ERROR_DIARY #76"
[feat/financials 3a8983a] docs(p67): 实施记录 §6-§8 ＋ ERROR_DIARY #76
 2 files changed, 546 insertions(+), 5 deletions(-)
```

**本站在 `feat/financials` 上的提交（新→旧）**：

| | 提交 | 内容 |
|---|---|---|
| D | `docs(p67): 更正 §6.7 基线导出…` 之后的计数更正 | 把 §6.1/§7/ERROR_DIARY 里 T1 新增用例数 `7` 更正为 **8**（实测 `--collect-only` 计数），并补 §6.8 提交表 |
| C | `5f8fcc8` `docs(p67): 更正 §6.7 基线导出得到的提交是 c52b75f（不是 6a1e5c5）` | 基线导出实为 `c52b75f`；反查证据 ⇒ 用例基数不变 |
| B | `3a8983a` `docs(p67): 实施记录 §6-§8 ＋ ERROR_DIARY #76` | 本文件 ＋ ERROR_DIARY |
| A | `1eac824` `feat(p67): 资金流翻页越界即停 ＋ ⑦ 拆子项 …` | 4 个源文件 ＋ 4 个测试文件（8 files, +600/−23） |

> C/D 是**自我复核**抓出来的：C 是哈希对不上（写文档时凭开工时的 `git log` 记成了
> `6a1e5c5`，而导出实际抓到了 nanobot 中途落的 `c52b75f`）；D 是用例数写错（T1 新增
> 8 个写成了 7 —— 「八」= 3 参数化 + 4 边界 + 1 `--days 30`）。两处都不是代码问题，
> 但**记录里的数字必须是真跑出来的**，所以照实更正、不静默。

**未提交、也不该提交的东西**：`reports/ops/latest-patrol.json`（§6.4 的回执，
`reports/` 已被 `.gitignore` 覆盖，`verify.sh` 第 4b 项实测「✅ reports/ 已忽略」）。

### 6.9 与任务书不一致之处（如实列出，不静默）

1. **§1③ 的「882 请求 / 269s」是估算，偏大**：那是 `4019 行 ÷ 100 = 42 页 × 21 只`，
   而 `4019` 是**股票**行数、4 只 ETF 各只要 5 页。实测旧口径 **641 请求 / 226.37s**
   （§6.2 ③）。结论方向与量级不变（都是「两个数量级的浪费」），但记录里用实测值。
2. **改了一条既有用例的断言**（非新增、非删除）：`tests/test_data_ingest.py::test_ingest_records_job_run`
   原本断言 `finished_at == NOW` —— 它钉住的**正是 T3 要修的那个假读数**。改为
   `started_at == NOW` ＋ `finished_at != NOW && > NOW`（更严，不是放宽）。
3. **`ops patrol` 自己的回执不在 T3 范围**：`journal.seal` 用同一个 `stamp` 写
   `started_at`/`finished_at`（本次 `run_id=138` 两字段相同）。§2 T3 只点名
   bars/actions/valuation/moneyflow **三处**，故未动，如实记在 §6.4。
4. **§1③ 的「每只 4019 行」与源站当前可回溯范围不一致**（§6.2 ③ 探页证据）：
   不影响本站判据（对拍与请求数都在同一窗口内比），如实记下。

## 7. 进度

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | `fetch_money_flow_daily` 翻页到 `start` 边界即停（输出逐条不变） | ✅ 已验证 | 新增用例 8 个（`test_data_fetch_p28.py` 15 passed）＋真库只读抽 2 只标的实测「逐条相同=True」；请求数 641→21、墙钟 226.37s→7.38s（§6.1/§6.2） |
| T2 | ⑦ 拆子项，`lag` 不计异常但仍排补步（证据驱动） | ✅ 已验证 | 夹具库 7 个新用例（`test_ops_patrol.py` 54 passed）：`lag`⇒exit 0 且仍排 `ingest_moneyflow`、差 2 天⇒1、当天发布证据⇒stale；真库 `ops patrol --now 2026-09-24T15:35:00` ⇒ `⑦lag` / `verdict.exit_code=0` / `plan.steps` 含 `ingest_moneyflow` / `EXIT=0`（§6.3/§6.4） |
| T3 | `job_runs.finished_at` 用完成时刻 | ✅ 已验证 | `test_job_runs_timing.py` 5 passed（bars/actions/valuation/moneyflow 四处，慢采集 `finished_at > started_at` 且差 ≥1s）；RED 失败原文＝真库同款「两字段逐字相同」（§6.5） |
| T4 | 全量回归 + ERROR_DIARY 一条 + 显式路径提交 | ✅ 已验证 | 全量 `-o addopts=""`：基线 2984 收集/2982 passed/2 skipped（`git archive HEAD` 导出后跑）→ 本站 3004 收集/**3002 passed/2 skipped**，差 +20 ＝ 新增用例数；`bash scripts/verify.sh` **exit 0**；红线三目标 `0acf35c9…`/175、`73ffbfb1…`/1330、`ddac0a88…`/171 **全部 == 基线未 regen**；ERROR_DIARY **#76** 已加；提交 `1eac824`（代码＋用例）＋ docs 提交，均只用显式路径（§6.7/§6.8） |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败


---

## 8. 实施记录（nanobot 独立复核，2026-09-24 16:4x）

复核方式＝**自跑，不采信站自报**：脚本 `/tmp/p67_audit.sh`、日志 `/tmp/p67-audit.log`、基线与
`git archive` 对照均在本机独立跑出。结论：

1. **真库**：sha `a7c68feb…` → `331a7efe…`，差异**恰好**＝`job_runs` **137 → 138**（`run_id=138`：
   §4.4 点名的那次 `ops patrol --now 2026-09-24T15:35:00` 的回执，`⑦lag fixed=- anomalies=0`）。
   逐表比对：`bars_daily 97323` / `money_flow_daily 62484` / `valuation_daily 34961` /
   `paper_trades 15` / `paper_nav_daily 40` / `m2_channel_runs 3` —— **全部与 15:35 副本逐表相同**。
   ⚠️ **口径更正（本站学到的，后续任务书按新说法写）**：`ops patrol` **不是零写入** ——
   它每次都写一行 `job_runs` 回执（launchd 的 60 个槽、巡检本身都是这样）。「只读」指的是
   **业务数据**只读；「零写入」的说法此前不够精确。
2. **改动面**：10 文件，全在允许清单内；`paper/**`、`m2/**`、`plugin/**` **零改动**；
   无 DB / `data/backups/` / `redlines.json` 改动；提交四笔（`1eac824` ＋ docs ×3）只用显式路径。
3. **回归**：`.venv/bin/python -m pytest -o addopts=""` ⇒ **3002 passed / 2 skipped**（基线 2982/2，
   +20 ＝ 新增用例数）；`bash scripts/verify.sh` **exit 0**；红线三目标 `0acf35c9…`/175、
   `73ffbfb1…`/1330、`ddac0a88…`/171 **== 基线（未 regen）**。
4. **口径落地自检**（源码逐条对上）：`LAG` **不在** `ANOMALY_STATUSES`（`patrol.py:70`）；
   `plan()` 排补步含 LAG 分支（`patrol.py:600-601`）；`min(page dates) <= start` 即停
   （`fetch.py:345-349`，且在「短页到头」判定**之后**）。
5. **生产生效＝无需 rollout**：launchd 的 patrol/close 直接跑仓库代码 ⇒ 下一个 09:00 巡检与
   15:30 收盘链自动用新版。留档：`reports/ops/latest-close.json` 仍是 15:30 那轮的旧回执（exit 1），
   次日那轮才会覆盖；`MAX_PAGES` 余量 42/50 页（触发线：任一标的 > 4800 行）已在 §6 点名。
6. **站收尾读数**（`/tmp/cc-p67.log`）：`is_error=false` / 137 轮 / `$6.6553` / `duration 1594s`。
