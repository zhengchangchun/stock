# P52 AI 操盘手臂重构 ＋ 对照臂接线（D-34 / D-36）

创建：2026-09-22（nanobot 需求澄清）｜ 实现：Claude Code（superpowers 全流程）
澄清：[`docs/plans/2026-09-21-需求澄清录.md`](../plans/2026-09-21-需求澄清录.md) §7（**D-34 覆盖 D-18**、D-35、D-36）
前站：P37 阶段 1（`4c524db`，验收记录 [`2026-09-21-动态编排臂-验收记录.md`](../plans/2026-09-21-动态编排臂-验收记录.md)）
设计（**已部分作废**）：[`superpowers/specs/2026-09-21-智能体动态编排臂-design.md`](../superpowers/specs/2026-09-21-智能体动态编排臂-design.md)

> 本站**取代 P37 阶段 2/3 的原设计**：那套设计的核心是「AI 只能在 5 个纪律数字里改」，
> 用户 2026-09-22 已把它改成「**AI 当操盘手**」。**不改写历史**：阶段 1 的代码与台账保留，
> 白名单不再约束决策空间（D-34）。

---

## 0. 一句话

把 `arm-agent` 从「改纪律数字的臂」重构成「**每交易日独立做一次决策的操盘手**」：
**方向 ＋ 仓位 ＋ 池内自由选标的**，每次决策全字段落 append-only 台账，
与 **5 条对照臂**（D-36）同口径、同成本、同页面并列。

## 1. 决策口径（三条，写死）

1. **决策空间**（D-34）：`每交易日一条决策` ＝
   `{asof, decisions:[{code, side, target_weight_pct, reason}], cash_pct, rationale}`。
   - `code` **必须落在当日候选池**（短/中/长三池并集，`candidate` 快照；池外 ⇒ 拒绝）；
   - `target_weight_pct ∈ [0, 100]`，全部 `target_weight_pct` 之和 ＋ `cash_pct` = **100（不许杠杆、不许负权重）**；
   - **A 股无做空**：「看空」只能表达为**降低总仓位 / 清仓**。这一条要写进文档与报错文案 ——
     否则「AI 想卖空」会被读成静默失败。
2. **频率**：每交易日一次，按「最新已收盘交易日」判定（复用 `session.tick` 的口径，**不看墙上时钟**）；
   非交易日不出决策（留痕，不补造）。
3. **护栏（不许的）**：杠杆/负权重、池外标的、接券商下单、用 `> asof` 的数据、参数搜索、
   改主干常量（熔断阈值 / 自评估边界 / `approve` 闸门）。这些**进护栏**，不再进「白名单」。

**决策由谁来产生**：**在项目外**。项目内只做「校验 → 落库 → 执行 → 计账」四件事，
由 `paper agent decide --asof <day> --file <decision.json> [--model-id … --prompt-sha256 … --seed …]` 写入。
理由：① 与 D-1「系统内不实现自动生成」同精神；② 项目保持**离线可测、零 API key**；
③ 台账里 `model_id / prompt_sha256 / context_sha256 / seed` 足以复现与追责。
（若你日后要改成项目内直连模型，代价是：离线测试失效、密钥进环境、`paper/` 引入网络依赖 —— 三条都要另立 ADR。）

## 2. 对照臂（D-36，5 条并列）

| # | 臂 | 口径 | 现状 |
|---|---|---|---|
| ① | `arm-agent-<策略版本>` | AI 操盘手（本站重构） | 已有 `arm-agent` 空账户（阶段 1 建的） |
| ② | `arm-now` | 用户实盘账本镜像（ADR-006） | 已有，LIVE 1 笔种子 |
| ③ | 真实主动权益基金**等权平均** | 净值源＝天天基金 `pingzhongdata/<code>.js` 的 `Data_netWorthTrend`；**持仓不公开 ⇒ 只能比净值曲线** | 待接线；须在页面标注「近似/非官方」 |
| ④ | `arm-hold` | 什么都不做（冻结快照） | 已有 |
| ⑤ | `sh000300` | 大盘 | 已有（`index_300`） |
| ＋ | `arm-agent-random`（**归因用，不属 D-36 五条**） | 每交易日**随机**从池内选标的与仓位，其余护栏与成本口径**完全相同**（D-19 精神延续：没有它，AI 的读数一律「不可归因」） | 待接线 |

**硬规则**：`n_trials`／`n_rejected` 必须与净值同时读；样本 < 120 交易日或`arm-agent-random` 无数据时，
页面**只给读数、不给结论**（措辞与 P41 的 `insufficient` 同源）。

## 3. 必须新增的测试（判据，先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | **越界不写入**：权重和 ≠ 100 / 负权重 / 池外标的 / 缺 `reason` → 拒绝，**该日不下单、账户与净值一个字节不变** | 逐条拒绝用例 + 账户前后全量比对 |
| T2 | **幂等 + append-only**：同 `(arm, asof)` 重放 → 成交与净值逐行不变；台账 UPDATE/DELETE/`INSERT OR REPLACE` 被触发器拒（`recursive_triggers=ON`） | 库副本连跑两次 + 三条 SQL |
| T3 | **PIT**：把 `asof` 之后的 `bars_daily`/`paper_nav_daily` 行塞进库 → 决策上下文指纹**不变** | 复用既有 `agent_context` 守卫 |
| T4 | **成交语义复用**：AI 臂的一笔成交与既有引擎同输入逐字段相同（`fill_price`/`fee_total`/`slippage_cost`/`price_source`/`price_asof`）；全仓只有一处费用常量真源 | 字段级比对 + 源码扫描 |
| T5 | **纪律护栏按臂分作用域**：`arm-agent*` 允许方向与仓位，但**台账缺行即判红**；`arm-now` / `arm-discipline-*` 仍禁方向择时 | `tests/test_paper_discipline_guard.py` 更新 + 反向自检 |
| T6 | **对照臂同轴**：`/lab/paper` 上 5 条臂 ＋ random 臂同轴出现；基金等权臂标注「近似/非官方 + 数据源」；不可比的行写「不可比」而不是 0 | 页面渲染测试 + DOM 文本断言 |
| T7 | **缺随机臂不得出结论**：`delta_vs_random` 无数据时是 `null` 并写明「不存在，不是 0」 | 已有口径，扩展到新形态 |
| T8 | `pytest -q` exit 0（用例数只增不减）＋ `bash scripts/verify.sh` exit 0（红线三目标不变；红了先归因，**禁止 regen**） | 收尾命令 |

## 4. 反目标

- ❌ 不删/不改阶段 1 的台账与历史行（`paper_agent_decisions` 只增；新列用 `ALTER TABLE ADD COLUMN`）。
- ❌ 不引入杠杆/做空/融券；不做参数搜索；不做「AI 自动生成脚本」（D-1/D-24 不变）。
- ❌ 不改主干常量、不放宽 `approve` 闸门、不动 `predict/verify` 与 `MODEL_VERSION`。
- ❌ 不碰模块2 的 P47/P48/P49/P50 任务书涉及的文件（`m2_*` 插桩、绩效指标集）。
- ❌ 不把「基金等权臂」写成指数（它是**持仓未知的基金组合**，不是指数）。
- ❌ 不 `git commit`、不建分支/worktree。

## 5. 环境与收尾

- `.venv/bin/python`；测试全离线；真库 `data/stocklab.db` **只读**（AI 臂在真库上是空账户，`init` 幂等不回补）。
- 收尾：`pytest -q` exit 0 + `bash scripts/verify.sh` exit 0；进度表打勾；
  报告写**改了哪些文件 + 实测命令与原始输出**（尤其 T1/T2/T4 的比对）+ 剩余不确定性。
- 交付物另含：一条 ADR（AI 臂的决策空间与护栏口径 —— 覆盖 D-18 的落地形态）。

## 6. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | 决策载荷与校验（越界即拒、不夹紧、不静默） | ✅ 已验证 |
| T2 | `paper agent decide` 写入口 + 台账扩展（append-only 不破） | ✅ 已验证 |
| T3 | 接线到 `paper step`（每交易日一次；幂等） | ✅ 已验证 |
| T4 | 5 条对照臂 + `arm-agent-random` 随机臂接线 | ✅ 已验证 |
| T5 | `/lab/paper` 同轴展示 + 口径标注 + `insufficient` 措辞 | ✅ 已验证 |
| T6 | §3 的 T1–T8 测试 + `pytest`/`verify.sh` | ✅ 已验证 |
| T7 | ADR：AI 臂决策空间与护栏 | ✅ 已验证 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

> **判据对照**：任务书 §3 的 T1–T8 → 实现的测试文件见文末「实施记录」。
> 本文的 T1–T7 是**任务**编号，§3 的 T1–T8 是**判据**编号，两套编号不同，勿混读。

---

## 实施记录（2026-09-22，Claude Code）

命令一律 `.venv/bin/python`；真库 `data/stocklab.db` 全程只读（未 `init`、未 `step`）。

### 一、改了哪些文件

**新增**：`stocklab/paper/agent_pool.py`（候选池快照，裸 SQL）、
`stocklab/paper/agent_decide.py`（载荷校验 / 台账 / 目标权重执行 / 随机臂 / 台账审计）、
`stocklab/paper/comparison.py`（5 条 ＋ 随机臂同轴表）、
`stocklab/fund/{__init__,nav}.py`（`pingzhongdata` 解析 + `fund_nav_daily` + 等权曲线）、
`docs/decisions/2026-09-22-ADR-024-AI操盘手决策空间与护栏.md`。

**修改**：`paper/config.py`（P52 常量：载荷键名、`NO_SHORT_SIDE_MSG`、`NOT_COMPARABLE`、
`SAMPLE_THRESHOLD`、`RULE_CITATIONS_AGENT_DECISION`…）、`paper/rules.py`
（`plan_target_weight` ＋ `AGENT_DECISION_PARAMS`）、`paper/engine.py`（按臂分派、
`arm_state_for`、`agent_decision` 块、`comparison` 块、报告 §3）、`paper/agent_context.py`
（`build_decision_context` ＋ `GUARDRAILS`）、`store/schema.sql`、`store/migrate.py`
（P52 前滚）、`cli/main.py`（`paper agent decide|random|show`、`fund ingest|show`）、
`labweb/paper_data.py` ＋ `paper_render.py`、`config/universe.py`（白名单 ＋
`fund.eastmoney.com`）、`docs/errors/ERROR_DIARY.md`（#61–#63）、
`docs/decisions/README.md`（索引 ADR-024）。

**测试**：改 6 个（`test_paper_engine` / `test_cli_paper_spec` / `test_labweb_paper_agent`
/ `test_paper_discipline_guard` / `test_store_schema` / `test_config_universe`），
新增 4 个（见下表）。**没有删除任何用例**。

| 文件 | 条数 |
|---|---|
| `tests/test_paper_agent_decide.py` | 27 |
| `tests/test_paper_comparison.py` | 25 |
| `tests/test_cli_paper_agent.py` | 9 |
| `tests/test_store_migrate_p52.py` | 7 |
| `tests/test_paper_discipline_guard.py`（含新增作用域判据） | 48 |

### 二、§3 的 T1–T8 逐条实测

| 判据 | 落在哪 | 实测命令与关键输出 |
|---|---|---|
| **T1 越界不写入** | `test_paper_agent_decide.py::test_t1_*`（8 个参数化用例 ＋ 卖空 ＋ side 矛盾 ＋ 负现金 ＋ 全现金反向自检） | `pytest tests/test_paper_agent_decide.py -q` → `27 passed`。每个用例都在**拒绝前后全量比对四张表**（`paper_accounts` / `paper_nav_daily` / `paper_trades` / `paper_agent_decisions` 的每一行），断言 `_dump(db) == before`；卖空那例断言报错含「无做空」与「降低总仓位」 |
| **T2 幂等 ＋ append-only** | 同名 `test_t2_*` | 同 `(arm, asof)` 同载荷（**键序打乱**）→ exit 0 且 `status=已存在且一致（未写入）`、台账逐行不变；不同载荷 → **exit 1** 且原行逐字节不变；三条 SQL（`UPDATE` / `DELETE` / `INSERT OR REPLACE`）全部抛错且原行逐字段不变；断言 `PRAGMA recursive_triggers == 1` |
| **T3 PIT** | `test_t3_*` | 往 `bars_daily`(更远的日期) / `paper_nav_daily`(asof 之后) / `candidate_snapshots`(asof 之后) 塞行 → `decision_context_sha256` **前后相等**；正面判据断言 `marks[*].price_asof <= asof`、池快照 `<= asof`；注入未来价 → `LookaheadError` |
| **T4 成交语义复用** | `test_t4_*` | ① ETF：`plan_etf_buy` 与 `plan_target_weight` 同输入 → `qty` / `fill_price` / `fees` / `asset_class` **逐字段相等**；② 落库版：从 `paper_trades` 取行，用 `CostModel.fill_price` ＋ `_fee_parts` 重算，`commission/stamp_tax/transfer_fee/slippage_cost/fee_total` 全等（**重算必须用未取整的成交价**，否则差 0.02 元 —— 已写进用例注释）；③ 源码扫描：`paper/*.py` 里除 `rules.py` 外**零处**出现费率标识符 |
| **T5 护栏按臂分作用域** | `test_paper_discipline_guard.py`（48 条） | ① 原有的「不 import 模型/验证/凯利/选股链路」原样保留；② 新增：操盘口径词只许出现在 `agent_decide/agent_pool/rules/engine`，`config.py` **只许声明** `target_weight_pct`（出现下单构造即红）；③ 静态臂执行函数 `_plan_steps`/`_build_etf`/`evaluate` **一个操盘词都不许出现**；④ **台账缺行即判红**：`agent_decide.audit_decisions` 对「有成交、无决策行」判红，并区分「零违规」与「零比对」；⑤ 反向自检 3 个（含「字典键写法必须被抓到」） |
| **T6 对照臂同轴** | `test_paper_comparison.py::test_six_arms_*` / `test_page_*` | `comparison.build` 返回六条臂且点数 == 横轴长度；基金臂 `is_index=False` / `tradable=False` / `has_cost=False` / `approximate=True`，`note` 含「近似」「非官方」；DOM：同轴一节里六个 arm id 全出现，含「不可比」且**同一节里不出现 `0.00%`**，含「持仓未知的基金组合」「不是指数」 |
| **T7 缺随机臂不得出结论** | `test_paper_comparison.py::test_missing_random_arm_is_null_and_says_it_is_not_zero` ＋ `test_page_shows_null_delta_as_missing_not_zero` | `delta_vs_random is None`、`available is False`、说明含「不是 0」；DOM 里出现「不存在」且**不出现 `+0.00%`** |
| **T8 收尾命令** | —— | 见下 |

### 三、收尾命令（原始输出）

> ⚠️ 上一轮在**预算上限处被切断**，本节数字是收尾（修好
> `test_missing_fund_table_is_not_comparable_not_a_500`）之后**重跑**的实测值。
> 早先那版 `2570 passed / 2572 collected` 是 22:36 写的，而
> `tests/test_paper_comparison.py` 的最后一个用例 22:41 才写完 —— 记录早于现状，
> 所以那一版既漏了那条红用例，也少算了一个用例。现按重跑结果改正。

```
$ .venv/bin/python -m pytest -q
# 注：命令行 -q 与 pyproject `addopts` 里的 -q 叠加成 -qq，汇总行被整个吃掉，
#     所以这条命令的 stdout 只有进度点、没有「2571 passed」；退出码才是判据。
PYTEST_EXIT=0

$ .venv/bin/python -m pytest -q -o addopts=""      # 去掉叠加的 -q，让汇总行出来
2571 passed, 2 skipped in 138.67s (0:02:18)

$ .venv/bin/python -m pytest --collect-only -q -o addopts=""
2573 tests collected in 0.54s          # 基线 2459 → 净增 114

$ bash scripts/verify.sh
  ✅ predict_synthetic            sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致
  ✅ predict_real_2026-09-14      sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致
  ✅ backfill_real_2013-12-23_2026-09-14  sha256 ddac0a889c623d75… == 基线；叶子 171 项一致
✅ 回归红线全部成立
✅ 验证通过: all
VERIFY_EXIT=0
```

（三条红线 sha 与基线逐位相同，**未 regen**。`verify.sh` 内部跑的 pytest 同样回显
`2571 passed, 2 skipped in 140.50s`，与上面单独跑的那次一致。）

### 四、两处判断（按任务书与 D-34/D-36 复核后的落地）

1. **`arm-agent` 不再跑纪律条文**。依据是任务书 §0「把 `arm-agent` 从『改纪律数字的臂』
   **重构成**『每交易日独立做一次决策的操盘手』」与 D-34「5 字段白名单**不再约束** AI 臂的
   决策」—— 这是**替换**而不是并存，所以没有做成「两套下单来源同时生效」。
   `paper spec set` 与其台账**原样保留**（阶段 1 历史不删），但不再决定这条臂下不下单；
   这一条由 `test_a_spec_no_longer_drives_the_agent_arm` 正向钉住（旧判据改写、意图保留）。
   ⚠️ **请复核**：若你要的是并存，这一处要回退（改动集中在 `engine.has_rules` /
   `_step_all` 与 `params_for_account`）。
2. **基金净值未接实盘抓取**。`fund ingest --file` 走本地文本，`--fetch` 是唯一出网点
   （`stocklab/data/http.py` ＋ 白名单新增 `fund.eastmoney.com`）但本次**未联网**：
   真库没有 `fund_nav_daily` 行 ⇒ 页面上基金臂如实显示「**不可比**」（这正是判据要的行为，
   `test_fund_arm_without_data_is_not_comparable_not_zero` 钉住）。清单是先验选定的
   5 只主动权益基金（`fund/nav.py::EQUAL_WEIGHT_POOL`），**未按业绩挑选**、**未编数**。
   真源核对请 nanobot 跑一次 `stocklab fund ingest --code 110011 --fetch --db data/stocklab.db`。

### 五、剩余不确定性（点名，别当成已验证）

1. **毫秒时间戳约定**：源站 `Data_netWorthTrend[].x` 是「当日 UTC 零点」还是「当日北京零点」，
   **离线无法证实**。实现按 `+08:00` 读（对两种约定给出同一天），并在
   `test_timestamp_convention_is_read_as_beijing_time` 里把这条推理钉住 ——
   但这是**推理**，不是对源站数据的核对。
2. **`paper step` 只在本仓夹具上跑过**（`data/stocklab.db` 的最晚 bar 是 2026-09-22）。
   真库上 `arm-agent` 仍是空账户（`init` 幂等不回补，且没有决策行）—— 这是**设计**，
   不是缺口；但「真库上跑一天 AI 臂」这件事没有被执行过。
3. **页面的对照一节只在 DOM 文本层断言**（六条臂出现、「不可比」出现、无转义标签），
   没有逐像素/截图回归。
4. **样本量门槛永远不可能在近期满足**（起跑日 2026-09-15，需要 120 交易日）⇒
   页面长期只给读数、不给结论，这是判据要求的形态，不是缺陷。
