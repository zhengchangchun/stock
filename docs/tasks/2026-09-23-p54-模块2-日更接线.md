# P54 模块2 · 日更接线（通路 A/B 与打分进收盘链）

创建：2026-09-23（nanobot 需求澄清）｜ 实现：Claude Code（superpowers 全流程）
来源：`docs/tasks/2026-09-22-p48-模块2-三方对标与预测校验.md` **§8.6 口子 ③** ——
实测 `stocklab/ops/*.py` 里 **`m2` 零处出现**（`grep -rn m2 stocklab/ops/` 为空）⇒
`/lab/m2` 的读数**不会自己长**，通路与打分全靠手工敲。
上游：[`P47`](2026-09-22-p47-模块2-双通路与镜像.md)（`m2 channel a|b` 已交付）、
[`P48`](2026-09-22-p48-模块2-三方对标与预测校验.md)（`m2 score`/`/lab/m2` 已交付）、
[`P49`](2026-09-22-p49-模块2-自评估与熔断.md)（周期状态 `validating`/`frozen` 的真源）、
[`P38`](../decisions/2026-09-22-ADR-020-调度载体改用系统launchd.md)（链的顺序写在项目里）、
**P46**（[`tasks`](2026-09-22-p46-patrol收盘抢跑与预测定型闸门.md) 的 K 线定型闸门 —— 本站必须复用同一道）。
口径：ADR-024/025、ADR-020、`docs/ops/2026-09-22-预测定型闸门与当日口径.md`。

---

## 0. 一句话

让模块2 的读数**自己长**：把「通路 A（按**在飞**策略版本）→ 通路 B → 事后打分」接进 `ops close`
的固定顺序，闸门复用 P46 那道「当天 K 线定型了吗」；**没有在飞版本时如实 skipped**，
只有「配置了却坏了」才把链报红。

> 本站**只接线**：不动 `m2 channel a|b`、`m2 score` 的语义，也不动 P41/P48 的读数口径。

## 1. 口径（本站拍板；可单条回退）

1. **单一入口**：新增 CLI **`m2 daily --asof <交易日> [--db] [--now]`**，`ops close` 里**只加一步**
   （步骤名 `m2_daily`，`supports_db=True`，位置 = `paper_step` **之后**、`doctor` **之前**）。
   理由：`CLOSE_STEPS` 是**静态元组**，而「这一轮要跑哪些策略版本」只有运行期才知道；
   把遍历放进 CLI，链的顺序语义与页面的「一行一步」都不会被动态步数搞乱。
2. **跑什么**（顺序即依赖顺序）：
   - **通路 B 总是跑**：`m2 channel b --asof {asof}`（镜像 `arm-now`，与人当天有没有下单无关，幂等）；
   - **通路 A 遍历「在飞版本」**：账户存在（`arm-agent-*`）**且**该版本的验证周期不是 `frozen`、
     未被熔断终止 ⇒ 逐个 `m2 channel a --asof {asof} --strategy-version <v>`；
     **0 个在飞版本 ⇒ 记一行 `skipped` 并把原因写清**（`没有在飞的策略版本`），**这不是失败**。
     > P49 尚未落地时（状态真源还没有 `validating`/`frozen`），退化为「账户存在即跑」，
     > 并在「实施记录」里**点名这条退化**（不许假装已经按状态过滤）。
   - **打分总是跑**：`m2 score --asof {asof}`（只给 `target_date <= asof` 且还没打过分的那批）。
3. **K 线定型闸门（复用 P46，不许另写判据）**：`asof == 今天` 且
   `session/close.py::bars_finalized_on(conn, asof)` 判「未定型」⇒ **exit 2、一行都不写**
   （与 `predict run --asof 今天` 同款 fail-closed；理由见 P46 事故：`m2_forecasts` 也是 append-only）。
4. **退出码**（沿用既有语义，不发明新的）：
   | 码 | 含义 |
   |---|---|
   | 0 | 全跑完（含 `already` 幂等、含「通路 A 无在飞版本」的 `skipped`） |
   | 2 | 未定型 / 库不可用 / 输入不合法（`--asof` 不是交易日） |
   | 4 | **有通路或打分 `rejected`**（配置了却坏了 ⇒ 链上必须报红，不许静默） |
5. **不做重试、不做补写**：通路与打分各自的幂等键已经够（`ran_run` / `forecast_id` UNIQUE）；
   本站**不许**给它们加「重试」「补跑昨天」之类的兜底（兜底会把「漏跑」变成看不见）。
6. **页面**：`/lab/ops` 的步骤表**自动**多一行（步数不许写死）；`/lab/m2` 顶部加一句
   「上次通路运行：`<日期>` / `ran|skipped|rejected`（没有就写「从未」）」，**只读、不重算**、
   不提供任何触发按钮。

## 2. 反目标

- ❌ **不在 `ops patrol` 里加 m2 补步**（与 `paper step` 同族：收盘后才有意义的动作；
  链条回执 + `job_runs` 已经覆盖「今天跑没跑」这件事）；
- ❌ 不改 `m2 channel a|b` / `m2 score` 的语义与退出码；不改 P41/P48 的指标与门禁；不改主干常量；
- ❌ 不改 `ops schedule` 的时点与槽数（生成的 plist 应**逐字节不变**）；
- ❌ 测试里**绝不真起子进程**（一律注入 FakeRunner，ERROR_DIARY **#55**：CLI 用例真跑 `ingest *`
  会把半截 bar 写进真库）；
- ❌ 不 `git commit`、不建分支/worktree、不动 P50 范围的文件。

## 3. 测试（先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | **链上只多一步且顺序正确** | FakeRunner 记录 argv：`m2_daily` 紧跟 `paper_step`、在 `doctor` 之前；plist 生成物与接线前**逐字节相同** |
| T2 | **没有在飞版本 ⇒ skipped 且链仍绿** | 构造「无 `arm-agent-*` 账户」的库副本 ⇒ 步骤 `m2_daily` exit 0、回执里能看到 `skipped` 与原因、`ops close` **exit 0** |
| T3 | **配置了却坏了 ⇒ 报红且不静默** | 构造通路 `rejected`（如账户存在但无 active 插桩 / NAV 冲突）⇒ `m2_daily` exit 4、close 回执 `anomalies` 里点名该步 |
| T4 | **未定型闸门 fail-closed** | 当日 `bars_daily.fetched_at < 15:00` ⇒ `m2_daily` exit 2，且 `m2_channel_runs`/`m2_forecasts`/`m2_forecast_scores` **三表行数不变** |
| T5 | **幂等** | 同一天跑两次 ⇒ 第二次各通路 `already`、三表行数不变、NV/预测条数不变 |
| T6 | **真库只读 + 副本端到端** | 真库 `shasum` 复核前后一致；在 `/tmp` 副本上真跑一次 `m2 daily`（跑完删副本），记录关键输出行 |
| T7 | 全量 | `.venv/bin/python -m pytest -q` exit 0（用例数只增不减）＋ `bash scripts/verify.sh` exit 0（红线三目标 sha 不变） |

## 4. 环境与收尾

- 解释器 `.venv/bin/python`（3.12）；测试全离线 fixture；真库 `data/stocklab.db` **只读**，
  写实测在 /tmp 副本上做（用完删）。
- 判据＝`pytest -q` exit 0 ＋ `bash scripts/verify.sh` exit 0；红线三目标
  `0acf35c90f54ab6a…`/175、`73ffbfb136e8620d…`/1330、`ddac0a889c623d75…`/171 不变。
- 交付物：代码 + 测试 +（若需）`docs/ops/` 一段说明；§5 进度表逐条打勾 + 文末「实施记录」
  （每条判据的实测命令与关键输出行，含真库只读读数与副本 E2E 输出）。
- ⚠️ 上一站（P48）在**最后一跳** `403` 失联，工作区改动全在、任务书一个字都没写 ⇒
  **先写 §5 打勾与实施记录，最后再跑一遍全量 pytest/verify.sh 把最终数字补进去**。

## 5. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | `m2 daily` 入口（闸门 + 在飞版本遍历 + 打分，退出码语义） | ⬜ 未开始 |
| T2 | `ops close` 接线（`m2_daily` 一步，位置与预算） | ⬜ 未开始 |
| T3 | `/lab/m2` 「上次通路运行」一句（只读、不重算） | ⬜ 未开始 |
| T4 | §3 的 T1–T7 测试 + `pytest`/`verify.sh` | ⬜ 未开始 |
| T5 | 文档同步（`docs/ops/`、README 索引） | ⬜ 未开始 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败
