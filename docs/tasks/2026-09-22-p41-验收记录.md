# P41 验收记录 · 模块2 绩效对比指标集

- 日期：2026-09-22（第二跑；第一跑 15:09 因 P37 未提交占用 `labweb/paper_*.py` 与
  `paper/engine.py` 而主动停止，未改任何文件、花费 $0.17）
- 任务书：`docs/tasks/2026-09-22-p41-模块2-绩效对比指标集.md`
- 决策记录：`docs/decisions/2026-09-22-ADR-023-模块2绩效指标口径.md`
- 基线（开工前实测）：`pytest -q` **2404 passed, 2 skipped**、`scripts/verify.sh` exit 0

## 1. 改了哪些文件

| 文件 | 改动 |
|---|---|
| `stocklab/backtest/metrics.py` | 新增 `profit_loss_ratio(returns)`（唯一真源；`win_rate` 的兄弟函数，同域） |
| `stocklab/labweb/paper_data.py` | 新增 `performance()` / `METRIC_KEYS` / `METRIC_LABELS` / `PERFORMANCE_*`（**CLI 与页面同源数据函数**）；`track()` 与 `_empty()` 增列 `performance` |
| `stocklab/labweb/paper_render.py` | 新增 `performance_block()`（页面节）、`performance_text()`（CLI 文本表）；`paper_page()` 与 `_empty_body()` 各加一节（键名**只增不减**） |
| `stocklab/cli/main.py` | 新增 `cmd_paper_metrics` + `paper metrics` 子命令（**只加**，未动 P44 的 `--plugin-id` 帮助那一处） |
| `tests/test_backtest_metrics.py` | +3 条（`profit_loss_ratio`） |
| `tests/test_paper_metrics.py` | 新建，24 条（5 组） |
| `docs/decisions/2026-09-22-ADR-023-模块2绩效指标口径.md` | 新建；`docs/decisions/README.md` 追加索引行 |

**未碰**：`stocklab/plugin/*`、`stocklab/config/*`、`stocklab/store/*`、`stocklab/ops/*`、
`stocklab/paper/agent_*.py`、`labweb/ops_*.py`、`paper/engine.py`、ADR-021/022、
以及 `cli/main.py` 里那处 `--plugin-id` 帮助（P44/P45 在制品一件未动）。
**未** `git commit` / 建分支 / 建 worktree / 动真库 / regen 红线。

## 2. 实测命令与原始输出

### 2.1 真库读数（**只读副本**，真库 `data/stocklab.db` 一个字节未改）

```bash
cp data/stocklab.db /tmp/p41-real-copy.db
.venv/bin/python -m stocklab.cli.main paper metrics --db /tmp/p41-real-copy.db --asof 2026-09-22
```

```
# 绩效对比（模块2 §4）· 2026-09-22

窗口 2026-09-15 ~ 2026-09-22，5 个交易日（门槛 120）｜样本不足，仅供观察；不得据此选策略或改口径

| 线 | 总收益 | 年化 | 最大回撤 | 盈亏比 | 胜率 | 相对大盘 | 相对「不动」 |
|---|---|---|---|---|---|---|---|
| `arm-discipline-05` AI 纪律臂 · ETF 目标 5% | -3.23% | -80.88% | 3.24% | 0.0045 | 40.00% | -5.35% | -0.91% |
| `arm-discipline-10` AI 纪律臂 · ETF 目标 10% | -3.16% | -80.23% | 3.18% | 0.0218 | 20.00% | -5.29% | -0.85% |
| `arm-discipline-15` AI 纪律臂 · ETF 目标 15% | -3.17% | -80.26% | 3.19% | 0.0263 | 20.00% | -5.29% | -0.85% |
| `arm-hold` 什么都不做 · 起跑日冻结快照（不动臂，不是 AI 表现） | -2.32% | -69.29% | 3.16% | 0.4167 | 40.00% | -4.44% | 未知 |
| `arm-now` 我 · 实盘账本镜像 | -2.32% | -69.29% | 3.16% | 0.4167 | 40.00% | -4.44% | 0.00% |
| `sh000300` 沪深300 指数 | 2.12% | 188.53% | 0.45% | 1.4274 | 80.00% | 未知 | 4.44% |

> 窗口 2026-09-15 ~ 2026-09-22，共 5 个交易日（净值行数 = 日收益数；横轴另外含起跑日锚点一个点）。
> 期初口径：账户 = `paper_accounts.initial_nav`、基准 = 起跑日 `sh000300` 收盘；五个指标全部由这一条序列推出。
> 本载荷的 `max_drawdown` 为**负值**（`backtest/metrics` 口径）；渲染层按页面既有列的正值口径显示同一个数。
> 样本不足，仅供观察；不得据此选策略或改口径（5 < 120 个交易日）—— 只给读数：不出任何「谁更好」的结论，也不改口径。
```

⚠️ **「当前 6 个交易日」的口径澄清（实测更正）**：真库 `paper_nav_daily` 里
`DISTINCT date` = **5**（09-16/17/18/21/22），横轴因为多一个**起跑日锚点**（09-15）
而显示 **6 个点**。`n_sessions` 数的是**日收益个数 = 5**，与
`paper.engine.build_report.sample_note`（「共 5 个交易日」）和
`paper_data.track()["n_sessions"]`（5）**同值**。两个数都写进 ADR-023。

⚠️ **真库只有 5 个账户**：`arm-now` / `arm-hold` / 三档 `arm-discipline-{05,10,15}`。
**没有** `arm-agent` / `arm-agent-random` —— P37 的智能体臂没在真库 `init` 过。
本表按库里的账户如实列，**不补行**。

⚠️ **年化那一列是纯外推**：5 个交易日上，基准 +188.53%、纪律臂 −80%。它出现在表里
正是为了让读者看见「这么短的窗口谈年化有多荒谬」；门禁没过时它是读数、不是结论。

### 2.2 CLI 的 JSON 路径

```bash
.venv/bin/python -m stocklab.cli.main paper metrics --db /tmp/p41-real-copy.db --asof 2026-09-22 --json | head -30
```

```json
{
  "asof": "2026-09-22",
  "available": true,
  "metric_keys": ["total_return", "annualized_return", "max_drawdown", "profit_loss_ratio", "win_rate"],
  "n_folds": null,
  "n_sessions": 5,
  "sample_gate": {
    "gate_status": "insufficient",
    "label": "样本不足，仅供观察；不得据此选策略或改口径",
    "meets": false,
    "n_sessions": 5,
    "threshold": 120
  },
  "window": ["2026-09-15", "2026-09-22"]
}
```

（`--json` 的完整载荷另含 `excess_vs_index_300` / `excess_vs_hold` 两张按
`account_id` 的减法表，以及每行的 `missing`（缺值原因）与 `n_sessions`。）

文本表的落盘路径：

```bash
.venv/bin/python -m stocklab.cli.main paper metrics --db /tmp/p41-real-copy.db \
  --out /tmp/p41-metrics.md
wc -l /tmp/p41-metrics.md
# 17 /tmp/p41-metrics.md   （§2.1 那张表原样落盘，父目录自建；默认**不落盘**：只读命令）
```

### 2.3 测试与红线

```bash
.venv/bin/python -m pytest
# 2431 passed, 2 skipped in 135.19s  （基线 2404 + 2，差 = +27 条 = 本任务新增）
```

```bash
bash scripts/verify.sh > /tmp/p41-verify.log 2>&1; echo "EXIT=$?"
# EXIT=0
# ✅ 验证通过: all
# ✅ 回归红线全部成立
```

> 退出码单独跑一次取值（不接管道）—— ERROR_DIARY #58 第 2 条。
> 真库复核：`data/stocklab.db` 仍是 196571136 B / 9月22 15:30（**一个字节未改**），
> `data/backups/` 无新增文件。

## 3. 独立复查（`requesting-code-review`）与处置

派了一个独立 agent 读 diff（不看本次会话上下文），提了 5 类问题。**核实后处置**：

| 复查发现 | 核实结论 | 处置 |
|---|---|---|
| `axis = [start, *nav_dates]`：起跑日自己也有净值行时会**重复占位** ⇒ 基准凭空多一个 0% 的「交易日」，把 `n_sessions` / 胜率 / 回撤一起带偏 | **真错**（真库与既有夹具都没触发，是潜在错） | **已修**（去重）；新增 `test_a_nav_row_on_the_start_date_does_not_duplicate_the_axis`，并做**反向自检**：把修法退回去 → 该用例**实测变红**，恢复后绿 |
| `test_page_section_comes_from_the_same_function_as_the_cli` **名不副实**（它没测 CLI，只断言数字出现在 HTML 里 —— 页面完全可以自己再算一份而照样通过） | **真错**（正是 #50「文档/测试名点名的东西要么可执行要么别说」） | **已修**：改名为 `..._renders_exactly_the_data_function_numbers`，并把钉子换成真正判据 —— `track()["performance"] == performance()` |
| `test_drawdown_is_shown_with_the_pages_positive_convention` 里 `ratio_pct(-mdd) in html` 不判别（`10.00%` 是 `-10.00%` 的子串） | 部分成立 —— 同一用例的第二条断言（负号形态**不出现**）才是判别性的，两条合起来有效 | **不改**（已在用例里写明哪条是钉子） |
| `bt.daily_returns` 会在「前一日净值 ≤ 0」时丢区间 ⇒ 极端情形下 `total_return ≠ Π(1+r)−1` | 成立，但这是 `backtest/metrics.py` 的**既有**行为、非本次引入 | **不改**（改它等于改回测口径，超出本任务范围；已在此记账） |
| `start = accounts[0]["start_date"]` 依赖 `load_accounts` 的排序 | 与 `track()` / `build_report()` **同一个既有写法** | **不改**（保持同源，不在这里另立一套） |

## 4. 五组测试（`tests/test_paper_metrics.py`，24 条）

| 组 | 代表用例 | 钉住的东西 |
|---|---|---|
| 1 指标正确性 | `test_five_metrics_match_a_hand_computed_series`、`test_total_return_is_the_compounded_product_of_the_daily_returns`、`test_metrics_come_from_the_backtest_metrics_true_source` | 手算逐位一致；总收益 == Π(1+日收益)−1；五个数 == `backtest.metrics` 同名量 |
| 1' 边界 | `test_monotonic_benchmark_has_no_profit_loss_ratio_and_says_why` | 单调上涨的基准 → 盈亏比 `None` + 原因（不是 0、不是 inf） |
| 2 门禁 | `test_six_sessions_is_insufficient_and_says_so`、`test_130_sessions_clears_the_gate`、`test_gate_status_flips_with_the_same_series_length`、`test_insufficient_run_never_uses_conclusive_wording` | 6 → `insufficient`、130 → `ok`；单变量只有长度在变；逐词禁结论性措辞 |
| 3 基准缺失 | `test_missing_index_rows_yield_none_not_a_substituted_neighbour`、`test_gap_in_the_middle_also_invalidates_the_benchmark`、`test_excess_is_a_subtraction_and_absent_when_the_benchmark_is_absent`、`test_excess_vs_hold_is_absent_until_the_hold_arm_exists` | 缺数据 → `None` + 原因、**不用相邻日顶替**；超额是一次减法、缺基准时为 `None` 不是 0 |
| 4 页面契约 | `test_page_section_renders_exactly_the_data_function_numbers`、`test_page_renders_no_bare_none_or_nan`、`test_existing_paper_page_sections_survive`、`test_metrics_are_rendered_as_a_table_with_the_gate_in_the_header`、`test_drawdown_is_shown_with_the_pages_positive_convention` | 与 CLI 同源；无裸 `None`/`nan`；既有节只增不减；表头带门禁；回撤两处同一个数 |
| 5 CLI（ERROR_DIARY #55） | `test_cli_metrics_json_is_the_same_payload_as_the_page`、`test_cli_metrics_writes_the_report_and_exits_zero`、`test_cli_metrics_does_not_advance_the_paper_account`、`test_cli_metrics_on_a_missing_db_exits_two_with_a_reason` | **注入假 runner**（`subprocess.run/Popen/check_output` 一开就炸）+ `paths.DB_PATH` 指向 tmp 哨兵文件 ⇒ 「一次子进程都没起」且真库字节不变；只读性用**逐表行数前后相同**判 |

既有测试一件未改（`tests/test_labweb_paper.py` 的 `/lab/paper` 断言全数保留并通过）。

## 5. 剩余不确定性 / 需要拍板的地方

1. **期初基与既有「累计收益」列相差 0.2155%**（本任务最大的一处取舍）。
   任务书 §1 字面写「期初口径：`initial_nav`」，我照办；代价是新节「总收益」
   与同一页上方「累计收益」列差一个**对全部臂相同**的常数（起跑日持仓浮盈）。
   **我没有靠改 `cum_return` 的基去对齐**（那是改口径迎合新页面）。若你希望两处
   逐位相同，改法是让 `_metrics` 的期初基取 `net_deposits` —— 一行的事，
   但要连带改 ADR-023 与相应测试。**请拍板。**
2. **`n_sessions` 与横轴点数不同值**（5 vs 6）。我选了与 `build_report` / `track` 同值
   的 5。若按横轴点数计，年化与门禁的门槛含义都会变（130 天的界会挪一天）。
3. **真库没有 `arm-agent*` 两臂**。这是 P37 的部署事实，不是本任务的 bug；
   本表如实列出。要不要在真库上补跑 `paper init`（幂等、只加不改）**不在本任务范围**，
   未执行。
4. **`/lab/paper` 页面上另有一处裸 `None`**（智能体臂证据表的 `INCONCLUSIVE` 行，
   以及 `nanobot` 一词里的 `nan`）。**不是**本次新增，属既有内容，
   按任务书 §3 反目标未动。

## 6. 进度表

见 `docs/tasks/2026-09-22-p41-模块2-绩效对比指标集.md` §6（T1–T6 已全部打勾）。
