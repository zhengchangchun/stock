# P50 模块2 · 误差归因与配置视图

创建：2026-09-22（nanobot 需求澄清）｜ 实现：Claude Code（superpowers 全流程）
需求书：[`docs/plans/2026-09-22-模块2-需求书.md`](../plans/2026-09-22-模块2-需求书.md) §3（本行）
上游需求：`stock/04_P1_P2_P3_扩展组件需求.md` §P1-2（误差归因：大盘冲击／行业黑天鹅／个股突发利空／因子失效）、
§P2-1（事件旁路触发）、§P3-1（可视化报表）、§P3-2（统一配置中心）、§P3-4（人工交易理由字段）
前站：[`P41`](2026-09-22-p41-模块2-绩效对比指标集.md)、[`P44`](2026-09-22-p44-模块2-主干-状态机与常量.md)、
[`P47`](2026-09-22-p47-模块2-双通路与镜像.md)、[`P48`](2026-09-22-p48-模块2-三方对标与预测校验.md)、
[`P49`](2026-09-22-p49-模块2-自评估与熔断.md)
口径：D-31（归因不自动填）、D-32（配置视图只读）、ADR-021（Web 零写入口）、ADR-023（指标口径）

---

## 0. 一句话

把模块2 的四件「外围」补齐：**误差归因结构位（人工确认，禁自动填）**、
**只读配置视图（主干常量 + 当前生效策略参数 + 来源）**、**可视化报表（复用既有图，不另画一套）**、
**事件旁路触发（幂等、留痕、不等周期结束）**。

## 1. 误差归因结构位（D-31）

- **四分类**（原文）：大盘冲击 / 行业黑天鹅 / 个股突发利空 / 因子失效。
- **程序只给候选**：由**可计算信号**推出候选标签，必须写明用了哪个信号、阈值是多少、算到哪一步，例如
  - 大盘冲击 ⇐ 当日 `sh000300` 涨跌幅 ≤ 阈值（组合超额与指数同向）；
  - 个股突发利空 ⇐ 该 `code` 当日出现 `corp_actions` 新事件 / 停牌 / `data_quality` 异常 / 单日跌幅超阈值；
  - 行业黑天鹅 ⇐ **同行业同日**多只标的同向异动（`financial_reports.industry_name` 或既有 `sector_daily`，取既有真源，不许新造行业分类）；
  - 因子失效 ⇐ 该因子**同期横截面 IC / 命中率**显著掉档（复用既有因子读数，不许新造因子算法）。
- **人工确认**：`attribution_manual` 是**唯一**的"结论"字段；`attribution_auto` 只放**候选**并标注「候选」；
  两者都有的行，页面显示以人工为准并保留候选（对账用）。
- **禁止**：自动填 `attribution_manual`、把候选当结论展示、用「未定」以外的默认值填充
  （既有口径：DATA 之外一律 `UNDETERMINED`）。
- 候选标签的判据（信号 + 阈值）**必须落成常量**（`stocklab/config/`），不许散落在页面/脚本里。

## 2. 只读配置视图（D-32）

- 内容：**主干常量**（熔断阈值 / 自评估边界 / 门禁 120 / 账户命名 / 频率）＋
  **当前生效的策略参数**（`validation_cycles.params_json` 最近一行）＋ **来源**（哪个文件/哪个常量名）。
- **只读**：零写入口（ADR-021 判据照旧：`labweb/` 对写路径零引用 + 页面无表单/按钮提交）。
- 每一项必须能指到**代码里的定义处**（文件:符号），页面不复制数值的逻辑、只在渲染时引用同一常量对象。

## 3. 可视化报表

- 净值曲线 / 回撤曲线 / 因子胜率曲线：**复用既有渲染函数**（`labweb/paper_render.py` 的曲线与
  `dashboard/` 的既有图表代码），**禁止**再写第二套画图/取数实现（源码扫描 + 反向自检）。
- 输出两种形态：页面（`/lab/m2` 或既有页追加小节）＋ **离线单文件报告**（`dashboard build` 的既有产物路径），
  两者**同源**（同一取数函数）、页面不重算。
- 缺数据 ⇒ 图例写「无」＋原因，不画空曲线、不填 0。

## 4. 事件旁路触发（04 §P2-1）

- 语义：某标的出现**重大公告利空**（或等价的机械信号）时，**不等验证周期结束**即触发一次**重打分/重评估**。
- **触发源**：机械信号清单（落常量，可配置）：① `corp_actions` 新增事件（类型白名单）② 单日跌幅 ≥ 阈值
  ③ `data_quality` 出现 `ohlc_inconsistent` 之类异常。**不接外部推送、不新增网络依赖**。
- **幂等**：同一 `(code, 信号指纹, asof)` 只触发一次（结构性防线：唯一索引/幂等键），重复触发返回「已存在」。
- **留痕**：触发事件写入既有台账（`system_events` 或 `plugin_audit`，取既有真源；**不新造事件表**），
  带信号原文与算到哪一步；**触发 ≠ 结论**：重打分的产物仍走既有 `candidate`/`plugin` 链路与人工闸门。
- 网页端**不提供触发按钮**（触发走 CLI）。

## 5. 测试（先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | **归因不自动填** | 构造错误样本 ⇒ `attribution_manual` 恒空、`attribution_auto` 只含候选并带信号与阈值；页面出现「候选」标记 |
| T2 | **候选可证伪** | 给定构造输入（大盘跌 3%、同行业两只同向异动、`corp_actions` 新增）⇒ 候选标签与预期一致；边界（恰好等于阈值）有断言 |
| T3 | **配置视图只读且同源** | 页面显示的常量值 == 直接 import 的常量对象值（逐项）；源码扫描 `labweb/` 对主干常量写路径零引用（**带反向自检**） |
| T4 | **图表不平行实现** | 扫描：曲线取数/绘图函数只有一处真源；喂一段违规实现要判红 |
| T5 | **页面/报告同源** | 页面字段 == 报告生成器函数返回（逐字段）；页面不重算（同一函数被调用两次得到同一结果） |
| T6 | **旁路触发幂等** | 同信号重复触发 ⇒ 台账不增行、返回「已存在」；不同 `asof` 可各触发一次 |
| T7 | **触发不越权** | 触发后策略状态/账户/参数未变；未 `approve` 的脚本不会因此上线（静态扫描：触发路径不调用 approve） |
| T8 | **缺数据不造数** | 无净值/无指数/无行业数据 ⇒ 显示「无」+原因，不写 0、不画空曲线 |
| T9 | 全量 | `.venv/bin/python -m pytest -q` exit 0 ＋ `bash scripts/verify.sh` exit 0（红线三目标；红了先归因，**禁止 regen**） |

## 6. 反目标

- ❌ 不做自评估判定与熔断（P49）；❌ 不改 P41/P48 的读数口径；❌ 不改主干常量。
- ❌ 不自动填归因、不自动上线脚本、不接券商、不引入新的外部数据源/网络依赖。
- ❌ 不新造事件表/图表实现/行业分类（一律复用既有真源）——发现需要第二份真源 ⇒ **上报，别自己加**。
- ❌ 不 `git commit`、不建分支/worktree（提交由 nanobot 拆批）。

## 7. 环境与收尾

- 解释器 `.venv/bin/python`（3.12）；测试全离线 fixture；真库 `data/stocklab.db` **只读**，
  写实测在 `/tmp` 副本上做（用完删）。
- 判据＝`pytest -q` exit 0 ＋ `bash scripts/verify.sh` exit 0；红线三目标
  `0acf35c90f54ab6a…`/175、`73ffbfb136e8620d…`/1330、`ddac0a889c623d75…`/171 不变。
- 交付物：代码 + 测试 + 页面/报告 +（若需）ADR；§8 进度表逐条打勾 + 文末「实施记录」（实测命令与关键输出行）。

## 8. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | 误差归因结构位（候选标签 + 信号阈值常量 + 人工确认字段） | ✅ 已验证 |
| T2 | 只读配置视图（主干常量 + 生效参数 + 来源） | ✅ 已验证 |
| T3 | 可视化报表（复用既有图）＋ 离线报告同源 | ✅ 已验证 |
| T4 | 事件旁路触发（机械信号 + 幂等 + 留痕，不越权） | ✅ 已验证 |
| T5 | §5 的 T1–T9 测试 + `pytest`/`verify.sh` | ✅ 已验证（`2753 passed, 2 skipped` exit 0；`verify.sh` → `✅ 验证通过: all`；红线三目标 == 基线；见 R6） |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败
>
> ⚠️ 第一轮（2026-09-23 上午）因预算上限中断，留下 5 条红与半成品记录（见 git 历史）；
> 第二轮已把它全部收口，**两条发现改了实现**（见实施记录 §第二轮 · 发现）。

---

## 实施记录（P50 · Claude Code，**未收口**）

> ⚠️ 本轮因预算耗尽**中途停止**，下面是如实状态：**没有**跑全量 `pytest` / `verify.sh`，
> **没有**在真库（或副本）上做端到端实测，**没有**做变异注入（先红后绿的「红」只在
> 用例首次运行时观察到，未做脚本化变异反证）。任务书的「先写完记录再补最终数字」
> 这一步因此**没有走到**——按 §1 的教训，这部分必须由接手方重跑后补。

### 已落地的文件

| 文件 | 变更 |
|---|---|
| `stocklab/config/m2_signals.py` | 新增：四分类标签、7 条信号名与原文、阈值（大盘 −2% / 跌幅 −5% / 行业 −3% 且 ≥2 只 / 命中率 ≤0.40 且 n≥5）、`corp_actions` 与 `data_quality` 白名单、旁路模块名与指纹版本 |
| `stocklab/store/schema.sql` | 追加 `m2_attributions`（append-only，`UNIQUE(score_id, source, label, signal)`）+ `system_events` 上的**表达式唯一索引** `uq_system_events_m2_bypass`（`json_extract(context_json,'$.fingerprint')`，只覆盖 `module='m2_bypass'`） |
| `stocklab/m2/store.py` | 追加 `insert_attribution` / `find_attribution` / `list_attributions` |
| `stocklab/m2/attribution.py` | 新增：候选计算（大盘/个股四条/行业/命中率掉档）+ `scan`（只写 `source='auto'`）+ `confirm`（人工结论的唯一写入口，四分类 CHECK） |
| `stocklab/m2/bypass.py` | 新增：机械信号扫描 + 语义指纹（不含实测值）+ 幂等触发 + 复用 `m2/score.py` 重打分 + 留痕进 `system_events` |
| `stocklab/m2/config_view.py` | 新增：只读配置视图（值走 `selfeval.BOUNDARIES`，逐项 `文件:符号` 来源；生效参数取 `validation_cycles` 最近一行） |
| `stocklab/labweb/m2_charts.py` | 新增：净值（`paper_data.track` + `paper_render._races`）/ 回撤（逐日 `paper/engine.py::drawdown`）/ 逐日累计命中率，全部交给 `race_svg` 渲染 |
| `stocklab/labweb/m2_data.py` | 追加 `attribution_readings` / `bypass_readings`；`panel` 增加 `attribution` / `charts` / `config` / `bypass` |
| `stocklab/labweb/m2_render.py` | 错判表加「候选（程序）／结论（人工）」两列；`m2_page` 加三节；`m2_text` 加三段；新增 `charts_section`（与离线报告共用） |
| `stocklab/dashboard/summary.py` `html.py` | 摘要加顶层 `m2`（`SCHEMA_VERSION` 1→2；`docs/architecture/dashboard-json.md` **未同步**）+ 离线报告渲染同一段 HTML |
| `stocklab/cli/main.py` | `m2 attribute scan\|confirm`、`m2 bypass scan`；`_m2_conn` 表守卫 4→5 |
| `tests/test_p50_attribution.py` / `test_p50_config_charts.py` / `test_p50_bypass.py` | 新增 41 条用例；`tests/test_store_schema.py` +1 表名、`tests/test_m2_channels.py` 白名单 +1 |

### 实测（`.venv/bin/python -m pytest -o addopts="" tests/test_p50_*.py`）

`36 passed, 5 failed`。仍红的 5 条（**未定位完**）：

| 用例 | 现象 |
|---|---|
| `test_t2_a_corp_action_in_the_whitelist_yields_a_stock_news_candidate` | 白名单命中的 `corp_actions` 行未产出候选（同批 `data_quality` / 跌幅用例是绿的，说明扫描链路本身在跑） |
| `test_t2_a_suspended_bar_is_a_candidate_on_its_own_signal` | 同上，`is_suspended=1` 那条信号未产出候选 |
| `test_t3_the_write_scan_flags_a_planted_violation` | 反向自检的断言形态（`import ` 前缀）与实现不一致 |
| `test_t8_without_nav_or_forecasts_every_curve_says_none_with_a_reason` | 断言写的是「理由文本里不许出现 `0%`」，而命中率那条理由里有一句「**不是 0% 命中**」——**断言过严**（该修的是用例措辞，不是实现） |
| `test_t8_a_curve_with_data_is_actually_drawn` | 正例未画出线（与上面两条同源：命中率块的取数范围） |

### 未验证 / 存疑（点名）

1. **全量 `pytest` 与 `verify.sh` 未跑**：红线三目标 sha **未核**、真库 `data/stocklab.db`
   **未做行级只读核对**（开工前/收工 sha 与模块2、验证台账各表行数都没记）。
2. **真库副本上的端到端未做**：`m2 attribute scan` / `m2 bypass scan` / `/lab/m2` 页面
   只在离线夹具上跑过；`ensure_schema` 对真库的**新表前滚**未验（真库尚未前滚
   `m2_attributions` 与那条表达式索引）。
3. **零外部写入口的扫描未破**：`dashboard/summary.py` 新增的 `m2` 字段进了
   `TOP_LEVEL_FIELDS`，`docs/architecture/dashboard-json.md` 与
   `tests/test_dashboard_summary.py`（该文件读模块常量，理论自动通过）**未逐个核**。
4. **因子失效候选是本站的解读**：本仓没有横截面 IC 真源，用的是**既有命中率读数**
   （P48 `win_rate` 的同一分子分母）且判据是**绝对水平**（≤0.40）而不是「相对自己的
   历史掉档」——真「掉档」需要一条读数基线，属另一轮。已写进 `m2/attribution.py`
   模块 docstring 与常量注释。
5. **P49 的 F1/F2 未碰**（任务书要求），`ops/chain.py` 未碰，主干常量未改。

### 交接清单（接手方按序做）

1. 先跑 `.venv/bin/python -m pytest -q`（用例数只增不减：基线 2711 采集，本站 +41）
   与 `bash scripts/verify.sh`，红灯**先归因、禁止 regen**；
2. 定位上面 2 条 `corp_actions` / 停牌候选的红（建议直接跑
   `m2.attribution.scan_signals` 同款的 `candidates_for` 看 `steps` 与命中）；
3. 收窄 `test_t8_*` 里那句过严的 `0%` 断言，改成「理由里不许把缺数据写成数值 0」；
4. 在 `/tmp` 副本上跑真库端到端（`db init` 前滚 → `m2 attribute scan` →
   `m2 bypass scan` → 页面 POST 404），记录模块2/验证台账各表行数；
5. 补 `docs/architecture/dashboard-json.md` 的 `m2` 字段与 `SCHEMA_VERSION=2`；
6. 最后回填本节的最终数字（任务书 §1 的教训：先把记录写完，再补最后一跳的数字）。

---

## 实施记录 · 第二轮（2026-09-23，收口）

> 第一轮因 `error_max_budget_usd` 中断（5 条红、全量未跑）。第二轮把那 5 条弄清、
> **改了两处实现**（不是改断言放水），跑全量与红线，并留了变异反证。

### R1 五条红的根因（都不是「测试写错」那么简单）

| 用例 | 根因 | 处置 |
|---|---|---|
| `t2_a_corp_action_in_the_whitelist_…` | **实现 bug**：白名单是子串 `配股`，而源站原文是 `10配3股`（`配` 与 `股` 不连续）⇒ 真库里几乎永不命中。 | 白名单改**正则**（`("配股", r"配\s*\d+\s*股")`），匹配改 `re.search`；补一条「两种原文都要命中」的用例 |
| `t2_a_suspended_bar_is_a_candidate…` | **实测发现**：`verify/score.py` 对停牌的目标日判 `SUSPENDED`（不可评分）⇒ 错判案例里永远不会出现停牌日，CLI 扫描路径产不出这条候选。 | 保留信号与判据（旁路路径可达），用例改为**直接喂构造样本**验这一支 + 另一条用例把「不可达」如实钉住（`reason_code == "SUSPENDED"`） |
| `t3_the_write_scan_flags_a_planted_violation` | 扫描器只看 `node.module`，`from stocklab.plugin import lifecycle` 被漏掉（模块名是 `stocklab.plugin`） | 扫描器拼全名 `module.name`；写动作改判**调用名**（`approve` / `insert_` / `log_event` / `commit` …）——`store.validation` 是读写混装模块，不能按 import 判死 |
| `t8_without_nav_or_forecasts_…` | 断言「理由里不许出现 `0%`」，撞上我自己的文案「不是 0% 命中」 | 改文案（`不打分就没有这条线 —— 这与「命中率为零」是两件事`），**判据不变**：缺数据仍是「无」+ 理由、`svg=None` |
| `t8_a_curve_with_data_is_actually_drawn` | 夹具只建了预测、**没跑打分** ⇒ 命中率曲线无数据（实现没错，是用例漏了一步） | 用例里先 `m2_score.score_all`，再断言三条曲线都画出来 |

### R2 判据逐条实测（命令 + 关键输出）

| 判据 | 命令 | 关键输出 |
|---|---|---|
| T1 候选落库、结论恒空 | `.venv/bin/python -m pytest -q -o addopts="" tests/test_p50_attribution.py` | `44 passed`（三个新文件合计）；`test_t1_candidates_are_written_but_the_manual_field_stays_empty` 断言 `manual_rows == []`（扫描**没有**写人工结论行）+ `case["attribution_manual"] is None` |
| T1 人工确认是唯一写入口 | 同文件 `test_t1_manual_conclusion_is_written_only_by_the_human_cli` | 未知标签 → `exit 2`；`--label stock_news --reason …` → `exit 0`，页面该案例 `attribution_manual == "stock_news"` 且**候选保留**（对账用） |
| T1 幂等 + 结构防线 | 同文件 `test_t1_the_scan_is_idempotent_…` | 重放扫描行数不变；绕接口裸插同键 → `sqlite3.IntegrityError` |
| T2 四类候选可证伪 | 同文件 `test_t2_*` | 大盘 −3% ⇒ `market_shock`（`at_value=-0.03` / `threshold=-0.02`）；**恰好等于阈值触发**、差 0.5 点不触发；两只同行业同向异动 ⇒ `industry_blackswan`（`at_value=2 ≥ INDUSTRY_PEER_MIN`）；`10配3股` ⇒ `stock_news`；`10派20元转15股` ⇒ **不**给候选；`data_quality` 异常与跌幅**分列**两条候选；命中率 ≤0.40 且 n≥5 ⇒ `factor_decay`（`at_value` 与 P48 `win_rate` 逐位相同） |
| T2 判据落常量 | 同文件 `test_criteria_the_thresholds_are_not_restated_in_the_modules` | AST 扫 `m2/attribution.py`、`labweb/m2_{data,render,charts}.py` 的浮点字面量 ⇒ `[]`（阈值只在 `config/m2_signals.py` 一次） |
| T3 值同源 | `.venv/bin/python -m pytest -q -o addopts="" tests/test_p50_config_charts.py` | 逐项 `== limits.*` / `== paper_data.PERFORMANCE_THRESHOLD` / `== m2_config.*`；每项 `source` 的 `文件:符号` 能在该文件里找到 |
| T3 只读 | 同文件 `test_t3_the_config_view_has_no_write_path_at_all` + 反向自检 | `config_view.py` 的写路径命中 `[]`；喂 `from stocklab.plugin import lifecycle` + `UPDATE …` + `INSERT INTO …` + `approve(...)` ⇒ `["UPDATE ", "INSERT INTO", "call:approve", "import stocklab.plugin.lifecycle"]` |
| T3 生效参数 | 同文件 `test_t3_the_effective_params_…` | `params == {"top_n": 3, "rebalance_cadence": 5}`、`params_source == "validation_cycles#1.params_json"`；没有周期 ⇒ `params is None` + 理由（不是 `{}`、不是 0） |
| T4 不平行实现 | 同文件 `test_t4_*` + 反向自检 | 五个模块里 `<svg`/`<polyline`/`viewBox` 命中 `[]`；`m2_charts.py` 引用 `race_svg` / `paper_data` / `drawdown`；喂一段自拼折线 ⇒ 判红 |
| T5 页面/报告同源 | 同文件 `test_t5_*` | `panel["charts"] == m2_charts.chart_panel(...)` 两次逐字段相同；`summary["m2"] == panel["charts"]`；同一个 `m2_render.charts_section(...)` 的返回串**同时**出现在 `/lab/m2` 与 `dashboard build` 的单文件 HTML 里 |
| T6 幂等 | `.venv/bin/python -m pytest -q -o addopts="" tests/test_p50_bypass.py` | 首扫 `fired ≥ 1`、重扫行数不变且输出含 `already`；不同 `asof` 各触发一次；**跌幅被修正 −6%→−8% 指纹不变**（语义键）；绕接口裸写同指纹 ⇒ `IntegrityError` |
| T6 留痕 | 同文件 `test_t6_the_ledger_keeps_the_signal_text_and_the_step` | 事件在 `system_events`（`module='m2_bypass'`），`context` 带 `fingerprint/signal/threshold/at_value/step`；`m2_events` 表**不存在**（不新造事件表） |
| T7 不越权 | 同文件 `test_t7_*` | 触发前后 8 张表（插桩/审计/账户/周期/轮次/事件/预测/分数）**逐行相等**；`m2/bypass.py`+`attribution.py` 无 `approve` 名、无 `INSERT INTO`、不 import 执行路径；页面无 `<button>/<form>/<input>/method="post"` |
| T7 重打分走既有链路 | 同文件 `test_t7_the_rescore_uses_the_existing_scorer…` + `…an_unscored_forecast_is_scored…` | 全打过分 ⇒ 分数/预测行数不变、输出 `"rescored": 0`；有未打分的 ⇒ 补上（`m2_forecast_scores` 由 0 变正） |
| T8 缺数据不造数 | 同文件 `test_t8_no_signal_means_no_row_at_all` + `test_p50_config_charts.py::test_t8_*` | 无信号 ⇒ `system_events` 一条不写；无净值/无预测 ⇒ 三条曲线 `available=False`、`svg is None`、理由非空，页面无 `<svg>`/`<polyline>` |

### R3 判据先红后绿（变异反证，脚本化：改文件 → 跑用例 → 还原）

| 注入的缺陷 | 期望 | 实测 |
|---|---|---|
| `CANDIDATE_MARK` 改成空串 | T1 判红 | ✅ `test_t1_a_candidate_is_marked_…` 红（**第一次注入时它仍绿** —— 用例只拿常量自比，已改成钉死「候选」二字重跑） |
| 幂等索引的键换成不存在的字段（= 结构防线失效） | T6 判红 | ✅ `test_t6_the_unique_index_is_the_defense` 红 |
| 配置视图的 `source` 指向不存在的符号 | T3 判红 | ✅ `test_t3_every_item_points_at_its_definition_site` 红 |
| 缺数据的命中率曲线标成 `available=True` | T8 判红 | ✅ `test_t8_without_nav_or_forecasts_…` 红 |

> 备注：第一轮那次「把 `CREATE UNIQUE INDEX … m2_bypass` 改名」的注入**仍绿**——
> 因为改名不改谓词，索引照样生效，是**注入无效**而非判据失效；换成改键之后即判红。

### R4 真库 `data/stocklab.db`（行级只读）

- 开工前：`shasum -a 256` = `2b11e63d3971e47759f87784ed1dbbddcd2294a711034086a682840222172664`
- 行数（`mode=ro` 只读连接）：`validation_cycles/rounds/events` = `0/0/0`、
  `m2_channel_runs/forecasts/forecast_scores/judgements/attributions` = `0/0/0/0/0`、
  `plugin_scripts 7`、`plugin_audit 25`、`paper_accounts 5`、`system_events 174`。
- `m2_attributions` 在真库里**已经存在**（0 行）——不是我写的：`schema.sql` 一改，
  此后任一写库入口的 `ensure_schema` 自动前滚（ADR-015），本机 launchd 的 `ops` 链跑过。
- 收工读数与全量/红线实测见 R6。

### R5 未验证 / 存疑（点名）

1. **两条「个股突发利空」信号在归因扫描路径上的可达性**（本轮实测发现，已写进代码注释）：
   停牌日被判 `SUSPENDED`（结构上不可达）；除权日在**没有 `adj_factors` 因子链**的库上被判
   `NOT_ADJUSTABLE`（夹具即如此，真库有因子链 ⇒ 可能可达，**未在真库验证**）。
   两条信号在**旁路触发**路径上不受影响（那条路径不看案例）。
2. **因子失效是绝对水平判据**（命中率 ≤ 0.40 且 n ≥ 5），不是 04 §P1-2 原文的
   「相对自己的历史掉档」——本仓没有横截面 IC 真源，也不许新造因子算法。
3. **停牌那条判据在真库上没跑过**（真库模块2 表全 0 行，没有样本）。
4. `dashboard` 摘要的 `SCHEMA_VERSION` 1→2 与新增顶层字段 `m2`：`summary.py` 的 docstring
   一直指向 `docs/architecture/dashboard-json.md`，但**那个文件不存在** —— 本轮把它**新建**出来
   （见 R7 第 3 条），三处（`TOP_LEVEL_FIELDS` / `SCHEMA_VERSION` / 契约文档）一致。
5. P49 的 F1/F2 未碰；`ops/chain.py` 未碰；主干常量未改。

### R6 全量 / 红线 / 副本端到端（最终数字）

**全量测试**：`.venv/bin/python -m pytest -o addopts="" -q` → `2753 passed, 2 skipped in 163.22s`，
**exit 0**。用例数**只增不减**：基线 `2709 passed / 2 skipped` ⇒ **+44**（全在
`tests/test_p50_{attribution,config_charts,bypass}.py`）；`--collect-only` 采集 **2755**
（= 2753 + 2 skipped，`-q` 叠加 `addopts` 会变 `-qq` 不打印统计行，见 ERROR_DIARY #68）。

**红线**：`bash scripts/verify.sh` → `✅ 验证通过: all`，**exit 0**；
`scripts/check_redlines.py` 三目标与基线**逐位一致**（**未 regen**，
`git status --porcelain docs/baselines/` = 0 行）：

```
✅ predict_synthetic          0acf35c90f54ab6a… == 基线；叶子 175 项一致（a59e8c1906bc4ce2…）
✅ predict_real_2026-09-14    73ffbfb136e8620d… == 基线；叶子 1330 项一致（3af6008903703950…）
✅ backfill_real_2013-12-23_2026-09-14  ddac0a889c623d75… == 基线；叶子 171 项一致（06bf461ef1234b4e…）
✅ 回归红线全部成立
```

**副本端到端**（`/tmp/p50copy.db` = `cp -c` 克隆真库；**已删**）：

| 步骤 | 关键输出 |
|---|---|
| `m2 attribute scan --asof 2026-09-22` | `{"scanned": 0, "inserted": 0}` exit 0（真库 `m2_forecast_scores` 0 行 ⇒ 没有错判样本，**不编候选**） |
| `m2 bypass scan --asof 2026-09-22` | `{"n_signals": 0, "n_fired": 0, "rescored": 0}` ⇒ **一个字节都不写** |
| `m2 bypass scan --asof 2010-07-16` | **真数据里唯一命中白名单的日子**（510880 `ohlc_inconsistent`）：`{"n_signals": 1, "n_fired": 1}`，留痕 `旁路触发 data_quality · 510880 · 2010-07-16：… 在白名单内 ⇒ 触发一次重打分`，指纹 `ed2aff840cf1d97d…`；`system_events(m2_bypass) = 1` |
| 同一天**重放** | `{"n_fired": 0, "n_already": 1}`，`system_events(m2_bypass)` 仍 **1**（结构防线生效） |
| `m2 report --asof 2026-09-22 --out /tmp/p50-report.md` | exit 0；文本含 `# 可视化报表（P50 §3）`、`# 只读配置视图（P50 §2 / D-32）`、`## 归因候选判据`、`# 事件旁路触发（P50 §4 / D-45）` |
| 页面 `m2_render.m2_page` | 23321 B；`<button>/<form>/<input>/method="post"` **全 False**、「原因」二字 False；三节标题齐；`charts` = `nav(True)/drawdown(True)/win_rate(False，真库无分数行)`；`config.params is None` + 理由；`bypass.n_events = 1`（2010 那条，`asof <= 2026-09-22`） |
| `dashboard build --asof 2026-09-22` | 19668 B、含 `模块2 · 可视化报表（P50 §3）` 与 `<polyline`、页头 `schema v2` —— **离线单文件报告与页面同源** |
| `POST /lab/m2` | 无写入口（P49 用例继续钉住 404；本轮页面级复查未再断言，见 R5） |

**真库 `data/stocklab.db` 行级只读**（`mode=ro` 连接）：

- `shasum -a 256` 开工前 = **收工后** = `2b11e63d3971e47759f87784ed1dbbddcd2294a711034086a682840222172664`
  （**未变**；本轮所有写实测都在 `/tmp` 副本上）；
- 行数前后一致：`validation_cycles/rounds/events` = `0/0/0`、
  `m2_channel_runs/forecasts/forecast_scores/judgements/attributions` = `0/0/0/0/0`、
  `plugin_scripts 7`、`plugin_audit 25`、`paper_accounts 5`、`system_events 174`、
  `system_events(module='m2_bypass') = 0`（那条触发写在副本上）。

### R7 偏离任务书 / 改动的既有文件（逐条留痕）

1. **`tests/test_m2_scores.py::test_t3_missing_data_renders_none_not_zero`（P48 用例）**：
   `assert "0.00%" not in html` 从**整页**收窄到**数据块**（三方对标 / 预测校验 / 错判案例）。
   原因：P50 新增的只读配置视图显示主干常量 **熔断阈值 `10.00%`**（真实的常量取值，不是缺数据），
   整页子串扫描把它误判成「漏 0」。**判据没放宽** —— 缺数据处仍一个 0 都不许有，
   裸 `None`/`nan` 仍整页禁；配置视图那些值由 `test_p50_config_charts.py::test_t3_*` 反方向钉住必须显示。
2. **`tests/test_m2_channels.py` / `tests/test_store_schema.py`**：表清单与 `store.py` 的 INSERT 白名单
   各 **+1**（`m2_attributions`）—— 按既有「白名单只增」纪律，不是放宽。
3. **新增 `docs/architecture/dashboard-json.md`**：`dashboard/summary.py` 的 docstring
   从 P14 起指向这个文件，而它**一直不存在**（审计项 A6「文件在说谎」）。本轮补上，
   写清 9 个顶层字段、`m2` 段的口径与 `SCHEMA_VERSION=2`。
4. **`SCHEMA_VERSION` 1→2**（新增顶层字段 `m2`）：同时改 `TOP_LEVEL_FIELDS`、
   `dashboard/html.py`（渲染同一段 HTML）与新落盘的契约文档 —— 三处一致。
