# P56 AI 操盘手开跑（决策台账驱动的手臂上线）

创建：2026-09-23（nanobot 需求澄清）｜ 实现：Claude Code（superpowers 全流程）
来源：用户 2026-09-23 授权（**让 AI 当操盘手接受考核、对照真实金融操盘手**）的执行站。
上游：[`P52`](2026-09-22-p52-AI操盘手臂重构与对照臂.md)（决策台账 `paper_agent_decisions` / 写入口
`paper agent decide` / 随机对照臂 `paper agent random` / 窄页面**已交付**）。
口径：需求澄清录 **§8.6 的 D-48 / D-49 / D-50**。

---

## 0. 一句话

把 P52 的地基接成**能日更跑的操盘手**：AI 臂的**日终认领权**从 `paper step` 划给「决策循环」
（沿用既有 `params.executor` 显式字段），并补齐三样缺的东西 ——
① 项目外生成器的**唯一 PIT 输入源**（`paper agent context`）；
② **预注册的机械执行**（模型 / 提示词指纹写进账户行，不匹配即拒，换模型 = 开新版本账户）；
③ **「今天没决策」看得见**（台账 / 页面 / 回执三处）。
本站**不产生任何决策内容**（那是项目外的生成器），只保证「产出决策 ⇒ 可落库、可执行、可归因、可复算」。

## 1. 口径与判据

1. **谁落 AI 臂的日终**（D-50）：`arm-agent` / `arm-agent-random` / `arm-agent-<版本>` 三种账户在
   `params_json` 里带 `executor='agent_decision'` ⇒ `ops close` 的 `paper step` 按既有
   `engine.external_executor` **让出**它们（**不改让出逻辑**，只改「谁写这个键」）；日终改由新命令
   `paper agent run --asof <交易日>` 落：先要决策在台账里，再按当日收盘价成交并写净值。
   **理由（必须写进代码注释与 runbook）**：`paper step` 的幂等判据是「当日净值行存在」，
   收盘链 15:30 先跑完 ⇒ 之后写进去的决策**永远不会被执行**（同 P47 `external_executor`
   docstring 里那条坑：症状出现在净值表上，排查方向会跑到订单生成上去）。
2. **记账约定与其它臂一致**（AI 不特殊化）：全项目的约定是「**当日收盘价成交 ＋ 当日收盘数据可用**」
   （`check_no_lookahead(asof, marks)` 允 `<= asof`）。AI 臂不例外 ——
   既不许喂未来数据，也不许为它单开「次日成交」这种第二套口径。
3. **PIT 约束输入、不约束模型**（D-49）：生成器只能从 `paper agent context --asof <交易日> --arm <臂>`
   拿输入（`agent_context.build_decision_context` 的**只读投影**，只含 `<= asof` 的行）；
   决策载荷里带 `context_sha256`，`paper agent decide` 落库前**重算比对**，不一致即拒。
   ⇒ 「PIT 能不能和别的模型结合」的答案是：**能**，PIT 管的是输入与留痕，不管谁产出决策。
4. **预注册是机械的**（D-48）：`paper agent enroll --arm-name arm-agent-<版本> --model-id <m>
   --prompt-sha256 <s>` 建账户时把 `(model_id, prompt_sha256)` 写进 `params_json`；此后
   `paper agent decide --arm <该臂>` 的 `--model-id / --prompt-sha256` **不匹配即拒**（不 warn、
   不静默改写）。**换模型 / 换提示词 = 开新版本账户**，旧账户保留。这条纪律要挡的只有一件事：
   「换到好看为止」。`--prompt-sha256` 的值用新命令 `paper agent sha256 --file <提示词文件>` 算
   （对文件字节的 sha256，纯 stdlib，避免手算各算各的）。

   **首次预注册（用户 2026-09-23 拍板「用 deepseek 吧」→ 晚间拍板「现在用 pro」）**：
   账户 `arm-agent-ds-v1`、`model_id=`**`deepseek/deepseek-v4-pro`**（**原样字面量**：这是
   commandcode 网关自己的命名，不是 DeepSeek 官方型号串、也不是官方现行名 `deepseek-flash`；
   台账必须与实际调用的一致，**不替它改写**）、首个决策日即预注册日。详见 §1a。
   网关无快照 id ⇒ **模型漂移的风险由「新版本账户 + 逐日 `system_fingerprint` 留痕」兜**：一旦网关改指向，
   就开 `arm-agent-ds-v2`，**不改旧账户、不重写台账**（旧账户＝旧口径的历史）。
   ⚠️ 这个账户名前缀（`arm-agent-`）与**通路 A** 的账户相同 —— **不是冲突**：两者靠
   `params.executor` 区分（通路 A = `m2_channel_a`，本站 = `agent_decision`），这正是那字段存在的理由；
   §2 的 T2/T5 已把两条分别钉住。
5. **缺决策看得见**：`engine._decision_entry` 已有 `present: False` + note（**别推翻**，本站把它提到台面）：
   - `paper agent show` 加 `missing_decision` 块：`is_trading_day` / `account_in_flight` / `expected`（三个字段都要有）；
   - `/lab/paper` 的 AI 行显式写「今日无决策」——与「有决策但不动手」**不同形**；
   - `paper agent run` 的回执带 `anomaly.missing_decision`（交易日缺决策 ⇒ 退出码 1；非交易日 ⇒ 不算异常，退出码 0）。
6. **随机对照臂同日一条**：`paper agent random --asof <交易日>` 由**项目内**跑（种子固定 ⇒ 逐字节可复现），
   否则 `delta_vs_random` 恒 `null`、结论一律「不可归因」（P52 的原文纪律）。本站只把它排进循环。
7. **认领关系 fail-closed**：`params.executor` 的值必须**有对应执行者**
   （`m2_channel_a` ↔ 通路 A、`agent_decision` ↔ `paper agent run`）；出现未知值 ⇒ **点名报错**。
   静默跳过 = 那条策略悄悄不下单，而症状只会在净值表上出现。
8. **模型调用通路在项目外**（本机现状，2026-09-23 核过）：`~/.nanobot/config.json` 里
   **没有任何 DeepSeek 官方 key**（`providers.deepseek.apiKey = null`），nanobot 自己走的是
   本机托管代理（`providers.anthropic.apiBase = http://127.0.0.1:15721`，`apiKey=codemaker-managed`）。
   ⇒ 正式考核口径需要一条**可 pin、可核账单**的通路：优先 DeepSeek 官方 key
   （`https://api.deepseek.com`，OpenAI 兼容）；托管代理只允许用于**联调链路**（模型名与额度都不透明，
   不能当预注册的 `model_id`）。**两条路都不进项目代码** —— 项目里仍然零 API key、零联网、零模型调用。
   日循环建议挂 **18:30 之后**：按 2026-09-22 查到的现行价目（高岘＝北京 09–12 与 14–18，其余低谷半价），
   18:00 后属于低谷；具体价格以官方定价页为准，**一天一次调用**，量级可忽略。

### 1a. 通路已拍板（2026-09-23，用户给了凭据；联调实测记录）

用户指定的接入方式＝**OpenAI 兼容的网关**（不是 DeepSeek 官方直连）：

```
POST https://api.commandcode.ai/provider/v1/chat/completions
Authorization: Bearer $CMD_API_KEY     # 存 ~/.nanobot/workspace/.secrets/stock-agent.env（600，不进仓）
{"model": "deepseek/deepseek-v4-flash", "messages": [...]}
```

实测（2026-09-23 11:41，三次最小调用，均在项目外）：

| 项 | 结果 |
|---|---|
| `deepseek/deepseek-v4-flash` | **HTTP 200**，2.5s；返回是标准 OpenAI 形状（`choices[0].message.content`）**且多一个 `reasoning` 字段**（思维链模型） |
| `deepseek/deepseek-v4-pro` | **HTTP 200**，可用（同网关的强档） |
| `response_format={"type":"json_object"}` | **接受**，`content` 是纯 JSON（driver 可以要结构化输出） |
| `usage` | 每次响应带 `usage`（prompt/completion/total + `reasoning_tokens`）⇒ **可核账** |
| `system_fingerprint` | 每次响应带（实测 `fp_ng46uka9f9`）⇒ 可当作**漂移证据**逐日留痕 |
| 坑 | **`max_tokens` 要给够**：思维链 token 也占预算，`max_tokens=16` 时 pro 档只出 `finish_reason=len`、`content` 为空 |

**口径三条**（写进 runbook）：
1. 这是「**经网关**」不是厂商直连 ⇒ 台账/结论里标「经 commandcode 网关」，免得日后读成 DeepSeek 官方口径。
2. 预注册的 `model_id` **原样字面量**记 `deepseek/deepseek-v4-flash`（网关自己的命名，**不替它改写**成官方现行名 `deepseek-flash`）——
   台账必须与实际调用的一致；网关哪天改指向，项目侧察觉不到，靠 `system_fingerprint` 留痕 + **换模型/换通路 = 开新版本账户**（D-48）兜。
3. 凭据只在**项目外**读取 ⇒ 项目内仍然零 API key、零联网（T2 的 enroll 只存 `model_id` 字符串，不存任何凭据）。

> **拍板（2026-09-23）：首个版本用 pro 档** ⇒ 预注册 `model_id=deepseek/deepseek-v4-pro`（原样字面量，
> 经 commandcode 网关）。改用 flash 档、或换网关/换厂商 ＝ 开新版本账户（`arm-agent-ds-v2`…），enroll 之后不改旧账户。

## 2. 测试（先红后绿）

| # | 判据 | 怎么测 |
|---|---|---|
| T1 | `paper agent context` 只读 + 指纹稳定 | fixture 库同 asof 连跑两次 stdout **逐字节一致**；PIT 守卫：塞一条 `asof+1` 的收盘价 ⇒ 不采用/报错；输出含 `context_sha256` 且等于 `decision_context_sha256(ctx)` |
| T2 | `paper agent enroll` 幂等 + 形状 | 新账户 `arm='agent'`、`params.executor='agent_decision'`、**无** `m2_channel_a`；重跑 `created:false` 且行不变；拒 `arm-agent` / `arm-agent-random` / 非法名（大写、空格、超长、空版本） |
| T3 | 让出生效 | fixture：账户带 `executor='agent_decision'` ⇒ `paper step` **不写**它的当日净值行；`arm-hold` 等 5 条照旧写 |
| T4 | `paper agent run` 幂等与三种结果 | 有决策 ⇒ 成交 + 净值行（与 `paper step` 同口径算出的值一致）；无决策 ⇒ **平盘净值行 + `anomaly.missing_decision`**；重跑 ⇒ `already`、行数不变、无第二笔成交 |
| T5 | 未知 `executor` fail-closed | 造一条 `executor='nobody'` 的账户 ⇒ `paper agent run` 与 `paper show` 都**点名报错**，不静默 |
| T6 | 预注册不匹配即拒 | 账户预注册 `(m1, s1)`；`--model-id m2 --prompt-sha256 s1` ⇒ exit ≠ 0、**零写入**；`(m1, s1)` ⇒ 落库 |
| T7 | `context_sha256` 复核 | 载荷里塞错的 `context_sha256` ⇒ 拒绝且零写入；正确 ⇒ 落库（P52 已可能实现，若已实现则本项只补测试） |
| T8 | 缺决策显式化 | `paper agent show` 的 `missing_decision` 三字段；`/lab/paper` 渲染出「今日无决策」且禁词纪律照旧（无买卖建议词） |
| T9 | patrol 不误报 | 被认领账户当日无净值行时，`ops patrol` §⑥**不**报异常（显式写「由决策循环认领」）；`arm-hold` 缺行仍判异常 |
| T10 | 全量 | `.venv/bin/python -m pytest -q` exit 0（用例数只增不减）＋ `bash scripts/verify.sh` exit 0（红线三目标 sha 不变） |

## 3. 真库动作（nanobot 在验收后做，写进回执）

1. 备份（`data/backups/`，走 `store/migrate.py::backup_db`）；
2. **先在副本上**验两件幂等：`paper init` 只补 `arm-agent`/`arm-agent-random` 两条（5 条静态臂逐字段不变）；
   迁移 `migrate_p56_agent_arms_executor` 可重入（跑两次结果相同）；
3. 真库：`paper init`（补两条账户）→ 迁移（把 `executor='agent_decision'` 补进 3 条 `arm-agent*` 账户的 `params_json`）；
4. 核对：`paper_accounts` 由 5 → 7 条；`params_json` 里有 `executor` 的三条；静态 5 条的
   `paper_nav_daily` 行数与值**逐行不变**（`shasum` 前后对照）；
5. 首次循环实跑：`paper agent random --asof <上一交易日>` → 项目外产出一条决策 →
   `paper agent run --asof <同一日>`；截图 `/lab/paper`（AI 行 + 缺决策标记）交付。

## 4. 反目标

- ❌ 不在项目内**产生**决策：没有模型调用、没有 API key、没有「自动生成脚本」（D-1）；
- ❌ 不改主干常量（成本口径 / PIT 判据 / 整手 / 护栏 / 熔断阈值）；不做参数搜索；
- ❌ 不碰通路 A（`m2_channel_a` 的账户照旧让出，插桩路径留给后续站）；不动 `m2 daily`（P54 的地盘）；
- ❌ **不把 `paper agent run` 塞进 `CLOSE_STEPS`**（决策在项目外产出 ⇒ 链条里没有它可读的东西），
  也不在 `ops patrol` 里补跑它；
- ❌ 不给「缺决策」补造一条默认决策（不许新增「按纪律办」的替代路径 —— 那会把「没决定」
  显示成「决定按默认纪律办」）；
- ❌ 不 `git commit`、不建分支/worktree；`ops/chain.py` 一律不碰（P54/P55 同改该文件 ⇒ 必须串行）。

## 5. 环境与收尾

- 解释器 `.venv/bin/python`（3.12）；测试全离线 fixture；真库 `data/stocklab.db` **只读**
  （开工前 `shasum -a 256`，收工再核；只许 `-shm`/`-wal` 书签变）。
- 判据＝`pytest -q` exit 0 ＋ `bash scripts/verify.sh` exit 0；红线三目标
  `0acf35c90f54ab6a…`/175、`73ffbfb136e8620d…`/1330、`ddac0a889c623d75…`/171 不变。
- 交付物：代码 + 测试 + `docs/ops/2026-09-23-AI操盘手-日更循环.md`（顺序、命令、判据、失败处置、
  预注册纪律、换模型流程）；§6 进度表逐条打勾 + 文末「实施记录」。
- ⚠️ **先写打勾与实施记录，最后再跑一遍全量 pytest/verify.sh 把最终数字补进去**（P48 的 403 教训：
  实现者最后一跳失联时，任务书上留下的是一个字都没写的空白）。

## 6. 进度

| # | 任务 | 状态 |
|---|---|---|
| T1 | `paper agent context`（PIT 输入唯一源）＋ `paper agent sha256`（指纹工具） | ✅ 已验证 |
| T2 | `paper agent enroll`：决策台账驱动的新版本账户（含预注册字段） | ✅ 已验证 |
| T3 | 认领让出 ＋ `paper agent run --asof`（幂等 / 缺决策 / `already`） | ✅ 已验证 |
| T4 | `paper agent decide` 的预注册比对与 `context_sha256` 复核 | ✅ 已验证 |
| T5 | 缺决策显式化（`show` ＋ `/lab/paper` ＋ patrol §⑥ 排除被认领账户） | ✅ 已验证 |
| T6 | §2 的 T1–T10 测试（含既有用例的同步改写，不许删） | ✅ 已验证 |
| T7 | 日更循环 runbook ＋ 索引/实施记录（真库动作留给 nanobot） | ✅ 已验证 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

---

## 7. 实施记录（Claude Code，2026-09-23）

### 7.1 两处判断（任务书未明写；按最严口径落地，**请复核**）

| # | 歧义 | 落地 | 理由 / 后果（**点名**） |
|---|---|---|---|
| J1 | 内置 `arm-agent` 没有预注册（迁移只补 `executor`），而 `enroll` 明令拒收 `arm-agent` 这个名字 —— 那 `decide --arm arm-agent` 怎么办？ | **严格 fail-closed**：没有预注册 ⇒ `decide` **拒**（exit 2、零写入） | 依据 §1.4「预注册是**机械的**」＋硬约束 2「一个字都不许松闸门」＋硬约束 6 的 fail-closed 精神。放宽会让 D-48 只挡「已声明过的那部分」，而「换到好看为止」恰恰发生在没声明的那条路上。**后果**：内置 `arm-agent` 从此不能再写 `llm` 决策，只能跑平盘 + 页面显示「今日无决策」；要接模型必须开 `arm-agent-<版本>`（§1a 的 `arm-agent-ds-v1` 正是这么做的）。若 nanobot 要保留 `arm-agent` 可写，改 `engine.require_preregistration` 的一处判定即可 —— 但那是**松闸门**，需要你点头 |
| J2 | 载荷里的 `context_sha256` 必填还是可选？（P52 的载荷只有 4 个键，这个键是本站新增的） | **CLI 的两个写入口恒传真值** ⇒ 载荷**必须**回传且一致，缺 ⇒ 拒（exit 2、零写入） | D-49 的原文是「载荷回传 `context_sha256`，`decide` 写入前重算比对」——「回传」= 必填。实现上把「期望值」作为 `validate_payload` 的**参数**（默认 `None` = 不启闸），于是纯函数调用方（单测）不受影响，而真实路径上没有「不启闸」这个状态 |
| J3 | 迁移目标「3 条 `arm-agent*`」在真库上对不上（`paper init` 只建 2 条） | 迁移按 `paper_accounts.arm IN ('agent','agent_random')` 判定、**只加一个键**；同时 `init_accounts` 给新库直接写这个键 | 真库的 5 条静态臂里没有 AI 臂 ⇒ `paper init`（新代码）建出的 2 条**自带** `executor`，`enroll` 建的版本账户也自带 ⇒ 真库迁移**返回空**（这正是「迁移不该是键的唯一来源」）。第 3 条 = `arm-agent-ds-v1`（enroll 时写入），与 §3.4「`params_json` 里有 executor 的三条」一致 |

### 7.2 改了哪些文件

| 文件 | 改了什么 |
|---|---|
| `stocklab/paper/config.py` | 新增 `EXECUTOR_AGENT_DECISION` / `EXECUTOR_CHANNEL_A` / `KNOWN_EXECUTORS` / `AGENT_ARM_PREFIX` / `ARM_VERSION_RE` / `PREREGISTERED_KEY` / `PREREGISTRATION_KEYS` / `CONTEXT_SHA256_KEY`；`DECISION_PAYLOAD_KEYS` += `context_sha256` |
| `stocklab/paper/engine.py` | `UnknownExecutorError`；`executor_kind`（fail-closed，`external_executor` 委托它，**让出逻辑本身未改**）；`preregistration` / `require_preregistration` / `validate_arm_name` / `enroll_agent_arm`；`is_trading_day` / `decision_expectation`；`decision_context_for`；`agent_claim_accounts` / `agent_run`；`_step_all(claim_handover=)`；`_initial_state` / `_agent_params`（`init_accounts` 与 `enroll` 共用一份起跑口径，两条 AI 臂现在**自带** `executor`） |
| `stocklab/paper/agent_decide.py` | `validate_payload(..., expected_context_sha256=)`：D-49 的 PIT 闸门（缺失/不符即拒）；`context_sha256` 随载荷进 canonical JSON |
| `stocklab/paper/comparison.py` | 每条 AI 臂加 `decision` 块（「今日无决策」/「今日有决策」**开头就不同形**） |
| `stocklab/labweb/paper_render.py` | 对照表备注列渲染 `decision` 的 note |
| `stocklab/cli/main.py` | 新命令 `paper agent context|sha256|enroll|run`；`show` 加 `missing_decision` / `preregistered`；`decide` 重排（先建 context 再校验）＋预注册闸门；`_agent_arms_check` 改为按 `executor` 字段判定 |
| `stocklab/ops/patrol.py` | §⑥ 排除被认领账户（`claimed_accounts` / `expected_accounts` / `note`），未知 `executor` 单列报 stale |
| `stocklab/store/migrate.py` | `agent_arms_need_executor` + `migrate_p56_agent_arms_executor`（事务内摘/挂 append-only 触发器、只加一个键、核对行数与其余键值，**可重入**）；登记 `_KNOWN_MARKERS` / `_pending_column_migrations` / `_apply_schema` |
| `stocklab/paper/engine.py` `state_payload` | 先逐条验 `executor` 再按「当日有没有净值行」过滤（否则坏账户只要那天没净值行就被跳过） |
| `tests/test_p56_agent_arms.py` | **新增** 40 条：T1/T2/T3/T4/T5/T6/T7/T8(show) + 迁移可重入 + `paper init` 幂等 |
| `tests/test_ops_patrol.py` | 追加 T9 三条（认领不误报 / 静态臂缺行仍报 / 未知 executor 报 stale） |
| `tests/test_labweb_paper_agent.py` | 追加 T8 页面两条（「今日无决策」+ 禁词；两种形态并存） |
| 既有用例**同步改写**（未删任何一条） | `test_cli_paper.py`（step 只认领 5 条）、`test_paper_engine.py`（同上 + AI 臂日终走 `agent_run`）、`test_cli_paper_agent.py`（enroll + context + `agent run`）、`test_paper_agent_decide.py`（统一用预注册账户 + context 指纹 + `agent run`）、`test_labweb_paper.py` / `test_labweb_paper_agent.py`（夹具改走决策循环）、`test_paper_discipline_guard.py`（`comparison.py` 加白名单：**只许查询** `portfolio_decision_on`） |
| `docs/ops/2026-09-23-AI操盘手-日更循环.md` | **新增** runbook（顺序 / 命令 / 判据 / 失败处置 / 预注册纪律 / 换模型流程 + §1.1 那条硬理由） |
| `docs/plans/2026-09-23-p56-AI操盘手开跑-实现计划.md` | **新增**实现计划 |

### 7.3 T1–T10 实测（命令 + 输出原文）

夹具：`tests/test_p56_agent_arms.py`（`paper init` + `enroll arm-agent-ds-v1`，
`model_id=deepseek/deepseek-v4-pro`、`prompt_sha256=b*64`）。以下命令在仓库根目录跑。

**T1–T8（新增文件全绿）**

```
$ .venv/bin/python -m pytest tests/test_p56_agent_arms.py -p no:randomly
40 passed in 2.50s
```

逐条对应（用例名 → 判据）：
`test_t1_context_is_byte_identical_on_two_runs` /
`..._context_sha256_matches_the_pure_function` /
`..._a_future_bar_does_not_leak_into_the_context` /
`..._a_future_price_in_the_injected_marks_is_rejected`（T1）；
`test_t2_enroll_creates_a_preregistered_version_account` / `..._is_idempotent_and_changes_nothing` /
`..._refuses_a_different_model_on_the_same_account` / `..._refuses_bad_arm_names[9 例]` /
`..._refuses_a_prompt_sha_that_is_not_hex` / `test_t2_sha256_command_is_the_single_algorithm`（T2）；
`test_t3_paper_step_lets_go_of_the_claimed_accounts` / `..._the_executor_field_is_what_decides_not_the_name`（T3）；
`test_t4_with_a_decision_it_trades_and_writes_nav` / `..._without_a_decision_it_writes_a_flat_row_and_flags_it` /
`..._on_a_non_trading_day_a_missing_decision_is_not_an_anomaly` / `..._rerunning_writes_nothing_and_makes_no_second_trade`（T4）；
`test_t5_an_unknown_executor_is_named_not_skipped` / `..._fail_closed_is_not_the_same_as_letting_go`（T5）；
`test_t6_*`（4 条）（T6）；`test_t7_*`（4 条）（T7）；
`test_t8_show_carries_the_three_field_missing_decision_block` / `..._marks_a_day_that_does_not_require_a_decision` /
`..._distinguishes_present_from_missing`（T8）；
`test_migration_adds_executor_and_is_reentrant` / `..._is_a_noop_on_a_fresh_db` /
`test_paper_init_is_idempotent_and_keeps_the_static_arms_byte_identical`（迁移幂等）。

**T5 的关键一条（点名报错，不静默）** —— 上面 `test_t5_an_unknown_executor_is_named_not_skipped`
里逐条断言：`engine.UnknownExecutorError` 的文本含 `'nobody'` 与 `'arm-discipline-10'`；
`paper agent run` / `paper show` / `paper agent context` 三条 CLI 都是 **exit 2 + stderr 里有点名的值**。

**T8 页面 + T9 patrol（追加用例）**

```
$ .venv/bin/python -m pytest tests/test_labweb_paper_agent.py tests/test_ops_patrol.py -p no:randomly
63 passed in 2.37s
```

其中 T8 的「不同形」判据是 `note.startswith`：有决策 → `**今日有决策**…`；
无决策 → `**今日无决策**…`（有决策那条会在正文引用「今日无决策」做对比，
所以不能拿子串否定 —— 这一条踩过一次，已写进断言注释）。
T9 三条：被认领账户缺净值行 ⇒ §⑥ `status=ok` + `note` 写「由决策循环认领」+
`expected_accounts=1`；`arm-hold` 缺行 ⇒ `missing` + `plan` 里仍有 `paper_step`；
未知 `executor` ⇒ `stale` + `unknown_executors=['arm-agent-v9=\'nobody\'']`。

**T3 的让出（既有用例同步改写后仍钉住）**

```
$ .venv/bin/python -m pytest tests/test_cli_paper.py tests/test_paper_engine.py -p no:randomly
33 passed in 1.50s

$ .venv/bin/python -m pytest tests/test_paper_agent_decide.py tests/test_cli_paper_agent.py -p no:randomly
36 passed in 1.78s
```

`test_paper_init_then_step_then_show` 断言 `paper step` 当天只写 **5** 条静态臂；
`test_init_wires_the_agent_arms_to_the_ledger_without_copying_a_decision` 额外断言
「`arm-agent` **不在** `paper step` 的净值里」＋「`agent_run` 之后它与 `arm-hold` **同值**」。

**T10 全量与脚本（收尾跑，数字见 §7.5）**

### 7.4 真库（`data/stocklab.db`）核过什么

- 开工 sha256 = 收工 sha256 = `cfed72b0bf42ace357d8fe43082c9fba28377aaac3360b94882ba747e0157b09`
  （**真库零改动**；`-shm` / 空 `-wal` 未出现在 `data/` 清单里变化）。
- **§3 的真库动作（备份 / `paper init` / 迁移 / 首次循环实跑）一条都没做** ——
  按任务书交给 nanobot。本站对「迁移幂等 / `paper init` 幂等 / 静态臂逐字段不变」
  的验证全部在 **pytest 的临时库**上完成（见 §7.3 的两条迁移用例 + 一条 init 用例），
  未在真库或 `/tmp` 副本上跑过写操作。

### 7.5 收尾数字

```
$ .venv/bin/python -m pytest --collect-only        # 不带 -q：pyproject 的 addopts 已有 -q
2883 tests collected in 0.62s

$ .venv/bin/python -m pytest
2881 passed, 2 skipped in 163.38s (0:02:43)        # exit 0

$ bash scripts/verify.sh
--- 4c. 回归红线（P25 / 错误日记 #34）---
  ✅ predict_synthetic               sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致
  ✅ predict_real_2026-09-14         sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致
  ✅ backfill_real_2013-12-23_2026-09-14  sha256 ddac0a889c623d75… == 基线；叶子 171 项一致
✅ 回归红线全部成立
✅ 验证通过: all                                    # exit 0
```

⚠️ **基线的口径要说清**：任务书写的基线（2788 collected / 2786 passed / 2 skipped）
是在**上一个 HEAD（P57 落 50 条单测之前）**测的 —— 本站开工时的 HEAD 是 `c2c789d`
（P57，已含那 50 条）。逐项对账：

| 项 | 数 |
|---|---|
| 任务书基线 | 2788 collected |
| ＋ 开工前已在 HEAD 的 P57 50 条 | 2838 |
| ＋ 本站新增（`test_p56_agent_arms.py` 40 ＋ patrol 3 ＋ labweb 2） | **2883** ✓ |
| 通过 | 2881 passed ＋ 2 skipped = 2883 ✓ |

⇒ **用例数只增不减**（既有用例零删除，全部为同步改写或新增）。

### 7.6 未做到 / 需要 nanobot 的

1. **§3 的全部真库动作**（备份 → 副本验幂等 → 真库 init/迁移 → 核对 5→7 条 →
   首次循环实跑 → 截图 `/lab/paper`）：按任务书留给 nanobot，本站**没有实测**。
2. 上面 J1 的取舍若与你的口径不同，**请复核**：那会改变内置 `arm-agent` 能不能接模型。
3. 页面截图（`/lab/paper` 的 AI 行 + 缺决策标记）由 nanobot 在真库上截。

---

## 8. nanobot 独立复核（2026-09-23 23:16–23:50）

所有判据**自跑**，不采信 §7 自报。

| # | 判据 | 实测 |
|---|---|---|
| A1 | 全量 pytest | `2883 tests collected`；`.venv/bin/python -m pytest -q` **exit 0** |
| A2 | 红线三目标 | `0acf35c90f54ab6a…`/175、`73ffbfb136e8620d…`/1330、`ddac0a889c623d75…`/171 全部 **== 基线** ✅ |
| A3 | `bash scripts/verify.sh` 整体 | **exit 1**。逐节看：红线三目标 ✅、其余各节 ✅，**唯一的 ❌ 是第 5 节「工作区有未提交改动」** —— 那是**并发中的 P58 站**正在写的文件（`stocklab/plugin_review/`、`stocklab/m2/cases.py`、`stocklab/store/{migrate.py,schema.sql}` 等），**本站改动已全部提交**（`24c1611`/`57bbf97`）。⇒ 想拿干净的 exit 0，等 P58 落 commit 后重跑即可 |
| A4 | 用例数只增不减 | 2788（基线条目，P57 落 50 条之前）→ 2883 ＝ ＋P57 的 50 ＋ 本站 45 ✅（既有用例零删除） |
| A5 | §3.1 备份 | `data/backups/stocklab.pre-p56-agent-arms.20260923T150943Z.db`（改动前状态，5 账户）✅ |
| A6 | §3.2 副本幂等 | 在 A5 副本上 `paper init --db` 连跑两次：第 1 次 `created:true` 且 `accounts:["arm-agent","arm-agent-random"]`；第 2 次 `created:false`、`accounts:[]`；静态 5 臂 `static_arms_params_sha`(`6632b09b…`) 与 `nav_sha`(`63d7e6dc…`) **两次逐位相同** ✅ |
| A7 | §3.3 / §3.4 真库 | `paper_accounts` **5 → 7**；两条新账户 `params_json` 带 `executor: agent_decision`；静态 5 臂的 params sha 与 6 天 nav 的 `nav_sha` 在「改动前 vs 改动后」**逐位相同** ✅ |
| A8 | T8 缺决策显式化 | `paper agent show --asof 2026-09-23 --arm arm-agent-ds-v1` 输出 `missing_decision{is_trading_day:true, account_in_flight:true, expected:true, present:false}` + `why:trading_calendar` ✅ |
| A9 | 真库只读性 | 本站**开工→收工**真库字节 sha 未变（CC 自报 `cfed72b0…`）；此后真库的每一次变动都能归因到**本审计的 §8.1 循环实跑**（时间戳 `2026-09-23T23:24:25`）✅ |

### 8.1 §3.5 首次循环实跑 —— **跑通了，但暴露出一个真库可复现的缺陷**

- 预注册版本账户 `arm-agent-ds-v1`：`model_id=deepseek/deepseek-v4-pro`、`prompt_sha256=231c12ad93d90058…`，
  提示词在**项目外** `~/.nanobot/workspace/tools/stock/prompts/trader-v1.txt`（驱动 `tools/stock/ai-trader.py`）。
- 链路：`paper agent random` → 决策 1；调网关（**经 commandcode 网关**，非厂商直连）→ 决策 2；`paper agent decide` 落库 → `paper agent run` 成交 1 笔（`trade_id=12`）。
- ⛔ **写下的净值行是执行前状态**：`nav 11320.0`（−43.4%），按它自己的成交重放应为 `19553.68`（−2.23%），
  差额 **¥8,233.68**；`positions_json` 里还留了零股条目 `[{"code": "000333", "qty": 0}]`。
  nanobot 在**库副本**上独立复算确认（`engine._ledger_arm_state` → cash 19553.68 / positions {}）。
- 根因不在本站：出问题的那一行是 `stocklab/paper/agent_decide.py:616`，`git blame` = **`be72c6d`（P52, 2026-09-22）**。
  ⇒ 已开 **P62** 任务书（含证据、根因、修法、真库修复纪律）。

> **P56 验收结论：代码与 §3.1–§3.4 全部成立（A1–A9 全绿）；但 §3.5「首次循环实跑」不成立 ⇒ 本站在 P62 修完并修好 `(arm-agent-ds-v1, 2026-09-23)` 那一行之前，验收保持「不通过」。**

### 8.2 模型通路实测（写进 runbook 用）

- 网关前置 **Cloudflare** 会把 `Python-urllib/3.x` 的默认 UA 挡成 **`403 error code: 1010`**；带一个正常 UA 即 200。
  （`curl` 一直 200 ⇒ 一开始会误判成 key 或 URL 的问题）
- 偶发 **503**，同一个请求重试即成功；单次调用 **35–110 s**（思维链模型）。
- `max_tokens` 要给够（思维链 token 占预算）：本次实测 `completion_tokens` 1651–5019，其中 `reasoning_tokens` 1442–4771。

### 8.3 顺带实测（**不属本站**，供 P62 §8 拍板与后续选品用）

池内「买 1 手」所需**最小权重**（总资产 ¥19,553.68、含滑点 ×1.0009）：
`601398` 4.14% / `002508` 8.44% / `600900` 14.37% / `603868` 16.00% / `002415` 16.95% / `000651` 19.64% /
`002032` 20.01% / `600036` 20.78% / `601318` 27.57% / `000333` 42.21% / **`600519` 640.48%（¥2 万账本上永远买不起 1 手）**。
⇒ AI 臂在这个账本上最多只能持有 2–4 个标的，且**只能挑便宜的**。已按 P62 §8 的**方案 A** 出提示词 v2
（`prompts/trader-v2.txt`，加了「整手」硬要求与 `手数 = floor(权重%×总资产/(价格×1.0009)/100)` 的算法），
并预注册为新版本账户 **`arm-agent-ds-v2`**（`prompt_sha256=e3c50bc268f3fcaf…`）—— v1 **保留不删**（D-48：换提示词＝开新版本）。
⚠️ v2 在 **P62 修好前不许跑 `paper agent run`**（驱动已加 `--no-run`），否则会再写出一行同类坏净值行。


