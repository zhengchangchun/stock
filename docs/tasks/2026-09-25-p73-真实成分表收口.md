# P73 真实成分表收口（板别前缀 ＋ 真名单 fixture）

- 立项人：nanobot（2026-09-25 01:1x）
- 上游：P71（`d9a0828` feat ＋ `c90cda4` docs）。与 P72 **互不重叠**：P72 改 `cli/universe.py` 的
  `active` 与四条 `ingest`；本档改 `config/universes.py` 的板别表与 fixture。

## 0. 一句话

把**真实抓到的沪深300 ∪ 中证500 名单**变成仓内 fixture，补上它撞出来的板别前缀缺口（`302`），
让 `universe build --source csi300+csi500` 在真源上跑得通；**默认路径与既有 21 只逐位不变**。

## 0.1 实证（nanobot 只读真跑，不是推测）

```
$ .venv/bin/python -m stocklab.cli.main universe build --source csi300+csi500 --out /tmp/csi_test
exit 2
❌ universe build：'302132' 的板别前缀未收录 —— 拒绝静默归 'main'
```

⇒ 抓取与解析**都是好的**（拿到 800 行、`name` 非空 800/800、`code` 0 空），
**唯一缺口是板别表**。前缀分布（只读实测，两页 300 ＋ 500 = 800 行、去重后 **800**）：

| 前缀 | 只数 | 板别 | 表内已有？ |
|---|---|---|---|
| `000` / `001` / `002` / `003` | 93 / 11 / 119 / 5 | `main` | ✅ |
| `300` / `301` / **`302`** | 89 / 13 / **1** | `gem` | `302` ❌ |
| `600` / `601` / `603` / `605` | 194 / 119 / 60 / 4 | `main` | ✅ |
| `688` / `689` | 91 / 1 | `star` | ✅ |

无 `4`/`8`/`920` 北交所代码（表里那三条保留不动）。

## 0.5 拍板点（nanobot 定，站不许另选；有异议写 §7.7）

| # | 锁定值 |
|---|---|
| **G1** | `_BOARD_BY_PREFIX` 只补 `("302", "gem")` **一项**，位置在 `("301","gem")` 之后、`("688","star")` 之前；其余前缀**一律不许增删** |
| **G2** | 依据写进注释：`302132`＝**中航成飞**（创业板，深交所 `302` 段）—— 不是猜的 |
| **G3** | 真名单 fixture 进仓：`tests/fixtures/csi800_rows.json`（nanobot 已抓，800 行，字段 `code`/`name`/`sector`；来源与抓取日写在文件里） |
| **G4** | `derive_board` 的 **fail-closed 语义不许动**（未知前缀照样抛 `UniverseError`） |
| **G5** | 红线**不 regen**；**真库不许写**（本站根本不碰库） |

## 1. 任务

### T1 补板别前缀
- `stocklab/config/universes.py::_BOARD_BY_PREFIX` 按 G1 补一行 ＋ G2 注释。

### T2 真名单 fixture ＋ 测试
- 把 `/tmp/p73/csi800_rows.json`（nanobot 已生成）拷进 `tests/fixtures/csi800_rows.json`。
- 新增 `tests/test_universe_real_components.py`：
  - 用 fixture 构造**假 fetcher**（`pages`/行数与真源一致）跑
    `build_universe("csi300+csi500", out_dir=tmp_path, fetcher=…)` ⇒ **800 行**、
    `meta.n_members == 800`、`members_sha256 == "eff7b478ea9a812fff152e4e8d87a2ff345963fdf30532492a124b14096ec302"`
    （该 sha 是 nanobot 2026-09-25 01:0x 只读真跑得到的值；**若对不上就停下来写 §7.7，别改这个数**）；
  - `{code[:3] for code in codes}` == 实测的 14 个前缀集合；
  - 逐个前缀各取一个真实代码断言 `derive_board` 结果；
  - 未知前缀（如 `"302"` 改成 `"999"` 之类）仍抛 `UniverseError`（反向自检）。

### T3 更新两处「未实测」口子
- `stocklab/data/sources/eastmoney.py` 的 `_CODE_KEYS` / `_NAME_KEYS` 注释：
  **已实测有效**（2026-09-25，800/800 行 code 与 name 非空）—— 把 P71 §7.8 的第一条口子消掉。
- `stocklab/data/fetch.py::fetch_index_components` 的 docstring 同步（保留 fail-closed 说明）。

### T4 ERROR_DIARY
- 新增 **#79**：症状＝「板别表靠『收了多少前缀』的静态列表，而真实名单第一次跑就撞上未收录的 `302`」；
  教训＝**枚举类常量要被真实数据打一次**；本档用真名单 fixture 把它钉住。

### T5 判据（站自跑，逐条贴原始输出）
见 §3。

## 2. 硬约束（违反即返工）

1. **不许联网**：测试一律用 fixture ＋ 假 fetcher。
2. **不许写库**（本站一行都不写；真库只读 `mode=ro&immutable=1`）。
3. `stocklab/cli/universe.py`、`stocklab/cli/main.py`、`ops/`、`predict/`、`m2/`、`paper/`
   **零改动**（P72 的地盘，别抢）。
4. 默认路径逐位不变；红线三目标 `0acf35c9…`（叶 175）/ `73ffbfb1…`（1330）/ `ddac0a88…`（171）逐位不变，绝不 regen。
5. 提交只用显式路径（严禁 `git add -A`），docs/ 与代码分开提交。
6. 不删既有测试用例（只允许改断言对象）。

## 3. 判据（站自跑，逐条贴原始输出）

1. `.venv/bin/python -m pytest -o addopts=""` 全绿（开工基线 **3147 passed / 2 skipped**，本站后应 ≥ 3160）。
2. `bash scripts/verify.sh` exit 0。
3. 红线三目标 sha == `0acf35c9…` / `73ffbfb1…` / `ddac0a88…`，**未 regen**。
4. fixture 驱动的 `build_universe` ⇒ 800 行、`members_sha256 == eff7b478…c302`（贴原始输出）。
5. `derive_board("302132") == "gem"`；未知前缀仍抛错（贴正反两例）。
6. 真库 `shasum -a 256 data/stocklab.db` 跑前 == 跑后。

## 4. 进度

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | 补 `("302","gem")` ＋ 注释依据 | ✅ 已验证 | 只动板别表一处（`git diff stocklab/config/universes.py`）；`derive_board("302132")=="gem"` |
| T2 | 真名单 fixture ＋ `test_universe_real_components.py` | ✅ 已验证 | 22 passed；fixture 驱动 build ⇒ 800 行 ＋ `eff7b478…c302`（§7.6 判据 4） |
| T3 | 消 eastmoney/fetch 两条「未实测」口子 | ✅ 已验证 | 注释按实测改写（800/800 非空），fail-closed 说明一字未删 |
| T4 | ERROR_DIARY #79 | ✅ 已验证 | `docs/errors/ERROR_DIARY.md` 新增 #79（含「枚举类常量要被真实数据打一次」＋ 检查清单） |
| T5 | §3 六条判据逐条自跑 | ✅ 已验证 | §7.6 逐条贴原始输出 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败

## 5. 反目标自查（完成时逐条贴证据）

| 反目标 | 证据 | 状态 |
|---|---|---|
| 不碰 P72 的地盘（`cli/universe.py`/`cli/main.py`） | `git diff --stat` 里除本任务书自身与 `docs/errors/ERROR_DIARY.md` 外，只动了三个**代码**文件：`stocklab/config/universes.py`、`stocklab/data/fetch.py`、`stocklab/data/sources/eastmoney.py`；新文件两个（`tests/fixtures/csi800_rows.json`、`tests/test_universe_real_components.py`）。`cli/`、`ops/`、`predict/`、`m2/`、`paper/` **一个都没有** | ✅ |
| 真库 sha/mtime 跑前跑后一致 | 跑前/跑后两次 `shasum -a 256 data/stocklab.db` 逐位相同（§7.6 判据 6） | ✅ |
| 没有 regen 红线 | `git status --short` 不含 `docs/baselines/redlines.json`（该文件本档未出现在改动清单里） | ✅ |
| 只动了板别表一项 | `git diff stocklab/config/universes.py`：`_BOARD_BY_PREFIX` 只插入 `("302","gem")` 一项，其余前缀未增删 | ✅ |

## 7. 实施记录（P73 站，2026-09-25）

### 7.1 开工基线（本档开工前实测，不是抄任务书的数）

- 全量 pytest（`.venv/bin/python -m pytest -o addopts="" -q`）原始输出尾部：

```
........................................................................ [100%]
3166 passed, 2 skipped in 198.06s (0:03:18)
exit=0
```

⚠️ 实测 **3166 / 2**，不是任务书写的 3147 / 2 —— 差 19 条**正是 P72 新增的用例**
（P72 自己的记录：开工 collect 3149＝3147＋2；该档案新加 `test_universe_cli.py` ＋3 与
`test_ingest_universe_scope.py` ＋16 ＝ 19 ⇒ 3166）。任务书那个数是从 P72 的开工基线抄过来的。
§3 判据 1 的「≥3160」门槛开工时**已经满足**（3166 ≥ 3160），本档在此之上再加 22 条
（见 §7.6 判据 1）。

- 真库 `data/stocklab.db`（跑前）：`e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b`
  （mtime `1790253029`、size `197779456` 字节）
- 红线三目标（读自 `docs/baselines/redlines.json`，本档**未 regen**、该文件未改动）：

| 目标 | 叶数 | sha256（开工 = 跑后） |
|---|---|---|
| `predict_synthetic` | 175 | `0acf35c90f54ab6ac84d3a59ec0297d827e57c40a95e2b113ac6857b156210e0` |
| `predict_real_2026-09-14` | 1330 | `73ffbfb136e8620d8f5e36615f124a36abeea7e2172b8a467679687098cb13b9` |
| `backfill_real_2013-12-23_2026-09-14` | 171 | `ddac0a889c623d752b567589ffa5ac50573d3aad0f326f4fbd5aa52793b83183` |

### 7.2 T1 补板别前缀（一处一行，位置按 G1）

`git diff stocklab/config/universes.py`：

```diff
 #: 代码前缀 → `board`（决定涨跌停幅度）。按前缀匹配，**顺序即优先级**。
 #: ⚠️ 比 §9 裁决 2 多认一个 `301`：深市创业板注册制后 `301xxx` 是创业板
 #: （±20% 涨跌停），中证500 里就有这一类；把它当 `main` 会算错涨跌停。
+#: ⚠️ `302` 同理（P73）—— 依据**不是猜的**：2026-09-25 抓的真名单（沪深300 ∪ 中证500，
+#: 800 行）里就有 `302132`＝**中航成飞**（深交所创业板 `302` 段，全名单仅此一只）。
+#: 漏了它时 `build` 是 fail-closed 抛错（不是静默归 `main`），所以缺口可见、但功能不可用。
 _BOARD_BY_PREFIX: tuple[tuple[str, str], ...] = (
-    ("300", "gem"), ("301", "gem"),
+    ("300", "gem"), ("301", "gem"), ("302", "gem"),
     ("688", "star"), ("689", "star"),
     ("920", "bse"), ("4", "bse"), ("8", "bse"),
     ("600", "main"), ("601", "main"), ("603", "main"), ("605", "main"),
```

`302132`＝**中航成飞** 这一依据**不是从网上抄的**：它就写在真名单 fixture 里
（`tests/fixtures/csi800_rows.json` 的那一行 `{"code": "302132", "name": "中航成飞", …}`），
并且有用例钉住（§7.3 第 6 条）。除这一项外，`_BOARD_BY_PREFIX` 其余前缀未增删（G1）。

### 7.3 T2 真名单 fixture ＋ `tests/test_universe_real_components.py`

**fixture 落法**：`tests/fixtures/csi800_rows.json` ＝ nanobot 抓的那 800 行（`code`/`name`/
`sector` **逐字未改**、顺序也未动）＋ 一个顶层 `pages`。`pages` 是真源两页的**分页归属**
（`TYPE=1`→300 个代码、`TYPE=3`→500 个），取自同一次真抓取落下的
`/tmp/csi_test/csi300-500.csv` 的 `index_membership` 列。

⚠️ **为什么扁平的 800 行不够**：`index_membership` 是 canonical CSV 的**列**，直接进
`members_sha256` ⇒ 只留扁平名单复现不出真 sha。实测反例：按 code 排序取前 300 行当
`TYPE=1`、后 500 行当 `TYPE=3`，`members_sha256` ＝ `08b5bf44a679aa22…fe83c2`
（≠ 真值 `eff7b478…c302`）；按真分页归属则**逐字节**等于真产物。

**用例清单**（`tests/test_universe_real_components.py`，22 条）：

| 用例 | 钉住什么 |
|---|---|
| `test_fixture_rows_have_no_empty_code_or_name` | fixture 800 行、`code`/`name` 0 空（T3 的实测结论本身进用例） |
| `test_fake_fetcher_pages_match_the_real_source` | 假 fetcher 页行数与真源一致（300 ＋ 500） |
| `test_real_components_reproduce_the_real_sha` | 800 行 ＋ `n_members=800` ＋ `members_sha256=eff7b478…c302`（＋ `load_universe` 回读同值） |
| `test_real_prefix_set_is_exactly_the_13_seen` | 真名单前缀集合**逐个**对账（漏一个前缀即红） |
| `test_derive_board_on_one_real_code_per_prefix`（13 条参数化） | 每个前缀取**一个真实代码**断 `derive_board` |
| `test_302132_is_the_real_gem_stock_that_cracked_the_table` | G2 的依据：`302132`＝中航成飞、board＝`gem` |
| `test_unknown_prefix_still_fails_closed`（3 条参数化） | G4 反向自检：未知前缀/非 6 位数字仍抛错 |
| `test_real_303_segment_is_not_silently_main` | `303999` 塞进真名单里 ⇒ 仍 fail-closed 抛错 |

定向跑原始输出：

```
$ .venv/bin/python -m pytest -o addopts="" -p no:cacheprovider -q tests/test_universe_real_components.py
......................                                                   [100%]
22 passed in 1.37s
```

**先红后绿**（T1 的缺口必须真能让用例红）：临时把 `("302","gem")` 那一项去掉，同一个用例文件立刻红，
报的错与 §0.1 的真跑**逐字同款**：

```
$ .venv/bin/python -m pytest -o addopts="" -p no:cacheprovider -q tests/test_universe_real_components.py   # 缺 302 时
E       stocklab.config.universes.UniverseError: '302132' 的板别前缀未收录 —— 拒绝静默归 'main'（猜错的 board 会算错涨跌停，而症状是零）
=========================== short test summary info ============================
FAILED tests/test_universe_real_components.py::test_real_components_reproduce_the_real_sha
FAILED tests/test_universe_real_components.py::test_real_prefix_set_is_exactly_the_13_seen
FAILED tests/test_universe_real_components.py::test_derive_board_on_one_real_code_per_prefix[302132-gem]
FAILED tests/test_universe_real_components.py::test_302132_is_the_real_gem_stock_that_cracked_the_table
4 failed, 18 passed in 0.09s

$ .venv/bin/python -m pytest -o addopts="" -p no:cacheprovider -q tests/test_universe_real_components.py   # 补回后
......................                                                   [100%]
22 passed in 0.08s
```

（`git diff --stat stocklab/config/universes.py` 复核为 `4 insertions(+), 1 deletion(-)`，即恢复原状。）

### 7.4 T3 消两条「未实测」口子

- `stocklab/data/sources/eastmoney.py` 的 `_CODE_KEYS` / `_NAME_KEYS`：注释由「代码/名称的
  列名未实测」改为「**已实测有效**（2026-09-25 只读抓取两页 800 行，`code`/`name`
  800/800 非空）」；并**如实留下**仍未核的那一点：命中的是候选集里的**哪一个**键没逐键隔离
  （手上只有解析后的名单，没留原始响应）。fail-closed 语义（列名真变了 ⇒ `code` 取不到
  ⇒ 调用方因「0 行」抛错）一字未删。
- `stocklab/data/fetch.py::fetch_index_components` docstring：把「列的真实名字未在本站逐列
  实测」的段落换成实测结论（300 ＋ 500 行、无交集、去重后 800），**保留** fail-closed 两条
  （非末页截断抛 `FetchError`、翻页超上限抛），并点名 fixture 就是这一次的名单。

### 7.5 T4 错误日记 #79

`docs/errors/ERROR_DIARY.md` 新增 #79「板别前缀表是『凭记忆枚举』的静态清单 —— 第一次喂真名单
就撞上 `302`」：症状（P71 假 fetcher 的 `60xxxx`/`00xxxx` 全落在已有前缀里 ⇒ 全绿；真名单第一次
跑 exit 2）→ 根本原因（**假数据的「真实感」＝造数据的人已经知道答案**，只能证明「清单里的能用」）
→ 教训 ①②③（枚举类常量要被真实数据打一次／造假数据别复用被测常量／真实输入的附属信息会进指纹）
→ 检查清单 4 条 → 已加判据。

### 7.6 §3 六条判据逐条原始输出

**判据 1 —— `.venv/bin/python -m pytest -o addopts=""` 全绿（≥3160）**

```
$ .venv/bin/python -m pytest -o addopts=""
tests/test_verify_store.py .............                                 [100%]

================= 3188 passed, 2 skipped in 202.04s (0:03:22) ==================
exit=0
```

（3188 ＝ 开工基线 3166 ＋ 本站新增 22；`2 skipped` 与基线一致，没有把既有用例改红或删掉。）

**判据 2 —— `bash scripts/verify.sh` exit 0**

```
$ bash scripts/verify.sh
...
--- 4. 项目测试 ---
  ▶ .venv/bin/python -m pytest
3188 passed, 2 skipped in 200.07s (0:03:20)
...
==========================================
✅ 验证通过: all
==========================================
exit=0
```

（`--- 5. git 工作区状态 ---` 那一段是脚本对**未提交改动**的提示，不是 ❌ 项 —— 本档的提交在跑完之后。）

**判据 3 —— 红线三目标 sha 未变、未 regen**

```
--- 4c. 回归红线（P25 / 错误日记 #34）---
==============================================================================
🔍 回归红线检查（all）  baseline=docs/baselines/redlines.json
==============================================================================
  ✅ predict_synthetic
       sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致（leaves_sha256 a59e8c1906bc4ce2…）
  ✅ predict_real_2026-09-14
       sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致（leaves_sha256 3af6008903703950…）
  ✅ backfill_real_2013-12-23_2026-09-14
       sha256 ddac0a889c623d75… == 基线；叶子 171 项一致（leaves_sha256 06bf461ef1234b4e…）
==============================================================================
✅ 回归红线全部成立
==============================================================================
```

补充：`git status --short docs/baselines/redlines.json` **无输出**（该文件本档未改、未 regen）；
`--regen` 一次都没跑过。

**判据 4 —— fixture 驱动的 `build_universe` ⇒ 800 行 ＋ `members_sha256`**

```
$ .venv/bin/python - <<'PY'   # 只读 fixture，out_dir 是临时目录，不联网不碰库
... 用 fixture 的 pages 造假 fetcher，跑 build_universe("csi300+csi500") ...
PY
[c4] pages: 300 + 500
[c4] n_members = 800 | 载入成员数 = 800
[c4] members_sha256 = eff7b478ea9a812fff152e4e8d87a2ff345963fdf30532492a124b14096ec302
[c4] 载入 members_sha256 = eff7b478ea9a812fff152e4e8d87a2ff345963fdf30532492a124b14096ec302
[c4] 真抓取产物 /tmp/csi_test/csi300-500.csv sha = eff7b478ea9a812fff152e4e8d87a2ff345963fdf30532492a124b14096ec302
```

（第三、四行说明：**本站复现的 sha 与 2026-09-25 那次真抓取产物的 sha 逐位相同** ——
判据里的 `eff7b478…c302` 不是站内自算的期望值，它就是真产物的 sha。）

**判据 5 —— `derive_board` 正反两例**

```
[c5] derive_board("302132") = 'gem'
[c5] 999132 -> UniverseError: '999132' 的板别前缀未收录 —— 拒绝静默归 'main'（猜错的 board 会算错涨跌停，而症状是零）
[c5] 303999 -> UniverseError: '303999' 的板别前缀未收录 —— 拒绝静默归 'main'（猜错的 board 会算错涨跌停，而症状是零）
```

**判据 6 —— 真库 sha256 跑前 == 跑后**

```
跑前：e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b  data/stocklab.db
跑后：e393f76d6b946206967009f298868b87b830412be2c4c82a291cb785016dc53b  data/stocklab.db
```

逐位相同；`stat` 也一致（mtime `1790253029`、size `197779456`，跑前跑后同值）。全量 pytest 与
`verify.sh`（含 `check_redlines.py` 的两条真实库目标）跑完后再取一次，仍是这个值。

（本档一行都不写库：测试全部 `out_dir=tmp_path`；真库只被 `check_redlines.py` 以
`shutil.copy` 复制出临时副本后使用 —— 真实库零写入。）

### 7.7 偏离（一处一行，照做别自造）

- **T2 的「14 个前缀」是 13**：真名单实测前缀集合为 13 个（`000/001/002/003/300/301/302/600/601/603/605/688/689`），任务书 §0.1 那张表的行合计也是 13 ⇒ 测试按**真源的 13** 断言，不按 14 凑数。
- **G3 的 fixture 字段清单不够复现真 sha**：`index_membership`（分页归属）进 canonical 文本 ⇒ 顶层加 `pages`（`1`→300 码、`3`→500 码）；**行内容与顺序逐字未改**，`note`/`source`/`index_types` 原样保留。
- **§3 判据 1 的开工基线 3147 与实测 3166 不符**：差 19 条 ＝ P72 新增用例（＋3 ＋16，P72 记录 collect 3149），任务书抄的是 P72 的开工基线；门槛「≥3160」开工时已满足，本档另加 22 条。

### 7.8 未验证口子（如实写，别抹掉）

1. **命中的是哪一个候选键未隔离**：`_CODE_KEYS` / `_NAME_KEYS` 各是 4 个候选键的元组，实测只证明「候选集整体有效」（800/800 非空），没有逐键核出真源用的是哪一个（该次抓取没留原始响应，仓内 `raw_cache` 也没有 `indexcomp:` 条目）。
2. **真源联网抓取与真库写入仍未在本站执行**：本档全程离线（fixture ＋ 假 fetcher）、零写库；`universe build/sync` 在真库上的效果由 nanobot 执行（与 P71 §7.8 第 2、3 条同）。
3. **fixture 是 2026-09-25 的「现成分」快照**：指数调样后它会过时 —— 它钉住的是「当时真源如此」，**不是**长期不变量；本档不承诺它与未来真源一致（判据 4 的 sha 也因此是「这一份快照」的指纹）。
4. **`302` 段属创业板这一结论的证据面**：来自真名单（`302132` 在中证500 内）＋ 东财的板块注记（`sector` ＝ 航空装备Ⅱ），**未**另行核交易所规则原文；本仓 `board` 的语义是涨跌停幅度，若该段涨跌停口径与创业板不同，这一项要重定。
5. **判据 6 的「跑前 == 跑后」只证明本站没写库**：不排除同一时段别的 actor（nanobot）对真库有写入 —— 实测两次读数之间 mtime/size 也未变，无并发写入迹象，但这不是「全时段无写入」的证明。

### 7.9 提交表

| 提交 | 内容 |
|---|---|
| `b74a270` `feat(p73)` | 代码 ＋ 用例：`config/universes.py`（补 `302`）、`data/fetch.py` ＋ `data/sources/eastmoney.py`（消口子）、`tests/fixtures/csi800_rows.json`、`tests/test_universe_real_components.py` |
| 本提交 `docs(p73)` | 文档：本任务书 §4/§5/§7 ＋ `docs/errors/ERROR_DIARY.md` #79 |

（代码与文档分开提交；两笔都用**显式路径**，未用 `git add -A`；未提交 `data/` 下任何东西。）
