# P43 任务书 · P37+P38 复核建议项收口（S1–S7）

> 来源：`docs/plans/2026-09-22-P37P38未提交改动-复核意见.md` §2「建议改（不阻断）」。
> 背景：复核判「修完再提交」，唯一阻断项 **B1** 已由 **P42** 修掉（patrol 70→65 槽 + 无交集断言）。
> 本任务书收口剩下的 7 条建议项，让 P37+P38 这批改动**干净地**躺进历史。
>
> ⚠️ 本任务书由 nanobot（我）编写，**不开工**。按 2026-09-22 全局规则，代码一律交 Claude Code。

## 0. 前置事实（开工前先核对，别信本文的转述）

- 复核意见书：`docs/plans/2026-09-22-P37P38未提交改动-复核意见.md` §2，**逐条实测命令都在 §5**。
- P42 已落地：`patrol` = 65 槽（13 槽 × 工作日），真机 plist 已由 `ops schedule install` 重装（2026-09-22 15:10）。
- `ERROR_DIARY.md` **#57（两任务撞同一分钟）已由 nanobot 于 2026-09-22 补写**，本任务**不要再写一条**。
- 工作区状态：P37 / P38 / P39 / P40 / P42 五批改动**全部未提交**（提交由 nanobot 做，见 §4）。

## 1. 目标（T1–T7，一条建议项一条）

| # | 项 | 要改成什么 | 判据（可执行） |
|---|---|---|---|
| T1 | **S1** `steps_total` 是死分支 | `OpsLab._verdict` 里「／共 N 步」整段删掉，或让 `steps_total` **真被写入**。二选一，**选删**（现在没人写这个键） | `grep -rn "steps_total" stocklab/ tests/` 只剩 0 处；死代码守卫（`tests/test_source_no_dead_code.py`）保持绿 |
| T2 | **S2** `next_trigger` 在每月 1 日 08:00 后返回 `None` 并谎称「形态认不出」 | 月末最后一次触发之后，`next_trigger` 必须给出**下一个月的 1 日 08:00**；真的认不出时才返回 None，且**原因文案与「无下一槽」区分开** | 新增用例：`now = 某月 1 日 08:01`（每月任务）→ 断言返回下月 1 日 08:00；`now` 在最后一个槽之后 → 断言不是 None。先红后绿 |
| T3 | **S3** `paper spec set` 冲突有两个退出码 | 预检冲突与异常路径**统一为 `EXIT_CONFLICT`**（1），异常另给 2 只在真异常时 | 两条路径各一条用例断言 `== 1`；真异常（如库只读）仍 2 |
| T4 | **S4** `render_plist` 注释说「不设 `RunAtLoad`」，实际设了 `False` | **改注释**（说清「显式设 False」的理由：避免 install 立刻触发一次不可逆写库），不动行为 | 注释与实测一致；plist 断言里 `RunAtLoad is False` 保留 |
| T5 | **S5** `close` 窗口文案「每交易日 15:30」与 plist 实际（工作日，含节假日）不符 | 文案改成 plist 说的那件事（工作日 15:30），**并补一句**非交易日整轮跳过（代码已有该行为） | 文档/`why` 文案与 plist 断言一致；`close` 非交易日跳过行为有用例钉住（若无则补） |
| T6 | **S6** `arm-agent-random` 空账户在阶段 1 就建（任务书 T6 说阶段 3） | **改任务书**：把 `arm-agent-random` 的创建挪到阶段 1 并写明「阶段 3 前只记净值不下单、`delta_vs_random` 恒 null」 | `docs/tasks/2026-09-21-p37-动态编排臂-阶段1.md` 与实现一致；真库 `paper_accounts` 现状 5 条（**不得为了对齐去改真库**） |
| T7 | **S7** ADR-020「已 bootout」与本机 `launchctl list` 不符 | 更正 ADR-020 §落地状态与 `docs/ops/2026-09-22-launchd-定时任务.md` §3：写**当前**三条 loaded（2026-09-22 15:10 重装后）+ nanobot 三条已撤 | 文档所述与本机 `launchctl list | grep stocklab`（3 条）、nanobot `cron list`（无 stock 任务）一致 |

## 2. 反目标（不许做）

- **不许 `git commit` / 建分支 / 动 worktree**（提交由 nanobot 按批拆，见 §4）。
- 不许改真库：`data/stocklab.db` 与 `~/Library/LaunchAgents/`（读可以；重启 launchd 一律不在此任务内）。
- 不许把 §1 里任何一条「顺手重构成更多东西」（尤其 T1 不许顺手删别的键、T3 不许改动 CLI 参数）。
- 不许碰 P39 / P40 / P41 涉及的文件（`session/tick.py`、`predict/*`、`experiments/*`、`backtest/metrics.py`）。
- 不许改判据口径（阈值、窗口、`MODEL_VERSION`）。

## 3. 测试与验证

- 每条「改行为」的项 **先红后绿**（T1/T2/T3/T5/T7 有行为或断言变化；T4/T6 是文案/文档）。
- 收尾命令（判据＝退出码）：
  - `.venv/bin/python -m pytest -q` → exit 0（基线：P42 后 **2313 passed**）
  - `bash scripts/verify.sh` → exit 0，4c 三条红线成立
- 完工后勾选本文件 §5 进度表，并把**实测命令与输出**追加到 §6。

## 4. 提交边界（交给 nanobot，不属本任务）

本批 5 组改动混在工作区，nanobot 按下面的草稿拆 commit（`cli/main.py` 与 `ERROR_DIARY.md`
需要 hunk 级切分）：

| commit | 内容 |
|---|---|
| 1 | **P38 + P42**：`stocklab/ops/**`、`labweb/ops_*`、ops 相关 tests、ADR-019/020、`docs/ops/*`、`stock/07_*`、ERROR_DIARY #57；`cli/main.py` 里 ops 命令的 hunk |
| 2 | **P37**：`paper/agent_*`、`paper/{engine,rules,config}.py`、`labweb/paper_*`、相关 tests、`cli/main.py` 里 `paper spec` 的 hunk、P37 设计/任务书/台账 |
| 3 | **P39**：`session/tick.py`、`verify/*`、对应 tests、ERROR_DIARY #56 |
| 4 | **P40**：`predict/{model,service}.py`、`experiments/{variants,runner}.py`、`tests/test_experiments_fin_prior.py`、`docs/experiments/*` |

> P43 落地后，本表再追加 commit 5（P43）。**在 P43 之前不要提交 P37/P38** —— 复核结论是「修完再提交」，
> 而 B1 之外的 7 条建议项属于「修完」的一部分；若你（用户）想先提交再修，把 §1 的 T1–T7 挪到 commit 之后也可以，届时如实标注。

## 5. 进度表

| # | 项 | 状态 |
|---|---|---|
| T1 | S1 死分支删除 | ✅ |
| T2 | S2 `next_trigger` 跨月 | ✅ |
| T3 | S3 冲突退出码统一 | ✅ |
| T4 | S4 `RunAtLoad` 注释 | ✅ |
| T5 | S5 `close` 窗口文案 | ✅ |
| T6 | S6 任务书与实现对齐 | ✅ |
| T7 | S7 ADR-020/docs-ops 现状对齐 | ✅ |

## 6. 实测报告（完工后追加，不改上面任何一行）

> 执行者：Claude Code（`.venv/bin/python` 3.12，离线）｜ 日期：2026-09-22 17:2x
> 分支：`feat/financials`，改动**未提交**（按 §4 交 nanobot）

### 6.0 ⚠️ §0「前置事实」已过时（开工先核对，结果如下）

| §0 写 | 实测（开工时） |
|---|---|
| P37/P38/P39/P40/P42 五批**全部未提交** | **错**：`488cd86`(P40) / `c565c59`(P39) / `4c524db`(P37) / `cf82859`(P38) **已提交**；§4 那张提交边界表已成历史 |
| `patrol` = **65 槽**（13×5） | **过时**：P46 已收到 **60 槽（09:00–14:30）**，本任务以 60 槽为准 |
| 基线 `.venv/bin/python -m pytest -q` = **2313 passed** | **过时**：开工实测 **2452 passed, 2 skipped**（P46/P44/P45 的在制品又添了用例） |
| 真机 plist 已由 `ops schedule install` 重装（15:10） | **对**（三份 plist mtime 均为 09-22 15:10），但盘上 `patrol` 仍是 **P42 的 65 槽**，与源码的 60 槽**不同步**（P46 不许 install，有意为之） |

⇒ 因此**没有**执行任何 `git reset` / `git stash` / 分支清理：那批改动早已进历史，不需要「保护」。

### 6.1 收尾判据（判据＝退出码）

```bash
$ .venv/bin/python -m pytest
2457 passed, 2 skipped in 139.91s (0:02:19)          # exit 0

$ bash scripts/verify.sh
  ▶ .venv/bin/python -m pytest
  2457 passed, 2 skipped in 147.22s (0:02:27)
  --- 4c. 回归红线（P25 / 错误日记 #34）---
  🔍 回归红线检查（all）  baseline=docs/baselines/redlines.json
       sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致（leaves_sha256 a59e8c1906bc4ce2…）
  ✅ 回归红线全部成立
✅ 验证通过: all                                      # exit 0
```
2457 = 开工基线 2452 + 本次新增 **5** 条（T1 守卫 1 / T2 3 / T3 1）。
`MODEL_VERSION` 未动、红线三目标未漂移（上面是 `verify.sh` 自己的读数）。

### 6.2 逐项（先红后绿）

**T1（S1）`steps_total` 死分支** —— 选「删」
```bash
# 红
$ .venv/bin/python -m pytest tests/test_labweb_ops.py::test_verdict_has_no_total_steps_branch_whose_key_nobody_writes
E  AssertionError: assert ['stocklab/la...ps_render.py'] == []
1 failed
# 绿（连同死代码守卫）
$ .venv/bin/python -m pytest tests/test_labweb_ops.py tests/test_source_no_dead_code.py
27 passed
$ grep -rn "steps_total" stocklab/
(0 处)
```
改动：`ops_render._verdict` 删掉 `total = latest.get(...)` 与「／共 N 步」那半句。
> **判据口径的一处说明（不是打折）**：`grep -rn "steps_total" stocklab/ tests/` 在 `tests/` 里
> 仍有 **4 处**，全部在新增守卫用例的 docstring 与断言里 —— 守卫必须点名这个键才守得住
> 「不许重新引入」。**生产代码 `stocklab/` 是 0 处**，正是这条判据的本意。

**T2（S2）`next_trigger` 跨月**
```bash
# 红
$ .venv/bin/python -m pytest tests/test_labweb_ops.py -k next_trigger
3 failed, 3 passed
# 绿
$ .venv/bin/python -m pytest tests/test_labweb_ops.py -k next_trigger
6 passed
```
复核意见 §5-6 的复现脚本，改后：
```
2026-10-01T07:00 -> 2026-10-01T08:00:00+08:00
2026-10-01T08:00 -> 2026-11-01T08:00:00+08:00
2026-10-01T09:00 -> 2026-11-01T08:00:00+08:00      # 改前是 None（页面谎称「形态认不出」）
2026-10-15T09:00 -> 2026-11-01T08:00:00+08:00
close @Fri23:59  -> 2026-09-28T15:30:00+08:00       # 最后一个槽之后仍给得出
```
「原因文案与『无下一槽』区分开」：新增 `ops_data.next_trigger_reason()`，
`形态认不出` 与 `认得出，但往后 62 天内没有下一槽` 分成两句；`ops_render._job_section` 按它渲染。

**T3（S3）冲突退出码统一**
```bash
# 红
$ .venv/bin/python -m pytest tests/test_cli_paper_spec.py -k races
E  AssertionError: 竞态撞的也是「冲突」，不是「输入非法」
E  assert 2 == 1
1 failed
# 绿
$ .venv/bin/python -m pytest tests/test_cli_paper_spec.py
24 passed
```
改动：`_paper_fail(exc, code=2)` 加可选码；`DecisionConflict` 从原 `except` 拆出来 → `EXIT_CONFLICT`。
「真异常仍 2」由同文件既有用例继续钉住（未新增）；竞态路径的复现方式是让第 1 次
`decision_on`（预检）返回 `None`、第 2 次（`record_decision` 内部）返回真实行。

**T4（S4）`RunAtLoad` 注释**（文案项，无红）
```bash
$ .venv/bin/python -m pytest tests/test_ops_close.py -k plist
2 passed
```
注释改成「**显式**写 `RunAtLoad=False`，不是省略」；`test_plist_is_generated_for_the_three_jobs`
补 `plistlib.loads(...)["RunAtLoad"] is False` 断言（**行为未动**）。

**T5（S5）`close` 窗口文案**
```bash
# 红
$ .venv/bin/python -m pytest tests/test_ops_close.py -k plist
E  AssertionError: assert '每交易日 15:30' == '工作日 15:30（非交易日整轮跳过）'
1 failed, 2 passed
# 绿
$ .venv/bin/python -m pytest tests/test_ops_close.py tests/test_ops_patrol.py tests/test_labweb_ops.py
98 passed
```
`Job.window` 改成 `工作日 15:30（非交易日整轮跳过）`；同步了 `ops/chain.py` 模块文档表、
ADR-020 表、`docs/ops/…launchd-定时任务.md` 表。非交易日整轮跳过**本来就有**用例钉住
（`test_non_trading_day_skips_the_whole_round_with_exit_zero`），**未新增**。
页面侧改为断言「页面出现的窗口文案 == `schedule.JOB_BY_NAME["close"].window`」。

**T6（S6）P37 任务书与实现对齐**
改 `docs/tasks/2026-09-21-p37-动态编排臂-阶段1.md` 任务 6：写明随机臂空账户**阶段 1 就建**、
阶段 3 之前 `has_rules=False` ⇒ 只 mark-to-market、不下单、`delta_vs_random` 恒 `null`；
并记下真库现状（**只读**实测）：
```
$ .venv/bin/python -c "… mode=ro … select … from paper_accounts"
paper_accounts: 5
['arm-discipline-05','arm-discipline-10','arm-discipline-15','arm-hold','arm-now']
agent decisions: 0
```
⇒ 与 §1-T6 判据一致（真库 5 条）；**真库未被修改**（全程 `mode=ro` 打开）。

**T7（S7）ADR-020 / docs-ops 写成当前现状**
```
$ launchctl list | grep stocklab
-	0	com.stocklab.monthly
-	1	com.stocklab.close
-	0	com.stocklab.patrol
$ ls -l ~/Library/LaunchAgents/com.stocklab.*.plist
（三份 mtime 均为 9月 22 15:10）
$ plutil -convert json -o - ~/Library/LaunchAgents/com.stocklab.patrol.plist | …
65 {'Hour': 15, 'Minute': 0, 'Weekday': 5}
```
ADR-020 §落地状态 与 `docs/ops/…` §3/§4 改成「**复算命令 + 带时间戳的读数**」，
并写明「盘上 plist（65 槽）≠ 源码（60 槽）」这条已知差异、以及 12:2x 那段结论已过期。
⚠️ **nanobot 那一半没能当场复算**：本机没有 `nanobot` 命令（`which nanobot` → not found），
所以文档里写的是「已撤（据交接记录）」+「请用 `nanobot cron list` 自行确认」，
**没有**把未经核实的结论当成现状写死。

### 6.3 独立复查（只读，审的只是本次 diff）

派了一个**只读**复查（不看本次对话，只读 `git diff`/`git show` + 单文件用例）。结论：

- **确认无误**：S2 修复对三种真实日历 × 2026–2027 逐时点与独立参照实现 **0 处不符**；
  `LOOKAHEAD_DAYS=62` 够用（monthly 实测最大间隔 31 天）；S1 守卫在旧代码上**必红**；
  S3 对其它 4 个 `_paper_fail` 调用方透明、无二次 close；S5 与 plist/链行为逐字相符。
- **已采纳的措辞修正**：`next_trigger_reason` 原文案「往后 62 天内没有下一槽」可能被读成
  「就是没有」—— 改为 `（更远的不推）`，并加断言把 `LOOKAHEAD_DAYS` 这个范围**钉在文案里**。
- **未采纳（记录在案，不在本次范围）**：`stocklab/cli/main.py:2441`（`_spec_conflict`
  那条 `return` 之前没有 `conn.close()`）是**既有**缺陷（`git show HEAD` 确认 P43 之前
  就如此），不属于 T1–T7 任何一条，本次**未改**，交 nanobot 决定是否另立一项。
- **无法判断**：nanobot 三条是否已撤（本机没有 `nanobot` 命令，见 6.2-T7）。
- 复查确认**没有**任何阈值 / 窗口 / `MODEL_VERSION` 被放宽。

### 6.4 反目标遵守情况（自查）

- 未 `git commit`、未建分支 / worktree、未 `ops schedule install`、未重启 launchd。
- 未动真库：`data/stocklab.db` 全程以 `file:…?mode=ro` 只读打开。
- 未碰 P44/P45 在制品：`plugin/{contract,lifecycle,store}.py`、`config/limits.py`、
  `store/validation.py`、`store/schema.sql`、`store/migrate.py` 的 P44 段、
  `cli/main.py` 的两个 `build_parser` hunk、`tests/test_{plugin_lifecycle_module2,
  plugin_contract_m2,validation_ledger,config_limits,p44_constants_guard}.py`。
- 未改判据口径 / `MODEL_VERSION` / 红线；P39/P40 涉及的文件未碰。
