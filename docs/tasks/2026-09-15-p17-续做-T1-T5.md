# P17 续做（T1–T5）任务记录

日期：2026-09-15 ｜ 计划：`docs/plans/2026-09-15-p17-etf标的池与成本口径.md`
上一轮预算触顶中断（工作区有红灯 + 未提交改动），本轮先把红灯灭掉再推进。

| # | 任务 | 状态 | 提交 |
|---|------|------|------|
| T1 | 修红灯：`pytest -q` 全绿 | ✅ | `aeab16f` |
| T2 | ETF 复权口径 ADR-008 + 硬拒绝测试 | ✅ | `8859c9f` |
| T3 | `CostModel` 按标的口径计价（ETF 免印花税） | ✅ | `83e9aec` |
| T4 | lab 应用「查看详细」分层 | ✅ | `4c02e25` |
| T5 | 收尾：ETF 现价与来源日期 / verify.sh / 记录 | ✅ | 本提交 |

## T1 红灯根因（systematic-debugging）

- **现象**：`test_load_bars_adjusted_is_pit_when_future_events_are_stored`
  报 `sqlite3.IntegrityError: UNIQUE constraint failed: instruments.code`。
- **根因**：`_seed_db` 被**同一个 `tmp_path`** 调用两次（该测试要对比
  「带未来事件的库」与「不带未来事件的库」），上一轮新增的裸 `INSERT instruments`
  不幂等，第二次撞主键。同函数里 `insert_bars` / `insert_corp_actions` 本就幂等，
  所以只有新加的那行炸。
- **修法**：改走项目自己的幂等入口 `repo.upsert_instruments`
  （`ON CONFLICT(code) DO UPDATE` 四列，`test_adjust_blackout.py` 已在用）。
  **未改任何断言、未跳过、未弱化。**

## T5 验证证据（真实命令输出）

```
$ .venv/bin/python -m pytest            → 1321 passed in 85.80s
$ bash scripts/verify.sh                → ✅ 验证通过: all（工作区干净）

$ 已登记标的： ['000333','510300','510880','512890','518880','600690']
  000333: 现价 87.23  来源 snapshot 来源日期 2026-09-15
  510300: 现价 4.523  来源 bars     来源日期 2026-09-15
  510880: 现价 3.382  来源 bars     来源日期 2026-09-15
  512890: 现价 1.196  来源 bars     来源日期 2026-09-15
  518880: 现价 8.829  来源 bars     来源日期 2026-09-15
  600690: 现价 21.17  来源 snapshot 来源日期 2026-09-15
```

真起服务 `lab serve` 后 `curl /lab/`：8 个结论块、6 个「查看详细」入口，
「怎么做」居首；纪律摘要「1 条不通过：000333 占总资产 43.53%…」；
口径与限制「已登记 6 只标的，510300/510880/512890/518880 不可复权
（事件源不可见，ADR-008）—— 只用于估值展示」。

## 遗留问题（未做，明写）

1. **ETF 除权除息事件仍拿不到**（腾讯通道 2000 根 K 线内 0 条事件行）——
   ADR-008 只是把它变成**显式异常**，没有解决它。ETF 因此不能进
   `backtest`/`features`/`predict`/`verify`/`risk` 复权链，
   分红收益无法进入任何回测收益曲线。
2. 计划 Task 2 Step 3 的 **CLI 留痕**未做：`ingest actions` / `adj rebuild`
   尚未对 ETF 显式跳过并写 `system_events`（读/写层已硬拒绝，命令不会静默出错，
   但缺「跳过并留痕」的友好路径）。
3. 计划 Task 3 的 **`cost quote` 子命令**与 `risk/sizing.py`、`risk/panel.py`
   按标的取模型未做 —— `CostModel.asset_class` 已就位，接线未做。
4. `docs/decisions/README.md` 索引仍缺 ADR-006 / ADR-007 两行（**本轮之前就缺**，
   属既有遗漏，未在本轮顺手补）。
