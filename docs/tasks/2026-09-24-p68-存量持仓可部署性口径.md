# P68 敞口上界不许假设「picks 里的存量能减仓」（两臂同型：随机臂 v3 ＋ A1 v1.0.4）

创建：2026-09-24（nanobot 需求澄清＋实测；用户当日要求「你自己看下需要怎么优化」）
实现：Claude Code（superpowers 全流程）
来源：P66 站的**如实上报**（任务书 §6「残余只有这一种形状」）＋ nanobot 当日的代码走链与真库只读探针复核。
相关：ERROR_DIARY #72/#73（P64「偶然的副产物当闸门」）、#75（P66 的敞口上界）、
P65 任务书 §7.7-1（被拒 11/40 → 4/40）、**P66 任务书（本站要修的正是它留下的那一半）**。

---

## 0. 一句话

**同一条错口径**在随机臂与 A1 各写了一份：**把「存量持仓的市值」当成可部署资金**。

P66 只把**「不在 picks 里的存量」**扣了出来（那是对的一半）；**「在 picks 里、但目标市值低于现市值」的存量**
照样被当成「一减仓就变成现金」，而执行层是**整手**的：

> `plan_target_weight`：差额 < 1 手 ⇒ **不动**（`rules.py` 的 `C_LOT` 分支）。

于是「计划里那 42% 现金会释放出来」这件事**在执行层不成立**，计划总额超出现金 ⇒ `_gate_cash` fail-closed
整轮拒绝（**没有一笔透支落地**，闸门是对的；错在生成器的口径）。

**修法（两臂同款、一句话）：`reserved` 一律 = 全部存量持仓占比之和，不因 picks 而减免。**
= 生成器**不依赖任何一笔减仓**也能成交 —— 这正是 A1 自己 docstring 写的那句话
（「A1 **只能部署真正空闲的钱**」，v1.0.3 的 step 3 恰好违背了它）。

---

## 1. 实测证据（nanobot 2026-09-24，真库只读 ＋ 代码走链）

### ① 随机臂 seed 97：目标敞口 83.79%、`reserved=0.0`、`cap=90.0` ⇒ 被拒 `cash=-¥657.06`

真库只读探针（账本状态＝`arm-agent-random` @ 2026-09-23；现金 ¥11,320.00、存量 `000333` 100 股）：

```
seed=97  exposure=83.79  reserved=0.0  cap=90.0  verdict=cash_reject
arm-agent-random 在 2026-09-23：账上可用现金 ¥11,320.00，本轮需要 ¥11,977.06，
成交后现金 ¥-657.06（越界 ¥657.06）—— … **整轮拒绝、零写入**。code=cash
```

seed 97 的 picks ＝ `000333(10.6%) / 000651(23.72%) / 002508(12.38%) / 601398(37.09%)`：
`000333` 现市值 ¥8,247.00、目标 ¥2,074.10、差 ¥6,172.90 **< 一手 ¥8,242.88** ⇒ 执行层「不动」
⇒ 那 42.15% 的存量市值**一分钱都不会变成现金**，而 cap 公式把它当成了可部署的钱。
**P66 的 200 种子整体读数＝被拒 1/200**（旧口径 v1 是 11/40）；本站判据要求 **0/200**。

### ② 机制链（为什么闸门是对的、生成器是错的）

| 环节 | 位置 | 行为 |
|---|---|---|
| 逐笔规划 | `paper/rules.py::plan_target_weight` | 卖出：`floor_lot(差额/成交价)`；`qty<=0` ⇒ **hold「差 < 1 手 → 不动」** |
| 组计划 | `paper/agent_decide.py::plan_orders` | 每笔都用**同一份初始 `cash`** 规划（各自不超现金，**合计可以超**）【纯函数，不结算】 |
| 结算 | `paper/agent_decide.py::_settle` | 先卖后买逐笔结算到状态上 |
| 闸门 | `paper/agent_decide.py::_gate_cash` | `cash_after < -CASH_TOL` ⇒ `CashShortfall`，**整轮拒绝、零写入**（Q1-A / P65） |

⇒ 「生成器算得乐观 → 闸门拒」这条链**是设计如此**（P64/#73 的结论：偶然的副产物不能当闸门，
但闸门也不许替上游兜底）。**所以要在生成器侧把口径改对，不是放松闸门。**

### ③ A1 v1.0.3 的 step 3 ＝ 同一个假设（源码原文）

`stocklab/m2/builtin/a1_pick.py`（`SOURCE` 字符串常量）：

```
3. **一次细化**：`reserved1 = Σ over holdings whose code ∉ picks`（picks 里的
   存量**本来就持有**，不该再占它的额度）；`w1 = round(min(CAP_PCT,
   ((100 − CASH_FLOOR) − reserved1) / n), 2)`。若 `w1 > w0` ⇒ 用 `w1` 重跑
   一次 affordability 并取最终 `(S, w1)`；否则取 `(S, w0)`。**最多两轮，不迭代到不动点**。
```

`w1 > w0` 只在「picks 含存量票」时发生 ⇒ **A1 与随机臂同型**（同一账户上实测：
`000333` 100 股占 42.15%，一手 ¥8,242.88，任何 `0 < 目标 < ¥8,247.00` 都是「减不动」）。
09-24 生产那轮 `m2_a1(1.0.3)` 恰好没有选中存量票（`held_out_pct 42.2007`、3 只 15.93%），
所以**这一枪还没响**；响的那天 = 计划越界 ⇒ `m2_daily` exit 4 ⇒ `ops close` exit 2（G 级红）。

---

## 2. 口径（实现必须逐条对上）

### T1 随机臂：敞口上界口径 v3

- `stocklab/paper/agent_decide.py`：`reserved` **不再排除 picks 里的存量** ——
  `_reserved_pct` 的 `picks`/`exclude_codes` 参数**删掉**（不许留一个恒不被使用的参数：
  `tests/test_source_no_dead_code.py` 的教训）；
  `cap = round((100.0 - RANDOM_CASH_FLOOR) - reserved, 2)`、`exposure = rng.uniform(0, max(0, cap))` 不变。
- `stocklab/paper/config.py`：`RANDOM_CASH_FLOOR` 数值**不动**（=10.0），注释补一句 v3 的理由。
- 报告/标签文案：`arm-agent-random` 那段改成 **v3** 的说法，并把「P66 只修了一半」写进同一句。
  载荷**键只增不减**：`reserved` / `cap` / `exposure` 的含义不变，只改算法与文案。
- `cli/main.py` 里 P66 加的说明文本同步（若引用了「不在 picks 里的」这个限定语）。

### T2 A1：`m2_a1` v1.0.4 = 删掉 step 3（细化）

- `stocklab/m2/builtin/a1_pick.py` 的 `SOURCE`：`reserved = Σ over **全部** ctx["holdings"].weight_pct`；
  **删除** `reserved1` / `w1` 整段（连同那段注释），保留：
  - `n` 从 `min(LIMIT_N, 去重后只数)` 往下的下降式循环 ＋ `_affordable` 过滤（**v1.0.2 的修法不动**）；
  - `total_assets is None` / 任一 holding 缺 `weight_pct` ⇒ `reserved = 0.0` 的**退化口径逐位不变**；
  - `w <= 0` ⇒ 空 picks ＋ `cash_pct=100.0` 的分支；
  - `LIMIT_N` / `CAP_PCT` / `CASH_FLOOR` / `LOT` **数值一个字都不改**。
- 源码 docstring 加 **v1.0.4** 段：写清「step 3 为什么被删」（整手执行层兑现不了那个假设），
  并点名 v1.0.1 的去重、v1.0.2 的 affordability、v1.0.3 的 reserved **都保留**。
- **本站不碰库**：`plugin submit/approve` 由 nanobot 在验收后做（`--version 1.0.4`）。

### T3 读数断点（必须写进实施记录）

- 生效日 = rollout 日（由 nanobot 定）。台账 `m2_channel_runs.plugins_json` **每天记 `script_id`/`source_sha256`**
  ⇒ 版本边界可从台账自动分段；随机臂的敞口口径断点写进 `/lab/paper` 与运维手册的同一处。
- **周期一旦开跑，脚本版本不许再动**（`validation_cycles` 目前**空表**，所以现在改是免费的；
  这正是本站值得现在做、而不是等开跑后再做的理由）。

---

## 3. 判据汇总（可证伪）

1. **200 种子探针（真库只读，与 P66 同款）：被拒 `0/200`**（v2 是 `1/200`）。
   探针形状照抄 `/tmp/randarm_probe.py`（`random_payload` → `rebase_payload` → `execute_decision`，
   每次传**全新**的 `positions` dict）。**原文贴进实施记录**。
2. **反例钉住（最硬的一条）**：seed 97 在 v3 下 `reserved == 42.15`（±0.01）、`cap == 47.85`（±0.01）、
   `execute_decision` **不抛 `CashShortfall`**、`cash_after ≥ 0`；并**同时**断言 v2 的读数（`cap 90.0`
   ⇒ 被拒 ¥657.06）—— 两版并排写进用例注释，证明修的是这一处。
3. **A1 反例钉住**：用**真库 09-24 的账户形态**（`000333` 100 股 ＋ 3 笔新仓、现金 ¥2,925.72、
   总资产 ¥19,565.72）造 ctx，令 `picks` 含 `000333` ⇒ `run()` 的权重按 `reserved(全部存量)` 算；
   再走 `plan_orders + execute_decision` ⇒ **无 `CashShortfall`**（v1.0.3 在同输入下会生产出一份越界载荷）。
4. **黄金表**：随机臂 `k` / `picks` **逐位不变**（只动敞口）；A1 在「picks 不含存量票」的日子
   **v1.0.3 与 v1.0.4 的输出逐位相同**（此时 `reserved0 == reserved1`，两版必须一致）。
5. **全量回归**：`.venv/bin/python -m pytest -o addopts=""` 只增不减（开工先记基线，写明当前值）；
   `bash scripts/verify.sh` exit 0；红线三目标 == `0acf35c9…`/175、`73ffbfb1…`/1330、`ddac0a88…`/171
   （**严禁 regen**）；真库 sha 与开工一致或差异能归因到 launchd 的 patrol/close。
6. **提交只用显式路径**（禁 `git add -A`）：`stocklab/paper/agent_decide.py`、`stocklab/paper/config.py`、
   `stocklab/m2/builtin/a1_pick.py`、`stocklab/cli/main.py`（若需）、`tests/<改动或新增的用例>`、
   `docs/tasks/2026-09-24-p68-*.md`、`docs/errors/ERROR_DIARY.md`。

---

## 4. 反目标

- ❌ **不许放松闸门**：`_gate_cash` 的两条判据、`CASH_TOL` / `EXPOSURE_TOL`、`CashShortfall` 的
  fail-closed 语义一个字不改；也不许改成「等比缩小目标市值」（P65 明文：夹紧会让机制不可见）。
- ❌ 不许把 `plan_orders` 改成「逐笔滚动扣现金再规划」（那是把执行层的整手规则搬进写入口 ⇒ 两处口径）。
- ❌ 不动 `RANDOM_CASH_FLOOR` / `CAP_PCT` / `CASH_FLOOR` / `LIMIT_N` / `LOT` 的数值；不动成本口径与滑点。
- ❌ 不动 `arm-agent`（AI 臂）的决策通路、`paper_agent_decisions` 台账、D-34–D-38 的任何决定。
- ❌ 真库 `data/stocklab.db` **只读**：只允许不带 `--fix` 的 `ops patrol`；`ingest *`、`--fix`、
  任何写库动作只在 `/tmp` 副本上做；**不许** `plugin submit/approve`（nanobot 的 rollout 动作）。
- ❌ 不许「删掉 step 3 就完事」而不写判据 2/3 的反例用例 —— 没有反例的修复等于没修。

---

## 5. ERROR_DIARY 要加的一条（本站必须写）

> 题目自拟，必须覆盖三句教训：
> ① **修一处 ≠ 修一类**：P66 扣掉了「不在 picks 里的存量」，却把「在 picks 里但**减不动**的存量」
>    留在了公式里 —— 同一条错假设换了个分支继续存在（旧账：P64 的「偶然副产物当闸门」同族）。
> ② **生成器里的数必须由执行层能兑现的粒度决定**：写入口按**连续值**算钱，执行层按**整手**动手，
>    两边的粒度差就是一个稳定的透支缺口（本仓还有第二个同型处：`plan_orders` 用同一份初始现金
>    逐笔规划，各自不超、合计可超）。
> ③ **判据要拿「反例种子」钉，而不是拿「平均表现」看**：1/200 的概率在 120 个交易日里足够上演一次，
>    而它上演的那天整条收盘链会红、AI 臂会白白少一天。

---

## 6. 实施记录（由实现站填写）

> 逐条贴实测输出（命令 ＋ 原文），**不许**出现「应该可行」「大概够」。判据 1/2/3 三条**必须**给原文。

### 6.0 开工基线与只读纪律

```
$ shasum -a 256 data/stocklab.db
331a7efe4f574d6e8b750bca22652e2b87b9c4d7994c23503c6398f33580c2c2  data/stocklab.db     ← 开工
（完工复核：同一个 hash，未变）

$ .venv/bin/python -m pytest -o addopts=""
3002 passed, 2 skipped in 196.57s (0:03:16)                                        ← 开工基线
```

真库全程只读（`sqlite3.connect("file:…?mode=ro", uri=True)`）；`ops patrol` 只跑过
不带 `--fix` 的形态；**没有** `plugin submit/approve`、**没有** `ingest *`、**没有** `--fix`。
唯一一次写库是把真库 `cp` 到 `/tmp/p68_probe.db` 之后在副本上做的。

### 6.1 判据 1：真库只读探针 200 种子 ⇒ 被拒 **0/200**（形状照抄 `/tmp/randarm_probe.py`）

改动**前**（口径 v2 ＝ 当前 HEAD）——建立对照，证明「1/200」确实复现得出来：

```
$ PYTHONPATH=. .venv/bin/python /tmp/randarm_probe.py 200 2026-09-23
asof=2026-09-23 cash=11320.0 positions={'000333': 100} total_assets=19567.0 pool=11 只
存量持仓已占总资产 42.15%（可用现金 57.85%）

seeds=200  被闸门拒=1 (0.5%)  通过=199 (99.5%)  其它错=0
被拒的 seed（前 12 个）： [97]
被拒者的目标敞口(cash_pct 越小越激进)： [83.79] …
样例： (97, 16.21, 4, 'arm-agent-random 在 2026-09-23：账上可用现金 ¥11,320.00，本轮需要 ¥11,977')
通过者的目标敞口： [0.06, 0.21, 0.65, 1.41, 2.64, 2.65, 3.54, 3.67] …
```

改动**后**（口径 v3 ＝ 本站）：

```
$ PYTHONPATH=. .venv/bin/python /tmp/randarm_probe.py 200 2026-09-23
asof=2026-09-23 cash=11320.0 positions={'000333': 100} total_assets=19567.0 pool=11 只
存量持仓已占总资产 42.15%（可用现金 57.85%）

seeds=200  被闸门拒=0 (0.0%)  通过=200 (100.0%)  其它错=0
通过者的目标敞口： [0.06, 0.21, 0.65, 0.75, 2.06, 2.64, 2.65, 3.54] …
```

**被拒 1/200 → 0/200**（判据要求 0/200 ✅），「其它错」两版都是 0。

### 6.2 判据 2：seed 97 两版并排（反例钉住）

同一条只读探针（`/tmp/p68_seed97_probe.py`，真库只读），改动前后各跑一次：

**v2（改动前）—— 与任务书 §1① 逐字相同：**

```
$ PYTHONPATH=. .venv/bin/python /tmp/p68_seed97_probe.py        （改动前）
picks: [('000333', 10.6), ('000651', 23.72), ('002508', 12.38), ('601398', 37.09)]
reserved = 0
cap = 90
exposure = 83.79
cash = 11320.0  total_assets = 19567.0  positions = {'000333': 100}

VERDICT: CashShortfall  code=cash
arm-agent-random 在 2026-09-23：账上可用现金 ¥11,320.00，本轮需要 ¥11,977.06，
成交后现金 ¥-657.06（越界 ¥657.06）—— 这笔钱不在账上，买它就是借钱（杠杆）。
**整轮拒绝、零写入**：不夹紧、不缩单、不部分成交。code=cash
```

**v3（改动后）：**

```
$ PYTHONPATH=. .venv/bin/python /tmp/p68_seed97_probe.py        （改动后）
picks: [('000333', 5.64), ('000651', 12.61), ('002508', 6.58), ('601398', 19.72)]
reserved = 42.1475
cap = 47.85
exposure = 44.55
cash = 11320.0  total_assets = 19567.0  positions = {'000333': 100}

VERDICT: 未被拒  cash_after=8077.37
```

`reserved == 42.15`（±0.01 ✅）、`cap == 47.85`（±0.01 ✅）、不抛 `CashShortfall` ✅、
`cash_after == 8077.37 ≥ 0` ✅；`picks` 与 v2 **逐位相同**（判据 4 的黄金表）。
单测 `tests/test_paper_agent_decide.py::test_p68_seed_97_counterexample_is_pinned_in_both_versions`
把两版并排钉住（v2 那半边断言 `cap==90.0`、`越界 ¥657.06`、`¥11,977.06` 三条原文，
用的是 **11 只真池 ＋ 真收盘价** 的夹具 —— 池子不同 `rng.sample` 的 `usable` 就不同，
seed 97 的 picks 也就复现不出来）。

### 6.3 判据 3：A1 v1.0.4 反例钉住（真库 09-24 账户形态）

真库只读取出的形态（`engine.arm_state_for(conn, "arm-agent-v1", "2026-09-24")`）：

```
arm-agent-v1 @ 2026-09-24: cash=2925.72 positions={'000333': 100, '603868': 100,
'600900': 100, '601398': 300} mv=16640.0 total=19565.72
```

与任务书 §3 判据 3 的「`000333` 100 股 ＋ 3 笔新仓、现金 ¥2,925.72、总资产
¥19,565.72」逐字相同。

**3a 同形态下两版的输出（`/tmp/p68_a1_probe.py`，冻结的 v1.0.3 副本 vs 现役）：**

```
reserved(全部存量) = 85.0466
cap(全部存量) = 4.9534

v1.0.3: {"picks": [{"code": "601398", "weight_pct": 17.42, …}], "cash_pct": 82.58}
  trace: ['n=5 w0=0.99', 'n=4 w0=1.24', 'n=3 w0=1.65', 'n=2 w0=2.48', 'n=1 w0=4.95',
          '  reserved0=85.0466 reserved1=72.5809 w0=4.95 w1=17.42']
v1.0.4: {"picks": [{"code": "601398", "weight_pct": 4.95, …}], "cash_pct": 95.05}
```

v1.0.4 的 `reserved` ＝ **全部**存量 85.0466（不再因 picks 减免）⇒ `cap = 4.9534`
⇒ `w = 4.95`；v1.0.3 的 `reserved1 = 72.5809`（把 picks 里的 `601398` 排除了，
少算 12.4657）⇒ `w = 17.42` > `cap` —— **那一份就是「越界载荷」**。
两版各自走 `plan_orders` ＋ `execute_decision`：v1.0.4 无 `CashShortfall`
（`cash_after ≥ 0`）。

> **⚠️ 如实记录一处与判据字面的差异（判据未改，照实写）**
>
> 判据 3 写「令 `picks` 含 `000333`」。在 09-24 这个账户上**做不到**：
> `_affordable` 要求目标市值 ≥ 一手市值，`000333` 一手 ＝ ¥8,265 ⇒ 需要
> `w ≥ 42.2422%`；而 `w ≤ min(CAP_PCT, cap/n) ≤ CAP_PCT = 25%` ⇒ 上限 ¥4,891.43
> **< ¥8,265** ⇒ 任何 `n` 都选不中它。同一账户上真正进了 picks 的存量票是
> `601398`（占 12.4657%），机制完全一样 —— 所以 3a 用它把那一枪钉住。
> 另：v1.0.3 在 09-24 这个形态上**没有真的透支**（`601398` 是加仓：目标
> ¥3,408.14 > 现市值 ¥2,439.00 ⇒ 执行层只买增量一手＝¥813，账上 ¥2,925.72
> 够）—— 与 §1③「这一枪还没响」逐字一致。**「越界载荷」成立、「实际透支」不成立**，
> 两件事都写进用例。
>
> `000333` 那种「进了 picks 的存量票」在**能兑现**的账户上确实会透支 —— 见 3b。

**3b 判据 3 的另一个分支：v1.0.3 真的越界、v1.0.4 不越界**（真库之外的合成形状，
总资产 ¥40,000、`C05 ×400 @29.26（29.26%）` ＋ `C01 ×500 @15.74（19.675%）`、
可用现金 ¥20,426.00 ＝ 51.065%）：

```
v1.0.3: picks 5 只 × 18.0%  ⇒ Σw = 90.0%，cash_pct = 10.0
        （reserved0=48.935 ⇒ w0=8.21；picks 含两笔存量 ⇒ reserved1=0 ⇒ w1=18.0）
v1.0.4: picks 5 只 × 8.21%  ⇒ Σw = 41.05%，cash_pct = 58.95
```

执行层：v1.0.3 买单一共 **¥21,678.05 > ¥20,426.00** ⇒ `CashShortfall`
（`code=cash`，越界 ¥1,252.05）；v1.0.4 无异常（成交后现金 ¥21,616.95）。
两个分支都进了 `tests/test_m2_builtin.py::test_p68_t3_a1_v104_*`。

**量化旁证**（4000 次随机账户形态的对照扫描，参数随机、种子固定 7）：
v1.0.3 越界 **74/3317**、v1.0.4 越界 **0/3317**。

### 6.4 判据 4：黄金表（只动敞口，不动抽取）

- 随机臂：`test_p68_the_residual_overdraw_is_gone_and_only_that_changed` 对 200 个种子
  逐个比对现役实现与 v2 冻结副本的 **`picks` 逐位相同**（权重不逐位相同 —— 权重 ＝
  敞口 × rand/Σrand，敞口那一档本来就该变）；
  `test_p66_the_draw_sequence_is_unchanged_from_the_old_cadence` 的黄金表
  （`k` / `codes`）**一字未改**仍绿。
- A1：`test_p68_t2_a_holding_inside_the_picks_is_still_reserved` 与判据 3 的
  「picks 不含存量票」形态都做了 v1.0.3/v1.0.4 对拍；`holdings` 为空 /
  `weight_pct` 缺失 / `total_assets is None` 三条退化路径的既有用例
  （`test_p65_t2_no_holdings_is_bit_identical_to_v101` 等）**未改动且仍绿**。

### 6.5 判据 5：全量回归 ＋ 红线

```
$ .venv/bin/python -m pytest -o addopts=""           （开工）
3002 passed, 2 skipped in 196.57s (0:03:16)

$ .venv/bin/python -m pytest -o addopts=""           （完工）
3005 passed, 2 skipped in 179.07s (0:02:59)          ← 只增不减（净 +3）

$ bash scripts/verify.sh                             （完工）
  ✅ predict_synthetic                    sha256 0acf35c90f54ab6a… == 基线；叶子 175 项一致
  ✅ predict_real_2026-09-14              sha256 73ffbfb136e8620d… == 基线；叶子 1330 项一致
  ✅ backfill_real_2013-12-23_2026-09-14  sha256 ddac0a889c623d75… == 基线；叶子 171 项一致
✅ 回归红线全部成立
✅ 验证通过: all
verify.sh EXIT=0
```

三目标逐位对得上（`0acf35c9…`/175、`73ffbfb1…`/1330、`ddac0a88…`/171），
**没有 regen**。真库 sha 与开工一致（`331a7efe…`）。净 +3 的来源：
新增 4 条（`test_p68_seed_97_counterexample_…`、`test_p68_every_holding_is_reserved_…`、
`test_p68_t3_a1_v104_reserves_…`、`test_p68_t3_a1_v104_prevents_…`）
− 删除 1 条（`test_p66_the_locked_code_inside_picks_is_not_reserved`，
它的语义已被 `test_p68_every_holding_is_reserved_regardless_of_picks` 反向覆盖）。

### 6.6 反目标核对（逐条）

| 反目标 | 核对 |
|---|---|
| 不许放松闸门 | `_gate_cash` / `CASH_TOL` / `EXPOSURE_TOL` / `CashShortfall` **一个字未改**；`test_p66_the_gate_still_rejects_an_old_cadence_payload`（旧口径载荷仍被拒、`old_rejected == 11/40` 逐 seed 钉住）与判据 3 直接喂 `_gate_cash(cash_after=-1.0)` 的反向自检均绿 |
| 不许把 `plan_orders` 改成滚动扣现金 | `plan_orders` / `_settle` 未改动 |
| 不动常数数值 | `RANDOM_CASH_FLOOR` / `CAP_PCT` / `CASH_FLOOR` / `LIMIT_N` / `LOT` ＝ 10.0 / 25.0 / 10.0 / 5 / 100（`test_p65_t1_…` 逐位断言），成本与滑点未动 |
| 不动 AI 臂决策通路 / 台账 / D-34–D-38 | 未触碰 |
| 真库只读 | sha 未变；写库动作只在 `/tmp` 副本 |
| 不许「删掉 step 3 就完事」 | 判据 2（seed 97 两版并排）＋判据 3a/3b 三条反例用例已补 |

### 6.7 口径断点（T3）

- **生效日 ＝ rollout 日**（由 nanobot 定）。本站**只改源码**：`plugin submit --version 1.0.4`
  与 `approve` 是 nanobot 验收后的动作，本站一次都没跑（真库 `plugin_scripts` 未变）。
- 台账可自动分段：`m2_channel_runs.plugins_json` **每天记 `script_id`/`source_sha256`**
  ⇒ 版本边界从台账里读得出，不必靠日期猜。
- 断点写在**同一处**三份文案里：`paper/agent_decide.py::random_payload` 的
  `rationale`（进 `paper_agent_decisions.rationale`，`口径 v3（P68）` ＋
  「不因 picks 而减免」）、`paper/config.py::RANDOM_CASH_FLOOR` 的注释、
  `cli/main.py::cmd_paper_agent_random` 的 `note`。
- **周期一旦开跑脚本版本不许再动**：`validation_cycles` 目前空表 ⇒ 现在改是免费的，
  这正是本站值得现在做的理由。
- 口径 v1/v2 的历史行**原样保留**（append-only），`decision_id=1` 一行未改。

## 7. 进度

| # | 任务 | 状态 | 验证 |
|---|------|------|------|
| T1 | 随机臂敞口上界口径 v3（reserved = 全部存量） | ✅ 已验证 | 判据 1（0/200）、判据 2（seed 97）、判据 4；`test_p68_*` 全绿 |
| T2 | `m2_a1` v1.0.4 删掉 step 3（细化） | ✅ 已验证 | 判据 3a/3b、`test_p68_t2_*`；`SOURCE` 无 `reserved1`/`exclude_codes` |
| T3 | 读数断点写进实施记录与报告文案 | ✅ 已验证 | §6.7；三处文案（rationale / config / cli note） |
| T4 | 判据 1/2/3 的反例用例 ＋ 全量回归 ＋ ERROR_DIARY ＋ 显式路径提交 | ✅ 已验证 | 3005 passed/2 skipped；verify.sh exit 0；红线三目标未 regen；ERROR_DIARY #77 |

> 状态：⬜ 未开始 / 🟡 进行中 / ✅ 已验证 / ❌ 失败
