# P14 任务记录（Task 61-65）：风险与仓位工具箱

- **日期**：2026-09-15
- **计划**：`docs/plans/2026-09-15-p14-风险与仓位工具箱.md`
- **状态**：代码全部落地并测试；**文档与面板并入由 P15 补记**（原因见 §4）
- **缺口**：本文件是**事后补写**的 —— P14 会话在写文档这一步撞上 `--max-budget-usd`
  上限被中断（与 ERROR_DIARY #11 同一形态）。代码当时已落盘并提交
  （`e8c6241` / `3d7955c` / `5198027`）。

## 1. 交付物与验证

| 任务 | 产物 | 测试 | 状态 |
|---|---|---|---|
| T61 单文件看板 | `stocklab/dashboard/html.py` | `tests/test_dashboard_html.py` | ✅ |
| T62 只读本地服务 | `stocklab/dashboard/server.py` | `tests/test_dashboard_server.py` | ✅ |
| T63 凯利核心 | `stocklab/risk/kelly.py` | `tests/test_risk_kelly.py` | ✅ |
| T64 仓位裁剪 | `stocklab/risk/sizing.py` | `tests/test_risk_sizing.py` | ✅ |
| T65 风险预算指标 | `stocklab/risk/metrics.py` | `tests/test_risk_metrics.py` | ✅ |
| T65 止损位并列 | `stocklab/risk/stops.py` | `tests/test_risk_rules.py` | ✅ |
| T63 规则回放 | `stocklab/risk/rules.py` | `tests/test_risk_rules.py` | ✅ |
| **step4 面板并入** | `stocklab/risk/panel.py` | `tests/test_risk_panel.py` | ⏳ **P15 补** |

## 2. 真实验证证据（P14 已跑）

```
$ .venv/bin/python -m stocklab.cli.main risk kelly --code 000333 --asof 2026-09-14
结论: NO_BET（不下注）
理由: edge_not_significant
  保守胜率 p_used = 0.2838  ≤  盈亏平衡 p_be = 0.7223（b_used = 0.3843）
  有效样本（交易日）110 / 门槛 120 → 样本不足，仅供观察
```

**两条门都没过**（样本不足 **且** edge 不显著），页面必须原样显示，不许美化。

## 3. 口径定稿

见 ADR-007（p/b 只来自 PIT 回放；Wilson 下界；四分位保守 b；扣成本；
按日聚类 CI；样本门 120；两道门分开报；`NO_BET` 是合法结论）。

## 4. 欠账与取舍

1. **`risk.panel` 未接入**：`dashboard/summary.py` 的 docstring 早已声明
   `risk` 段来自 `stocklab.risk.report.build_risk_block`，但**该模块当时没写**，
   于是 `build_summary()` 的 `risk` 恒为 `None`，页面永远渲染
   「风险面板本轮未接入」。
   → P15 补：新建 `stocklab/risk/panel.py::build_risk_block()`，并让
   `dashboard build` 与 Web 页面**共用**它（一个真相来源）。
   注：原计划里的模块名 `risk.report` 与既有 `stocklab/verify/report.py`
   容易混，落地时取名 `risk/panel.py`，并同步改 summary 的 docstring。
2. **本文件与 ADR-007 迟到**：机制在 `kelly.py` 的 docstring 里是完整的
   （`## 输入只许来自回放统计`），但没有正式的 ADR 与任务记录。
3. **`risk/__init__.py` 的 `__all__` 未含 panel**：P15 补入。

## 5. 教训

预算纪律：**收尾步骤（写文档 / 写索引 / 提交）的成本要从实验成本里预留**，
不能把全部预算花在「跑通」上 —— 跑得再好，不交付就是白跑（ERROR_DIARY #11）。
