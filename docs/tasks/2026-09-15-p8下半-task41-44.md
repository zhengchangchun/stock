# P8 下半（Task 41–44）任务记录

- **日期**：2026-09-15
- **起点**：HEAD `f07f584`，工作区干净，`pytest` 720 passed，`verify.sh` ✅
- **预算**：$7（实际见收尾）

## Task 41 · 口径诚实性修正（只改标注，不改数字）✅

| 项 | 内容 |
|---|---|
| 改了什么 | `verify/report.py::_baselines` 的 `index_300.note` 改写 + 新增 `pit_comparable`；`render_markdown` 输出两条 caveat；计划文档那两句作废并加更正块 |
| **没改什么** | 任何计算 / 分母 / 阈值 / split / 样本区间；报告里已有的数字**一个都没变** |
| 钉住 | `test_index_baseline_is_labelled_contemporaneous_and_untradable`（note 与 markdown 双查） |
| 红线 | `predict run --asof 2026-09-14` sha256 仍 `a61d026b…f4eb`（实测复跑一致） |

**为什么**：`index_300` 用的是 `index[target]/index[asof]-1`，与个股被预测窗口**同一段** ——
同期共动，含未来信息、不可交易。原计划里「领先模型 13.8pp / 跑不赢最蠢的动量基线」
是**把同期读成领先**。可比口径的可比对手是 `index-mom-dir` / `always_up` / `always_down`。

## Task 42 · 异质子群诊断（只诊断，不改策略）✅

结论 **选 (b)**：`invalidate_if` 的命中条件是**次日收盘的确定性函数**。
- 复现 665/6038 → 66.92% vs 34.58%；
- `close_asof` 按构造落在 `[support, resistance]` 之间 → 突破行 369/382 是 `up`、
  破位行 268/283 是 `down`（命中子群 flat 仅 4.06%）；
- **决定性检验**：PIT 可观测的「边界最窄 10%」选行 → 37.48%（全体 38.14%，无差别）；
  反向对照「最宽 20%」→ 42.42%。**无任何 PIT 选行规则能复现 66.9%**。

产物：`docs/diagnostics/2026-09-15-invalidated-subgroup.md`（含全部中间量）、
`scripts/diagnose_invalidated_subgroup.py`（只读可复现）、`ERROR_DIARY.md` #15。
**本轮未改任何口径** —— 呈现口径的修正留作下一轮预注册实验。

## Task 43 · 实验决策落库 ✅

`experiment_decisions`（append-only，迁移 + 前滚 + 触发器 + 单测）。
- 粒度 `(variant_id, split, metric)`，`metric ∈ {direction, brier}`；
  `gate_status` 是**分段级**（gate 同时看两个指标），同段两行共享；
  `decision` 是整场 verdict；
- 幂等键 `(variant_id, split, metric, metric_version, report_sha256)`：
  同报告重跑 → `identical`；换区间/配置 → 新 sha256 → **追加**；同键内容不同 →
  抛 `DecisionConflict`（不覆盖）；整批单事务（不留部分写入）；
- **只落决策不落预测**：变体预测仍一行不写生产表（有测试钉住行数不变）。

真实库证据（`experiment run --variant rw-mu0 --from 2013-04-16 --to 2026-09-14`）：

```
rw-mu0 train    direction d=+0.0080 ci=[-0.0124,+0.0283] FLAT  falsified
rw-mu0 train    brier     d=-0.0060 ci=[-0.0096,-0.0024] FLAT  falsified
rw-mu0 validate direction d=-0.0376 ci=[-0.0806,+0.0053] LOSE  falsified
rw-mu0 validate brier     d=-0.0040 ci=[-0.0085,+0.0004] LOSE  falsified
```

落库 sha 与磁盘报告 `sha256_json` 一致；`predictions` / `verifications` 行数不变（6055/6055）。

## Task 44 · 第二批预注册变体 ⏸ 只预注册、不跑

见 `docs/experiments/2026-09-15-p8b-口径修正与变体准入.md`。
**依据**：Task 42 的结论是 (b)，而 `ForecastSpec` 的唯一旋钮是 `mu_mode` ——
诊断没有给出任何**值得预注册**的 `mu_mode` 变体，
第二轮变体的准入条件本身要先被预注册。
