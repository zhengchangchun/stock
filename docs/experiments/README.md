# 实验台账（append-only）

> 每一次「提出假设 → 改动 → 评估」都必须在这里留一条记录，**失败/被否证的实验同样要留**。
> 目的：让优化可追溯、可复现、不重犯。

## 规则

1. **单变量**：一条记录只改一个变量。同时改多处 → 无法归因 → 无效实验。
2. **样本外**：结果必须来自 walk-forward 验证窗；样本内数字只能作为附注。
3. **样本量门槛**：不足 120 个交易日的结论标注「样本不足，仅供观察」，不得据此改策略。
4. **扣成本**：净值曲线必须含佣金（最低 5 元/笔）、印花税、滑点。
5. **基准对照**：必须与 index_300 / buy_and_hold 对比。
6. **不许改口径救结果**：失败就写失败。
7. **不许删改历史记录**：结论被推翻时，追加新的记录引用旧的，不修改旧的。

## 命名

`YYYY-MM-DD-<short-name>.md`，从 `TEMPLATE.md` 复制。

## 台账（`experiment_decisions`）的两处口径注意

1. **幂等键取语义，不取呈现**（ADR-005）：键 = `(variant_id, split, metric,
   metric_version, gate_status, delta, ci_low, ci_high)`。措辞 / 时间戳 / 排版 /
   `report_sha256` 都不进键 —— 修报告里的一句话**不会**产生新决策行
   （以前会：ERROR_DIARY #17 曾把 `sigma-vol-z` 的 4 行刷成 8 行）。
2. **历史遗留：老键下的 4 行同语义重复（append-only，不清算）。**
   `variant_id='sigma-vol-z'` 有 8 行，其中 4 行 `report_sha256='a192a6d6…'`
   与另 4 行 `'c32941fc…'` **语义完全相同**（同一串闸门数字，只是报告文案改过一版）。
   这是 **P9-b 新键生效前**的产物，按 append-only 纪律**不删不改**。
   凡按行数统计决策条数的查询，请按上面的语义键去重（或排除老 sha 那批）。

## 索引

（每新增一条，在下面追加一行：日期 / 假设 / 结论 / 是否采纳）

| 日期 | 实验 | 假设 | 结论 | 采纳 |
|------|------|------|------|------|
| 2026-09-15 | [trend-state-hit-rate（双均线三态趋势标签的命中率与延续率）](2026-09-15-trend-state-hit-rate.md) | 趋势状态标签「未来 5 日方向」的条件命中率显著高于无条件基线，且状态具显著延续性 | **预注册·未跑**；探索性读数（全样本、非结论）：方向 Δ=+0.7pp（CI 含基线，标的上符号不一致）→ (a) 预期否证；延续率 68–74% vs 基线 30–35%（+17~45pp）→ (b) 预期成立 | ⏸ 待跑（预期：采纳为标签层/风险用途，不采纳为交易信号） |
| 2026-09-16 | [trend-state-hit-rate **正式版**（跑完上面那条预注册）](2026-09-16-trend-state-hit-rate-正式版.md) | 同上（口径未改） | **判据 (b) 成立 / 判据 (a) 否证**：validate 652 天（UP 延续率 53.57% vs 无条件 18.61%，Δ=+33.66pp CI[+27.70,+39.76]；DOWN 73.04% vs 38.50%，Δ=+32.04pp CI[+28.09,+35.67]）；**方向增量仍为负**（UP 命中率 32.42% vs 基线 37.87%，Δ=−3.26pp）。test 段（654 天）按达标规则打开一次，(b) 仍成立（UP 延续 70.35%、DOWN 70.66%）；框架记为 `WIN`（=判据 b 两段成立，**非**方向能力提升）。报告 sha256 `8098808f…7730`，两次运行逐位一致 | ✅ **采纳为标签层（状态描述 / 风险-波动条件化）**；❌ 不采纳为交易信号；❌ 判据 (a) 方向轴再确认否证 |

| 日期 | 实验 | 假设 | 结论 | 采纳 |
|------|------|------|------|------|
| 2026-09-15 | [trend_ma 默认参数 · walk-forward 样本外基线](2026-09-15-trend_ma-default-walkforward.md) | trend_ma 样本外总收益 > buy_and_hold | **否证**：510.79% vs 881.75%，输 370.96pp（2751 交易日，已扣成本） | ❌ 不采纳 |
| 2026-09-15 | [rw-mu0（把漂移 mu 置 0）](2026-09-15-rw-mu0.md) | `mu := 0` 提升样本外方向准确率、降低 Brier | **否证**（`falsified`）：validate 方向 **0.3310 vs 基线 0.3687**（Δ=-0.0376，CI 下界 ≤ 0，符号错）；Brier 改善落在噪声内。pre-registered gate 记 `LOSE` → **不打开 test** | ❌ 不采纳 |
| 2026-09-15 | [index-mom-dir（用沪深300 当日方向定漂移符号）](2026-09-15-index-mom-dir.md) | 指数 `asof` 方向对次日个股方向有信息量 | **否证**（`falsified`）：validate 方向 **0.3372 vs 基线 0.3687**（Δ=-0.0315，CI 下界 ≤ 0，符号错）；Brier 改善落在噪声内。pre-registered gate 记 `LOSE` → **不打开 test** | ❌ 不采纳 |
| 2026-09-15 | [P8 下半 · 口径修正与第二批变体准入](2026-09-15-p8b-口径修正与变体准入.md) | 把 `invalidated` 子群标注为「结果条件、不可交易」后，既有数字逐位不变，且读者不再把 66.9% 读成模型能力 | **口径修正部分已执行（2026-09-16，P24，提交 `e95361a`/`99a974a`）**：`invalidated` 块补 `note` + `pit_comparable` 纯文字字段 → 判据 F1 ✅（数字叶子 0 差异）；**判据 F2 ❌ 但非本轮所致** —— 该红线（`a61d026b…f4eb`）早在 2026-09-15 09:13/10:50（`be9c343`/`015c466` 给 `evidence.inputs` 加口径回显字段）已静默作废，现行实测 `b69afb71…`，归因见 ERROR_DIARY #34 + nanobot 独立复核（`reports/2026-09-15-predict-2026-09-14.json` mtime 2026-09-15 16:49 且 `grep -rn a61d026b` 全仓只命中 docs）；**判据 F3 未执行**（预注册明示本轮不新增变体） | ✅ 采纳（标注层）；变体准入部分 ⏸（无候选变体，F3 可证伪出口仍开） |
| 2026-09-15 | [sigma-vol-z（量能 z 分位条件化 sigma）](2026-09-15-sigma-vol-z.md) | 量能 z（Φ 映射）条件化 sigma 改善样本外校准度 | **否证**（`falsified`）：validate 方向 -0.0061（CI 跨 0）、Brier **+0.0060（显著变差）** → gate `LOSE`，不开 test | ❌ 不采纳 |
| 2026-09-15 | [sigma-rv-pct（自身 20 日 RV 分位条件化 sigma）](2026-09-15-sigma-rv-pct.md) | 自身 20 日 RV 分位条件化 sigma 改善样本外校准度 | **否证**（`falsified`）：validate 方向 -0.0108（CI 跨 0）、Brier **+0.0150（显著变差，三个里最差）** → gate `LOSE`，不开 test | ❌ 不采纳 |
| 2026-09-15 | [sigma-index-rv-pct（指数（沪深300）RV 分位条件化 sigma）](2026-09-15-sigma-index-rv-pct.md) | 沪深300 RV 分位条件化 sigma 改善校准，增量小于自身 RV 变体 | **否证**（`falsified`）：validate 方向 -0.0069（CI 跨 0）、Brier **+0.0116（显著变差，幅度确实小于自身 RV 变体）** → gate `LOSE`，不开 test | ❌ 不采纳 |
| 2026-09-15 | [resid-quantile（区间/概率的形状来源改取训练窗标准化残差经验分位）](2026-09-15-residual-quantile-interval.md) | 把 `Normal(mu,sigma)` 的形状假设换成训练窗残差经验分位，提升样本外 Brier，覆盖率不劣化 | **否证**（`falsified`，判据 (a) 未达）：Brier Δ=**-0.0017**，gate ci95 [-0.0048,+0.0014] 与按日 bootstrap [-0.0049,+0.0014] 两个估计量一致**跨 0**（符号对但不显著）；覆盖率副指标**成立**（\|cov−0.80\| 0.0195→**0.0003**，81.95%→80.03%）；方向 Δ=+0.0031（CI 跨 0）。651 交易日，`test_evaluated=false` | ❌ 不采纳 |
| 2026-09-17 | [P29 · 资金流 / 估值作为「次日方向先验」（预注册）](2026-09-17-valuation-moneyflow-oos.md) | ① `main_net` 符号（V1）/ ② `PE_TTM` 近 756 行分位（V2）对次日方向有信息量，能提升方向准确率、降低 Brier | **预注册 · 未跑**（判据：validate 段 `Δdirection` CI 下界>0 且 `Δbrier` CI 上界<0；不达 → test 不打开） | ⏸ 待跑（预期 `LOSE`/`FLAT`；V1/V2 各自独立判定） |
| 2026-09-17 | [P29 · 资金流 / 估值作为「次日方向先验」**（跑完）**](2026-09-17-valuation-moneyflow-oos.md)（同上文件 §4.2.1） | 同上（口径未改、未换窗口/阈值） | **两条各自独立判定，均 `LOSE`（falsified）→ test 封存段一次未开**。V1 `mf-dir-prior`（2013-04-16→2026-09-14，validate n=650）：方向 **0.3569 vs 基线 0.3687**，Δ=−0.0108 CI[−0.0450,+0.0235]（下界<0，**符号错**）；Brier Δ=+0.0029 CI[−0.0024,+0.0082]（跨 0 且符号错）。V2 `val-pe-pct-mu`（2018-01-02→2026-09-14，validate n=422）：方向 **0.3389 vs 基线 0.3472**，Δ=−0.0083 CI[−0.0499,+0.0334]（下界<0，**符号错**）；Brier Δ=−0.0049 CI[−0.0140,+0.0043]（跨 0）。覆盖：V1 用 4890/跳 8；V2 用 1866/跳 1470（756 行窗）。nanobot 亲跑复核 `pytest` **1635 passed** / `verify.sh` all ✅ / 红线三目标逐位不变 | ❌ **两个变体都不采纳**（撤规则生效，不进默认路径；默认 `mu_mode='sample_mean'`、`MODEL_VERSION` 未变） |
| 2026-09-16 | [P25 · 回归红线可执行化（把快照常量改成不变量）](../plans/2026-09-16-p25-回归红线可执行化.md) | 「报告是否被意外改动」这件事能被**自动**发现：字节级 sha + 数字叶子级键路径两档判据，且失败时告诉人怎么有意重新基线 | **已执行**（非实验，属基础设施；本轮**不改任何预测数字/口径/MODEL_VERSION**）：三条红线目标全绿（`predict_real_2026-09-14` sha `b69afb71…` 叶子 175 项；`backfill_real_2013-12-23_2026-09-14` sha `18f2eeef…` 叶子 89 项；`predict_synthetic` hermetic）。**基线取值**：`predict run --asof 2026-09-14` = `b69afb711f60ec533882bcc98b42c32c0503717a33708d8e74555c6a1a910bcb`；`verify backfill 2013-12-23→2026-09-14` md = `18f2eeeff7168d419506539e3f321c4c69e3eda3c1a73dc2fb5e1809b2bd9d0d`、summary JSON = `149f1094911bbca435f7cf5529c8ede03c6ff7e751abbf37ecdc12240c3b2ee6`（2026-09-16 实测；**旧值 `a61d026b…f4eb` 保留在 P8 plan 原处，已标注 2026-09-15 09:13/10:50 因 `be9c343`/`015c466` 加口径回显字段而作废**）。**发现**：测试套件存在全局策略注册表泄漏（`tests/test_strategies_evaluate.py` 注册 `tests_once_per_fold` 不清理），使 in-process 报告内容依赖执行顺序 —— 已用子进程隔离规避，见 ERROR_DIARY #35 | ✅ 采纳（防线）；变体/口径本轮不动 |

---

> 台账维护边界（2026-09-16 22:0x，nanobot）：上表 P25 那一行由 claude 在 commit `fad689e` 中越界追加（本目录约定由 nanobot 维护）。已核为**纯追加**（`git show --numstat fad689e -- docs/experiments/README.md` → `1 0`）、内容与本轮 nanobot 自跑验证一致（pytest 1528 / verify all / 三目标全绿），故保留不追改。
